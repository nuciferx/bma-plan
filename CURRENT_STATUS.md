# CURRENT_STATUS.md — BMA-Plan Current Status

Date: 2026-07-03 (INV-20260703-layer-linkage plan B COMPLETE + UX-batch-1 + save-fix follow-up SHIPPED — the staged B0-B5 rollout of the layer↔measurement redesign that reached its human GO checkpoint earlier the same day shipped end-to-end across 6 commits: B0 (`edc89ae`) NEW `lite/static/js/object-agg.js` single tuple-stream aggregation engine + I11 invariant oracle; B1-B2 (`6909486`+`00ab9b9`) report-vars/Summary/layer-tree/Review all rerouted onto the same tuple stream, closing H2 (dual-engine disagreement, now structurally impossible) + M6; B3 (`34594b7`) orphan-`catId` self-heal on `removeLayer` + load-time sweep, closing H3; B4 (`750d2f6`) NEW `lite/static/js/layer-move.js` move-object-to-layer UI, closing H1 — all 3 HIGH findings now closed; B5 (`3d3741e`) report-vars operand dropdown Σ/▸ optgroups, closing M4. Bundled: UX batch 1 (`34aefa3`) — F-7 modalOpen keydown guard, F-1/F-2/F-3 hotkey/discoverability fixes, cheatsheet corrections; and the save-fix bug-archive follow-up (`d40b20b`, already shipped in the prior finalize). Every commit independently verified pre-commit; `MEASURE_PARITY_OK` green at B4. Zero proto/ edits. Previous ship: UX-REVIEW-20260703 + BUG-20260703-lite-save-wipes-data SHIPPED — CRASH-tier save-wipe fix + full UI/UX journey review (9 FRICTION + 6 COSMETIC findings) + the `INV-20260703-layer-linkage` investigation reaching its GO checkpoint. Regression 17/17.)

> Full status details: [docs/status/LATEST_STATUS.md](docs/status/LATEST_STATUS.md)
> Next actions: [docs/status/NEXT_ACTIONS.md](docs/status/NEXT_ACTIONS.md)
> Known issues: [docs/status/KNOWN_ISSUES.md](docs/status/KNOWN_ISSUES.md)

## One-Line Status

2026-07-03: INV-20260703-layer-linkage plan B COMPLETE + UX-batch-1 + save-fix follow-up SHIPPED (commits `d40b20b`+`34aefa3`+`edc89ae`+`6909486`+`00ab9b9`+`34594b7`+`750d2f6`+`3d3741e`) — the staged B0-B5 layer↔measurement redesign rollout shipped end-to-end: NEW `lite/static/js/object-agg.js` single tuple-stream aggregation engine + I11 invariant oracle (B0) makes the Summary-vs-Review disagreement (H2) structurally impossible; existing consumers rerouted onto it (B1-B2), closing H2 + M6 (root-unfiled layers uncounted); orphaned-`catId` self-heal on `removeLayer` + load (B3) closes H3; NEW `lite/static/js/layer-move.js` (B4) finally ships the move-object-to-layer UI, closing H1 — all 3 HIGH findings from the checkpoint now closed; report-vars operand-dropdown Σ/▸ optgroups (B5) closes M4. Bundled: UX quick-wins batch 1 (F-7 modalOpen keydown guard fixing the hotkey-leak-into-Summary bug, F-1 dead ⇧D hotkey, F-2 F-key/Focus collision, F-3 Page Manager menu entry, cheatsheet corrections) and the save-fix bug-archive follow-up (already shipped/documented in the prior finalize). Every commit independently verified pre-commit with its own guard test + a `t0` measure-parity sweep; `MEASURE_PARITY_OK` green at B4. Zero `proto/` edits, zero forbidden-surface edits. Next: UX quick-wins batch 2 (F-4/F-5/F-6/F-9), B4 CFSS-undo follow-up, worker-recycle RSS re-probe, V2 U3/U4 migration.

2026-07-03 (earlier): GO-20260703-invariants-streaming-worker-recycle SHIPPED — continuation of the same-day "GO" loop block: V2-U1 invariant registry (`lite/tests/INVARIANTS.md`, 10 invariants I1-I10 + 16-row `PHASE_INDEX.md` reconcile + `ptToScreen` parity card closed with rationale, `6d6e39b`); Range-streaming spike run for real on RAMA4 19MB + CHH 95MB — streaming cut RSS only 10% vs a ≥50% GO bar (FAILS), pdf.js worker-heap bug #10730 CONFIRMED on 4.0.379, VERDICT NOGO-as-memory-fix (`9466fe4`+`f8c2981`); worker-recycle BUILT as the RESHAPE — explicit `PDFWorker` lifecycle (`_docWorker`/`_docSource`/`recycleDocWorker()`), `LITE_WORKER_RECYCLE_OK` 7/7 (`d52ddbb`); streaming-as-bandwidth card DEFERRED-until-remote-deployment (localhost bytes are free today). Lite suite now 72 files; `MEASURE_PARITY_OK` green throughout. Zero proto/ edits, zero forbidden-surface edits. Only item still open: production RSS re-probe of worker-recycle on the real CHH binder.

2026-07-03 (earlier still): BLOCK-20260703-clear-queue SHIPPED — 5-ship "ทำทั้งหมด" session: `AUDIT-20260702-s2-fitz-lock` (`d0a5dde`, per-case fitz lock incl. threadpooled overlay export), render-followups a+c (`aec375b`, raster fallback + scanned-page detection), V2 tiers + pixel-level overlay-registration proof + Range-streaming research HALTED at human checkpoint (`13054b6`), `ACC-20260703-verify-scale-port` (`bea2119`, closes the last known accuracy gap vs. Foxit), and 2 new process docs (`16e6495`+`b676652`). 5 NEW guard markers: `LITE_CASE_LOCK_OK` / `LITE_RENDER_FB_SCAN_OK` / `LITE_OVERLAY_REG_OK` / `LITE_VERIFY_SCALE_OK` (+ tiered runner, no dedicated marker). Full lite suite validated 70/70 files green (grown 60→70 across 2026-07-02 → 2026-07-03); `MEASURE_PARITY_OK` green throughout every ship. Zero proto/ edits, zero forbidden-surface edits. Range-streaming checkpoint resolved by `GO-20260703-invariants-streaming-worker-recycle` above.

2026-07-02: PERF-20260702-lite-foxit-smoothness SHIPPED — 4-sprint perf block driven by an empirical probe (`artifacts/perf/probe_results_20260702.txt`): page-cache LRU (`ae0f168`, CHH heap 766→~628 MB), local-first open (`3ec9239`, paint before server upload lands, `/raw` never fetched), worker warm-up + adjacent prefetch (`e0fb856`, hides the flat ~1.2s PDF.js boot + instant page-switch), sequential thumbnail warm (`a0c1152`, overview grid opens hot). Probe also REFUTED pan-blanking (0/10 across 3 files) and found "overview thumbs 0/0" was a probe selector artifact (real: 45/45 in 9.2s cold). All 4 guard tests RED→GREEN (or new); regression 10/10 + 10/10 + 9/9 + 8/8; `MEASURE_PARITY_OK` green throughout. Lite suite now 67 files, all green. Zero proto/ edits, zero forbidden-surface edits. Only remaining perf piece (doc-level memory, Range-streaming) is routed to the `PERF-20260702-open-streaming` invent card, not attempted same-day.

2026-07-02 (earlier): BUG-20260702-lite-pagerot-registration SHIPPED — fixed the top-priority BROKEN bug filed earlier today by the AUDIT-20260702-infra-bundle render-engine accuracy review. Manual page rotate (`pageRot`) rotated the PDF.js raster canvas but `ptToScreen`/`screenToPt` ignored `pgRot` (`getRot()` hardcoded to 0) — pre-existing geometry detached from the visible plan by up to a page diagonal, geometry drawn while rotated bound itself to the wrong feature (correct area, wrong location), and `/export-pdf-overlay` never applied `pageRot`. Fixed (commit `9f4b298`) by routing `ptToScreen`/`screenToPt` through the vendored, parity-tested `pdfToC`/`cToPdf` rotation branches (net 0 lines) and server-side prerotating the export raster via a new `_rp` coordinate helper. New guard test `LITE_PAGEROT_REG_OK` proven RED→GREEN; 42 distinct regression files green. Zero proto/ edits, zero forbidden-surface edits (only the host contract fn `getRot()` in `measure-engine.js` changed, not the drift-locked math).

2026-07-02 (earlier): AUDIT-20260702-infra-bundle SHIPPED — same-day follow-on to the 2-bug measurement-accuracy audit closed earlier today. NEW `lite/tests/run_all_tests.py` aggregate runner with disk/dependency PREFLIGHT (`LITE_RUN_ALL_OK` 60/60 in 8.5 min, first full run) closes the "no aggregate runner" gap both audit bugs flagged; `/export-pdf-overlay` + `/export-xlsx` now validate payloads BEFORE rendering with 5 caps (HTTP 400, no silent truncation, bonus fix for a latent 500) + `LITE_EXPORT_ENDPOINTS_OK` 14/14 (first real HTTP tests of either endpoint); read-only Opus render-engine accuracy review verdict SOUND for the coordinate contract but found one real BROKEN bug — `BUG-20260702-lite-pagerot-registration` (manual page rotate desyncs stored geometry from the raster, `ptToScreen`/`screenToPt` ignore `pgRot`), filed as top-priority next work. Zero proto/ edits, zero forbidden-surface edits.

2026-07-02 (earlier): BUG-20260702-lite-cfss-summary SHIPPED — CFSS shared-shape instances had no `.pts`/`.catId`, so every area rollup skipped them (silent total loss on promote) and `buildExportData`/`exportPdfOverlay` called `catOf(o.catId)` before the `kind` guard (TypeError → XLSX + annotated-PDF export crashed outright on any page with an instance); fixed by passing `{catId, semanticTag}` into `addMaster` at promote time plus new `rollupAreaM2`/`rollupCatId` dispatch helpers reused at all 6 rollup sites; new `LITE_SUMMARY_CFSS_OK` guard test proven RED→GREEN (14/14 tests exit 0). Zero proto/ edits, zero forbidden-surface edits. This closes the 2-bug 2026-07-02 measurement-accuracy audit (bug 1 = `BUG-20260702-lite-arc-summary`, shipped earlier today).

2026-07-02 (earlier): BUG-20260702-lite-arc-summary SHIPPED — arc-edge polygon areas were correct on the canvas label but silently wrong in every rollup because 6 call sites dropped `o.edges` when computing area; fixed by swapping to `polyMetricsAnyShape` at all 6 sites; new `LITE_SUMMARY_ARC_OK` guard test proven RED→GREEN. Zero proto/ edits.

2026-06-05: SLICE report-edit-1 SHIPPED — editable lite report with jspreadsheet-ce grid, custom cell-click formula picker, stable-row-id subtotal mapper, NaN-guard, and localStorage v1 persistence. LITE_REPORT_EDIT_OK 7/7. Zero proto/ edits. Grid behind #re-toggle dev gate.

## Latest Sprint

- INV-20260703-layer-linkage plan B COMPLETE + UX-batch-1 + save-fix follow-up — staged B0-B5 rollout of the layer↔measurement redesign: PASS (2026-07-03) — the redesign that reached its human GO checkpoint earlier the same day (`INV-20260703-layer-linkage`, Approach B "one aggregation engine" won 25/30) shipped end-to-end across 6 commits. **B0 (`edc89ae`):** NEW `lite/static/js/object-agg.js` — single tuple-stream aggregation engine `{pg, catId, role, floorKey, area, counting}` + a NEW I11 invariant oracle (`assertEnginesAgree`) that proves any two consumers reducing the same partition of the same tuple stream cannot disagree — the H2 finding (Summary-widget vs. Review-panel dual engines can disagree on net area) is now structurally impossible, not just carefully synchronized. Guard `lite/tests/test_object_tuples.py` (`LITE_OBJECT_TUPLES_OK`). **B1 (`6909486`):** `report-vars.js` + Summary per-floor `gfa`/`ded`/`net` rerouted onto the tuple engine behind opt-in `{useLive:true}` (default path byte-identical). Guard `test_b1_role_reroute.py`. **B2 (`00ab9b9`):** layer-tree Σ totals + Review-panel Section-3 rows reduce the SAME tuple stream; oracle check (c) proves `byFloorRole` partitions equal `byRole` partitions by construction, closing H2 for good; root-unfiled layers (M6) now counted where they previously silently dropped. Guard `test_b2_single_engine.py`. **B3 (`34594b7`):** `layer-system.js` gains `reassignObjectsOfLayer` on `removeLayer` plus a load-time `sweepOrphanCatIds` heal pass, closing H3 (a deleted category's orphaned `catId` silently dropped objects from every total, crashed on one path). Guard `test_b3_orphan_heal.py`. **B4 (`750d2f6`):** NEW `lite/static/js/layer-move.js` (120 lines, DOM-injected — `ui-lite.html` itself untouched) — the move-object-to-layer UI closing H1, the "no UI exists to move an object between layers" finding: a Properties-panel select + a canvas context-menu entry "ย้ายไปเลเยอร์ ▸"; CFSS shared-shape instances retarget the MASTER with a confirm dialog. Guard `test_b4_move_layer.py`. Known flag carried forward: CFSS-master-only moves are not undoable (pre-existing `_docSnap` gap, candidate follow-up, not a new regression). All 3 HIGH findings (H1/H2/H3) from the checkpoint are now closed. **B5 (`3d3741e`):** report-vars.js operand dropdown groups references into Σ role-refs vs. ▸ single-layer refs (two optgroups) — closes M4, display-only, persisted expression format unchanged. Guard `test_b5_ref_badges.py`. **Bundled UX batch 1 (`34aefa3`):** F-7 `modalOpen()` keydown guard recognizes `#sum`/Summary as an open modal, closing the exact hotkey-leak class of bug the UX review filed the same morning; F-1 dead ⇧D Path hotkey wired; F-2 F-key/Focus collision resolved; F-3 Page Manager gained a menu entry (was ⇧F12-only); cheatsheet corrected to match real bindings. Guard `test_ux_quickwins.py`. **Save-fix bug-archive follow-up (`d40b20b`, already fixed and recorded in the prior finalize):** included here for the closed arc's narrative completeness. Every commit independently verified pre-commit with its own targeted suite + a `t0` measure-parity sweep; `MEASURE_PARITY_OK` verified green at B4 — the natural checkpoint for a block this heavy on layer/aggregation plumbing. No `proto/` E2E run — zero `proto/` files touched (lite-only block; proto's 22-marker baseline unaffected by construction). Proto NOT TOUCHED; zero forbidden-surface edits. Next: UX quick-wins batch 2, B4 CFSS-undo follow-up, worker-recycle production RSS re-probe, V2 U3/U4 migration.
- UX-REVIEW-20260703 + BUG-20260703-lite-save-wipes-data — CRASH fix (Ctrl+S wiped all measurements) + full UI/UX journey review + layer↔measurement invent checkpoint: PASS (2026-07-03) — **(1) Save-wipe CRASH fix (`d40b20b`):** Ctrl+S wrote an empty `.bmaplan` and destroyed the in-memory session. Root cause: `pageMgr` seeded `PS_by_id` as `deepCopy` snapshots of `PS` taken at upload time (empty at that point); all drawing thereafter mutated live `PS` only; `mi-save` → `_pmCommit` projected those stale empty snapshots back over `PS` immediately before `buildPageStore()` serialized it — so save always wrote the empty upload-time state. 72 previously-green tests missed this because every existing save test called `buildPageStore()` directly (the API path), never the real `mi-save` click path. Fixed via new `projectToGlobals(livePS)` resolving each page id's content from live `PS` by identity (baseline via `_initialIds`, duplicates via a new `dupSrc` map deep-copying the SOURCE page's live content, merges stay blank); `_pmCommit` now passes `PS` through this resolver; model snapshots refreshed post-commit. Same latent wipe fixed on the Apply/merge path (`page-manager-ui.js`, 2 sites — commit order was backwards). NEW guard `lite/tests/test_save_clickpath.py` (`LITE_SAVE_CLICKPATH_OK`) drives the REAL `mi-save` click via `URL.createObjectURL` interception — closing the click-path-vs-API-path gap that let 72 tests miss this; proven RED pre-fix (polys 0, calib null — exact journey repro) → GREEN, 4 checks incl. dup-content-follows-identity. Regression 17/17 (metamorphic, page-manager suite, apply-mutations, all persist suites, arc/CFSS parities, `MEASURE_PARITY_OK`). **(2) UX review filed (`UX-20260703-review-findings`, commit `2249400`):** 29-screenshot journey review on the real 45-page permit + static code inventory → 9 FRICTION (dead ⇧D hotkey, `F`/Focus key collision, undiscoverable Page Manager ⇧F12-only, scale-verified state invisible post-modal, wizard blocks mouse silently, misleading "เปิด PDF ก่อน" during background upload, `modalOpen()` missing `#sum`/`#vs-modal`/cheatsheet so typing leaks tool hotkeys, raw error messages, `window.prompt()` in Verify Scale) + 6 COSMETIC (annotate no hotkeys, Thai/English label flips, stale cheatsheet, empty-state clutter, transient scanned notice, no fallback-render badge) + 3 GOODs pinned (setup wizard, local-first save status, live measure feedback). Findings-filing only — no app code changed by the review itself. **(3) Layer↔measurement invent at checkpoint (`INV-20260703-layer-linkage`, commit `2249400`):** investigation mapped 8 problems (3 HIGH: no move-between-layers UI; Summary-widget vs Review-panel dual aggregation engines can disagree on net area; orphaned `catId` silently drops objects / crashes). `bma-inventor` scored 5 approaches; Approach B "one aggregation engine" won 25/30 (`object-agg.js` tuple-stream generator, every consumer becomes a pure reduction, per-floor net becomes native, fully additive); fallback D (simple `floorKey` field) recorded. Staged B0-B5 rollout plan written; GO received at the human checkpoint. B0 (tuple generator + new `I11` invariant oracle) and a first UX quick-wins batch (F-7 modal-detection fix, F-1/F-2/F-3 hotkey/discoverability, cheatsheet accuracy pass) are being built right now by two `lite-builder` subagents in parallel — recorded as in-progress, not shipped as of this docs-write. Proto NOT TOUCHED; zero forbidden-surface edits.
- GO-20260703-invariants-streaming-worker-recycle — V2-U1 invariant registry + Range-streaming spike (NOGO→RESHAPE) + worker-recycle build: PASS (2026-07-03) — continuation of the same-day "GO" loop block, resumed right after `BLOCK-20260703-clear-queue` closed and the Range-streaming human checkpoint was cleared. (1) V2-U1 invariant registry (commit `6d6e39b`): NEW `lite/tests/INVARIANTS.md` — 10 invariants (I1-I10) mapped to guard test + tier + 2 mandatory SCOPE questions + new-object-kind fixture rule, born from the arc/CFSS-summary postmortem; same commit reconciles 16 stale `PHASE_INDEX.md` rows (`lpm-1..9`, `cfss-guard`, `EVOLT-1..5`, `INV-2026-05-25-CFSS`) with real `fixed_commit`, and closes the `ptToScreen`/`screenToPt` parity-fixture card with rationale instead of code (registered as invariant I7 — the behavioral lock already exists via `LITE_PAGEROT_REG_OK`). (2) Range-streaming SPIKE (commits `9466fe4`+`f8c2981`), GO received via `/loop`: the 5-step spike ran for real on RAMA4 (19MB) + the real CHH customer binder (95MB) in `lite/sandbox/invent-range-streaming/`. Results: both PDFs non-linearized yet stream fine (20% bytes fetched — linearization concern moot); Starlette's `/raw` already serves 206+Content-Range (zero backend work); streaming cut CHH RSS only 10% (1675→1503 MB) against a ≥50% GO criterion — FAILS; pdf.js worker-heap bug #10730 CONFIRMED on 4.0.379 (`destroy()` frees the main doc heap 409→98 MB but the worker heap survives — the real ~1.5 GB ceiling); `PDFDataRangeTransport` over `Blob.slice` works mechanically but delivers no memory win. VERDICT: NOGO on streaming-as-memory-fix, RESHAPE to worker-recycle — the spike prevented a doomed build sprint. (3) worker-recycle BUILT (commit `d52ddbb`), the RESHAPE: `lite/static/js/page-renderer.js` gains explicit `PDFWorker` ownership (`_docWorker`) + cheap `_docSource` re-open handle (local blob = zero-network reinit, or caseId = one refetch) + `recycleDocWorker()` with guards (in-progress / render-in-flight=SKIP / no-source; preserves `pageDims`/`pageRot`/`_scanned`) + transparent lazy `_reinitDoc` on next `loadPage` + triggers (tab hidden ≥60s; idle ≥5min AND pageCount>20; manual `recycleNow`); `_resetCache()` now destroys the explicit worker (would otherwise leak one per upload); `page-renderer.js` 790/1000, `ui-lite.html` +1 call-site line (1170/1200). Built by `lite-builder`, independently verified. NEW `lite/tests/test_worker_recycle.py` (`LITE_WORKER_RECYCLE_OK`, 7 checks incl. zero-refetch-via-blob and heap-not-worse −0.8%). Honest scope: the full CHH RSS −50% acceptance bar was measured in the spike's pattern, not re-verified against this specific production build — a production re-probe is queued as follow-up measurement. Regression 6/6 + `--tier t0` green. (4) Streaming-as-bandwidth card DEFERRED-until-remote-deployment with rationale (เสา 1): `/raw` bytes are localhost on the current desktop-only product (91MB `/raw` = 498ms measured) — the 80% byte saving buys nothing until a remote deployment target exists. Suite validation: lite test suite now 72 files; `MEASURE_PARITY_OK` green throughout every piece of this block. Proto NOT TOUCHED; zero forbidden-surface edits (spike isolated in `lite/sandbox/`, RESHAPE independently built + guarded). Only item still open: production RSS re-probe of worker-recycle on the real CHH binder.
- BLOCK-20260703-clear-queue — 5-ship "ทำทั้งหมด" session (fitz lock + render fallback/scan + V2 tiers/overlay-proof/streaming-research + Verify-Scale port + process docs): PASS (2026-07-03) — Ship 1 `AUDIT-20260702-s2-fitz-lock` (commit `d0a5dde`): per-case `threading.Lock` (`_case_lock`) in `lite/server_lite.py` serializing all `fitz.Document` access — `/page`+`/thumb` render blocks, `/pageinfo`, and the whole `/export-pdf-overlay` render path (now also threadpool-offloaded via new `_render_overlay` helper, completing the S2 goal deferred by `AUDIT-20260702-infra-bundle`), plus apply-page-mutations + merge-pages critical sections; NEW `LITE_CASE_LOCK_OK` — 96-request 8-thread hammer + mid-flight doc-swap + concurrent overlay export, zero 5xx (hardening hammer, not a deterministic RED-before-fix proof — native races are probabilistic); 9/9 regression. Ship 2 render-followups a+c (commit `aec375b`): (a) raster fallback — `loadPage()` falls back to server JPEG when PDF.js is unavailable instead of a blank canvas; (c) scanned-page detection via operator-list heuristic, re-render scale capped at `RS*4` with transform compensation so registration stays bit-identical, one-time Thai hint; NEW `LITE_RENDER_FB_SCAN_OK` 6/6 (2 false alarms fixed test-side, not app-side); 8/8 regression. Ship 3 (commit `13054b6`): `run_all_tests.py --tier t0/t1/t2` (V2 blueprint U2, `t0` runs in 1.26s); NEW `LITE_OVERLAY_REG_OK` — first PIXEL-level raster↔overlay registration proof (max offset 0.50 device px at zoom×2, tolerance 4), closing an evidence gap the 2026-05-28 render-quality spike never closed; `docs/invent/lite-range-streaming.md` Range-streaming research (`PRIOR_ART_PARTIAL` verdict, 5-step spike design + GO/NOGO criteria) HALTED at the human checkpoint per Pack H — no code shipped for this piece by design. Ship 4 `ACC-20260703-verify-scale-port` (commit `bea2119`): Verify-Scale ported from proto's `INV-2026-05-20-001` — the single remaining accuracy gap vs. Foxit; NEW `lite/static/js/verify-scale.js` (225 lines, additive `PS[pg].scale.verifyResult`); `ui-lite.html` +3 net lines (menu item, `Shift+S`, verify-mode routing); NEW `LITE_VERIFY_SCALE_OK` 7/7 incl. save/load round-trip + average-re-derives-areas; built by `lite-builder` subagent, independently verified. Ship 5 (commits `16e6495`+`b676652`): `docs/process/DEVELOPMENT_PILLARS.md` (6-pillar doctrine + incident evidence + halt conditions) + `docs/process/DEVELOPMENT_V2_BLUEPRINT.md` (6 weaknesses → 6 structural upgrades + migration order + success bar). Final validation: full lite suite 70/70 files green (chunked 17+27+26 after background runs kept getting killed; `artifacts/run_all_tests_20260703_final.log` covers the first 17); suite grew 60→70 files across 2026-07-02 → 2026-07-03; `MEASURE_PARITY_OK` green throughout every ship. Proto NOT TOUCHED; zero forbidden-surface edits (Ship 3's Range-streaming — the one piece that would touch the page-renderer's buffer-ownership contract — correctly HALTED rather than shipped). Only item still open: human GO/NOGO/RESHAPE decision on Range-streaming.
- PERF-20260702-lite-foxit-smoothness — Foxit-grade open smoothness (4-sprint block): PASS (2026-07-02) — driven by an empirical perf probe (`artifacts/perf/probe_results_20260702.txt`) measuring RAMA4 (18.3 MB, 3.9s first-paint), CHH (90.8 MB real customer file, 9.6s first-paint + 766 MB heap after 10 pages), and a flat ~1.2s PDF.js library+worker boot floor on every open; time attribution showed `UPLOAD` dominates ~80ms/MB while `/raw` is nearly free; pan-blank suspicion REFUTED 0/10 across 3 files; "overview thumbs 0/0" was a probe selector artifact (real: 45/45 in 9.2s cold). Sprint 1 — page-cache LRU (commit `ae0f168`): `MAX_PAGE_CACHE=4` LRU in `lite/static/js/page-renderer.js`, evicted `PDFPageProxy.cleanup()` when unreferenced, `curPage` never evicted; CHH heap 766→~628 MB (−18%), remaining ~600 MB is PDF.js doc-level shared state left to the queued Range-streaming invent card; `LITE_PAGECACHE_LRU_OK` RED→GREEN, 10/10 regression. Sprint 2 — local-first open (commit `3ec9239`): `uploadPdf()` opens local bytes via `PageRenderer.openLocal()` immediately, server upload backgrounds, `adoptCase(caseId)` binds the case so `/raw` is NEVER fetched, server-gated features guarded during the null-`caseId` window; paint at 475ms with upload in flight, removes the dominant ~80ms/MB wait on real binders; `LITE_LOCAL_OPEN_OK` RED→GREEN, 10/10 regression. Sprint 3 — worker warm-up + adjacent prefetch (commit `e0fb856`): pdf.js boot preloaded at idle (`window.__pdfjsWarmed`), n±1 page prefetched at idle after `loadPage(n)`, prefetched pages count toward the Sprint-1 cache budget; `LITE_WARM_PREFETCH_OK` RED→GREEN, 9/9 regression. Sprint 4 — sequential thumbnail warm (commit `a0c1152`): `warmThumbs()` fetches `/thumb/1..N` sequentially (120ms gaps), deliberately sequential pending the still-queued per-case `fitz` lock, token-cancelled on new upload; `LITE_THUMB_WARM_OK`, 8/8 regression. `MEASURE_PARITY_OK` green throughout all 4 sprints — zero vendored-math touch. Lite suite now 67 files, all green; `ui-lite.html` stays under 1200-line cap. Proto NOT TOUCHED; zero forbidden-surface edits. Only remaining perf piece (doc-level memory, Range-streaming) routed to the `PERF-20260702-open-streaming` invent card.
- BUG-20260702-lite-pagerot-registration — Manual page rotate desyncs stored geometry from raster + export: PASS (2026-07-02) — fixed the top-priority BROKEN bug filed same-day by the `AUDIT-20260702-infra-bundle` render-engine accuracy review. `pageRot` rotated the PDF.js raster canvas but `ptToScreen`/`screenToPt` ignored `pgRot` (`getRot()` hardcoded to 0) — pre-existing geometry detached from the visible plan by up to a page diagonal (~84 m at 1:100 A1); geometry drawn while rotated stored bound to the wrong feature (correct area, wrong location — "right value, wrong location" bug); `/export-pdf-overlay` never applied `pageRot` either. Intrinsic PDF `/Rotate` was always correct. Fixed (commit `9f4b298`, "Fix A — proto-parity" over "Fix B — geometry-baking" per an Opus specialist patch plan): `getRot()` (host contract fn) now returns `pageRot[pg]||0`; `ptToScreen`/`screenToPt` route through the vendored, parity-tested `pdfToC`/`cToPdf` rotation branches — net 0 lines, zero geometry mutation, no migration (`pageRotations` already persisted); server `/export-pdf-overlay` prerotates the raster via `Matrix(RS,RS).prerotate(rot)` + maps coordinates through new `_rp` helper mirroring `pdfToC` — prerotate direction verified empirically with a pixel test before wiring. New guard test `lite/tests/test_pagerot_registration.py` marker `LITE_PAGEROT_REG_OK` (5 checks: 4-angle mapping, exact-inverse round-trip, area invariance, save/load round-trip, export dimension/pixel check) proven RED (pre-fix mapped to (15,15) instead of (585,15), export un-rotated) → GREEN. Regression: 16 at-risk files + 26 more from a partial `run_all_tests.py` pass = 42 distinct files green; `MEASURE_PARITY_OK` green confirms drift-locked vendored math untouched. Proto NOT TOUCHED; zero forbidden-surface edits (only the host contract fn `getRot()` changed, not the vendored math itself). Known gap: combined `V.rot≠0` + `pgRot≠0` asserted only via exact-inverse round-trip, not raster-pixel alignment — the queued `AUDIT-20260702-render-followups` item (e) would add visual coverage.
- AUDIT-20260702-infra-bundle — Test-Runner Preflight + Export Payload Caps + Render-Engine Review: PASS (2026-07-02) — same-day follow-on to the 2-bug 2026-07-02 measurement-accuracy audit. Sprint A (`9c4c36e`): NEW `lite/tests/run_all_tests.py` aggregate test runner (discovers + runs every `test_*.py`, per-test timeout, summary table, `LITE_RUN_ALL_OK`/`FAIL`) with disk/dependency PREFLIGHT (fails fast on <2 GB free on repo or system drive, hardened after the 2026-07-02 ENOSPC incident, or missing uvicorn/playwright/fitz/node); first full run 60/60 PASS in 8.5 min. Sprint B (`60d424a`): `/export-pdf-overlay` + `/export-xlsx` now validate payloads BEFORE rendering (5 caps: `MAX_EXPORT_PAGES`/`MAX_OBJECTS_PER_PAGE`/`MAX_ANNOTS_PER_PAGE`/`MAX_PTS_PER_OBJECT`/`MAX_COORD_ABS`, HTTP 400 not silent truncation, ≥10x realistic worst case, bonus fix for a latent 500 on non-numeric page key); `wb.save()` offloaded via `run_in_threadpool`; NEW `lite/tests/test_export_endpoints.py` marker `LITE_EXPORT_ENDPOINTS_OK` 14/14 — first real HTTP tests of either export endpoint. Review C (read-only, Opus agent): `PDFJS-VIEWPORT-CLIPPED` render architecture verdict SOUND for `V.rot`/`pgRot`=0 (coordinate contract algebraically exact, residual ≈ ±0.5 device px = click-precision floor); filed real BROKEN bug `BUG-20260702-lite-pagerot-registration` (manual page rotate desyncs stored geometry from raster — `ptToScreen`/`screenToPt` ignore `pgRot`) as top-priority next work, plus bundle `AUDIT-20260702-render-followups` (pdfjs-fail fallback / pan double-buffer / scanned-PDF detection / memory-claim correction / real overlay-registration test) and `AUDIT-20260702-s2-fitz-lock` (per-case PyMuPDF lock needed before overlay-render threadpool offload). Proto NOT TOUCHED; zero forbidden-surface edits.
- BUG-20260702-lite-cfss-summary — CFSS shared-shape instances excluded from every rollup consumer + export crash: PASS (2026-07-02) — bug 2 of the 2026-07-02 measurement-accuracy audit; instances (`{kind:'instance', masterId, offsetPt}`, no `.pts`/`.catId`) skipped by all 6 rollup consumers (silent total loss on promote); `buildExportData`/`exportPdfOverlay` called `catOf(o.catId)` before the `kind` guard → TypeError → XLSX + annotated-PDF export crashed on any page with an instance; root cause `cfssCommitPromote` called `addMaster()` without the `opts` arg; fixed by passing `{catId, semanticTag}` into `addMaster` (additive schema, free persistence) + NEW `rollupAreaM2`/`rollupCatId` dispatch helpers in `cross-floor-shapes.js` (typeof-guarded, load-order-mandatory) reused at all 6 rollup sites + 2 crash-site fixes; patch plan by `bma-path-geometry-reviewer` on Opus (user-requested); NEW `lite/tests/test_summary_cfss_parity.py` marker `LITE_SUMMARY_CFSS_OK` exercises the real promote flow, proven RED (totals 2000, exports threw) → GREEN (totals 2100, no throw); 14/14 tests exit 0; commit `02e35af`; proto NOT TOUCHED; ui-lite.html line-neutral. Both audit bugs now SHIPPED.
- BUG-20260702-lite-arc-summary — Arc-edge polygon areas excluded from every rollup consumer: PASS (2026-07-02) — 6 call sites (`ui-lite.html:1049` computeSummary, `export-annotate.js:14/27/58` buildExportData/exportPdfOverlay/buildReportPayload, `layer-tree.js:62` _ltOwnArea, `overview-setup.js:642` _lovsLayerArea) passed `{pts:o.pts}` into `polyMetrics` (drops `o.edges` → straight-chord area) instead of `polyMetricsAnyShape(o,pg)` (arc-correct); triage found 4 sites, specialist review widened to 6; fixed by callee swap at all 6, zero edits to drift-locked measure-engine.js; NEW `lite/tests/test_summary_arc_parity.py` marker `LITE_SUMMARY_ARC_OK` proven RED (git stash) → GREEN; 8 regression suites GREEN (MEASURE_PARITY_OK, LITE_ARC_EDGE_OK, test_report, LITE_REPORT_VARS_OK, LITE_REPORT_VARS_ROLLUP_OK, LITE_EXPORT_SUBMENU_OK, LITE_TREE_ROLLUP_OK, LITE_OVERVIEW_SETUP_OK); commit `e5264e2`; proto NOT TOUCHED; ui-lite.html stays 1,154/1,200 cap.
Older sprints (SLICE report-edit-1, BUG-20260526-lite-stale-pf-folder-cleanup, Centerline Snap arc, SIM-2, LITE-BUG-2-OPUS47-FINDINGS, BUG-20260521-lite-pan-controls, BUG-20260521-lite-menu-clip, LITE-0, HT-ACC series and earlier) — see [docs/status/NEXT_ACTIONS.md](docs/status/NEXT_ACTIONS.md) "Recently Done" and archived logs.

## Test Baseline

```bash
python -m py_compile proto/server.py proto/e2e_ui_test.py  # PASS
python proto/e2e_ui_test.py smoke                          # PASS (18 baseline markers)
python proto/e2e_ui_test.py full                           # PASS (22 total: 21 baseline + PHASE_CENTERLINE_SNAP_OK 10/10)
```

Last proto full run: 2026-05-25 (Centerline Snap arc; 22 proto _OK total; all markers retained).
Last lite test run: 2026-07-03 (INV-20260703-layer-linkage plan B complete + UX-batch-1; B0-B5 guard tests all PASS incl. `LITE_OBJECT_TUPLES_OK`; `MEASURE_PARITY_OK` green at B4).
Full test detail: [docs/status/TEST_BASELINE.md](docs/status/TEST_BASELINE.md)

## Latest Commits

- `3d3741e` — feat(lite): B5 — Σ/▸ ref badges in report-var editor (INV-20260703, M4 closed)
- `750d2f6` — feat(lite): B4 — move-object-to-layer UI (INV-20260703, H1 closed)
- `34594b7` — feat(lite): B3 — orphan catId self-heal + catOf crash guards (INV-20260703, H3 closed)
- `00ab9b9` — feat(lite): B2 — single aggregation engine; Review/tree sums on tuples (INV-20260703)
- `6909486` — feat(lite): B1 — report-vars + Summary per-floor block on the tuple engine (INV-20260703)
- `edc89ae` — feat(lite): B0 — object-tuple aggregation engine + I11 oracle (INV-20260703-layer-linkage)
- `34aefa3` — feat(lite-ux): UX quick-wins batch 1 — F-7 hotkey leak + F-1/F-2/F-3 + cheatsheet truth (UX-20260703)
- `2249400` — chore: file UX-20260703 review bundle + layer-linkage invent checkpoint
- `912c3e2` — chore(lite): bug-archive — record save-wipes-data fixed_commit
- `d40b20b` — fix(BUG-20260703-lite-save-wipes-data): save no longer wipes all measurements; LITE_SAVE_CLICKPATH_OK NEW
- `d52ddbb` — perf(lite): pdf.js worker-recycle — reclaim the worker heap (RESHAPE from streaming spike); LITE_WORKER_RECYCLE_OK 7/7
- `f8c2981` — chore: PHASE_INDEX — record streaming spike verdict on the perf card
- `9466fe4` — invent(lite): Range-streaming spike complete — NOGO on memory, RESHAPE to worker-recycle
- `6d6e39b` — chore: V2-U1 invariant registry + PHASE_INDEX reconcile (16 stale rows)
- `b676652` — docs(BLOCK-20260703-clear-queue): Ship 5b — DEVELOPMENT_V2_BLUEPRINT.md
- `16e6495` — docs(BLOCK-20260703-clear-queue): Ship 5a — DEVELOPMENT_PILLARS.md
- `bea2119` — feat(ACC-20260703-verify-scale-port): Verify-Scale ported from proto; LITE_VERIFY_SCALE_OK 7/7
- `13054b6` — feat(BLOCK-20260703-clear-queue): Ship 3 — tiered test runner + LITE_OVERLAY_REG_OK pixel-proof + Range-streaming research (HALTED at checkpoint)
- `aec375b` — fix(AUDIT-20260702-render-followups): Ship 2 — raster fallback (a) + scanned-page detection (c); LITE_RENDER_FB_SCAN_OK 6/6
- `d0a5dde` — fix(AUDIT-20260702-s2-fitz-lock): per-case fitz.Document lock + threadpooled overlay export; LITE_CASE_LOCK_OK
- `a0c1152` — perf(PERF-20260702-lite-foxit-smoothness): S4 sequential thumbnail warm; LITE_THUMB_WARM_OK
- `e0fb856` — perf(PERF-20260702-lite-foxit-smoothness): S3 worker warm-up + n±1 adjacent prefetch; LITE_WARM_PREFETCH_OK 9/9

Full commit history: [docs/status/COMMIT_HISTORY.md](docs/status/COMMIT_HISTORY.md)

## Phase 1 Scope (Locked)

Phase 1 = Raster PDF Measurement Assistant only.
Forbidden: legal checker, OCR, AI, Rule Engine, FAR/OSR/setback pass-fail, K.1 generator,
auto boundary detection, draggable workspace, full autosave engine, save/load migration.

## Agent Operating Method

All agents must follow AGENTS.md (GTM Infinite Loop).
Read AGENTS.md + this file + docs/status/LATEST_STATUS.md before starting any sprint.
Run py_compile + smoke before any edit. Run full before commit.
