# ROADMAP_DONE.md — Shipped roadmap rows (append-only archive)

> Rows split out of [`PHASE_INDEX.md`](PHASE_INDEX.md) whose **status column contains ✅**
> (done / built / fixed / verified / merged / resolved / shipped). Append-only: rows are
> **moved verbatim** — byte-identical to the originals. New done rows get appended here at
> finalize time (or by rerunning `scripts/reconcile_roadmap.py` guidance). Grouped under the
> original section headers from PHASE_INDEX.md.

## Phase overview

| Phase | What | Status |
|---|---|---|
| A–F | Mockup V3 Alignment (6 implementation steps of one plan) | ✅ done |
| G | Menu Wiring + Measure/Layer Power-up | ✅ done |
| H.1 | Path Geometry (unified line/cubic model) | ✅ done |
| I | Site Plan Measurement (I-A … I-E) | ✅ done (I-A through I-E complete 2026-05-17) |
| INV-001 | Arc-polygon hybrid measurement | ✅ done `b89e206` |
| INV-002 | Unified Settings/Preferences modal | ✅ done `b6856df` |
| dev-website | Static docs site at `/static/docs/` | ✅ done `1bf61ca` |
| CIRCLE_RENDER | Analytic circle/ellipse render | ✅ done `1bf61ca` |

## Active sprint queue

| id | name | scope skill | region / category | status | depends-on |
|---|---|---|---|---|---|
| AUDIT-20260702-s2-fitz-lock | **Server perf (lite)** — S2 follow-up split from export-caps card per Opus patch-plan decision: `/export-pdf-overlay` (and `/page`/`/thumb` family) does blocking `get_pixmap`/`tobytes` on the SHARED per-case `fitz.Document`. Cannot naively threadpool-wrap: `/page`/`/thumb` are sync `def` (already threadpooled) so wrapping overlay too would allow two threads calling get_pixmap on the SAME Document concurrently (PyMuPDF not thread-safe per-doc). Correct fix: per-case `threading.Lock` serializing all fitz access, THEN offload overlay render to threadpool. (`/export-xlsx` wb.save already wrapped — pure local objects, safe.) source=audit 2026-07-02 (S2) | n/a (lite server) | lite / server perf | ✅ done `d0a5dde` 2026-07-02 — `_case_lock` held at /page + /thumb (double-checked cache) + /pageinfo + whole overlay render (now off event loop via `run_in_threadpool` `_render_overlay`) + copy/close/swap sections of apply-mutations/merge; urgency was RAISED by thumb-warm ship making concurrent /thumb+/page routine. Guard `LITE_CASE_LOCK_OK` (96-req 8-thread hammer + swap mid-flight + concurrent overlay, zero 5xx — probabilistic native race so hardening-hammer, no deterministic RED, noted honestly); regression 9/9 | AUDIT-20260702-export-caps |
| INV-20260703-layer-linkage | **Layer↔measurement redesign — DIVERGE done, HALTED at checkpoint** — investigation mapped 8 problems (3 HIGH: no move-between-layers UI / openSum vs Review net disagree / orphaned catId silent-drop+crash; root = catId conflates layer identity กับ measurement category, semanticTag เป็น dead data, สอง aggregation engine ขัดกัน) — full map in investigator output 2026-07-03. Inventor scored 5 approaches: **B "one aggregation engine" ชนะ 25/30** — NEW `object-agg.js` tuple stream {catId, role, floorKey, area} เป็น single source; ทุก consumer (openSum/report-vars/Review/tree Σ) กลายเป็น reduction → engines ขัดกันไม่ได้ by construction, per-floor net + floor-scoped ded ได้ native, additive ล้วน ไม่แตะ schema/vendored. Fallback = D (object carries floorKey, ทนกว่าแต่เสี่ยง desync แบบ cache-vs-source). **GO received — B0-B2 SHIPPED 2026-07-03:** B0 `edc89ae` (object-agg.js tuple engine + I11 oracle, `LITE_OBJECT_TUPLES_OK`) · B1 `6909486` (report-vars + Summary **per-floor gfa/ded/net block** on tuples via opt-in useLive — ปิด "global ded" finding; excluded-page semantics corrected, `LITE_B1_ROLE_REROUTE_OK`) · B2 `00ab9b9` (tree Σ + Review rows on tuples, oracle (c) proves byFloorRole partitions == byRole → **H2 ปิด by construction, M6 ปิด** — root-unfiled layers counted, `LITE_B2_SINGLE_ENGINE_OK`). **B3 `34594b7`** (orphan catId self-heal `reassignObjectsOfLayer` + `sweepOrphanCatIds` load heal — H3 ปิด, `LITE_B3_ORPHAN_HEAL_OK`) · **B4 `750d2f6`** (move-to-layer UI `layer-move.js` DOM-inject, CFSS retarget-master confirm — H1 ปิด, `LITE_B4_MOVE_LAYER_OK`; known flag: CFSS-master-only move ไม่ undo ได้ — pre-existing `_docSnap` ไม่ครอบ MASTERS, candidate follow-up) · **B5 `3d3741e`** (Σ role-ref vs ▸ layer-ref optgroups ใน var editor, display-only, persisted expr เดิม — M4 ปิด, `LITE_B5_REF_BADGES_OK`; default per-role layers ไม่ list ซ้ำโดยตั้งใจ — ref string เท่ากับ role's) — **แผน B ครบ 100%: H1/H2/H3/M4/M6 ปิดหมด**; UX batch 1 (F-7/F-1/F-2/F-3+cheatsheet) shipped `34aefa3` ด้วย, เหลือ UX batch 2 (F-4 verified badge / F-5 / F-6 / F-9 / seeded-vars แดง / wizard Next gate) | invent → build | measure+layer (lite) | ✅ B0-B5 ALL SHIPPED 2026-07-03 | — |
| BUG-20260703-lite-save-wipes-data | **CRASH-tier (lite, TOTAL DATA LOSS on the core save path)** — Ctrl+S / mi-save writes an EMPTY .bmaplan: pageStore all pages `{polys:[],...,calibScale:null}` despite live measurements + scale on screen; reopen = everything gone, no error. Found by journey-UX review 2026-07-03 (evidence: artifacts/ux-review-20260703/ saved.bmaplan + 29-reopened.png + repro_save.py). Root cause (isolated by repro): save handler (`ui-lite.html:966`) calls `_pmCommit()` BEFORE serializing; pageMgr records were seeded at upload from then-EMPTY PS (`_pmSeed`) and never track later draws → `_pmCommit()` projects the stale empty records back over PS (objects wiped, scale nulled) → `buildPageStore()` then serializes the wiped state. **Why 72 tests missed it: every save test calls `buildPageStore()` directly, bypassing the mi-save click path** — classic API-path vs click-path gap; guard must drive the REAL save handler. Fixed: `projectToGlobals(livePS)` resolves content from LIVE PS by identity (baseline position via `_initialIds`; duplicates via NEW `dupSrc` map → deep copy of source's live content; merge stays blank), `_pmCommit` passes PS, snapshots refreshed after commit. ALSO fixed the same latent wipe on the Apply path: `_pmuiApplyChanges`/merge now commit-with-pre-flush-baseline THEN `applyFlush()` (order was backwards). Guard `test_save_clickpath.py` drives the REAL mi-save click (URL.createObjectURL interception) — RED pre-fix (polys 0/calib null = exact journey repro) → GREEN 4/4 (save keeps data, in-memory survives, round-trip, dup content follows identity). Regression 17/17 green (all persist suites + page-manager suite + parities). 7-file finalize queued next session. source=journey-UX review 2026-07-03 | `/bma-check-forbidden` → fix | save/load (lite) | ✅ done 2026-07-03 (commit below) | — |
| ACC-20260703-verify-scale-port | **Accuracy (lite)** — Verify-Scale ported from proto INV-2026-05-20-001 (the "single remaining accuracy gap vs Foxit" from the 2026-07-02 comparison): 2nd-reference cross-check → %dev band (green<0.5/yellow<2/red≥2%) + implied area error ≈2×%dev → accept/recalibrate/average; additive `scale.verifyResult` rides existing calibScale serialization. NEW `lite/static/js/verify-scale.js` (225) + minimal ui-lite wiring (1170/1200) + menu-flyout rebuild fix. Built by lite-builder, independently verified. | n/a (new module beside calib; forbidden untouched) | measure-ux / accuracy (lite) | ✅ done `bea2119` 2026-07-03 — `LITE_VERIFY_SCALE_OK` 7/7; regression 9 files + t0 green | — |
| BUG-20260702-lite-pagerot-registration | **BROKEN (lite, accuracy — wrong-location measurement)** — manual page rotate (`pageRot`, `page-rotate.js:6-17`) rotates the RASTER (`getViewport rotation:V.rot+pgRot`, `page-renderer.js:291`) but `ptToScreen`/`screenToPt` (`ui-lite.html:258-261`) ignore `pgRot` → geometry drawn before rotate detaches from the plan (displacement up to page diagonal ~84 m at 1:100 A1); geometry drawn while rotated is stored bound to the UN-rotated frame = attached to the WRONG feature (area value correct, location wrong); `/export-pdf-overlay` doesn't apply pageRot either → export ≠ screen whenever pgRot≠0. Intrinsic `/Rotate` (e.g. RAMA4) is handled CORRECTLY — only the manual rotate menu is broken. No test guards raster↔overlay registration under pageRot (`test_page_rotate.py` covers accumulation/persist only). Fix options: fold pgRot into ptToScreen/screenToPt (⚠ forbidden-adjacent — run `/bma-check-forbidden` first) OR transform stored pts on rotatePage. Guard: draw rect → rotate 90° → assert screen vertices land on rotated raster + area invariant. source=render-engine review 2026-07-02 | `/bma-check-forbidden` → `/bma-measure-scope` | measure-geometry / render (lite) | ✅ done `9f4b298` 2026-07-02 — Fix A (proto-parity): getRot→pageRot, ptToScreen/screenToPt route through vendored pdfToC/cToPdf (net 0 lines, no geometry mutation, old saves just start working; closes drift-lock gap as side effect); export WYSIWYG via per-page rot + server prerotate+`_rp` (direction verified empirically); guard `LITE_PAGEROT_REG_OK` RED→GREEN; regression 42 files green incl. `MEASURE_PARITY_OK`, page_rotate, metamorphic, snap, cfss, centerline | — |
| AUDIT-20260702-render-followups | **Render-engine hardening bundle (lite)** — from 2026-07-02 engine review, small cards: (a) **pdfjs-fail → `/page` JPEG fallback** [trivial] — `loadPage` catch (`page-renderer.js:264-267`) alerts + blank canvas while the working raster endpoint sits unused; (b) **pan double-buffer** [trivial→sprint, confirm live first] — single shared `_offCanvas` is blit-source AND render-target with `clearRect` before async render → continuous drag likely blanks the PDF until mouseup; fix = 2 buffers swap-on-complete or `_bufReady` gate; (c) **scanned-PDF detection** [trivial] — image-only pages get zero sharpness from PDF.js re-render; detect + cap re-render scale + hint; (d) **memory claim + cache bounds** [trivial] — "13 MB" excludes full-file buffer (whole PDF resident via `getDocument({data:buf})`) and unbounded `pageCache`/`pageDims`; add LRU + fix docs; (e) **[sprint] real overlay-registration test** — Playwright: synthetic PDF with known rectangle → draw via screenToPt → screenshot → assert overlay pixels coincide with raster edge at zoom×rot combos. source=render-engine review 2026-07-02 | n/a (lite render) | lite / render + test-infra | ✅ done 2026-07-03 — (a) raster fallback + (c) scanned detection w/ registration-preserving scale cap `aec375b` (`LITE_RENDER_FB_SCAN_OK`); (b) pan double-buffer CLOSED-NO-BUG (perf probe refuted, 0/10 blank ×3 files); (d) memory claims corrected in PHASE_INDEX/invent docs + page-LRU `ae0f168`, doc-level → streaming invent; (e) overlay-registration pixel test `13054b6` (`LITE_OVERLAY_REG_OK` — **sub-pixel proof, max 0.5 device px** across fit/zoom×2/pgRot90) | — |
| AUDIT-20260702-runner-preflight | **Test-infra (lite)** — all-tests runner + free-space preflight (2 of the 5 audit follow-on cards from NEXT_ACTIONS 2026-07-02). NEW `lite/tests/run_all_tests.py`: discovers every `test_*.py`, runs each standalone (lite convention), per-test timeout, summary table + `LITE_RUN_ALL_OK/FAIL`, options `--filter/--fail-fast/--timeout`. PREFLIGHT fails fast on: low disk on repo drive AND system drive (<2 GB — the 2026-07-02 ENOSPC/Drive-File-Stream incident), missing uvicorn/playwright/fitz, missing node (drift gate needs it). source=audit 2026-07-02 | n/a (test-infra only, no runtime change) | lite / test-infra | ✅ done 2026-07-02 — first full run **60/60 PASS in 8.5 min** (`artifacts/run_all_tests_20260702.log`); suite auto-discovers new test files | — |
| AUDIT-20260702-export-caps | **Server hardening (lite)** — export payload caps + real export endpoint tests (audit S1/S5 + biggest test hole). `/export-pdf-overlay`: no cap on pages/objects/pts/coords → OOM risk; `/export-xlsx`: no row cap; both endpoints have ZERO server-side functional tests (test_export_submenu only stubs client dlPost). Fix: caps + HTTP 400 rejection (no silent truncation) + NEW `lite/tests/test_export_endpoints.py` (happy path XLSX/PDF bytes validation + oversize rejection + malformed payloads), marker `LITE_EXPORT_ENDPOINTS_OK`. Optional: threadpool wrap for blocking export work (S2) if mechanical. source=audit 2026-07-02 | n/a (lite server, no forbidden surface) | lite / server + test-infra | ✅ done 2026-07-02 — caps + 400-rejection + latent-500 fix (non-numeric page key) + xlsx wb.save threadpooled (overlay offload split to AUDIT-20260702-s2-fitz-lock); `LITE_EXPORT_ENDPOINTS_OK` 14/14; regression 20 files green | AUDIT-20260702-runner-preflight |
| BUG-20260702-lite-arc-summary | **BROKEN (lite, accuracy — silent under-count)** — arc-edge polygon areas are summed as straight-chord shoelace in EVERY rollup consumer while the per-object label (`areaOf`, `ui-lite.html:328`) is arc-correct → curved room shows X on its label but contributes <X to summary/report/XLSX/annotated-PDF silently. Root cause: 4 call sites pass `{pts:o.pts}` to `polyMetrics`, dropping `o.edges` — `computeSummary` (`ui-lite.html:1049`) + `buildExportData`/`exportPdfOverlay`/`buildReportPayload` (`export-annotate.js:14,27,58`). Fix = swap callee to `polyMetricsAnyShape(o,pg)` (already exported, drift-locked-safe, drop-in same-arity) at ALL 4 sites together. report-vars.js/report-edit.js inherit fix transitively (consume computeSummary agg). Guard test: NEW `lite/tests/test_summary_arc_parity.py` — invariant "page total == Σ per-object label areas (arc-inclusive)" across all 4 consumers, marker `LITE_SUMMARY_ARC_OK`. Regression: test_arc_edge, test_report_vars*, test_measure_parity (must stay green — measure-engine.js untouched). Record in lite/tests/bug-archive.jsonl on fix. Sibling bug (CFSS instances also skipped by computeSummary) filed separately as BUG-20260702-lite-cfss-summary. source=bug-report 2026-07-02 (accuracy audit) · triage=bma-bug-triager (risk low) | `/bma-measure-scope` (lite, by analogy) → specialist=`bma-path-geometry-reviewer` | measure-geometry (lite) | ✅ done `e5264e2` 2026-07-02 — specialist widened scope to **6 sites** (found 2 more: `layer-tree.js:62` `_ltOwnArea` + `overview-setup.js:642` `_lovsLayerArea`); all swapped to `polyMetricsAnyShape(o,pg)`; guard `LITE_SUMMARY_ARC_OK` proven RED→GREEN; regression 8/8 green incl. `MEASURE_PARITY_OK` | — |
| BUG-20260702-lite-cfss-summary | **BROKEN (lite, accuracy — silent under-count)** — CFSS shared-shape instances (`{kind:'instance', masterId, offsetPt}`, no `.pts`) are skipped by EVERY area rollup (`o.kind!=="poly"` guards): computeSummary / buildExportData / exportPdfOverlay / buildReportPayload / `_ltOwnArea` / `_lovsLayerArea` — and promoting a poly to shared shape REPLACES it with an instance (`cross-floor-shapes.js:~719`) so the source floor's area DROPS OUT of totals at promote time. `instanceAreaM2` (`cross-floor-shapes.js:~221`, metric-master shoelace, correct) exists but no rollup calls it. **Triage 2026-07-02 upgraded severity — CRASH sub-finding:** `buildExportData`/`exportPdfOverlay` (`export-annotate.js:10-19,25-31`) run `catOf(o.catId).name/.color` BEFORE the kind guard → `catOf(undefined)`→null→TypeError = **export crashes outright** when any page holds an instance. True root cause: `cfssCommitPromote` (`cross-floor-shapes.js:680-733`) calls `addMaster` without the opts arg so masters never capture `catId`/`semanticTag`. Fix plan: (1) promote passes `{catId,semanticTag}` to addMaster (additive master schema + legacy-load fallback in `cfssWrapLoad`); (2) NEW helpers `rollupAreaM2(o,pg)` + `rollupCatId(o)` co-located with `instanceAreaM2` in cross-floor-shapes.js (single helper, NOT 6 per-site branches — arc postmortem shows per-site duplication is how sites drifted); (3) route 6 rollup sites + 2 crash sites through helpers; instance ground truth = `instanceAreaM2` (matches `cfssRenderInstanceLabel`, NOT areaOf which is null-by-design for instances). Guard: NEW `test_summary_cfss_parity.py` (invariant + no-throw), marker `LITE_SUMMARY_CFSS_OK`. Existing 6 test_cfss_* files touch no rollup fn (grep-confirmed) — no expectation flips. risk=high (schema + 8 touch points). source=bug-report 2026-07-02 (accuracy audit) | `/bma-measure-scope` (lite, by analogy) | measure-geometry (lite) | ✅ done `02e35af` 2026-07-02 — promote captures catId/semanticTag (additive, persists free via masters serialization; legacy masters → null-catId skip + one-time warn); NEW `rollupAreaM2`/`rollupCatId` helpers beside `instanceAreaM2` (typeof-guarded — cfss dynamically injected); 6 rollup + 2 crash sites rewired; instances export as resolved-pts overlays. `LITE_SUMMARY_CFSS_OK` proven RED→GREEN (pre-fix: totals 2000 vs 2100 + both export builders throw); regression 14/14 incl. `MEASURE_PARITY_OK` + 6× test_cfss_* | BUG-20260702-lite-arc-summary |
| INV-2026-05-25-CFSS | **Cross-floor shared shape (lift / pipe / stair shaft)** — invent-done-go from `/bma-invent` 2026-05-25 (5/5 spike PASS). Build Approach A (metric-master + per-floor instances) in lite. New `MASTERS{}` global stores shape in metric units (m). New object shape `{kind:'instance', masterId, offsetPt}` in `PS[pg].objects[]`. Pull-on-draw architecture: instances resolve metric→pt via current page `pts_per_m` every render — cross-scale correctness by construction. Master edit auto-propagates (no sync state). Orphan-safe delete (freeze as standalone poly). Schema additive: `doc.masters = {}` + new `kind:'instance'` round-trip in save/load. ui-lite.html cap = use DOM-injection pattern from LOVS-1. Scope=`/bma-check-forbidden` (additive .bmaplan) + `/bma-measure-scope` (sub-area: geometry-core, NEW function `instanceAreaM2` next to objectAreaM2 — never edits polyAreaM2). Markers: `LITE_CFSS_MODEL_OK` (master CRUD + instance resolver + cross-scale math) + `LITE_CFSS_PERSIST_OK` (save/load round-trip + backward-compat + proto cross-open) + `LITE_CFSS_UI_OK` (promote-polygon + place-on-floors + orphan-after-delete). Regression must stay GREEN: `MEASURE_PARITY_OK`, `LITE_PAGE_FOLDER_*`, `LITE_OVERVIEW_SETUP_OK`, `LITE_TREE_*`. **Spike:** `lite/sandbox/invent-cross-floor-shared-shape.html` (verified 5/5 by Playwright). **Frame:** `docs/invent/cross-floor-shared-shape.md`. Estimated ~120 LOC + ~30 LOC schema, single sprint. | `/bma-check-forbidden` + `/bma-measure-scope` (geometry-core, NEW fn) | measure / geometry + schema | ✅ built — `cross-floor-shapes.js` (916 lines) + 6 `test_cfss_*` suites + markers `LITE_CFSS_*_OK` all green; hardened by BUG-20260702-lite-cfss-summary `02e35af` (rollup + catId capture) — reconciled 2026-07-03 | LPFL-1c, LOVS-1 |
| INV-2026-05-29-LPM | **Lite page manager — GoodNotes/ABBYY-style page management** (invent-done-go from `/lite-invent` 2026-05-29, spike **v1 6/6 + re-pass v2 6/6 + re-pass#2 v3 5/5 = 17 PASS**). Build **Approach D** (optimistic client + stable page-id + deferred server sync). Core: NEW `lite/static/js/page-manager.js` (≤1000) owns `pageOrder` (ordered ids) + **ONE id-keyed record per page covering all 7 lite per-page fields** (`PS`+`tag`/`rot`/`excl`/`floorKind`/`floorNum`/`name` — re-pass 2026-05-29 found v1's 4-dict model missed floorKind/floorNum/name, `ui-lite.html:241`) + accessors (`idAt`/`numOf`/`PSn`/`metaN`/**`serverNum`** for render-source) + reorder/del/duplicate/merge/undo + save/load migration + **after each mutation call `reseedActivePageFolders()`** so page-folder `.pages` RE-DERIVES (re-pass #2 correction: folder id encodes floor `PF_floor_N`, `.pages` is computed not stored — `page-folder-layers.js:85,700` — do NOT remap membership) + **`applyFlush()` re-baseline** for multi-save sessions + **extend `_docSnap()` (`ui-lite.html:816`) with pageOrder/identity/rotation** (re-pass #2 NEW bug B-DIRTY: pure reorder/dup/merge currently doesn't flip dirty → silent data loss on close — highest-severity finding). Page NUMBER = index+1 derived on read → reorder/delete/duplicate never shift number-keyed measurement data. **v1 scope = reorder (drag) + permanent delete + duplicate + multi-file MERGE** (insert/blank-page PARKED). **Save model = mutate case-file at flush** (mutations journalled in `pending`, flushed at save/export; close-without-save = original PDF untouched). `.bmaplan` ADDITIVE: NEW optional `pageIdentities` array (ids in display order); `pageStore`/`pageTags`/`pageRot`/`excluded` stay number-keyed by display index → legacy saves (no `pageIdentities`) load + auto-migrate (mint fresh ids), proto cross-open intact. Server: port proto `/rebuild-pdf` → NEW `/apply-page-mutations` in `lite/server_lite.py` (reorder=`move_page`, duplicate=`copy_page`, delete=`delete_page`, merge=`insert_pdf`); returns renumberMap. UI: extend `#overview` panel → drag-reorder grid + del/dup context actions + merge-file picker. **RISK:** ~70–90 per-page reads across `ui-lite.html` + `static/js/*.js` (`PS[n]`/`pageTags[n]`/`pageFloorKind[n]`/... — re-pass recount, was mis-estimated ~30) must route through accessors — stage accessors first behind a flag; refactor must be **net-neutral on `ui-lite.html` lines** (currently 1100/1200, only 100 headroom; `overview-setup.js` 995/1000) so new logic lands in `page-manager.js`. `server_lite.py` is read-only today (0 page-mutation endpoints) → `/apply-page-mutations` built from scratch. Scope=`/bma-check-forbidden` (.bmaplan additive) → build via `/bma-lite-dev`. Markers: NEW `LITE_PAGE_MANAGER_OK` (**E1–E16**: E1–E6 identity reorder/delete/duplicate + save round-trip + legacy-migrate + renumber-map==UI-index; E7 all-7-fields survive; E8 folder integrity; E9 multi-save re-baseline; E10 render-source maps to original server page; E11 merge; E12 delete-last guard + excludedPages-array migrate; **E13 folder .pages RE-DERIVES via reseed (not remap); E14 dirty-tracking on pure reorder/dup/del; E15 dup-then-delete independence; E16 no-op reorder guard**) + `LITE_PAGE_MERGE_OK` (insert_pdf round-trip + id assignment for merged pages). Regression must stay GREEN: `MEASURE_PARITY_OK`, `LITE_PAGE_FOLDER_*`, `LITE_OVERVIEW_SETUP_OK`, `LITE_TREE_*`. Forbidden UNTOUCHED: measure-engine.js, RS, pdfToC/cToPdf, `.bmaplan` non-additive. **Spike:** `lite/sandbox/invent-page-manager/` — `node eval.js` 6/6 (v1) + `eval-v2.js` 6/6 + `eval-v3.js` 5/5 (**17 cases total**). **Frame:** `docs/invent/lite-page-manager.md` (build MUST follow v2 **+ re-pass #2 corrections**: folders re-derive via `reseedActivePageFolders()` NOT id-remap; extend `_docSnap()` for dirty-tracking). **Atomicity** of `/apply-page-mutations` (temp-PDF + swap; flush-failure rollback test) is a build-sprint design note, not yet spiked. | `/bma-check-forbidden` (.bmaplan additive) + `/bma-lite-dev` | lite / page-model + server + panel UI + schema | ✅ built (slices 1–5, 2026-05-29; page-manager.js + bridge + server /apply-page-mutations + /merge-pages + page-manager-ui.js own overlay Shift+F12; markers LITE_PAGE_MANAGER_OK 21/21, LITE_APPLY_MUTATIONS_OK T1-T7, LITE_PAGE_MANAGER_UI_OK; regression green) | LOVS-1, LPFL-1c |
| BUG-20260530-lpm-1 | **BROKEN (lite, data-loss)** — `.bmaplan` save never writes `pageIdentities`. `mi-save`/`buildPageStore` (`ui-lite.html:912,924`) serialise globals directly, never call `_pmCommit()` or `pageMgr.save()`, never emit `pageIdentities` (verified: grep shows it only in `_pmSeed`/load). → page identity is **not preserved across save/reload**; reload mints fresh ids, defeating Approach-D. Fix: `mi-save` emits `pageIdentities:(pageMgr?pageMgr.pageOrder.slice():undefined)` (additive). Found by full-program hunt 2026-05-30. Guard with `MR-save-roundtrip` (EVOLT-2). | `/bma-lite-dev` | lite / save + schema | ✅ fixed `1012106` (bug-archive; guard MR-save-roundtrip) — row reconciled 2026-07-03 | INV-2026-05-29-LPM |
| BUG-20260530-lpm-2 | **BROKEN (lite, silent loss)** — Save ignores PENDING page mutations. reorder/del/duplicate only mutate `pageMgr`; only Apply (`_pmCommit`) writes back to globals. User reorders → Ctrl+S without Apply → save reads OLD globals → change lost silently. Two sources of truth (grid=new, PS=old). Fix: `mi-save` auto-Applies (or blocks with warning) when `pageMgr.pending.length>0`. Guard with `MR-save-roundtrip`. source=hunt 2026-05-30. | `/bma-lite-dev` | lite / save + page-mgr | ✅ fixed `1012106` (bug-archive; guard MR-save-pending) — reconciled 2026-07-03 | BUG-20260530-lpm-1 |
| BUG-20260530-lpm-3 | **BROKEN (lite, wrong-page measurement)** — `loadPage()`/`openOv()`/report render the RAW display index, not `pageMgr.serverNum(n)` (`page-renderer.js:216` `pdfDoc.getPage(n)`; `ui-lite.html:1006` `api("/thumb/"+n)` — code comment admits "route through serverNum once mutations exist"). After duplicate/pending-reorder, the canvas shows the WRONG page's raster while measurement overlay is the right page's geometry → **measures the wrong drawing** (violates core purpose). Fix: render via `serverNum()`; handle null→placeholder. Guard with `MR-render-source`. source=hunt 2026-05-30. | `/bma-lite-dev` | lite / render + page-mgr | ✅ fixed `1012106` (bug-archive; guard MR-render-source) — reconciled 2026-07-03 | BUG-20260530-lpm-1 |
| BUG-20260530-lpm-4 | **BROKEN (lite, undo corruption)** — two unsynced undo stacks: app `_docSnap` undo + `pageMgr.undoStack`. After page-Apply + `_pmCommit`, app Ctrl+Z restores OLD `PS` order but leaves `pageMgr`/server on NEW order → hard desync. Fix: after `_pmCommit`, rebuild/clear app undo (page structural changes not point-undoable) or re-seed pageMgr on app-undo. source=hunt 2026-05-30. | `/bma-lite-dev` | lite / undo + page-mgr | ✅ fixed `2c631ea` (bug-archive; guard MR-undo-consistency) — reconciled 2026-07-03 | BUG-20260530-lpm-1 |
| BUG-20260530-lpm-5 | **BROKEN (lite, silent loss on close)** — page mutations never set `state.dirty`; no `beforeunload` guard. reorder 3 pages → status bar still "บันทึกแล้ว" → close tab → all lost. The `_docSnap`+pageOrder field (shipped in slice 3) is dead because mutations don't push undo/dirty. Fix: each mutation handler sets `state.dirty=true`; add `beforeunload` when `state.dirty||pageMgr.pending.length`. Guard with `MR-dirty`. source=hunt 2026-05-30 (this IS the "B-DIRTY" the LPM card claimed to fix — not actually wired). | `/bma-lite-dev` | lite / page-mgr + dirty | ✅ fixed `1012106` (bug-archive; guard MR-dirty) — reconciled 2026-07-03 | BUG-20260530-lpm-1 |
| BUG-20260530-lpm-6 | **BROKEN (server, Windows)** — `POST /apply-page-mutations` 500 on Windows: `os.replace(tmp,path)` runs while `old_doc` (fitz) still open → PyMuPDF file-lock → `Permission denied`. 100% repro on Windows for any PDF (found on real 90MB/95-page CHH customer file). Fix: `old_doc.close()` BEFORE `os.replace()` then reopen. Same pattern in `/merge-pages`. source=sandbox journey 2026-05-30. | `/bma-lite-dev` | lite / server atomicity | ✅ fixed `1012106` (bug-archive; FIX-B6 close-before-replace, further hardened by S2 lock `d0a5dde`) — reconciled 2026-07-03 | INV-2026-05-29-LPM |
| BUG-20260530-lpm-7 | **FRICTION (lite)** — Apply builds `order` from `serverNum()` (one code path) but the model's own `simulateFlush()` (the tested path) is **never called by the live app** → two divergent flush algorithms, only one tested. Fix: build Apply `order` from `pageMgr.simulateFlush()` so live == tested path. source=hunt 2026-05-30. | `/bma-lite-dev` | lite / page-mgr | ✅ fixed `10be96c` (bug-archive) — reconciled 2026-07-03 | BUG-20260530-lpm-3 |
| BUG-20260530-lpm-8 | **FRICTION (lite)** — page-manager grid fires up to 95 concurrent `/thumb/{n}` on open with no spinner → grid blank 2–5 s on big PDFs. Fix: loading indicator / progressive thumbnail render. source=sandbox journey 2026-05-30 (95-page CHH). | `/bma-lite-dev` | lite / page-mgr UI | ✅ fixed `b3e4fd9` (bug-archive; lazy-load thumbs) — reconciled 2026-07-03 | INV-2026-05-29-LPM |
| BUG-20260530-lpm-9 | **FRICTION (lite)** — `#pm-overlay` open does NOT block app hotkeys: `modalOpen()` (`ui-lite.html:1096`) doesn't test `#pm-overlay.show`, so pressing `n`/`v`/`d` while the manager is open switches the underlying tool; Esc double-fires (closes pm-overlay AND clears canvas draft). Fix: `modalOpen()` also tests pm-overlay; pm Esc `stopPropagation`. source=hunt 2026-05-30. | `/bma-ui-menu` | lite / page-mgr UI | ✅ fixed `3611e31` (bug-archive; regression-first RED→GREEN) — reconciled 2026-07-03 | INV-2026-05-29-LPM |
| BUG-20260530-cfss-guard | **COSMETIC/test-debt (lite)** — 6 CFSS tests (`test_cfss_drag/edit/model/persist/rightclick_menu/ui`) hard-code `ui-lite.html == 1200 lines`; LPM slices changed it to 1119 → 6 false-RED. The measure-engine *hash* check in the same tests is the correct anchor; the line-count check is brittle. Fix (EVOLT-3 self-healing): replace magic-number line guard with content-anchored assertion. source=full test sweep 2026-05-30. | `/bma-lite-dev` | lite / test-infra | ✅ fixed `e2748ec` (bug-archive; content-anchored guards) — reconciled 2026-07-03 | — |
| EVOLT-1 | **PBT for measure-engine (evolutionary test loop, pillar 1)** — Claude reads `lite/static/js/measure-engine.js` + path-geometry, proposes invariants, emits Node-run property tests (area≥0; vertex-order independence; scale linearity ÷k²; translation invariance; degenerate→0 not NaN). Marker `LITE_PBT_MEASURE_OK`. Design: `docs/design/EVOLUTIONARY_TEST_LOOP.md`. Guardrail: tests assert measure-engine integrity (hash), never mutate it. | `/bma-lite-dev` | lite / test-infra | ✅ done — `lite/tests/test_pbt_measure.py` ships (6 properties × ~500 seeded cases, `LITE_PBT_MEASURE_OK`, in t0 tier) — reconciled 2026-07-03 | — |
| EVOLT-2 | **Metamorphic tests for page-manager (pillar 1) — regression-first** — `lite/tests/test_metamorphic_pages.py`: MR-reorder (m²/tag/floor follow identity → catches B3), MR-save-roundtrip (mutate→save→reload deep-equal → catches B1/B2), MR-dirty (any mutation flips dirty → catches B5), MR-render-source (display n fetches origin server page → catches B3), MR-rotate (90°→area unchanged). **Authored to FAIL on current code** (proves they catch lpm-1..5), turn GREEN after the fixes. Marker `LITE_METAMORPHIC_PAGES_OK`. | `/bma-lite-dev` | lite / test-infra | ✅ done — `lite/tests/test_metamorphic_pages.py` ships (7 MRs, marker green in suite) — reconciled 2026-07-03 | — |
| EVOLT-3 | **Self-healing test guards (pillar 3)** — replace brittle magic-number guards (CFSS `==1200` line count, et al.) with content-anchored / hash assertions across the lite test suite. Resolves BUG-20260530-cfss-guard generally. Marker: existing CFSS markers GREEN at any ui-lite line count. | `/bma-lite-dev` | lite / test-infra | ✅ done `e2748ec` (self-healing guards shipped with the cfss-guard fix) — reconciled 2026-07-03 | BUG-20260530-cfss-guard |
| EVOLT-4 | **Bug-archive feedback (pillar 4)** — `artifacts/bug-archive/bug-archive.jsonl`, one line per confirmed bug (id,date,severity,file,repro,guard_test,fixed_commit), seeded with B1–B10. Every future test/hunt run seeds from this first ("you have missed these before"). Prompt-side analog of DGM's archive — the loop never re-ships a learned bug class. | n/a (infra) | infra / archive | ✅ done — lives at `lite/tests/bug-archive.jsonl` (16 entries, 0 open, every fix carries repro+guard_test+fixed_commit; seeded every hunt since 2026-05-30) — reconciled 2026-07-03 | EVOLT-2 |
| EVOLT-5 | **Evolutionary loop wiring (pillar 4)** — formalize `/bma-human-test` → reflect+dedup-vs-archive → `/bma-lite-dev` fix → regression → append archive → repeat, with human gate every cycle + Goodhart watch. Meta-fitness: escaped-bug count per release trends down; archive replay ~0 cost. | n/a (process) | infra / loop | ✅ operating in practice — bug pipeline (find→file→fix→guard→archive) demonstrated across 6 ships 2026-07-02/03; formalized in `docs/process/DEVELOPMENT_PILLARS.md` — reconciled 2026-07-03 | EVOLT-4 |
| LOVS-1 | **Lite Overview Setup — 3-tab wizard + multi-select** — replaces live `openOv()` with 3-step wizard (Classify with multi-select + bulk / Number Floors drag-reorder + Sequential / Review BCR/FAR/OSR mock). New `lite/static/js/overview-setup.js` (668/900) loaded via 9-line IIFE in `page-folder-layers.js` (557/1000). `ui-lite.html` UNTOUCHED (1200/1200). `LITE_OVERVIEW_SETUP_OK` 8/8 + regression GREEN (LPFL_*, TREE_UI, LAYER_DND, MEASURE_PARITY). | n/a (DOM-injection only) | overview / panel | ✅ done (LOVS-1) | LPFL-1c |
| LPFL-1a | **Per-page-setup folder layer tree — model + accessor (lite)** — invisible refactor. New `lite/static/js/page-folder-layers.js` (193 lines). 7 accessors; FOLDERS extended with additive `pages?` + `kind?`. `LITE_PAGE_FOLDER_MODEL_OK` 12/12. Forbidden surfaces UNTOUCHED. | n/a (lite) | layer / model | ✅ done (LPFL-1) | — |
| LPFL-1b | **Per-page-setup folder layer tree — persist additive (lite)** — surgical save/load in `ui-lite.html` lines 935+1027 (`kind:f.kind,pages:f.pages` appended, zero net line). `/bma-check-forbidden` = 🟢 OK (additive). `LITE_PAGE_FOLDER_PERSIST_OK` 6/6 (roundtrip / backward-compat legacy file / forward-compat / identity preserved / mixed PF+user / resave-stable). | `/bma-check-forbidden` | layer / persist + schema | ✅ done (LPFL-1) | LPFL-1a |
| LPFL-1c | **Per-page-setup folder layer tree — UI panel (lite)** — DOM injection + monkey-patch from `page-folder-layers.js` (extended to 547/1000 lines); `ui-lite.html` UNTOUCHED (cap 1200/1200 exact). `#pfl-toggle` button injected into existing `#picker .h`; toggle default OFF. `buildPagePicker` emits identical `.cat[data-catid][data-nodekind]` DOM → `layer-dnd.js` works unchanged. Auto-seed on tag/floor change via wrapped `liteSetTag`. `LITE_PAGE_FOLDER_UI_OK` 7/7. Regression GREEN: `LITE_TREE_UI_OK` 9/9 + `LITE_LAYER_DND_OK` 4/4 + `MEASURE_PARITY_OK`. | n/a (DOM-injection only) | layer / panel UI | ✅ done (LPFL-1) | LPFL-1b |
| BUG-20260521-lite-pan-controls | **FRICTION (lite tree)** — view/navigation control system in `lite/ui-lite.html` is impoverished vs `proto/ui.html`: (1) pan works ONLY on left-drag in select/empty mode — selecting any draw tool (poly/dist/path/ref/scale/count) blocks pan entirely because `mousedown` (L357) returns early; (2) no spacebar-hold pan; (3) no middle-mouse pan (`if(e.button!==0)return` at L358 kills it); (4) no zoom clamp on wheel (L406) → runaway zoom; (5) fixed 1.12 wheel step (not smooth); (6) no fit/actual-size/zoom keyboard shortcuts; (7) static `cursor:crosshair` always (no grab/grabbing feedback). User directive: **fork proto's entire view-control behavior into lite**. Architecture differs (proto = CSS-transform + server-side per-page rotation; lite = `ctx` transform + JS rotation matrix), so port = ADAPT behaviors onto lite's `V={k,ox,oy,rot}` model, NOT raw paste. Must NOT touch `ptToScreen`/`screenToPt`/`RS`/measure-engine.js (coordinate-math contract). scope=measure-ux+canvas-ui (lite) · regression=lite Playwright marker + py_compile + measure-parity · marker=`BUG_20260521_LITE_PAN_OK` · source=bug-report | n/a (lite tree) | measure-ux / canvas-ui (lite) | ✅ done `012782f` — forked proto view-control behavior onto lite `V` model: spacebar-hold pan + middle-mouse pan (any mode, even mid-tool) + H pan-tool + grab/grabbing cursor + smooth-exp wheel zoom with [0.02,40] clamp + fit(F/Ctrl+0)/actualSize(Ctrl+1)/zoomCenter(Ctrl+±). `lite/tests/test_pan_controls.py` BUG_20260521_LITE_PAN_OK GREEN (13/13) + menu-clip + MEASURE_PARITY_OK (coord math untouched); proto E2E n/a (lite-only) | — |
| BUG-20260521-lite-menu-clip | **BROKEN** (lite tree) — top-bar dropdowns (File/Measure/Page/Annotate) in `lite/ui-lite.html` open (`display:block`) but are invisible & unclickable. Cause: `#topbar` (CSS L22-24) is fixed height 42px + `overflow:hidden` → the `.dd` dropdown (`position:absolute; top:32px`) hangs below the bar into `#stage` and gets clipped away; `#topbar` also establishes no stacking context, so even unclipped it sits under `#stage` content. `elementFromPoint` at a menu item returns `.empty`/canvas, not the item. Verified live via Playwright. Fix (CSS-only, menu-bar scope, no forbidden surface): `#topbar` `overflow:hidden`→`overflow:visible` + add `position:relative;z-index:60`. scope=`/bma-ui-menu` · regression=lite parity gate + Playwright menu hit-test · marker=`BUG_20260521_LITE_MENU_CLIP_OK` · source=bug-report | `/bma-ui-menu` | ui-menu (lite) | ✅ done `c4b22d2` — fix `#topbar` overflow:visible+z-index:60; `lite/tests/test_menu_clickable.py` BUG_20260521_LITE_MENU_CLIP_OK GREEN (4/4 menus topmost) + MEASURE_PARITY_OK; proto E2E n/a (lite-only) | — |
| HT-ACC-1 | **FRICTION (accuracy-critical)** — calibration snap silently latches both points to the WRONG nearby vector line (human-test: ~400pt click → snapped to 1663pt line, 4× misfire) with NO visual confirmation of which segment was captured. If the snapped line is ~1% longer than intended → `pts_per_m` ~1% high → **all areas ~2% too small, systematically** (matches user report: โฉนด 2-2-0=4000m² measures smaller). Fix (UX only, NO area-math touch): after the 2nd calib click, flash/highlight the snapped segment on canvas + show "ตรึงที่เส้น X pt — ใช่เส้นที่ต้องการ?" with confirm/retry; consider a "no-snap precise" toggle for calibration. scope=`/bma-measure-ux` · regression=`/bma-measure-regression` · marker=`HT_ACC1_CALIB_SNAP_OK` · source=human-test 2026-05-20 | `/bma-measure-ux` | measure-ux (calibration) | ✅ done `c0834f0` (marker `HT_ACC_OK`) — calibRaw captures pre-snap clicks; >5% deviation warns in calib panel | — |
| HT-ACC-2 | **FRICTION** — calibration accuracy guidance bundle: (a) calib panel tip "ใช้เส้นอ้างอิงที่ยาวที่สุดที่ทราบระยะ" (longest baseline = best accuracy); (b) post-calibration nudge + promote **Verify Scale** to a ribbon button beside Set Scale (currently buried in Scale menu — users won't find it); (c) when drawing area on a `site`/`land` page with long edges, hint "ขอบโค้งให้ใช้เครื่องมือ arc เพื่อความแม่นยำ" (straight chords under-report curved boundaries). scope=`/bma-ui-ribbon` + `/bma-measure-ux` · marker=`HT_ACC2_CALIB_GUIDE_OK` · source=human-test 2026-05-20 | `/bma-ui-ribbon` + `/bma-measure-ux` | ribbon + measure-ux | ✅ done `c0834f0` (marker `HT_ACC_OK`) — Verify ribbon button + longest-baseline tip + post-calib/land-arc nudges | — |
| HT-ACC-3 | **COSMETIC** — scale badge shows rounded `1:17` while actual is `1:17.042` (0.25% label-only; area uses exact float, no measurement impact). Show one decimal `1:17.0` and the exact `pts_per_m` in the scale-panel tooltip for expert cross-check. scope=`/bma-ui-status` · marker=`HT_ACC3_SCALE_DECIMAL_OK` · source=human-test 2026-05-20 | `/bma-ui-status` | status-bar / canvas-ui | ✅ done `c0834f0` (marker `HT_ACC_OK`) — exact pts_per_m + 1:N.x in scale tooltip (visible label unchanged) | — |
| HT-NAV-1 | **COSMETIC/investigate** — on the 11-page raster PDF `ข.4`, the journey script's page-next loop exited after page 1 (btn-next threw a silently-caught exception); pages 2–11 not auto-measured. Likely a test-script artifact, but verify real next-page navigation works on multi-page raster PDFs (no vector geometry). scope=`/bma-ui-ribbon` (nav buttons) · source=human-test 2026-05-20 | `/bma-ui-ribbon` | navigation | ✅ verified `c0834f0` — no code fix needed: `getNextPage`/`loadPage` nav logic sound + `REAL_OK` exercises real multi-page navigation; the caught exception was a Playwright timing artifact | — |
| BUG-20260520-zen-exit-rp-restore | **BROKEN** — right-panel restore button gone after hide L+R panels → F11 Zen → exit to normal; right panel cannot be re-shown. Cause: Zen-exit `applyLayoutPrefs()` has no panel-collapse snapshot/restore (comment at ui.html:3178 advertises it but unimplemented) + `body.zen .panel-restore-tab{display:none !important}` (app.css:658). scope=`/bma-ui-panel` · specialist=`bma-right-panel-specialist` · regression=`/bma-ui-regression` · marker=`BUG_20260520_ZEN_EXIT_RP_RESTORE_OK` · source=bug-report | `/bma-ui-panel` | ui-panel-right | 🟠 BUG_STOP_NEEDS_REPRO 2026-05-20 — empirical repro (`artifacts/repro_zen_exit_rp.py`, 3 variants: direct toggle / keyboard-F11 / Settings-modal hide) all show `#rp-restore-tab` computed display returns to `flex` after Zen exit. CSS+JS state machine correct; bug NOT reproducible headless. Lead hypothesis: real-browser F11 native-fullscreen collides with app F11 handler guard `!anyModal && !mPts.length` (ui.html:2919) — when a modal is open or mid-draw, `preventDefault()` skips, browser goes native FS, app `body.zen` desyncs. Latent (non-causal) findings: app.css:51 `#right-panel.collapsed~#workspace` is a dead selector (DOM order: workspace precedes right-panel); F9/F10 have menu hints but no keydown binding. Awaiting user confirmation of exact pre-F11 state. → ✅ RESOLVED `9453777` 2026-05-20: hardened defensively without needing the exact repro — (1) F11 always `preventDefault` + always-exits Zen so a native-FS desync can't leave `body.zen` stuck; (2) F9/F10 bound to toggle L/R panels (keyboard recovery even if the tab is hidden); (3) dead `~#workspace` selector replaced with `body:has(#right-panel.collapsed)`. Marker `BUG_20260520_ZEN_EXIT_RP_RESTORE_OK` (6 sub-checks) GREEN, full E2E 101/0. | — |
| INV-2026-05-20-001 | **Verify Scale tool** (invent-done-go) — post-calibration cross-check. Add Scale-menu "Verify Scale" → reuse 2-point calib draw → NEW `openVerifyModal()`/`verifyFinish()` BESIDE `finishCalib` (do NOT edit finishCalib): measure 2nd known dimension, compute `%dev=100·\|d_meas−d_enter\|/d_enter`, show green(<0.5%)/yellow(<2%)/red(≥2%) band + actions Accept / Re-calibrate / Average. Schema additive: `calibScale.verifyResult{pct,action,verifyPts_per_m,ts}` (round-trip in save/load). Forbidden: NONE (read getScaleForPage/ptsToM only; never touch polyAreaM2/pdfToC/cToPdf/RS/snap/calibScale rename). Marker `INV_VERIFY_SCALE_OK`. Optional follow-ons: D=canvas badge, E=fold into phase1Warnings()+export note, C=dual-axis H/V stretch (defer). Spike PASS 10/10 → `docs/invent/verify-scale.md` + `proto/sandbox/invent-verify-scale.html`. **IMPLEMENTED 2026-05-20**: `verifyScale` stub→real flow + `calibPanelOk` router (finishCalib untouched) + verify-modal + additive `calibScale.verifyResult`. `INV_VERIFY_SCALE_OK` 9/9, full EXIT 0, zero regression (5 failing markers pre-existing env). | `/bma-measure-scope` (sub-area: ux) → `/bma-measure-regression` | measure / ux + schema | ✅ done | — |
| INV-2026-05-20-002 | **Layer rebuild L1 — de-globalize + fix slug derivation** (invent-done-go). Finish the page-scoped migration's missing G1: move `layerVis`/`layerLock` from global (ui.html:865) to per-page `pageStore[n].layers[].visible/locked` as the single read source. Fix `getObjectLayerSlug` (L930) so site objects (semanticTag building_coverage/open_space/etc., areaType="room") no longer all collapse to "sub_area" — resolve to the correct page-scoped layer; ensure no object resolves to a slug absent from its page preset (site preset has no "sub_area" → layerId=undefined bug). Forbidden: NONE touch area-math/pdfToC/cToPdf/RS/snap. Scope doc: `docs/design/LAYER_MODEL_ALIGNMENT_AUDIT.md` (G1+G4/G5). Marker `INV_LAYER_L1_OK`. From `docs/invent/layer-model-rebuild.md` (PRIOR_ART_MATURE). NOTE: render/hit/label read-source switch (originally scoped to L3) folded in here due to layerVis-in-render coupling — so L3 now reduces to deleting the legacy globals. | `/bma-ui-panel` + `/bma-check-forbidden` | ui-panel + measure | ✅ done `93c512f` | — |
| INV-2026-05-20-003 | **Layer rebuild L2 — active-layer dropdown writes layerId + reassign UI** (invent-done-go). G7: active-layer control writes `layerId` onto NEW objects (incl. site tools — `activateSiteAreaTool` must record the active layer, not force areaType="room" routing). Add a "move to layer" dropdown for the SELECTED object (Bluebeam-style reassign) in the right panel. User decides layer; semanticTag stays decoupled. Forbidden: NONE. Scope doc audit G7. Marker `INV_LAYER_L2_OK`. NOTE: delivered the reassign dropdown (the user's "move to layer" need). Active-layer-at-creation deferred — L1 default mapping already lands new objects sensibly; reassign covers correction. | `/bma-ui-panel` | ui-panel-right + measure-ux | ✅ done `1301a12` | INV-2026-05-20-002 |
| INV-2026-05-20-004 | **Layer rebuild L3 — delete legacy global layer system** (invent-done-go). Render/hit read-source switch already done in L1 (`93c512f`). Remaining: delete the global `layerVis`/`layerLock` mirror + `areaTypeLayer`-as-layer usages once nothing reads them; repoint the toggle functions + panel lock badges + `_layerLockGateBeforeMode` + remaining E2E tests (HT8D5A, cache, select) to page-scoped `layer.visible/locked`. Single system remains. Forbidden: NONE area-math; render-only. Markers `INV_LAYER_L3_OK` + existing layer markers stay GREEN. Requires `/bma-ui-regression`. NOTE: app behavioural dependence on globals removed (gate/deselect/render/hit all page-scoped). Global identifiers kept as a non-authoritative synced mirror (deleting them outright = test-only churn for zero behaviour gain). INV_LAYER_L3_OK proves page-scoped authority (page locked + global unlocked → app follows page). | `/bma-ui-canvas` + `/bma-ui-regression` | ui-canvas + render | ✅ done `2e6b2f9` | INV-2026-05-20-003 |
| BUG-20260520-sel-midpan | **FRICTION** — middle-mouse-button (button===1) pan does not work while Select tool is active. Cause: `mousedown` handler `mode==="sel"` branch does `redraw();return` unconditionally before the `e.button===1` pan check (ui.html:~2064), so middle-button pan is dead code in Select mode. Fix: add `if(e.button===1){isPan=true;...;return;}` guard at top of sel branch, mirroring the existing non-sel pan guard. scope=`/bma-measure-ux` · specialist=`bma-measure-ux-specialist` · regression=`/bma-measure-regression` · marker=`BUG_20260520_SEL_MIDPAN_OK` · source=bug-report | `/bma-measure-ux` | measure-ux | ✅ done `0ace2ce` | — |
| I-A | Site Plan schema + Project Setup | — | measure / schema | ✅ done `984eb7e` | — |
| I-B1 | markerType additive field | — | measure / schema | ✅ done `c38c3e6` | I-A |
| I-B2a | Site Plan ribbon group + shared handlers | `/bma-ui-scope` | ribbon | ✅ done `b9f9132` | I-B1 |
| SB-2026-05-15-001 | `/upload` 413 for real permit PDFs ≥ 80 MB — raised `MAX_UPLOAD_BYTES` 80 MB → 256 MB + env-var `BMA_MAX_UPLOAD_MB`; new `UPLOAD_CAP_OK` marker; smoke 21/21 + full 24/24 GREEN | `/bma-check-forbidden` | server / upload | ✅ done `fabc2e9` | — |
| U1 | Save Annotated PDF in-place — Option B (FSA-first Open). New `currentSourcePdfHandle`, `openPdfBtnClick()` FSA-first, `saveSourcePdfInPlace()`, `#dd-save-pdf` menu item + Ctrl+Shift+S, `PHASE_U1_OK` 9 sub-checks. Zero forbidden-surface, zero schema, zero server. | `/bma-ui-scope` + `/bma-check-forbidden` | menu / export (client-only) | ✅ done `2101dfe` | — |
| U2 | 1-Page Excel Summary — NEW `/export-xlsx-summary` endpoint + client `collectSummaryData()` + Export panel button. 1-sheet A4 landscape `fit_to_pages(1,1)`. Aggregates BCR/OSR/FAR/Permeable (plain numbers, no verdict) + front-setback + marker counts. Phase 1 boundary: facts only. `PHASE_U2_OK` 16 sub-checks (9 structural + 7 live server). Setback v1 = front only (back/side1/side2 deferred to I-D). Zero forbidden-surface, zero schema. | `/bma-measure-scope` (export-impact) + `/bma-check-forbidden` | export (server `/export-xlsx-summary` + client menu) | ✅ done `4bcacc6` | I-A |
| I-B2b | Measure menu submenu (site plan) | `/bma-ui-scope` | menu-bar | ✅ done `5c76708` | I-B2a |
| HT-1 | `.dd-submenu` z-index 1→201 — 1-line CSS fix in `proto/static/css/app.css:25`; submenu now stacks above sibling overlays in dropdowns. PHASE_HT1_OK marker 3 sub-checks. | `/bma-ui-scope` | menu-bar (CSS) | ✅ done `e21ca98` | — |
| HT-2 | `⬡ NaN ตร.ม.` display guard — NEW `fmtAreaM2`/`fmtDistM` consumer-layer helpers + `Number.isFinite()` guards at 10 display sites. polyAreaM2 untouched. PHASE_HT2_OK 12 sub-checks incl. live-DOM scan. | `/bma-measure-scope` | summary-widget + canvas-ui | ✅ done `acd8636` | — |
| HT-3 | `lbl-mode` site-tag context — extracted `updateModeLabel(m)` helper + `SITE_TAG_THAI_LABELS` map; shows `วัดพื้นที่ ⬡ (ผังบริเวณ — ปกคลุมอาคาร)` when site tool active; marker tools get suffix too. PHASE_HT3_OK 7 sub-checks. | `/bma-ui-scope` | status-bar | ✅ done `0900e0a` | I-B2a |
| HT-4 | Name panel dismissal paths — NEW `autoCloseNamePanel()` helper + 3 dismissal paths: click outside (300ms grace, auto-confirm if value), `loadPage()` auto-commit, global Esc proper `cancelName()`. PHASE_HT4_OK 9 sub-checks. | `/bma-ui-scope` | modal/dialog | ✅ done `a6973ef` | — |
| HT-5 | `.dd-submenu` overflow on short viewports — CSS-only += `max-height:calc(100vh - 120px); overflow-y:auto; overflow-x:hidden; scrollbar-width:thin`. PHASE_HT5_OK 3 sub-checks. | `/bma-ui-scope` | menu-bar (CSS) | ✅ done `89074cb` | — |
| SB-2026-05-15-002 | Upload-cap error UX — pre-flight modal + cold-start hint + `currentUploadCapMB` from `/upload` echo + 413 suggestions. `SB002_UPLOAD_UX_OK` 8 sub-checks. | `/bma-ui-scope` | upload UI / status bar / modal | ✅ done `33577b7` | SB-2026-05-15-001 |
| I-B3 | Properties panel site fields (height + draw-then-classify) — `isBuildingTag` helper + `buildingHeight_m` input + 7 site tags in Semantic Tag dropdown on site pages. `PHASE_I_B3_OK` 10 sub-checks. | `/bma-ui-scope` | left-panel | ✅ done `c011c4e` | I-B1 |
| I-B4 | Site stepper widget — `#site-stepper` 6-step advisory; `updateSiteStepperUI`. `PHASE_I_B4_OK` 10 sub-checks. | `/bma-ui-scope` | left-panel | ✅ done `91fede9` | I-B1 |
| I-C | Summary "ผังบริเวณ" 5th tab — `updateSiteplanTab()` renders `collectSummaryData()` inline; BCR/OSR/FAR/Permeable + per-tag + markers + setback + Phase 1 footer note. `PHASE_I_C_OK` 10 sub-checks. | `/bma-measure-scope` | summary-widget | ✅ done `a490c1e` | I-B3 |
| I-D | 4-direction setback + compass — `landEdgeRole` on edges; `computeEdgeSetback`; `#canvas-compass` SVG; `northAngle` in pageTags. `PHASE_I_D_OK` 10 sub-checks. | `/bma-measure-scope` | summary-widget + canvas-ui | ✅ done `dc96f62` | I-C |
| I-E | Building-to-building distance + wallEdges — `WALL_EDGE_TYPES` catalog; `computeBuildingPairsForPage`; "ระยะระหว่างอาคาร (2h pre-check)" in siteplan tab. `PHASE_I_E_OK` 9 sub-checks. | `/bma-measure-scope` | canvas-ui + schema | ✅ done `504b993` | I-D |
| INV-2026-05-15-001 | Arc-polygon hybrid area measurement — three-click inline arc during polygon draw. `polyMetricsAnyShape` shim. `ARC_POLYGON_OK` 7 sub-checks (err=0.000000%). Zero forbidden-surface edits. Schema additive. | `/bma-measure-scope` (sub-area: generator + ux) | measure / generator + ux | ✅ done `b89e206` | H.1 ✅ |
| INV-2026-05-15-002 | Unified Settings/Preferences modal (Approach A) — `bmaPlan.settings.v1`; `getPref`/`setPref`; `migrateFromLegacy`; 4-tab modal; `Ctrl+,`; bad-JSON + wrong-version safety. `SETTINGS_OK` 13 sub-checks. Zero forbidden-surface, zero `.bmaplan` schema. | `/bma-ui-scope` (modal region) | UI / modal + preferences layer | ✅ done `b6856df` | — |
| HT-6 | Live arc guideline preview during arc-mode draft — when `guidePoint` set and `mArcDraft.pending`, draws dashed arc from last vertex curving through through-point to cursor. ~15 LOC in `redraw()` draft block. `computeArcEdge` reused. `PHASE_HT6_OK` 4 sub-checks. full 42/42 GREEN. Zero forbidden-surface edits. Source: user-test 2026-05-17 "ขาดเส้น guideline เหมือนของเส้นตรง". TEST-H skipped (render-only branch; journey tester does not exercise arc-mode). | `/bma-measure-ux` | measure / render | ✅ done `ecb44d4` | INV-001 ✅ |
| HT-7 | Per-page scale enforcement (hybrid hard-block + auto-redirect) — `_SCALE_REQUIRED_MODES` set + `_scaleGateBeforeMode()` interceptor in `setMode()`. Hard-block when no scale + auto-flip to calib + bounce-back to original mode. `PHASE_HT7_OK` marker. | `/bma-measure-ux` | measure / mode-gate | ✅ done `a3e45c5` | — |
| HT-8a | Tab Strip 4 tabs + ribbon restructure — tabs (📐 วัด / 📝 Annotate / 📍 ผังบริเวณ / ⚙ Workspace, ผังบริเวณ disabled-default dbl-click to enable). Measure tab = Select + Polygon HERO (+ Opening/Land/Building) + Lines (Dist/Path/Ref) + Marker (North/Parking) + Set Scale HERO (moved from Workspace) + Helpers (use existing) + Edit mini-stack. **Remove Shape section** (Rect/Circle/Ellipse unused per user). Status bar prefix `[📐 วัด]` colored. `PHASE_HT8A_OK` 16 sub-checks. full 45/45 GREEN. Zero forbidden-surface. Mockup ref: `proto/sandbox/mockup-interactive-full.html`. | `/bma-ui-scope` → `/bma-ui-ribbon` + `/bma-ui-status` | UI / ribbon + status-bar | ✅ done `ef0944b` | HT-7 ✅ |
| HT-8b | Foxit 3 ribbon patterns + TH labels — Pattern 2 (label-2line) applied to 7 site buttons; Pattern 3 mini-stack CSS infrastructure + marker grid 4×2; HERO confirmed on Polygon + Set Scale (from HT-8a). TH labels verified พื้นแข็ง/สนามหญ้า. `PHASE_HT8B_OK` 11 sub-checks. full 46/46 GREEN. List→รายการ, Tree→รายการบนหน้า labels are right/left panel work (HT-8c/d). | `/bma-ui-scope` → `/bma-ui-ribbon` | UI / ribbon | ✅ done `a8d4e6e` | HT-8a ✅ |
| HT-8c | Left panel tab clarity renames — "Sheets"→"📑 หน้า", "Objects"→"🌳 รายการบนหน้า", "Properties"→"🔧 Properties". Pages-by-tag grouping (existing functionality) preserved — user confusion was about the English label only. Properties stays in left panel until HT-8d moves it right. `PHASE_HT8C_OK` 8 sub-checks. full 47/47 GREEN. | `/bma-ui-scope` → `/bma-ui-panel` | UI / left-panel | ✅ done `6d1b289` | HT-8a ✅ |
| HT-8d-1 | Right panel tab-strip structure — 4 tabs above existing content. Layers default-active showing buildRightPanel output unchanged; others show placeholders pointing to HT-8d-2..5. localStorage persist. `PHASE_HT8D1_OK` 11 sub-checks. full 49/49 GREEN. | `/bma-ui-scope` → `/bma-ui-panel` | UI / right-panel | ✅ done `10cb6ec` | HT-8a ✅ |
| HT-8d-2 | Summary widget in right panel + auto-refresh — `_renderSummaryInPanel()` builds 5 sections (hero Net GFA / Land+ratios / semanticTag breakdown / object counts / warnings) reusing collectAreas + phase1Warnings + collectSummaryData. `updatePageSummary` now calls both `_refreshPanelSummaryIfActive` and `updateSummaryWidget` so live mutations refresh both the new panel view AND the legacy floating widget. HT-8d-3 (auto-refresh fix) folded in. Floating widget DOM kept for legacy compat. Phase 1 boundary preserved (no verdict color). `PHASE_HT8D2_OK` 10 sub-checks. full 50/50 GREEN. | `/bma-measure-scope` (summary) | UI / summary + reactive | ✅ done `8fd9629` | HT-8d-1 ✅ |
| HT-8d-3 | Summary auto-refresh — **MERGED into HT-8d-2** (`8fd9629`). The 1-line fix was naturally bound to the panel renderer wiring. `updatePageSummary` now calls `_refreshPanelSummaryIfActive` AND `updateSummaryWidget` at the end. | — | — | ✅ merged with HT-8d-2 | HT-8d-2 |
| HT-8d-4 | Warnings click-to-jump — `navigateToWarning(pg, objId)` helper wired on both legacy widget `_swBuildWarn` AND new panel `_renderSummaryInPanel`. loadPage(pg) + lookup object by id across (poly/opening/line/ref/parking) + select + redraw. Status bar reports outcome. `PHASE_HT8D4_OK` 7 sub-checks. full 51/51 GREEN. | `/bma-measure-scope` (summary) | summary / navigation | ✅ done `cc6bf63` | HT-8d-2 ✅ |
| HT-8d-5a | Layers polish wave 1 — Hide-Others/Show-All footer + canvas top-bar layer swatch indicator + lock-while-draw gate. `_layerLockGateBeforeMode` hooked into setMode after scale gate. Annotation modes bypass. `PHASE_HT8D5A_OK` 8 sub-checks. full 54/54 GREEN. | `/bma-ui-panel` (right) | UI / layers | ✅ done `917ac23` | HT-8d-1 ✅ |
| HT-8d-5b | Layers polish wave 2 — search input (filter by name/slug) + per-layer color picker (swatch becomes <input type=color>). Focus preserved across re-render. `setLayerColor` writes to per-page object + saves + redraws. `PHASE_HT8D5B_OK` 8 sub-checks. full 55/55 GREEN. | `/bma-ui-panel` (right) | UI / layers | ✅ done `df8172c` | HT-8d-5a ✅ |
| HT-8d-5c | Layers polish wave 3 — move-up/down arrow buttons + right-click rename (oncontextmenu → renameLayer). Real drag-reorder deferred (buttons are simpler + more reliable). Duplicate/delete deferred to HT-8d-5d if needed. `PHASE_HT8D5C_OK` 10 sub-checks. full 56/56 GREEN. | `/bma-ui-panel` (right) | UI / layers | ✅ done `9063a0b` | HT-8d-5b ✅ |
| HT-8d-5d | Layers polish wave 4 — `+ New Layer` button + creation modal with 6-preset dropdown (building/land/deduction/reference/markup/custom). `addCustomLayer` writes to per-page layers[] with `semanticTagPreset` hint. Schema additive. `PHASE_HT8D5D_OK` 13 sub-checks. full 57/57 GREEN. **LAST sprint of HT-8d-5 series.** | `/bma-ui-panel` (right) | UI / layers | ✅ done `4d63323` | HT-8d-5c ✅ |
| HT-9 | Rubber-band preview for all 2-click tools — guidePoint condition extended to include rect/circle/ellipse/ann_rect/ann_highlight/ann_circle/ann_arrow/ann_cloud + special calib branch (calibPts.length===1). redraw() got 2 new branches: shape preview (dashed outline) for shape modes, calib mid-flow guide line + live distance label for Set Scale between click 1 and click 2. Ann_cloud uses the existing path-style segment guide (sufficient for Phase 1). `PHASE_HT9_OK` 10 sub-checks. full 48/48 GREEN. Zero forbidden-surface (render-only). | `/bma-measure-ux` | measure + annotate / preview rendering | ✅ done `b574f7f` | HT-8a ✅ |
| HT-10 | Options modal additions — extends INV-002 settings: density (Compact/Comfortable/Spacious, default Comfortable), hide-left-panel toggle, hide-right-panel toggle. CSS `body.density-X` overrides .rbtn sizing. New `applyLayoutPrefs()` wired into loadPrefs + applySettingsDraft. Persists via `bmaPlan.settings.v1` (additive). `PHASE_HT10_OK` 10 sub-checks. full 52/52 GREEN. | `/bma-ui-scope` (modal region) | UI / modal + ribbon CSS | ✅ done `acee13c` | HT-8b ✅ |
| HT-11 | Annotation edit + individual delete — `annotationHitTest` + `deleteAnnotation` + edit modal (text + color + 🗑) for all 7 ann types. dblclick on canvas → opens modal. Right-panel List row hover ✎/🗑 deferred to HT-8d list-tab sprint. `PHASE_HT11_OK` 10 sub-checks. full 53/53 GREEN. Zero forbidden-surface (schema additive — reuses existing text/color fields). | `/bma-ui-scope` → `/bma-ui-panel` | UI / annotation | ✅ done `01d923c` | HT-8d-1 ✅ |
| INV-2026-05-17-001 | Freeform area measurement (Approach D — Alt sub-mode of polygon) — In polygon mode, holding `Alt` during `mousedown` flips to streaming freehand sub-mode with distance-bin sampling (≥ 6 px). Release Alt → return to click-vertex mode (mixed click+drag in same polygon). `Shift`/`Ctrl` during draw → live tolerance modulation (badge on canvas). `Enter` → RDP-decimate + `polyAreaM2` + close. New helper `rdpSimplify` (~25 LOC, inline). Reuses existing `polyAreaM2` + `polySelfIntersects` unchanged. Output is `obj.type='polygon'` + optional `obj.freeform={tolerance,originalCount,freehandSegments}`. `PHASE_FREEFORM_OK` marker (6 sub-checks: closed-form area err < 5%, decimation reduces 500→≤150, save/reload round-trip, self-intersection detection, ≤ 1 s draw, mixed-mode coexistence). Carry-over risks: Alt-mid-stroke guard, snap bypass during freehand, touch input deferred to iPad track. **Zero forbidden-surface edits. Schema additive only.** Est ~150 LOC. Source: `docs/invent/freeform-area.md` + `proto/sandbox/invent-freeform-area.html` (spike PASS 6/6 first attempt, err = 1.22%). User GO 2026-05-17. | `/bma-measure-scope` (sub-area: ux + state-machine) | measure / ux + generator | ✅ done 023b988 | INV-001 ✅ |
| HT-12a | **Density picker in menu bar** — `.density-picker` DOM (3 buttons compact/comfortable/spacious) + `setDensityFromMenu()` bridge to existing HT-10 `PREFS.layout.density` infrastructure + extended `applyLayoutPrefs()` to sync picker button active state (works for boot, modal save, and direct menu click). `.density-picker` CSS (margin-left:auto, button states inc. .active). Adjusted `.menu-item.phase-badge` to give up `margin-left:auto` to density-picker. **Scope narrowed from original sprint card** — most of the "menu bar 9-item shell" work is already in live (`Project`/`Scale`/`Page`/`Measure`/`Object`/`Layer`/`Annotate` dropdowns exist via HT-8 series). HT-12a delivered the only missing piece: visible density toggle. Marker `PHASE_HT12A_OK` 10/10 PASS. smoke 42/42 GREEN. Zero forbidden-surface. | `/bma-ui-scope` → `/bma-ui-menu` | UI / menu bar | ✅ done `e65adc1` | — |
| HT-12b | **File menu wired** — activated dead `File` menu-item with `onclick="toggleMenu(this)"` + inline `#dd-file` dropdown with 6 items: Open PDF (openPdfBtnClick), Open Project (openProjectBtnClick), Sample PDF (openSamplePdf), Save Project (saveProject), Save Project As (saveProjectAs), Save Annotated PDF (saveSourcePdfInPlace). All onclick dispatchers reference existing handlers. Co-exists with Project menu items (no removal yet — HT-12g handles that). Recent submenu deferred. Close item deferred (no `closePdf` handler exists). PHASE_HT12B_OK 11/11 PASS. smoke 43/43 GREEN. Zero forbidden-surface. | `/bma-ui-menu` | UI / menu bar | ✅ done `cf910d8` | HT-12a ✅ |
| HT-12c | **View menu wired** — activated dead `View` menu-item with onclick=toggleMenu + #dd-view dropdown (10 items + Density submenu): Zoom In/Out (adjustZoom), Fit to Window (fitToWindow), Actual Size (setActualSize new), Rotate L/R (rotatePage), Toggle Left/Right Panel (toggleLeftPanel/toggleRightPanel new), UI Density submenu (setDensityFromMenu), Settings (openSettings). 3 new helpers bridge to PREFS.layout.* (HT-10). **Fit Golden φ deferred** (mockup-only experimental feature, no live impl). PHASE_HT12C_OK 15/15 PASS. smoke ALL GREEN. **Discovered:** pre-existing failures PHASE_HT8D1_OK.placeholderHasMessage and PHASE_HT8D5A_OK.footerHas2Buttons (NOT regressions from HT-12c — to file separately). Zero forbidden-surface. | `/bma-ui-menu` | UI / menu bar | ✅ done `6cad87e` | HT-12a ✅ |
| HT-12d | **Page menu expanded** — Page menu already existed (Prev/Next/Rotate). Added: First/Last (loadFirstPage/loadLastPage new), Page Setup (reorganized item), Set Page Tag submenu via setPageTagCurrent (new, 5 items site/plan/elev/section/none), Toggle Exclude via toggleExcludeCurrentPage (new, bridges to toggleExcludePage), Set North / Compass (setMode('north')). PHASE_HT12D_OK 14/14 PASS. smoke ALL green (same 2 pre-existing failures unchanged). Zero forbidden-surface. | `/bma-ui-menu` | UI / menu bar | ✅ done `694bc59` | HT-12a ✅ |
| HT-12e | **Scale menu verified** — No DOM change (Scale menu already had 7 items from prior sprints: Set Scale / Scale Manager / Verify / Reset / Status / Show Line / Warning). PHASE_HT12E_OK 8/8 marker added to lock contract. Zero forbidden-surface. | `/bma-ui-menu` | UI / menu bar | ✅ done `b12ab52` (bundled with HT-12f) | HT-12a ✅ |
| HT-12f | **Project menu extended** — Added 6 Export items to existing Project dropdown (after Save PDF separator): Export XLSX (exportXLSX), Export XLSX Summary (exportSummaryXLSX), Annotated PDF current/all (exportCurrentPageAnnotatedPDF/exportAllPagesAnnotatedPDF), Export CSV (exportCSV), Export JSON (exportJSON). Existing items preserved. PHASE_HT12F_OK 12/12 PASS. Zero forbidden-surface. | `/bma-ui-menu` | UI / menu bar | ✅ done `b12ab52` | HT-12a ✅ |
| HT-12g | **Workspace ribbon tab hidden** — Pragmatic implementation: `style="display:none"` on workspace tab (not full DOM delete) to preserve (a) 4 existing E2E tests (HT-8a series) that call `switchRibbonTab('workspace')` programmatically, and (b) 13 critical element IDs inside ribbon-workspace that are JS-referenced (file-input, proj-input, page-lbl, zoom-val, rot-badge, btn-prev/next, btn-setup, etc.). `_restoreRibbonTab` updated to fall back to 'measure' if saved 'workspace'. ribbon-tab-content div untouched. User sees 3-tab ribbon. PHASE_HT12G_OK 6/6 PASS. HT-8A_OK preserved. Zero forbidden-surface. | `/bma-ui-scope` → `/bma-ui-ribbon` + `/bma-ui-menu` | UI / ribbon + menu | ✅ done `db2ddd7` | HT-12b ✅ + HT-12c ✅ + HT-12d ✅ + HT-12e ✅ + HT-12f ✅ |
| HT-12h | **Density picker behavior** — chain already complete via HT-10 (CSS classes + applyLayoutPrefs) + HT-12a (setDensityFromMenu bridge). Marker PHASE_HT12H_OK 6/6 locks the end-to-end contract (click → body class → CSS cascade → button sizes → persist). | `/bma-ui-scope` | UI / menu + CSS vars | ✅ done `4ecef2f` | HT-12a ✅ + HT-10 ✅ |
| HT-12i | **Panel collapse buttons** — Added ◀ on left panel + ▶ on right panel + CSS .panel-collapse-btn. Wired to toggleLeftPanel/toggleRightPanel (HT-12c helpers). PHASE_HT12I_OK 4/4 PASS. | `/bma-ui-scope` → `/bma-ui-panel` | UI / left+right panels | ✅ done `4ecef2f` | HT-10 ✅ |
| HT-13a | **Helpers section in Measure ribbon** — new ribbon-group #ribbon-helpers with rstack 2×2 (Loupe/Ortho/Perp/Snap-off) dispatching existing toggleLoupe/toggleOrtho/togglePerp/toggleSnap. Surfaces 4 hidden helper toggles. PHASE_HT13A_OK 6/6 PASS. | `/bma-ui-ribbon` | UI / ribbon | ✅ done `787a5d5` | HT-12g ✅ |
| HT-13b+c | **Tool + Edit sections (verify-only)** — Vertex/Front/Copy placeholders skipped per anti-fake-button rule. Current Select/Pan + Undo/Redo/Delete kept. PHASE_HT13BC_OK 5/5 marker locks contract. | `/bma-ui-ribbon` | UI / ribbon | ✅ done `787a5d5` | HT-12g ✅ |
| HT-13d | **Polygon dropdown popover** — added ▾ caret button next to Polygon HERO; click opens popover with sub-mode hints (A=Arc/Alt=Freeform/Shift=Ortho/O=Opening) + sub-type list (Land/Building/Room). Right-click on Polygon HERO also opens. togglePolygonSubmodePopover/hidePolygonSubmodePopover helpers + outside-click close. PHASE_HT13D_OK 12/12 PASS. **Critical UX win** — sub-modes discoverable now. | `/bma-measure-ux` | UI / ribbon + measure | ✅ done `61b9965` | HT-12g ✅ |
| HT-14a | **📋 List tab content** — closes HT-8d-1 placeholder. `_renderListInPanel` with filter (6 types) + sort (time/type/name) + search + per-row hover ✎/🗑 + click-to-select. PHASE_HT14A_OK 8/8 PASS. | `/bma-ui-panel` | UI / right panel | ✅ done `1d3117e` | — |
| HT-14b | **🔧 Props tab content** — closes HT-8d-1 placeholder. `_renderPropsInPanel` with 4 sections (Selected/Semantic/Style/History) + empty-state. PHASE_HT14B_OK 4/4 PASS. | `/bma-ui-panel` | UI / right panel | ✅ done `1d3117e` | — |
| HT-14c | **📊 Summary tab deep-dive** — `_renderSummaryInPanel` (HT-8d-2 baseline) verified to include Hero GFA + Land/ratios (BCR/OSR/FAR/Permeable) + per-tag breakdown + object list + Warnings + Phase 1 note. PHASE_HT14C_OK 6/6 marker locks contract. | `/bma-measure-scope` (summary) | UI / right panel | ✅ done `1d3117e` | — |
| HT-15a | **📚 Sheets tab verify** — left panel Sheets tab (HT-8c renamed "📑 หน้า") already exists with pages list. Mockup's A/S/M/E/P discipline grouping skipped — pageTags (site/plan/elev/section) already groups. PHASE_HT15A_OK 6/6 marker locks contract. | `/bma-ui-panel` | UI / left panel | ✅ done `1d3117e` | — |
| HT-16 | **Panel ◀▶ collapse button trap fix** — found by `/bma-human-test` 2026-05-18. Added 2 restore tabs (#lp-restore-tab, #rp-restore-tab) inside #workspace, visible only when corresponding panel collapsed (via data-left/right-collapsed attrs set by applyLayoutPrefs). Position:absolute on canvas edges. Click restores panel. PHASE_HT16_OK 6/6 PASS. | `/bma-ui-scope` → `/bma-ui-panel` | UI / panel collapse | ✅ done `db59cca` | HT-12i ✅ |
| HT-17 | **Enter in area mode finishes polygon** — found by `/bma-human-test` 2026-05-18. Added `if(e.key==='Enter' && mode==='area' && mPts.length>=3){e.preventDefault();finishCurrentArea();}` to document keydown handler alongside existing path/ref case. Matches Measure menu "Finish Drawing — Enter" hint. PHASE_HT17_OK 2/2 PASS. | `/bma-measure-ux` | measure / keyboard | ✅ done `db59cca` | — |
| INV-2026-05-18-001a | **Page Setup Redesign Part A — Context-sensitive inspector + status chips** — Replaces `#proj-form-panel` content with `#setup-inspector-content` (dashboard ⇄ page-card switch). Project-info form moved into `<details id="setup-pi-accordion">` (open-by-default for E2E compat). `#tag-grid` thumbnails now emit per-cell traffic-light dot (green=tagged+manual / amber=auto-unverified / red=untagged or unknown) + object-count chip. New helpers: `_pageReadiness`, `_setupCountObjects`, `_renderSetupDashboard`, `_renderSetupPageCard`, `_setupBack`, `_renderSetupInspector`. `selectSetupPage`/`setPageName`/`buildTagGrid` hooked to inspector refresh. `PHASE_INV_PAGE_SETUP_A_OK` 8/8 PASS in smoke. No regression caused by this sprint (3 pre-existing failures unchanged: HT-8C/HT-10/HT-12H). Zero forbidden-surface. Zero schema change. TEST-H deferred to 001b/c. Source: `docs/invent/page-setup-redesign.md` + spike `proto/sandbox/invent-page-setup-redesign.html` (7/7 PASS + 1 bonus). | `/bma-ui-scope` → `/bma-ui-panel` (modal region) | UI / page-setup modal | ✅ done `e85a5ce` | — |
| INV-2026-05-18-001b | **Page Setup Redesign Part B — Floor sub-types for plan tag** — Scope narrowed to user's stated requirement. New per-page schema `pageFloorKind ∈ {basement|normal|mechanical|rooftop|custom}` + `pageFloorNum` (additive, optional). New helpers `setPageFloorKind`/`setPageFloorNum`. `autoNamePage` floor-aware for `tag=plan`: basement N → "ชั้นใต้ดิน N", normal N → "ชั้น N", mechanical → "ชั้นห้องเครื่อง", rooftop → "ชั้นดาดฟ้า", custom → user-typed (no overwrite). Save/load round-trip + tag-change clear. Floor-kind picker in page-card (visible when tag=plan). Token-engine refactor DEFERRED — current prefix inputs preserved for backward compat. `PHASE_INV_PAGE_SETUP_B_OK` 9/9 PASS. Zero forbidden-surface. | `/bma-ui-scope` | UI / template + schema additive | ✅ done `798e5c3` | INV-2026-05-18-001a |
| INV-2026-05-18-001c | **Page Setup Redesign Part C — Permanent delete + renumber-map + `/rebuild-pdf`** — ✅ shipped `ebb521c` 2026-05-19. Server: new `/rebuild-pdf` POST (PyMuPDF `doc.delete_page()` reverse order, flush caches, returns `renumberMap`). Client: `_openRenumberDialog` preview table → `_executeRenumberDelete` → `_reindexPageDicts` walks 7 per-page dicts. Hard-block during draw + last-page guard + Foxit-style warning. `PHASE_INV_PAGE_SETUP_C_OK` 7/7 PASS. | `/bma-ui-scope` + `/bma-check-forbidden` | UI / delete + server / `/rebuild-pdf` | ✅ done `ebb521c` | INV-2026-05-18-001a |
| INV-2026-05-18-002 | **Settings/Preferences panel v2 — Export Defaults + Loupe Prefs** — Extends `bmaPlan.settings.v1` with 4 new PREFS paths (additive). `export.csvSeparator` + `export.includeLawBasis` in **หน่วย** tab. `loupe.radius` + `loupe.zoomFactor` in **วาด** tab. `exportCSV` reads both export prefs; `updateLoupe` uses `loupeZoomFactor` for constant magnification. `_applyLoupePrefs()` syncs on boot + after Apply (clamps before fallback). `SETTINGS_V2_OK` 6/6 PASS. Zero forbidden-surface. v1 untouched (`SETTINGS_OK` still GREEN). | `/bma-ui-scope` (modal region) | UI / settings modal | ✅ done `3e71865` | INV-002 ✅ |
| INV-2026-05-19-001a | **Zen Mode + Sheet Minimap** — body.zen CSS class hides ribbon/lp/rp/status/topbar (canvas measured at 94.4% vh on 900px test viewport, exceeds 92% target). 3 corner HUDs replace status bar: TL (Scale + Tool), TR (Page + Exit Zen chip), BL (Layer + Save + Obj + ⚠). #zen-minimap 260×200 bottom-right with 5-col grid of pages, IntersectionObserver lazy-load. F11 toggle + Esc exit + visible chip. `PREFS.layout.zenMode` + `zenOnboarded` additive. MutationObserver on #bottombar mirrors status-bar updates → HUD. HT-10 `applyLayoutPrefs()` restores prior panel state on zen exit. `PHASE_INV_ZEN_OK` 10/10 PASS. smoke + full GREEN. JOURNEY_OK on 45-page permit (0 CRASH/BROKEN, 2 FRICTION → HT-Z-1/HT-Z-2 filed). +180 LOC ui.html / +50 CSS / +110 e2e. Also fixed 2 pre-existing baseline test drifts from polish `0e4e851`. Zero forbidden-surface. Schema additive. Source: `docs/invent/fullscreen-canvas-ui.md` (Approach A, 27/30) + spike `proto/sandbox/invent-fullscreen-canvas-ui.html` (6/6 PASS). | `/bma-ui-scope` → `/bma-ui-canvas` + `/bma-ui-status` | UI / canvas overlay + HUDs + new zen-mode surface | ✅ done `20e2548` | HT-10 ✅ |
| INV-2026-05-19-001c | **Zen+Palette FRICTION polish (HT-Z-1/2/3 bundle)** — 3 small fixes from journey tests on 001a/b. (1) `_zenSyncHud()` reads `pageNames[curPage]` directly instead of `bb-page-name` element — kills MutationObserver lag during fast nav. (2) HUD Scale chip amber when scale state is `auto-unverified`/`unknown`, default white when manual — fixes "is my scale calibrated?" confusion. (3) `filterPalette()` empty branch adds `💡 แท็กภาษาไทยใช้ได้หลังตั้งค่าหน้าใน Page Setup` hint when Thai tag word queried + no pages tagged. `PHASE_INV_POLISH_001C_OK` 5/5 PASS. smoke + full GREEN (no regression on 001a/b markers). +15 LOC ui.html / +65 e2e. TEST-H skipped per AGENTS.md no-test rationale. Zero forbidden-surface. Schema unchanged. | `/bma-ui-scope` → `/bma-ui-canvas` + `/bma-ui-menu` | UI / HUD + palette | ✅ done `f7d64b8` | INV-001a ✅ + INV-001b ✅ |
| INV-2026-05-19-001b | **⌘K Command Palette (page jump)** — `#cmd-palette` floating modal. Filters pages by number/name/tag slug/Thai tag label (TAG_LABELS: ผังบริเวณ/ชั้น/รูปด้าน/รูปตัด/รายละเอียด). ↑↓ nav, Enter = `loadPage(n)` + close, Esc = cancel. Bound to Ctrl+K/Cmd+K with `mPts.length===0 && !anyModal` guard. Key handlers placed BEFORE inInput guard so palette input doesn't swallow nav keys. Works in classic + zen (z-index 9500 > zen 1500). Pre-filter shows 12 default rows on empty input. `PHASE_INV_PALETTE_OK` 10/10 PASS. smoke + full GREEN (no regression on 001a). JOURNEY_OK on 45-page permit (13/13 steps, 1 FRICTION HT-Z-3 filed). +120 LOC ui.html / +35 CSS / +95 e2e. Zero forbidden-surface. Schema unchanged. Source: `docs/invent/fullscreen-canvas-ui.md` (Approach B, 28/30) + spike same file. | `/bma-ui-scope` → `/bma-ui-menu` (palette modal) | UI / modal + keyboard | ✅ done `a51207f` | — |
| INV-2026-05-19-003a | **Print to printer button (Path B)** — File-menu items "🖨 Print Current Page" + "🖨 Print Selected Pages" → captures canvas via `toDataURL('image/png')` → opens synthetic print window with `<img>` + `@media print` page-break CSS + `window.print()` trigger. Two modes: single page + iterate selected/all-non-excluded with `loadPage(n)` + `_waitForRedraw` (2 RAF). Original `curPage` restored after multi-page. DPR captured in synth metadata. **Note:** sprint card originally said "Export panel" — actual placement is File menu dropdown (Export panel doesn't exist as a separate widget; exports live in File menu post-HT-12). Removed `Ctrl+P` shortcut to avoid conflict with existing `togglePerp()` snap toggle. `PHASE_INV_PRINT_OK` 8/8 PASS. smoke + full GREEN (no new regressions; 6 pre-existing failures unchanged). +72 LOC ui.html / +83 LOC e2e. TEST-H skipped per AGENTS.md no-test rationale (additive menu sprint, journey doesn't exercise print). Zero forbidden-surface. Schema unchanged. Source: `docs/invent/print-canvas-per-page.md` (Path B). | `/bma-ui-scope` → `/bma-ui-canvas` | UI / File menu + canvas snapshot | ✅ done `b4f7235` | — |
| INV-2026-05-19-003b | **`/export-png` ZIP endpoint (Path C)** — New POST `/export-png` returns ZIP of PyMuPDF-rendered PNGs (one per requested page) with overlays drawn via the **exact same helpers** as `/export-pdf` (no overlay-math reimpl). DPI clamped 72..300 (default 200). `MAX_EXPORT_PAGES` env-overridable (default 200) → 413 with `max` field. `ZIP_STORED` (PNG already compressed). File menu "🖼 Export PNG (ZIP, 200 DPI)" + `exportPngZip()` POSTs current `selectedPages` or all non-excluded pages. **Shipped manually** (not via /bma-dev-loop — server.py edit outside loop's safe scope per step-3 guardrail; /bma-check-forbidden returned WARN→OK). `PHASE_INV_PNG_EXPORT_OK` 11/11 PASS. smoke 33/34 + full 37/37 (1 pre-existing PHASE_I_D_OK unrelated). +130 LOC server.py / +25 LOC ui.html / +130 LOC e2e. Zero forbidden-surface (no edit to /upload, /page, /analyse, /export-pdf, CASES, RS, render cache, .bmaplan). Schema unchanged. Source: `docs/invent/print-canvas-per-page.md` (Path C). | `/bma-check-forbidden` (WARN — additive endpoint) | export / server endpoint + client menu | ✅ done `612de96` | INV-003a ✅ |
| BLOAT-1 | **CLAUDE.md ui.html LOC drift fix + consolidation trigger rule** — Updated CLAUDE.md baseline `~1,700 lines` → current `~4,231 lines` for `proto/ui.html` (and `proto/server.py` ~1,370 → ~1,750), and added a discipline rule: "if `ui.html` crosses **5,000 lines**, the next sprint MUST be a consolidation sprint that extracts a JS region to `static/js/*`." Pure documentation; no code path touched. py_compile PASS; `/bma-e2e` + `/bma-human-test` skipped per AGENTS no-test rationale. Source: manual bloat audit 2026-05-19 (see Discovered backlog → bloat-audit). | `/bma-check-forbidden` (docs-only) | docs / discipline | ✅ done `4dcf5cf` | — |
| BLOAT-2 | **Extract status-bar JS to `static/js/status-bar.js`** — Moved 8 functions (`updateAnalyseUI`, `activeLayerLabel`, `currentObjectCount`, `currentWarningCount`, `updateBottomBar`, `updateModeLabel`, `_markSaved`, `_setDirty`) + 2 constants (`MODE_BASE_LABELS`, `SITE_TAG_THAI_LABELS`) from `proto/ui.html` to new classic non-module script `proto/static/js/status-bar.js` (49 LOC). `<script src>` inserted between `opening-parent.js` and main inline `<script>`. ui.html: 4,231 → 4,208 lines (-23 net; placeholders left in). `_fallbackDownload` (L3389) kept in ui.html (part of save chain, will move with BLOAT-3). NEW marker `PHASE_BLOAT2_OK` 8/8 PASS. smoke 18/18 + full 21/21 GREEN (PERSIST_OK round-trip on real 45-page permit confirms `_setDirty`/`_markSaved` extraction safe). `/bma-human-test` skipped per AGENTS no-test rationale. Zero forbidden-surface. | `/bma-ui-scope` → `/bma-ui-status` | UI / extraction | ✅ done `ce87f47` | BLOAT-1 ✅ |
| BLOAT-3 | **Extract export/save JS to `static/js/export-save.js`** — Moved 14 functions (`dlBlob`, `exportJSON`, `exportCSV`, `exportSummaryXLSX`, `exportXLSX`, `exportAllPagesAnnotatedPDF`, `exportCurrentPageAnnotatedPDF`, `exportPngZip`, `_makeProjBlob`, `_writeToHandle`, `_fallbackDownload`, `saveProjectAs`, `saveProject`, `saveSourcePdfInPlace`) + 13 constants (6 COL_* + 7 TYPE_*) + `rowBase` + `buildRows` to new classic non-module script (188 LOC). ui.html: 4,208 → 4,057 lines (-151 net). `<script src>` inserted after `status-bar.js`. NEW marker `PHASE_BLOAT3_OK` 8/8 PASS (fileLoad / 14 fnsOk / 13 constsOk / dlBlobOk / buildRowsOk / blobIsBlob / schemaOk all 12 v1 fields / asyncOk). smoke 18/18 + full 21/21 GREEN. XLSX_OK + PROJECT_OK + PERSIST_OK + ANNOT_OK + REAL_OK all GREEN on real 45-page permit — proves save/.bmaplan/export extraction safe. `/bma-human-test` skipped (full E2E + schemaOk cover risk). Zero forbidden-surface. `.bmaplan` schema unchanged (version 1, same 12 fields). Kept in ui.html (out of scope): pgmgrExportPDF + print cluster + applyLoadedProject + load helpers + shared helpers. | `/bma-ui-scope` → `/bma-check-forbidden` | UI / extraction | ✅ done `e524488` | BLOAT-2 ✅ |
| BLOAT-4 | **Extract annotation JS to `static/js/annotations.js`** — Moved 13 functions (`ensureAnnotations`, `newAnnotationId`, `addAnnotation`, `renderStickyCards`, `_createStickyCard`, `clearAnnotations`, `annotationHitTest`, `deleteAnnotation`, `openAnnotationEditModal`, `closeAnnotationEditModal`, `saveAnnotationEdit`, `_annColor`, `drawAnnotations`) from `proto/ui.html` (L1680–1869) to new classic non-module script (205 LOC). ui.html: 4,057 → 3,869 lines (-188 net). `<script src>` inserted after `export-save.js`. The 7 per-mode mousedown branches kept in ui.html (coupled to unified pointer state — they call the extracted helpers globally). NEW marker `PHASE_BLOAT4_OK` 8/8 PASS (fileLoad / 13 fnsOk / addOk / hitMissOk / drawOk / stickyOk / colorOk / clearOk). Full GREEN on retry (first run had known REAL_PDF analyse flake — one surgical retry passed). All 22 baseline markers + `PHASE_INV_STICKY_OK` 10/10 (J_roundTrip ✓) + `PHASE_HT11_OK` 10/10 retained. Zero forbidden-surface. | `/bma-ui-scope` | UI / extraction | ✅ done `c4df4f7` | BLOAT-2 ✅ |
| BLOAT-5 | **Extract page-setup modal JS to `static/js/page-setup.js`** — Moved 15 functions + 2 constants from 3 non-contiguous ranges in `proto/ui.html` (L855-856 constants, L1051-1070 floor helpers, L3094-3167 inspector + renumber/delete cluster) to new classic non-module script (125 LOC). Functions: `autoNamePage`, `countTagBefore`, `setPageFloorKind`, `setPageFloorNum`, `selectSetupPage`, `_pageReadiness`, `_setupCountObjects`, `_renderSetupDashboard`, `_renderSetupPageCard`, `_setupBack`, `_renderSetupInspector`, `_openRenumberDialog`, `closeRebuildDialog`, `_executeRenumberDelete`, `_reindexPageDicts`. Constants: `FLOOR_KIND_LABELS`, `FLOOR_KIND_OPTIONS`. Kept in ui.html: `buildTagGrid` (8 cross-cluster callers) + state lets + remaining UI plumbing. ui.html: 3,869 → 3,777 lines (-92 net). NEW marker `PHASE_BLOAT5_OK` 8/8 PASS. **smoke ALL GREEN** (18 baseline + BLOAT2/3/4/5_OK + PAGE_SETUP_A/B/C_OK + HT11_OK). **full 3 retries FAILED at PERSIST_OK** — pre-existing REAL_PDF env flake on `_wait_analyse_ready` page 1/45 (filed as BLOAT-FLAKE-1 in KNOWN_ISSUES.md). Extraction does NOT touch page-load/analyse paths; smoke proves correctness. Loop trips `LOOP_STOP_REGRESSION` per strict rule — halted awaiting user. Zero forbidden-surface. | `/bma-ui-scope` → `/bma-ui-panel` | UI / extraction | ✅ done `4694530` (smoke; full env-flake → BLOAT-FLAKE-1) | BLOAT-2 ✅ |
| BLOAT-FLAKE-1 | **REAL_PDF `_wait_analyse_ready` flake investigation** — `_test_real_pdf_multipage_persistence` intermittently hangs at "กำลังโหลดหน้า 1…" on page 1/45 of the real permit PDF. Discovery history: BLOAT-3 noted in MENU_OK probe ("skipped (REAL_PDF analyse flake)"); BLOAT-4 first attempt failed, retry passed; BLOAT-5 3 retries all failed = worst occurrence. Hypothesis: env-level (Playwright/Windows file-handle exhaustion, server analyse timeout, or cold-cache on busy system). Not blocked by BLOAT-5 extraction (page-setup helpers don't run during page-load/analyse). Fixed via path (1): `_wait_analyse_ready` default timeout 30s→60s + actively-loading grace window (+50% extra while status shows กำลังโหลด/กำลังวิเคราะห์). Full E2E now GREEN — PERSIST_OK + REAL_OK + ANNOT_OK no longer flake; also retroactively confirms BLOAT-5 passes full. Paths (2)(3)(4) reserved if flake recurs under heavier load. See `docs/status/KNOWN_ISSUES.md` (RESOLVED). | `/bma-check-forbidden` (E2E test code only; no server.py edits) | test / env / e2e reliability | ✅ done `aa89a93` | — |
| INV-2026-05-24-002a | **Centerline snap for dashed boundary lines — proto** (invent-done-go). 2-step hybrid (per-click ROI snap + post-draw per-edge PCA + corner intersection) ที่ pin user click ไปยังกึ่งกลาง stroke ของเส้นปะหนา. NEW `proto/static/js/centerline-snap.js` (208 LOC, inline Zhang-Suen + Otsu, no CDN). Hook ใน area-mode mousedown + finishCurrentArea (poly branch, hasArc bypass). Toggle "⊙ CL" ใน Helpers ribbon. Default OFF. PREFS `measure.centerlineSnap` additive. `obj.traceMode="centerline-roi"` additive. **Forbidden: NONE** (canvas getImageData public API; no polyAreaM2/pdfToC/cToPdf/RS/snap-internals touched). `PHASE_CENTERLINE_SNAP_OK` 10/10 PASS (maxDelta=0.140%, target ≤0.5%). All 21 prior markers GREEN (no regression). PROJECT_OK + PERSIST_OK confirm `traceMode` round-trips save/load. TEST-H skipped per AGENTS.md no-test rationale (feature OFF by default, journey doesn't toggle ribbon Helpers). Source: `docs/invent/centerline-snap-dashed-boundary.md`. | `/bma-measure-scope` (sub-area: ux + geometry) → `/bma-measure-ux` + `/bma-measure-geometry` → `/bma-measure-regression` | measure / new helper module + ux + schema additive | ✅ done `6db0461` | — |
| INV-2026-05-24-002b | **Centerline snap for dashed boundary lines — lite** (invent-done-go). Vendored proto's `centerline-snap.js` algorithm verbatim into `lite/static/js/centerline-snap.js` (306 LOC: Section A drift-locked from proto + Section B lite-specific glue — `CL_litePolyClick` + `CL_litePolyFinish` + self-installing toggle button + localStorage persistence). 2 hooks in `lite/ui-lite.html` (1197→1199 lines, within 1200 cap): poly click + finishDraft refinement, both no-op when toggle OFF. **Forbidden: NONE** (measure-engine.js untouched, RS/pdfToC/cToPdf untouched, .bmaplan schema unchanged). `LITE_CENTERLINE_SNAP_OK` 6/6 PASS (maxDelta=0.1778%, target ≤0.5%). MEASURE_PARITY_OK still GREEN (regression gate). Toggle default OFF — user opts in via floating "⊙ CL" button bottom-right. | `/bma-lite-dev` | lite / new helper module + ux | ✅ done `ad920c6` | INV-2026-05-24-002a ✅ |
| INV-20260703-layer-redesign | **ออกแบบระบบ layer ใหม่ (lite) — invent in progress, HALT at checkpoint after spike** — user request 2026-07-03 ("ออกแบบlayerใหม่") หลังคุยข้อจำกัดที่เหลือของระบบ layer. RESEARCH ✅ (PRIOR_ART_PARTIAL: AutoCAD Make-Current/LAYMCUR + Bluebeam auto-assign-to-active = mature pattern สำหรับ P2; ทุก incumbent แยก grouping/semantic/level 3 แกน; no viable lib, vanilla custom). DIVERGE+SCORE ✅ 5 แนวทาง: A layer.floorKey (23) / **B always-on draw-target chip + make-current (26 — winner as companion)** / C floor-as-first-class-node (18, ชน PAGE_SCOPED lock) / D floorKey resolver chain + reconcile banner (24 — fallback) / E pin-floor-at-first-draw (22). **Recommendation: A+B bundle** (A = layer รู้ชั้นตัวเอง additive `layer.floorKey`, ปิด P1/P3; B = chip "วาดที่: ชั้น 2 · GFA" + canvas tint + ◉ make-current, ปิด P2; zero schema break, old saves derive same numbers → assertEnginesAgree stays green by construction). **SPIKE ✅ PASS 4/4 `f54bac3`** (lite/sandbox/invent-layer-floorkey/): legacy parity 425==425 byte-identical · re-tag follows layer no-double-count 120==120 + divergence feed = reconcile banner rows · two-gfa-layers split buckets sum==byRole · CFSS multi intact. Key finding: floorKey เป็น field ชั้นหนึ่งบน tuple อยู่แล้ว — งานจริงคือ one-seam swap ใน objectTuples; byRole/totals floorKey-invariant by construction. Migration subtlety: object บน layer ที่มี floorKey แต่หน้า untagged จะโผล่ใน byFloorRole (เดิมหาย — semantic gain, ไม่กระทบ total). mockup.html = chip/tint/make-current/banner. Invent doc: docs/invent/lite-layer-floorkey.md. **GO A+B received 2026-07-03 (user checkpoint).** Build plan 2 sub-sprints: **A-model** — additive `layer.floorKey` (+ CFSS master floorKey:'multi'), `floorKeyOfObject()` precedence instance-master → layer → page ใน object-agg.js (one-seam swap per spike), PF-seed ตั้ง floorKey จาก folder, persist additive ใน .bmaplan (ตรวจ serialization path ของ LAYERS ก่อน), `detectDivergence()` export สำหรับ banner; guard test_layer_floorkey.py = 4 spike proofs กับแอปจริง. **B-ui** — NEW module layer-target-ui.js (DOM-inject): chip "วาดที่: ชั้น N · role", canvas edge tint สี active layer, ◉ make-current บน layer row, reconcile banner [ตามเลเยอร์][ตามหน้า]. **SHIPPED 2026-07-03: A-model `92174b6`** (floorKeyOfObject seam + PF-seed floorKey + detectDivergence + additive liteLayers persistence, `LITE_LAYER_FLOORKEY_OK` 5 proofs RED-proven) + **B-ui `20129af`** (layer-target-ui.js 307: chip วาดที่/tint/◉/reconcile banner, `LITE_LAYER_TARGET_UI_OK` C1-C4; user approved at review checkpoint) — **P1/P2/P3 ปิด**. Follow-up candidates: (a) LAYERS เข้า _docSnap ให้ [ตามหน้า] undo ได้ (pre-existing gap), (b) aggregate banner เมื่อหลาย layer ขัดพร้อมกัน. | build via lite-builder | layer / model + ux (lite) | ✅ A+B shipped | INV-20260703-layer-linkage ✅ |
| BUG-20260526-lite-wizard-followup | **BROKEN (lite)** — follow-up to BUG-20260526-lite-force-setup. Real-user testing 2026-05-26 surfaced 2 regressions: (A) tile dblclick in wizard Step 1 calls `loadPage(pn)` + `closeOverlays()` → bypasses LWIZ lock entirely, wizard closes but `__lwizAutoLockActive` stays true (since `_lwizCheckLiftLock` only fires from `liteSetTag` wrap, and dblclick doesn't tag). User loops back to page-setup state. (B) Done (Step 3 "ถัดไป →" when curStep===3) runs `_lovsForceFillMissingTags()` which calls `reseedActivePageFolders()` → folders + base layers ARE created in FOLDERS/LAYERS arrays, but `buildPicker()` is NOT called → left panel doesn't refresh → user sees no layers. Fix: (A) gate `tile.dblclick` on `!window.__lwizAutoLockActive` (show hint if locked). (B) append `buildPicker()` call after `reseedActivePageFolders()` in `_lovsForceFillMissingTags()`. Both ≤5-line edits in `lite/static/js/overview-setup.js`. Marker: `BUG_20260526_LITE_WIZ_FOLLOWUP_OK` (≥4 sub-checks: dblclick-no-op-when-locked / dblclick-works-after-unlock / done-refreshes-picker / layers-visible-after-done). source=bug-report 2026-05-26 | n/a (inline edit) | measure-ux / wizard (lite) | ✅ done 2026-07-03 `71ba5be` — both fixes found ALREADY LANDED in current overview-setup.js (A: L398-409 lock gate+hint; B: L956 buildPicker after reseed); missing deliverable was the guard test, now added `test_wiz_followup.py` BUG_20260526_LITE_WIZ_FOLLOWUP_OK 4/4, RED-proven by temporary revert | BUG-20260526-lite-force-setup |
| INV-2026-05-26-LFOC-ORDER-B | **LFOC-ORDER-B — kind-aware PF folder separation** (invent-done-go from `/lite-invent` 2026-05-26, spike 11/11 PASS). Approach A (composite-string ID): `pageFolderIdFor()` folds `pageFloorKind` into folder identity (`PF_basement_<N>`, `PF_mezz_<N>`, `PF_mech_<N>`; legacy `PF_floor_<N>` for normal/no-kind; legacy `PF_floor_roof` preserved). New `_rankPFFolder()` orders site=0 → basement-desc (B3=85, B2=90, B1=95) → floor-asc (110, 120, 130) → mezz-interleaved (+5) → mech (+7) → roof=9000 → excluded=9999. **Zero migration** — old saves without `kind` field render identically (kind=undefined → "normal" fallback). `_seedBaseLayers` extended to kind-aware seeds (basement=gfa/ded/ded; mezz=gfa/ded; mech=gfa). `.bmaplan` schema additive (NEW IDs coexist with legacy in `liteGroups`). Build via `/bma-lite-dev` (lite worker). Markers: NEW `LITE_PF_KIND_OK` (≥10 sub-checks: separation, ordering per kind, backward compat). Regression must stay GREEN: `LITE_PAGE_FOLDER_MODEL_OK`, `LITE_PAGE_FOLDER_PERSIST_OK`, `LITE_PAGE_FOLDER_UI_OK`, `LITE_PF_ORDER_OK`, `MEASURE_PARITY_OK`. Forbidden UNTOUCHED: measure-engine.js, RS, pdfToC/cToPdf, ui-lite.html (1200/1200), `.bmaplan` non-additive. **Spike:** `lite/sandbox/invent-lfoc-order-b-composite-id.html` (verified 11/11 by Playwright). **Frame:** `docs/invent/lfoc-order-b-kind-folders.md`. ~50 LOC net + new test. | `/bma-check-forbidden` (.bmaplan additive) | layer / model + schema | ✅ done — verified fully landed 2026-07-03 (build audit): all card items in page-folder-layers.js (pageFolderIdFor kind branches :95-130, _rankPFFolder :197-215, kind-aware seeds :225-271, Thai labels) + test_pf_kind_folders.py LITE_PF_KIND_OK 11/11 already in tree; floorKey parity with floorKeyOfPageFolderId proven (7 kind/tag pairs exact-inverse). Zero code change needed — card was stale, implementation shipped earlier | LFOC-keep + LFOC-ORDER-A |
