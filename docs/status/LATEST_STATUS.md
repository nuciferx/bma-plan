# LATEST_STATUS.md — BMA-Plan Current Feature State

Date: 2026-07-03 (INV-20260703-layer-linkage plan B COMPLETE + UX-batch-1 + save-fix follow-up SHIPPED — the staged B0-B5 rollout of the layer↔measurement redesign that reached its human GO checkpoint earlier the same day shipped end-to-end across 6 commits: B0 (`edc89ae`) NEW `lite/static/js/object-agg.js` single tuple-stream aggregation engine + I11 invariant oracle; B1-B2 (`6909486`+`00ab9b9`) report-vars/Summary/layer-tree/Review all rerouted onto the same tuple stream, closing H2 (dual-engine disagreement, now structurally impossible) + M6; B3 (`34594b7`) orphan-`catId` self-heal on `removeLayer` + load-time sweep, closing H3; B4 (`750d2f6`) NEW `lite/static/js/layer-move.js` move-object-to-layer UI, closing H1 — all 3 HIGH findings now closed; B5 (`3d3741e`) report-vars operand dropdown Σ/▸ optgroups, closing M4. Bundled: UX batch 1 (`34aefa3`) — F-7 modalOpen keydown guard, F-1/F-2/F-3 hotkey/discoverability fixes, cheatsheet corrections; and the save-fix bug-archive follow-up (`d40b20b`, already shipped in the prior finalize). Every commit independently verified pre-commit; `MEASURE_PARITY_OK` green at B4. Zero proto/ edits. Previous ship: UX-REVIEW-20260703 + BUG-20260703-lite-save-wipes-data SHIPPED — CRASH-tier save-wipe fix + full UI/UX journey review (9 FRICTION + 6 COSMETIC findings) + the `INV-20260703-layer-linkage` investigation reaching its GO checkpoint. Regression 17/17.)

<!-- GEN:START gen_status_docs -->
## Ship Ledger (generated)

| id | date | area | guards | commits |
|---|---|---|---|---|
| PROC-20260703-v2-u6-changelog | 2026-07-03 | process / release-tooling | — | `a42bde6` |
| PROC-20260703-v2-u2-impact-map | 2026-07-03 | process / test-infra | LITE_RUN_ALL_OK | `2df65d4` |
| PROC-20260703-v2-u3gen-u5-truth | 2026-07-03 | process / docs-tooling | TRUTH_CHECK_OK | `ea03f53`, `770ee14` |
| PROC-20260703-v2-u3ledger-u4-roadmap | 2026-07-03 | process / docs-tooling | — | `77a610f` |
| SLICE-20260703-report-edit-default-grid | 2026-07-03 | report / ui (lite) | LITE_REPORT_DEFAULT_GRID_OK | `16698bb`, `99c37db` |
| PERF-20260703-worker-recycle-chh-probe | 2026-07-03 | perf-probe / docs | — | `39de379` |
| BUG-20260703-lite-cfss-undo-masters | 2026-07-03 | measure / undo (lite) | LITE_UNDO_MASTERS_OK | `81c4325` |
| UX-20260703-quickwins-batch2 | 2026-07-03 | ui (lite) | LITE_UX_BATCH2_OK | `036a49d` |
| INV-20260703-layer-linkage | 2026-07-03 | measure+layer (lite) | LITE_OBJECT_TUPLES_OK, LITE_B1_ROLE_REROUTE_OK, LITE_B2_SINGLE_ENGINE_OK, LITE_B3_ORPHAN_HEAL_OK, LITE_B4_MOVE_LAYER_OK, LITE_B5_REF_BADGES_OK | `edc89ae`, `6909486`, `00ab9b9`, `34594b7`, `750d2f6`, `3d3741e` |
| UX-20260703-quickwins-batch1 | 2026-07-03 | ui (lite) | LITE_UX_QUICKWINS_OK | `34aefa3` |
| BUG-20260703-lite-save-wipes-data | 2026-07-03 | save/load (lite) | LITE_SAVE_CLICKPATH_OK | `d40b20b` |
| GO-20260703-invariants-streaming-worker-recycle | 2026-07-03 | test-infra + perf (lite) | LITE_WORKER_RECYCLE_OK | `6d6e39b`, `9466fe4`, `f8c2981`, `d52ddbb` |
| DOCS-20260702-dev-pillars-blueprint | 2026-07-02 | process / docs | — | `16e6495`, `b676652` |
| ACC-20260703-verify-scale-port | 2026-07-03 | measure-ux / accuracy (lite) | LITE_VERIFY_SCALE_OK | `bea2119` |
| AUDIT-20260702-render-followups | 2026-07-03 | render + test-infra (lite) | LITE_RENDER_FB_SCAN_OK, LITE_OVERLAY_REG_OK | `aec375b`, `13054b6` |
| AUDIT-20260702-s2-fitz-lock | 2026-07-02 | server perf (lite) | LITE_CASE_LOCK_OK | `d0a5dde` |
| PERF-20260702-lite-foxit-smoothness | 2026-07-02 | perf (lite) | LITE_PAGECACHE_LRU_OK, LITE_LOCAL_OPEN_OK, LITE_WARM_PREFETCH_OK, LITE_THUMB_WARM_OK | `ae0f168`, `3ec9239`, `e0fb856`, `a0c1152` |
| AUDIT-20260702-infra-bundle | 2026-07-02 | test-infra + server (lite) | LITE_RUN_ALL_OK, LITE_EXPORT_ENDPOINTS_OK | `9c4c36e`, `60d424a` |
| BUG-20260702-lite-pagerot-registration | 2026-07-02 | measure-geometry / render (lite) | LITE_PAGEROT_REG_OK | `9f4b298` |
| BUG-20260702-lite-cfss-summary | 2026-07-02 | measure-geometry (lite) | LITE_SUMMARY_CFSS_OK | `02e35af` |
| BUG-20260702-lite-arc-summary | 2026-07-02 | measure-geometry (lite) | LITE_SUMMARY_ARC_OK | `e5264e2` |
<!-- GEN:END -->

## Phase

Phase 1 = Raster PDF Measurement Assistant. No legal checker, OCR, AI, Rule Engine, FAR/OSR/pass-fail.

## Active Feature State (2026-07-03 — INV-20260703-layer-linkage plan B COMPLETE + UX-batch-1 + save-fix follow-up: staged B0-B5 layer↔measurement redesign rollout)

The full staged rollout of the layer↔measurement redesign that reached its human GO checkpoint earlier the same day shipped end-to-end across 6 commits, changing the runtime aggregation model for every consumer of measured-object totals — recorded here in full since it replaces the core rollup mechanism, plus a UX quick-wins batch.

- **B0 (commit `edc89ae`):** NEW `lite/static/js/object-agg.js` — the single tuple-stream aggregation engine at the center of the whole redesign. One generator walks every measured object across every page and emits a flat stream of `{pg, catId, role, floorKey, area, counting}` tuples. Also NEW: invariant I11 and its oracle `assertEnginesAgree` — the oracle takes any two consumer reductions over the same partition of the same tuple stream and asserts they always agree, which converts the H2 finding ("the Summary-widget aggregation engine and the Review-panel aggregation engine can disagree on net area because they are two independently-maintained rollup implementations") from a bug that must be watched for into an outcome the architecture makes impossible. Guard `lite/tests/test_object_tuples.py` (`LITE_OBJECT_TUPLES_OK`).
- **B1 (commit `6909486`):** `lite/static/js/report-vars.js` and the Summary widget's per-floor `gfa`/`ded`/`net` figures are rerouted onto the tuple engine, but only behind a new opt-in `{useLive:true}` flag — the default (existing) code path stays byte-identical, a deliberate low-risk staging choice so B1 could ship and be verified before anything actually depended on it in production. Guard `test_b1_role_reroute.py`.
- **B2 (commit `00ab9b9`):** the layer-tree's own Σ (sigma) totals and the Review-panel's Section-3 rows are switched to reduce the SAME tuple stream as Summary — this is the commit that actually closes H2, since after it there is exactly one aggregation engine, not two. Oracle check (c) proves `byFloorRole` partitions equal `byRole` partitions by construction. Side effect: M6 ("root-unfiled layers uncounted") is also closed, since the single engine's partitioning naturally includes them where the old bespoke per-consumer logic did not. Guard `test_b2_single_engine.py`.
- **B3 (commit `34594b7`):** `lite/static/js/layer-system.js` gains `reassignObjectsOfLayer`, called from `removeLayer`, plus a load-time `sweepOrphanCatIds` heal pass that runs on every project open. Together these close H3 — the finding that deleting a category left its objects' `catId` orphaned, silently dropping them from every total with no warning and crashing on at least one code path. The heal pass means even pre-existing `.bmaplan` saves with orphaned `catId`s self-repair on next open, no migration UI needed. Guard `test_b3_orphan_heal.py`.
- **B4 (commit `750d2f6`):** NEW `lite/static/js/layer-move.js` (120 lines, self-contained, DOM-injected at runtime — `lite/ui-lite.html` itself is untouched, following the same injection pattern as `verify-scale.js` and `centerline-snap.js`). This is the move-object-to-layer UI that closes H1, the last of the 3 HIGH findings: a select control added to the Properties panel, plus a new canvas right-click context-menu entry "ย้ายไปเลเยอร์ ▸" listing every layer on the current page. CFSS shared-shape instances are a special case — an instance doesn't own its own layer (its master does), so moving one shows a confirm dialog and retargets the MASTER instead. **Known flag carried forward, not a new regression:** CFSS-master-only layer moves are not currently undoable — this is a pre-existing gap in `_docSnap` coverage that B4's new UI simply makes reachable for the first time; recorded as a follow-up candidate. Guard `test_b4_move_layer.py`. **All 3 HIGH findings from the `INV-20260703-layer-linkage` checkpoint (H1/H2/H3) are now closed.**
- **B5 (commit `3d3741e`):** `report-vars.js`'s operand dropdown in the variable editor now groups its options into two `<optgroup>`s — Σ role-refs (aggregate-by-role references) and ▸ single-layer refs — closing M4, the finding that the two reference kinds looked identical in the editor UI and were easy to confuse. Display-only change; the persisted expression string format is unchanged, so no save-format migration is involved. Guard `test_b5_ref_badges.py`.
- **Bundled UX batch 1 (commit `34aefa3`):** F-7 — `modalOpen()` now recognizes `#sum` (the Summary widget) as an open modal via a new keydown guard, closing the exact class of hotkey-leak bug the same-morning UX review filed (typing inside an open panel was firing tool hotkeys instead of being captured). F-1 — the dead ⇧D Path hotkey now wired to the actual tool. F-2 — the `F`/Focus key collision resolved. F-3 — Page Manager gained a proper menu entry (previously reachable only via the undiscoverable ⇧F12 shortcut). Cheatsheet text corrected to match the real current bindings. Guard `lite/tests/test_ux_quickwins.py`.
- **Save-fix bug-archive follow-up (commit `d40b20b`, already fixed and recorded as its own `Active Feature State` entry in the prior finalize):** included here only for the narrative completeness of this closed arc — no new behavior beyond what was already shipped.
- **Verification:** every commit above was independently verified pre-commit with its own targeted guard suite plus a `t0` measure-parity sweep. `MEASURE_PARITY_OK` was explicitly re-verified green at B4 — chosen as the checkpoint because B4 is the piece most likely to have disturbed layer/object plumbing near the drift-locked vendored math. Latest verify (B5): `test_b5_ref_badges.py` + `test_report_vars_ui.py` + `test_report_vars_rollup.py` + `test_b1_role_reroute.py` + `run_all_tests.py --tier t0`, all green.
- **Known gap / follow-up:** UX quick-wins batch 2 (F-4 visible scale-verified badge, F-5/F-6 wizard/upload messaging, F-9 replace `window.prompt()` in Verify Scale, plus summary seeded-vars red-error display and a wizard Next-button gate) is in progress at the time of this write. B4's CFSS-master-undo gap is a candidate follow-up sprint. Worker-recycle production RSS re-probe on the CHH binder and V2 U3/U4 migration remain queued from earlier same-day blocks.
- New E2E markers: `LITE_OBJECT_TUPLES_OK` (B0); B1-B5 and the UX batch each have a dedicated guard test without a separately-branded marker name. Lite test suite continues to grow; every new guard file adds to the `run_all_tests.py` aggregate count.
- Shipped as commits `d40b20b` + `34aefa3` + `edc89ae` + `6909486` + `00ab9b9` + `34594b7` + `750d2f6` + `3d3741e` on `main`.

## Active Feature State (2026-07-03 — UX-REVIEW-20260703 + BUG-20260703-lite-save-wipes-data: CRASH fix (Ctrl+S wiped all data) + full UI/UX journey review + layer↔measurement invent checkpoint)

CRASH-tier bugfix (changes save behavior for every project) plus a findings-filing review plus an invent checkpoint — the bugfix changes runtime persistence behavior, so it's recorded here.

- **(1) Save-wipe CRASH fix (commit `d40b20b`):** Ctrl+S wrote an empty `.bmaplan` and destroyed the in-memory session — the most severe class of defect this project tracks, total silent data loss on the single most-used action in the app. Root cause: `pageMgr` seeded `PS_by_id` as `deepCopy` snapshots of `PS` taken at upload time (empty at that point); all drawing thereafter mutated live `PS` only, never the snapshots; `mi-save` → `_pmCommit` then projected those stale empty snapshots back over `PS` immediately before `buildPageStore()` serialized it — so save always wrote the empty upload-time state. 72 previously-green tests missed this because every existing save test called `buildPageStore()` directly (the API/serialization path) rather than driving the real `mi-save` click a user actually triggers. Fix: new `projectToGlobals(livePS)` (`lite/static/js/page-manager.js`, +38/−) resolves each page id's CONTENT from live `PS` by identity — baseline position via `_initialIds`, duplicates via a new `dupSrc` map that deep-copies the SOURCE page's live content, merged pages stay blank as intended; `_pmCommit` now passes `PS` through this resolver, and model snapshots are refreshed post-commit so they stop going stale. The same latent wipe existed on the Apply/merge path (`lite/static/js/page-manager-ui.js`, +8/−, 2 call sites in `_pmuiApplyChanges` + merge) — commit order was backwards (flush-then-commit instead of commit-with-pre-flush-baseline-then-flush); reordered to commit first.
- **New guard:** `lite/tests/test_save_clickpath.py` (NEW, 190 lines, marker `LITE_SAVE_CLICKPATH_OK`) drives the REAL `mi-save` click via `URL.createObjectURL` interception — the click-path-vs-API-path gap that let 72 green tests miss this bug in the first place. Proven RED pre-fix (exact journey repro: polys 0, calib null after save) → GREEN; 4 checks including "duplicate page content follows identity, not position." Regression 17/17: metamorphic suite, page-manager suite, apply-mutations suite, all persist suites, arc-summary parity, CFSS-summary parity, `MEASURE_PARITY_OK` (drift-lock intact — zero vendored-math touch).
- **(2) UX review filed (`UX-20260703-review-findings`, commit `2249400`):** a full journey review on the real 45-page permit PDF (29 screenshots captured to `artifacts/ux-review-20260703/`) plus a static code inventory surfaced 9 FRICTION findings — F-1 dead ⇧D hotkey, F-2 `F`/Focus key collision, F-3 Page Manager undiscoverable (⇧F12-only, no menu/button path), F-4 scale-verified state invisible after the verify modal closes, F-5 the setup wizard silently blocks all mouse input with no visible modal cue, F-6 misleading "เปิด PDF ก่อน" message shown during a still-in-flight background upload, F-7 `modalOpen()` fails to recognize `#sum`/`#vs-modal`/the cheatsheet as open modals so typing in them fires tool hotkeys instead of being captured, F-8 raw/unfriendly error messages surfaced to the user, F-9 the Verify Scale flow uses a native `window.prompt()` instead of the app's own modal system — plus 6 COSMETIC findings (annotate mode has no hotkeys documented, Thai/English label inconsistency, a stale on-screen cheatsheet, cluttered empty-state UI, a transient "scanned PDF" notice that disappears too fast to read, no visible fallback-render badge when PDF.js degrades to raster) and 3 GOODs explicitly pinned so future refactors don't regress them (setup wizard flow, local-first "green dot" save status, live in-progress measurement feedback). Findings-filing only — no application code changed by the review itself (the CRASH it surfaced was already fixed same-morning by piece 1).
- **(3) Layer↔measurement invent at checkpoint (`INV-20260703-layer-linkage`, commit `2249400`):** an investigation mapped the current layer/measurement data model and enumerated 8 concrete problems (3 rated HIGH: no UI exists to move an object between layers once drawn; the Summary-widget aggregation engine and the Review-panel aggregation engine can disagree on net area because they are two independently-maintained rollup implementations; an orphaned `catId` silently drops the object from every total with no warning, and in one path crashes). `bma-inventor`'s diverge/score phase produced 5 candidate approaches; Approach B "one aggregation engine" won 25/30 — a single `object-agg.js` tuple-stream generator that every current consumer (summary, review, export, per-floor net) is rewritten as a pure reduction over, making per-floor net calculation native instead of bolted-on, and is fully additive (no schema break). Fallback approach D (a simpler `floorKey` field directly on objects) was also scored and recorded. A staged rollout plan B0→B5 was written. GO received at the human checkpoint. **B0 (the tuple generator + a new `I11` invariant oracle) and a first batch of UX quick-wins (F-7 modal-detection fix, F-1/F-2/F-3 hotkey/discoverability fixes, plus a cheatsheet accuracy pass) are being built right now by two `lite-builder` subagents running in parallel** — recorded as in-progress, not yet shipped; nothing from B0 or the UX quick-wins batch landed in this docs-write's commit range.
- **Known gap / follow-up:** B0 + UX quick-wins batch 1 need verification + commit as their own follow-on sprint. B1-B2 (reroute existing consumers onto the tuple-stream engine) queued after B0 lands. UX quick-wins batch 2 (F-4/F-5/F-6/F-9 + summary seeded-vars red-error display + wizard Next-button gate) queued. The click-path-vs-API-path testing gap is a candidate lesson for `lite/tests/INVARIANTS.md`, not yet added there (deliberately out of scope while two parallel builders are active in `lite/`).
- New E2E marker: `LITE_SAVE_CLICKPATH_OK`. Lite test suite regression: 17/17 targeted files green.
- Shipped as commits `d40b20b` + `912c3e2` + `2249400` on `main`.

## Active Feature State (2026-07-03 — GO-20260703-invariants-streaming-worker-recycle: V2-U1 invariant registry + Range-streaming spike NOGO→RESHAPE + worker-recycle build)

Continuation of the same-day "GO" loop block, resumed right after `BLOCK-20260703-clear-queue` closed and the Range-streaming human checkpoint was cleared. Four pieces of work; the worker-recycle piece changes `lite`'s runtime pdf.js worker lifecycle, so it's recorded here alongside the process/research pieces.

- **(1) V2-U1 invariant registry (commit `6d6e39b`):** NEW `lite/tests/INVARIANTS.md` — a canonical registry of 10 invariants (I1–I10), each mapped to its guard test + tier, plus the two mandatory SCOPE questions ("which invariants does this feature touch" / "does it create a new object kind") — the prevention mechanism `docs/process/DEVELOPMENT_V2_BLUEPRINT.md` U1 called for, born directly from the arc-summary/CFSS-summary postmortem where a new object kind silently bypassed 6 rollup consumers. New-kind rule: fixtures added to I2/I4/I5 in the same sprint a new kind is introduced, no exceptions. Same commit reconciles 16 stale `PHASE_INDEX.md` rows (`lpm-1..9`, `cfss-guard`, `EVOLT-1..5`, `INV-2026-05-25-CFSS`) that claimed `queued` but were long since shipped — each now carries its real `fixed_commit` pulled from `bug-archive.jsonl`/the test suite. The queued `ptToScreen`/`screenToPt` parity-fixture card is closed with a rationale instead of code: the behavioral lock already exists as invariant I7 (`LITE_PAGEROT_REG_OK` asserts the 4-angle closed-form mapping + exact inverse at 1e-9; runtime routes through the parity-tested `pdfToC`/`cToPdf` kernel since `9f4b298`).
- **(2) Range-streaming SPIKE (commits `9466fe4`+`f8c2981`), GO received via `/loop`:** the 5-step spike from `docs/invent/lite-range-streaming.md` was executed for real in `lite/sandbox/invent-range-streaming/` against RAMA4 (19 MB) and the real CHH binder (95 MB) — not simulated. Results: (S1) both customer PDFs are NON-linearized yet stream fine — only 20% of bytes fetched, the standing linearization concern is moot; (S2) Starlette's `/raw` route already serves `206 Partial Content`+`Content-Range` with zero backend changes needed; (S3) streaming cut CHH's RSS by only 10% (1675→1503 MB) against a ≥50% GO criterion — **FAILS**; pdf.js worker-heap bug #10730 CONFIRMED on the pinned 4.0.379 build (`doc.destroy()` frees the main document heap 409→98 MB but the separate pdf.js WORKER heap survives untouched — this worker heap, not the document heap, is the real ~1.5 GB ceiling); (S4) `PDFDataRangeTransport` over `Blob.slice` works mechanically (no full local copy needed) but delivers no memory win given (S3). **VERDICT: NOGO on streaming-as-a-memory-fix**, RESHAPE to worker-recycle — the spike's whole point was to prevent a doomed build sprint from being attempted, and it did.
- **(3) worker-recycle BUILT (commit `d52ddbb`), the RESHAPE, GO via `/loop`:** `lite/static/js/page-renderer.js` gains explicit `PDFWorker` ownership (`_docWorker`, passed into `getDocument` from both the `openLocal` and `/raw` paths — previously pdf.js silently created an implicit worker per document that `doc.destroy()` could never fully reclaim) plus a cheap `_docSource` re-open handle (a retained local `File`/`Blob` reinit with zero network cost, or the `caseId` for a one-shot `/raw` refetch) plus `recycleDocWorker()` — guarded against re-entrant recycling, skipped outright if a render is in-flight or no source is retained, and preserving `pageDims`/`pageRot`/`_scanned` metadata across the teardown. The next `loadPage()` call after a recycle transparently triggers a lazy `_reinitDoc()` — no visible state change to the user beyond a brief re-render. Recycle triggers: tab hidden ≥60 s, app idle ≥5 min AND `pageCount>20` (small documents aren't worth recycling), and a manual `PageRenderer.recycleNow` hook for tests. `_resetCache()` now also destroys the explicit worker on new-upload, closing a latent leak where the old implicit-worker pattern would otherwise accumulate one abandoned worker per upload in a long session. `page-renderer.js` grows to 790/1000 lines; `lite/ui-lite.html` gains exactly one call-site line (1170/1200). Built by `lite-builder`, independently verified. NEW guard `lite/tests/test_worker_recycle.py` (`LITE_WORKER_RECYCLE_OK`, 7 checks: teardown state, transparent reinit repaint, zero-refetch when a local blob is retained, metadata survival, in-flight-render SKIP, heap-not-worse assertion measured at −0.8%). Honest scope note: the full "CHH RSS −50%" acceptance bar was measured in the spike's pattern (S3), not re-measured against this specific production implementation — a production re-probe on the real CHH binder is recorded as the queued follow-up measurement, not assumed. Regression 6/6 + `run_all_tests.py --tier t0` green.
- **(4) Streaming-as-bandwidth card DEFERRED-until-remote-deployment:** the optional second card proposed by the spike (Range-streaming purely as a bandwidth optimization, independent of the memory question) was evaluated and explicitly deferred, with a recorded rationale (เสา 1 — "build only what today's product needs"): all `/raw` byte transfer happens over localhost on the current desktop-only product (a real 91 MB `/raw` fetch measured at 498 ms), so the spike's 80% byte-count reduction has no user-facing benefit until a remote/networked deployment target actually exists.
- **Known gap / follow-up:** production RSS re-probe of worker-recycle on the real CHH binder (extends `artifacts/perf/` probe tooling). V2 migration remainder: U3 (`SHIPS.jsonl` ledger), U4 (roadmap split ACTIVE/DONE + reconcile script — this block's 16-row fix was done by hand, not tooling). proto backlog decision (freeze vs. hardening pass) still pending. Release ritual per V2-U6 due before next build hand-off.
- New E2E marker: `LITE_WORKER_RECYCLE_OK`. Lite test suite: 72 files, all green.
- Shipped as commits `6d6e39b` + `9466fe4` + `f8c2981` + `d52ddbb` on `main`.

## Active Feature State (2026-07-03 — BLOCK-20260703-clear-queue: fitz lock + render fallback/scan-detect + V2 tiers/overlay-proof/streaming-research + Verify-Scale port + process docs)

Five same-session ships clearing the entire remaining queue per user directive ("ทำทั้งหมด"). Each ship changes runtime behavior in `lite`, so all five are recorded here.

- **Ship 1 — `AUDIT-20260702-s2-fitz-lock` (commit `d0a5dde`):** `lite/server_lite.py` gains a per-case `threading.Lock` (`_case_lock`) that now serializes ALL `fitz.Document` access for that case: `/page` + `/thumb` render blocks (inside the existing double-checked cache), `/pageinfo`, and — most significantly — the whole `/export-pdf-overlay` render path, which is now ALSO moved off the event loop via `run_in_threadpool` around a new `_render_overlay` helper, completing the S2 threadpool-offload goal that `AUDIT-20260702-infra-bundle` had deliberately deferred. The copy/close/swap critical sections of apply-page-mutations + merge-pages are also covered (sync-only regions, no `await` under lock). Urgency was raised by `PERF-20260702-lite-foxit-smoothness` Sprint 4's sequential thumbnail warm making concurrent `/thumb`+`/page` traffic routine in normal use. NEW guard `lite/tests/test_case_lock.py` (`LITE_CASE_LOCK_OK`): a 96-request 8-thread hammer test, plus a doc-swap mutation fired mid-flight, plus concurrent overlay-export calls — zero 5xx observed across all three scenarios. **Honesty note:** this is a hardening-hammer test, not a deterministic RED-before-fix proof — a native `fitz` race is probabilistic and cannot reliably be forced to fail pre-fix on demand. Regression 9/9.
- **Ship 2 — render-followups (a)+(c) (commit `aec375b`):** two items closed from the `AUDIT-20260702-render-followups` bundle filed by Review C. (a) raster fallback: `loadPage()` in `lite/ui-lite.html` no longer produces a blank canvas when PDF.js is unavailable — it falls back to the server JPEG render path (`/pageinfo` + `/page?rot` into `curImg`, drawn through the same view-transform math used elsewhere), gated by an `instanceof` guard that keeps the existing legacy render-path shims safe; measurement stays fully functional on the fallback. (b) scanned-page detection: a free, post-render operator-list heuristic flags image-only (scanned) pages; the re-render scale on such pages is capped at `RS*4` (~432 DPI) with a transform compensation (`up = want/use`) so on-screen registration stays bit-identical to before — the only thing saved is wasted re-raster CPU on pages that gain nothing from higher scale, not any accuracy change — plus a one-time Thai-language hint shown to the user. NEW guard `lite/tests/test_render_fallback_scanned.py` (`LITE_RENDER_FB_SCAN_OK`, 6 checks); two initial false-alarm findings were traced to the TEST harness itself (a uniform-fill sampling artifact and an un-recentered zoom in the test setup) and fixed there, not in application code. Regression 8/8.
- **Ship 3 — V2 tiers + overlay proof + streaming research (commit `13054b6`):** three parts, none touching runtime measurement behavior directly except the registration proof. (i) `lite/tests/run_all_tests.py --tier t0/t1/t2` implements migration step U2 from the new `docs/process/DEVELOPMENT_V2_BLUEPRINT.md`; `t0` (pure measure-math via a Node subprocess) now runs in 1.26 s, hitting the blueprint's <5 s target. (ii) NEW `lite/tests/test_overlay_registration.py` (`LITE_OVERLAY_REG_OK`), built by a delegated agent and independently re-run by the main agent — the first PIXEL-level proof that the raster canvas and the exported overlay stay registered: max device-pixel offset 0.50 at zoom×2, 0.33 at fit-to-page, 0.40 at `pageRot=90°` (tolerance 4 px). This closes the evidence gap the 2026-05-28 render-quality spike left open (its 24/24 PASS was measured only within PDF.js's own coordinate space, never cross-checked against `ptToScreen`) and adds the visual coverage `BUG-20260702-lite-pagerot-registration`'s guard test lacked for the combined `V.rot≠0`+`pgRot≠0` case. (iii) `docs/invent/lite-range-streaming.md` — Range-streaming research by `bma-researcher`: verdict `PRIOR_ART_PARTIAL`; key risks are PDF.js worker-heap bug #10730 (`destroy()` may not free `commonObjs`, so CHH's remaining ~600 MB doc-level heap may be irreducible without a RESHAPE fallback of idle worker-recycling), untested `Blob.slice` local transport, unknown customer-PDF linearization status, and the possibility that Starlette's `/raw` route may already support HTTP Range with no new server code. Includes a 5-step spike design + explicit GO/NOGO criteria. **HALTED at the human decision checkpoint per Pack H invention discipline — zero code shipped for this piece, by design.**
- **Ship 4 — `ACC-20260703-verify-scale-port` (commit `bea2119`):** ports Verify-Scale from proto's `INV-2026-05-20-001` — identified by the 2026-07-02 Foxit comparison as the single remaining accuracy gap vs. the competitive benchmark (no incumbent viewer ships an integrated 2nd-reference cross-check). NEW `lite/static/js/verify-scale.js` (225 lines, self-contained): 2-point verify capture that reuses the existing calibration pipeline; %deviation banding (green <0.5%, yellow <2%, red ≥2%) with implied area error shown as ≈2×%dev; three actions (accept / recalibrate / average); additive schema field `PS[pg].scale.verifyResult` riding the existing `calibScale` serialization (no migration needed, old saves load fine without it). `lite/ui-lite.html` +3 net lines: new menu item `#mi-verify-scale`, scale-tool routing on `state.verifyMode`, `Shift+S` shortcut, script tag — stays inside the 1170/1200 line cap. `lite/static/js/menu-flyout.js` +2 lines so the menu rebuild picks up the new item automatically. Built by the `lite-builder` subagent, independently verified by the main agent (line-diff review + a 9-test targeted subset re-run). NEW guard `lite/tests/test_verify_scale.py` (`LITE_VERIFY_SCALE_OK`, 7 checks including a save/load round-trip of `verifyResult` and confirming the "average" action correctly re-derives downstream areas).
- **Ship 5 — process docs (commits `16e6495` + `b676652`, late 2026-07-02):** `docs/process/DEVELOPMENT_PILLARS.md` — a 6-pillar doctrine distilled from the whole day's work, each pillar backed by a concrete incident from this project's own history (e.g. Ship 1's "hardening hammer, not a proof" honesty; Ship 2's test-side false alarms) plus an explicit halt condition; `docs/process/DEVELOPMENT_V2_BLUEPRINT.md` — 6 evidenced structural weaknesses mapped to 6 structural upgrades (U1 `INVARIANTS.md`, U2 tiered test runner, U3 `SHIPS.jsonl` ledger, U4 roadmap split+reconcile, plus 2 more), a migration order, and a measurable success bar. U2 is already partially landed via Ship 3's `--tier` flag; the rest remains queued.
- **Final validation:** the full lite test suite — 70 files, all 70 green — was run in three chunks after background `run_all_tests.py` invocations kept getting killed (17 files in a partial run whose log survives at `artifacts/run_all_tests_20260703_final.log`, plus 27 and 26 more in two foreground chunks). `MEASURE_PARITY_OK` stayed green throughout every ship — zero vendored-math touch across the whole block. The suite grew from 60 to 70 files across the two days (2026-07-02 → 2026-07-03).
- **Known gap / follow-up:** Range-streaming (Ship 3, item iii) awaits a human GO/NOGO/RESHAPE decision — the only item in the whole cleared queue still awaiting anything other than more agent work. `test_case_lock.py`'s hammer test is probabilistic, not deterministic — any future native-race regression here would need a more targeted repro. `ptToScreen`/`screenToPt` still are not literally inside the `test_measure_parity.py` parity fixture (runtime routes through the vendored kernel since `BUG-20260702-lite-pagerot-registration`, but the fixture entry itself is separate remaining work).
- New E2E markers: `LITE_CASE_LOCK_OK`, `LITE_RENDER_FB_SCAN_OK`, `LITE_OVERLAY_REG_OK`, `LITE_VERIFY_SCALE_OK` (Ship 3's tiered runner flag has no dedicated pass/fail marker of its own). Lite test suite: 70 files, all green.
- Shipped as commits `d0a5dde` + `aec375b` + `13054b6` + `bea2119` + `16e6495` + `b676652` on `main`.

## Active Feature State (2026-07-02 — PERF-20260702-lite-foxit-smoothness: page-cache LRU + local-first open + worker warm-up/prefetch + thumbnail warm)

Four same-day lite performance sprints, not a user-facing feature but a change to `lite`'s runtime open/render/caching behavior, so it's recorded here.

- **Driving evidence:** empirical perf probe `artifacts/perf/probe_results_20260702.txt` measured RAMA4 (18.3 MB) at 3.9s cold first-paint, CHH (90.8 MB real customer file) at 9.6s cold first-paint + 766 MB heap after 10 pages viewed, and a flat ~1.2s PDF.js library+worker boot floor on every open regardless of file size. Time attribution: `UPLOAD` dominates at ~80ms/MB while `/raw` is nearly free. The probe also REFUTED (0/10 across 3 files) a standing suspicion that panning blanks the canvas mid-drag, and found the earlier "overview thumbnails 0/0" finding was a probe selector artifact — real number is 45/45 thumbnails in 9.2s cold (~200ms/thumb), instant warm.
- **Sprint 1 — page-cache LRU (commit `ae0f168`):** `lite/static/js/page-renderer.js` — `MAX_PAGE_CACHE=4` with LRU eviction; an evicted `PDFPageProxy` gets `.cleanup()` called only when no remaining alias key references it; `curPage` (currently rendering) never evicted; `getPage()` transparently re-fetches on a cache miss. Measured: cache keys 12→4, CHH heap 766→~628 MB (−18%). Remaining ~600 MB is PDF.js document-level shared state (fonts, XObjects) unreachable by page-level eviction — left to the queued Range-streaming invent card. NEW guard `lite/tests/test_pagecache_lru.py` (`LITE_PAGECACHE_LRU_OK`), RED pre-fix, 10/10 regression.
- **Sprint 2 — local-first open (commit `3ec9239`):** `uploadPdf()` in `lite/ui-lite.html` opens the locally-selected file's bytes immediately via `PageRenderer.openLocal()` — first paint before the server upload even starts — while the server upload runs in the background; `PageRenderer.adoptCase(caseId)` binds the returned `case_id` once it lands so `/raw` is NEVER fetched. Server-gated features guarded during the null-`caseId` window (`openOv` no-op, exports alert). Direct-HTTP-upload path used by every other existing test preserved unchanged. Measured: paint at 475ms with upload still in flight; on a real 91 MB binder removes the dominant ~80ms/MB upload wait from the critical path (~7.5s → ~1-2s). NEW guard `lite/tests/test_local_open.py` (`LITE_LOCAL_OPEN_OK` — paint-before-upload ordering, feature gate, adoption, zero `/raw` fetches, post-adoption page switch), RED pre-fix, 10/10 regression.
- **Sprint 3 — worker warm-up + adjacent prefetch (commit `e0fb856`):** the flat ~1.2s PDF.js library+worker boot is preloaded at app idle via `requestIdleCallback` (`window.__pdfjsWarmed`); after `loadPage(n)`, the `n±1` `PDFPageProxy` is fetched at idle too, so page switches skip the worker round-trip; prefetched pages count toward the `MAX_PAGE_CACHE` budget from Sprint 1, `curPage` stays LRU-protected. NEW guard `lite/tests/test_warm_prefetch.py` (`LITE_WARM_PREFETCH_OK`), RED pre-fix, 9/9 regression.
- **Sprint 4 — sequential thumbnail warm (commit `a0c1152`):** `PageRenderer.warmThumbs()` fetches `/thumb/1..N` sequentially with 120ms gaps after upload lands, so the overview grid opens hot; deliberately sequential (not parallel) — avoids the `lpm-8` lesson and the still-queued per-case `fitz` lock (`AUDIT-20260702-s2-fitz-lock`) makes concurrent `get_pixmap()` unsafe. Token-cancelled on `resetCache()`/new upload. NEW guard `lite/tests/test_thumb_warm.py` (`LITE_THUMB_WARM_OK` — all pages warmed once, ≥60ms sequential gaps proven, mid-run cancel), 8/8 regression.
- **Known gap / follow-up:** `PERF-20260702-open-streaming` (queued invent) is the only remaining perf piece — CHH's remaining ~600 MB doc-level heap needs Range-streaming (`PDFDataRangeTransport`), which touches the page-renderer's buffer-ownership contract and has documented quirks, so it is deliberately routed to the invent pipeline rather than attempted same-day.
- New E2E markers: `LITE_PAGECACHE_LRU_OK`, `LITE_LOCAL_OPEN_OK`, `LITE_WARM_PREFETCH_OK`, `LITE_THUMB_WARM_OK`. Lite test suite: 67 files, all green.
- Shipped as commits `ae0f168` + `3ec9239` + `e0fb856` + `a0c1152` on `main`.

## Active Feature State (2026-07-02 — BUG-20260702-lite-pagerot-registration: ptToScreen/screenToPt now rotation-aware, export prerotates raster)

Bugfix, not a new feature — but it changes on-screen geometry registration and export output for any project that uses manual page rotation, so it's recorded here.

- **Root cause:** Manual page rotation (`pageRot`) rotates the PDF.js raster canvas via `getViewport({rotation: V.rot + pgRot})`, but the coordinate contract `ptToScreen`/`screenToPt` in `lite/ui-lite.html` ignored `pgRot` entirely — the host contract function `getRot()` was hardcoded to return 0. Intrinsic PDF `/Rotate` metadata was always handled correctly; only the manual rotate-page menu path was broken.
- **Effect ("right value, wrong location" bug):** pre-existing geometry detached from the visible plan by up to a page diagonal (~84 m at 1:100 A1) whenever a user manually rotated a page. Geometry drawn WHILE a page was rotated got stored bound to the un-rotated frame — the reported area/length value stayed correct, but the on-screen location was wrong (attached to the wrong feature). `/export-pdf-overlay` did not apply `pageRot` at all, so exported annotated PDFs never matched what the user saw on screen whenever `pgRot≠0`.
- **Dead-code gap:** the rotation-aware `pdfToC`/`cToPdf` branches already existed in the drift-locked, parity-tested `lite/static/js/measure-engine.js` (vendored from proto) but were never actually invoked at runtime because `ptToScreen`/`screenToPt` didn't route through them.
- **Fix (commit `9f4b298`, "Fix A — proto-parity"; chosen over "Fix B — geometry-baking" by an Opus specialist patch plan):** (1) `getRot()` (a host contract function, explicitly editable per `measure-engine.js`'s own header — NOT the drift-locked vendored math itself) now returns `pageRot[pg]||0`; (2) `ptToScreen`/`screenToPt` route through the vendored `pdfToC`/`cToPdf` rotation branches — net 0 lines added to `ui-lite.html`, zero user-geometry mutation, no undo interaction, no float drift, and old `.bmaplan` files (which already persisted `pageRotations`) simply start rendering correctly with no migration step; side effect closes the "un-vendored coordinate math" drift-lock gap flagged by the same-day audit; (3) export WYSIWYG: `export-annotate.js` now sends the per-page rotation with the export payload, and server `/export-pdf-overlay` prerotates the raster via `Matrix(RS,RS).prerotate(rot)` and maps every coordinate (objects, labels, annotations) through a new `_rp` helper mirroring `pdfToC` — the prerotate direction was verified EMPIRICALLY against all 4 angles with a standalone pixel test before wiring it into the endpoint.
- **Fix B rejected:** transforming stored points at rotate time would mutate ~6 different geometry stores, require new undo snapshotting, introduce float drift on repeated rotate operations, and need a save-format migration the additive-only `.bmaplan` schema cannot express.
- **New guard test:** `lite/tests/test_pagerot_registration.py` (marker `LITE_PAGEROT_REG_OK`) — 5 checks: 4-angle screen-coordinate mapping vs. a closed-form quadrant transform; `screenToPt` exact inverse of `ptToScreen` (tolerance 1e-9); area invariant under rotate (stored points never mutated); `pageRotations` round-trip through a real `loadProto` save/load cycle; export output dimensions swap correctly (600×450) + stroke pixels found at the expected rotated vertex (585,15). Proven RED on pre-fix code via `git stash` (mapped to (15,15) instead of (585,15); `rotRestored` false; export un-rotated 450×600) → GREEN after.
- **Regression:** 16 at-risk files green (`test_page_rotate.py`, `test_metamorphic_pages.py`, `test_snap_types.py`, `test_arc_edge.py`, `test_ortho.py`, `test_cfss_drag.py`, `test_cfss_ui.py`, `test_centerline_snap.py`, `test_annot_label.py`, `test_live_overlay.py`, `test_measure_parity.py`, `test_pbt_measure.py`, `test_export_endpoints.py`, `test_summary_arc_parity.py`, `test_summary_cfss_parity.py`, `test_pagerot_registration.py`) + 26 more from a partial `run_all_tests.py` pass = 42 distinct files green. `MEASURE_PARITY_OK` green confirms the drift-locked vendored math is untouched — only the host contract function `getRot()` changed.
- **Known gap:** combined `V.rot≠0` AND `pgRot≠0` is asserted only via exact-inverse round-trip, not raster-pixel alignment — per specialist analysis, `fit()`'s corner-min absorbs the translation. The queued `AUDIT-20260702-render-followups` bundle item (e) "real overlay-registration Playwright test" remains open and would add visual coverage for this combined case. `lite/static/js/page-rotate.js` also has a stale comment claiming the server handles rotation via `?rot=N` (actually client-side PDF.js rotation) — noted but not edited this sprint.
- Shipped as commit `9f4b298` on `main`. Discovered + fixed via the `/bma-bug-report` pipeline, sourced from the same-day `AUDIT-20260702-infra-bundle` render-engine accuracy review (Review C).

## Active Feature State (2026-07-02 — AUDIT-20260702-infra-bundle: aggregate test runner + export payload caps + render-engine accuracy review)

Test/render infrastructure hardening — same-day follow-on to the 2-bug 2026-07-02 measurement-accuracy audit. Not a user-facing feature change (Sprint A/B are test-infra + defensive validation; Review C is read-only).

- **Sprint A — AUDIT-20260702-runner-preflight (commit `9c4c36e`):** NEW `lite/tests/run_all_tests.py` — discovers every `test_*.py` in `lite/tests/`, runs each standalone with a per-test timeout (default 420s), prints a summary table, exits `LITE_RUN_ALL_OK`/`LITE_RUN_ALL_FAIL`. Options `--filter`/`--fail-fast`/`--timeout`. A PREFLIGHT step fails fast on low disk (<2 GB on both the repo drive and the system drive — hardening from the 2026-07-02 `ENOSPC` incident where a full C: made Google Drive File Stream unable to hydrate any repo file), missing `uvicorn`/`playwright`/`fitz`, or missing `node`. First full run: 60/60 tests PASS in 8.5 minutes (`artifacts/run_all_tests_20260702.log`). Closes the "no aggregate runner" gap flagged as a follow-up by both audit bugs shipped earlier today.
- **Sprint B — AUDIT-20260702-export-caps (commit `60d424a`):** `lite/server_lite.py` — S1: `/export-pdf-overlay` now validates the payload BEFORE rendering with 5 caps (`MAX_EXPORT_PAGES=2000`, `MAX_OBJECTS_PER_PAGE=500`, `MAX_ANNOTS_PER_PAGE=500`, `MAX_PTS_PER_OBJECT=2000`, `MAX_COORD_ABS=20000` — rejects NaN/inf), returning HTTP 400 with detail rather than silently truncating or hanging; all caps ≥10x realistic worst case observed on the real 45-page permit PDF so no legitimate flow is blocked. Bonus fix: this validation pass also fixes a latent HTTP 500 where a non-numeric page-key crashed `sorted(key=int)` before any cap check ran. S5: `/export-xlsx` gets a row cap `MAX_XLSX_ROWS=20000`. S2 (partial): `wb.save()` offloaded via `run_in_threadpool` (provably safe — pure local objects); the overlay-render offload was deliberately DEFERRED to `AUDIT-20260702-s2-fitz-lock` because `/page/{n}`/`/thumb/{n}` are already `sync def` (threadpooled by FastAPI) and PyMuPDF `Document` objects are not thread-safe — naive wrapping would allow concurrent `get_pixmap()` on the same `doc`; the correct fix needs a per-case lock first. NEW `lite/tests/test_export_endpoints.py` (marker `LITE_EXPORT_ENDPOINTS_OK`, 14 checks) — first real HTTP tests of either export endpoint (previously only client-side `dlPost` stubs existed): XLSX bytes openable by `openpyxl` with sheet+row assertions, overlay output is a valid `%PDF` with correct page count, oversize payloads (pages/pts/objects) all 400, malformed payloads (unknown `case_id`, non-numeric page key, `1e12` coordinate) all 400, XLSX row cap 400. Patch plan authored by an Opus reviewer agent (read-only, cap-justification table); main agent applied.
- **Review C — PDF render-engine accuracy review (read-only, Opus agent; zero code change):** Verdict: `PDFJS-VIEWPORT-CLIPPED` architecture (shipped 2026-05-28) is sound — the coordinate contract between the raster canvas and `ptToScreen`/`screenToPt` is algebraically exact when `V.rot` and `pgRot` are both 0 (residual ≈ ±0.5 device px ≈ 1 mm at working zoom / 30 mm at fit-to-page on a 1:100 A1 sheet — the click-precision floor of a mouse-driven canvas UI, not a measured-value error); intrinsic PDF `/Rotate` metadata is handled correctly; the stale-render token guard (`_renderToken`) is solid; vector sharpness holds to roughly 4320 DPI effective, versus proto's fixed 108 DPI raster.
- **NEW BUG filed (BROKEN, top priority):** `BUG-20260702-lite-pagerot-registration` — manual page rotate rotates the raster canvas but `ptToScreen`/`screenToPt` ignore `pgRot`, so pre-existing geometry detaches from the visible plan (up to a page diagonal); geometry drawn while the page is rotated is stored bound to the un-rotated frame (correct area value, wrong on-screen location); export does not apply `pageRot` either. No test currently guards this. Needs `/bma-check-forbidden` before fixing — likely touches `ptToScreen`/`screenToPt`.
- **NEW bundle filed:** `AUDIT-20260702-render-followups` — (1) no fallback from PDF.js failure to the legacy `/page/{n}` JPEG raster path (canvas goes blank today); (2) pan uses a single shared `_offCanvas` double-buffer cleared before the async render completes (likely blanks canvas mid-drag on slow renders); (3) no scanned-PDF detection (vector sharpness gain is real for vector PDFs but zero benefit + no messaging for raster-scanned PDFs); (4) the "13 MB" memory footprint claim excludes the whole-PDF resident buffer plus an unbounded `pageCache`; (5) no real overlay-registration Playwright test exists (the render-quality spike's 24/24 PASS was measured within PDF.js's own coordinate space only, never cross-checked against `ptToScreen`).
- **NEW card filed:** `AUDIT-20260702-s2-fitz-lock` — per-case PyMuPDF lock needed before the `/export-pdf-overlay` render step can safely move to a threadpool (deferred from Sprint B's S2).
- Export PDF output is still a 108-DPI PyMuPDF raster (unchanged from proto) even though the on-screen canvas is now vector-sharp via PDF.js — noted as a follow-on, not yet its own filed card.
- New E2E marker: `LITE_EXPORT_ENDPOINTS_OK` (14 checks) + `LITE_RUN_ALL_OK` (test-runner exit marker, not a per-suite check count). Total lite test count: 60 files, all green in the first `run_all_tests.py` run.

## Active Feature State (2026-07-02 — BUG-20260702-lite-cfss-summary: CFSS shared-shape instances now included in every rollup + export no longer crashes)

Bugfix, not a new feature — but it changes computed output for any project using CFSS shared shapes (cross-floor promote/instance), so it's recorded here.

- **Root cause:** `cfssCommitPromote` called `addMaster()` without the `opts` arg, so masters never captured `catId`/`semanticTag`. Every rollup consumer's `kind==='instance'` branch had no category/area to report and silently skipped the instance — promoting a polygon to a shared shape removed its area from every total with no replacement value.
- **CRASH sub-finding:** `buildExportData` and `exportPdfOverlay` (`lite/static/js/export-annotate.js`) called `catOf(o.catId).name`/`.color` BEFORE the `kind` guard. For instances, `o.catId` is `undefined` → `catOf(undefined)` → `TypeError` → XLSX export and annotated-PDF export crashed outright on any page holding an instance (not just under-counted — broken).
- **Fix (commit `02e35af`):** (1) `cfssCommitPromote` now passes `{catId, semanticTag}` into `addMaster` — additive master schema field, persists for free via the existing masters serialization in `cfssWrapSave`/`cfssWrapLoad`; legacy pre-fix masters (saved before this fix) have no `catId` → `rollupCatId` returns `null` → instance is skipped from bucket totals safely with a one-time `console.warn`, no crash, no migration UI required. (2) NEW `rollupAreaM2(o,pg)` + `rollupCatId(o)` dispatch helpers added in `cross-floor-shapes.js` next to the existing `instanceAreaM2` — a single reusable dispatch helper instead of re-deriving the same `kind`-branch logic at each of 6 call sites (the direct postmortem lesson from the arc-summary bug's 6 near-duplicate callee swaps). Both helpers are `typeof`-guarded at every consumer because `cross-floor-shapes.js` is dynamically injected AFTER `layer-tree.js`/`export-annotate.js` load — the guard is load-order-mandatory, not defensive boilerplate. (3) 6 rollup sites rewired to the new helpers: `computeSummary` (`ui-lite.html:1049`), `buildExportData`, `exportPdfOverlay` (instances now export as resolved-pts poly overlays with area labels via `resolveInstancePts`), `buildReportPayload` (`export-annotate.js`), `_ltOwnArea` (`layer-tree.js`), `_lovsLayerArea` (`overview-setup.js`); the 2 crash sites additionally moved `catOf` resolution after `catId` resolution and made it null-safe.
- **Scope of impact:** summary panel, XLSX export rows + summary sheet, annotated-PDF overlay labels, editable report rows/subtotal/net, layer-panel per-layer totals, site-setup layer rollup — same 6 consumers fixed in the arc-summary sprint. Plain (non-instance) objects and `instanceAreaM2` itself are byte-identical to before.
- **Note (specialist finding):** `instanceAreaM2` is ppm-independent (metric shoelace on `offsetPt`), so instance areas are valid even on pages without a calibrated scale — an intentional, documented difference from plain-poly behavior (which needs a scale).
- Patch plan authored by `bma-path-geometry-reviewer` running on Opus (user-requested model override).
- New guard test `lite/tests/test_summary_cfss_parity.py` (marker `LITE_SUMMARY_CFSS_OK`) exercises the REAL promote flow (`__cfssTestPromote`), then asserts all 6 consumers report ground truth 2100 m² (2000 plain + 100 instance) and that both export builders do not throw. Proven RED on pre-fix code via `git stash` (totals 2000, `master_has_catId` false, `edThrew`/`ovThrew` true) then GREEN after.
- Shipped as commit `02e35af` on `main`. Discovered + fixed via the `/bma-bug-report` pipeline — this is bug 2 of 2 from the 2026-07-02 measurement-accuracy audit (bug 1 = `BUG-20260702-lite-arc-summary`, shipped earlier the same day). Both audit bugs are now SHIPPED.
- **Known gap:** legacy pre-fix `.bmaplan` saves with masters lacking `catId` keep instances out of bucket totals (safe, warned, documented) — a migration helper could be a future COSMETIC card. Server `/export-pdf-overlay` draw path for instance-as-poly overlays verified only via payload shape, not a rendered-PDF pixel check.

## Active Feature State (2026-07-02 — BUG-20260702-lite-arc-summary: arc-edge areas now correct in every rollup)

Bugfix, not a new feature — but it changes computed output for any polygon that has arc edges (`obj.edges`), so it's recorded here.

- **Root cause:** 6 rollup call sites in `lite/` called `polyMetrics({pts:o.pts})` — a bare-pts stub that drops `o.edges` — instead of the arc-aware `polyMetricsAnyShape(o, pg)`. Result: the per-object canvas label (`areaOf`, which does call `polyMetricsAnyShape`) showed the correct arc-inclusive area, but every summary/export/report consumer downstream silently used the straight-chord (undercounted) area instead.
- **Fixed call sites (all now `polyMetricsAnyShape(o,pg)`):** `lite/ui-lite.html:1049` (`computeSummary`) · `lite/static/js/export-annotate.js:14` (`buildExportData`) · `:27` (`exportPdfOverlay`) · `:58` (`buildReportPayload`) · `lite/static/js/layer-tree.js:62` (`_ltOwnArea`) · `lite/static/js/overview-setup.js:642` (`_lovsLayerArea`).
- **Scope of impact:** summary panel, XLSX export rows + summary sheet, annotated-PDF overlay labels, editable report rows/subtotal/net, layer-panel per-layer totals, site-setup layer rollup. Non-arc polygons are byte-identical to before (`polyMetricsAnyShape` falls through to `polyMetrics` when there are no arc edges).
- **Zero edits** to the drift-locked vendored `lite/static/js/measure-engine.js` — `polyMetricsAnyShape` already existed there; this sprint was purely a caller-side fix.
- New guard test `lite/tests/test_summary_arc_parity.py` (marker `LITE_SUMMARY_ARC_OK`) locks the invariant "every rollup consumer == Σ areaOf labels (arc-inclusive)" going forward, using an independent closed-form fixture (10000 + 1250π ≈ 13926.99 m² arc room + 2000 m² plain room). Proven RED on pre-fix code via `git stash`, GREEN after.
- Shipped as commit `e5264e2` on `main`. Discovered + fixed via the `/bma-bug-report` pipeline: `bma-bug-triager` found 4 of the 6 sites, `bma-path-geometry-reviewer` specialist review widened the fix to all 6.
- **Sibling bug queued next:** `BUG-20260702-lite-cfss-summary` — CFSS shared-shape instances have no `.pts` of their own, so `computeSummary` skips them entirely; promoting a shared shape removes its source polygon from totals with no replacement.

## Active Feature State (2026-05-25 — Centerline Snap arc: INV-002a proto + INV-002b lite + 2 post-ship bugfixes)

New opt-in feature added to both `proto/ui.html` and `lite/ui-lite.html`:
- `PREFS.measure.centerlineSnap` (proto) / `window.centerlineSnapOn` (lite) — default `false`. Toggle via "⊙ CL" Helpers ribbon button in proto or the floating toggle button in lite. Persisted in proto's `bmaPlan.settings.v1` and lite's `localStorage["bma_cl_snap"]`.
- `CL_snapCanvasToCenterline(canvas, x, y, w=140, h=140)` — Step 1 (per-click, ~4ms). Grabs a 140×140 px ROI around the cursor from the canvas `getImageData`; Otsu-binarises; applies Zhang-Suen thinning to produce a skeleton; returns the nearest skeleton pixel. Fires only when vector snap (`sc.t`) did NOT match — vector snap stays authoritative.
- `CL_refineCornersOnSkeleton(canvas, pts)` — Step 2 (post-Enter, ~6ms). Samples 5 points along each polygon edge; snaps each to the skeleton; PCA-fits a line per edge; intersects adjacent edge lines to recover the true corner, eliminating corner-chamfering bias.
- `toggleCenterlineSnap()` (proto) / lite floating button — toggles the feature on/off; updates ribbon button visual state; calls `_applyCenterlineSnapPref()` to write PREFS and refresh UI.
- `_applyCenterlineSnapPref()` (proto) — called at boot and after Settings Apply. Reads `PREFS.measure.centerlineSnap`; sets `centerlineSnapOn` state variable; updates "⊙ CL" button active class.
- Hook in area-mode `mousedown` (both trees): if `centerlineSnapOn && !sc.t`, run Step 1 to snap the click before committing to `mPts`.
- Hook in `finishCurrentArea` poly branch (proto) / `finishDraft` (lite): if `centerlineSnapOn`, run Step 2 on all pts before `pushUndo()` / area-math pass. Skipped for hasArc polygons (arc geometry would be invalidated by corner replacement).
- New additive schema field: `obj.traceMode = "centerline-roi"` on corrected polygons. Optional; old saves load without it; cross-open parity between proto and lite preserved.
- Proto implementation lives in `proto/static/js/centerline-snap.js` (208 LOC, IIFE, no CDN).
- Lite implementation lives in `lite/static/js/centerline-snap.js` (306 LOC: Section A = proto algorithm IIFE byte-identical per drift-locked vendoring contract; Section B = lite glue).
- HiDPI bugfix (commit `ff3f9fe`): on displays with `devicePixelRatio > 1` (Windows 125%/150% text scaling, Retina 200%), canvas bitmap width = `clientWidth × dpr`, so `getImageData` reads bitmap pixels but `e.offsetX/Y` are CSS pixels. Lite centerline-snap glue now multiplies CSS coords by `window.devicePixelRatio` before calling the algorithm and divides results back before `screenToPt`. Proto resize code does not multiply canvas attribute width by dpr, so proto was not affected.
- `.active` CSS for lite toggle button: injected as `<style id="lite-btn-centerline-css">` at glue init; sets green background + glow when active, so toggle on/off state is visually distinct.

New E2E markers: `PHASE_CENTERLINE_SNAP_OK` (proto, 10 sub-checks, maxDelta=0.140%); `LITE_CENTERLINE_SNAP_OK` (lite, 8 sub-checks including `dprBridge` + `activeCssRule`, maxDelta=0.1778%). Total proto full E2E: 22 _OK markers.

## Active Feature State (2026-05-20 — HT-ACC series: Calibration accuracy UX)

New runtime state and functions added to `proto/ui.html`:
- `calibRaw[]` — in-memory array capturing the raw (pre-snap) click coordinates for each calibration click alongside the snapped `calibPts`. Not persisted to `.bmaplan`. Resets in `cancelCalib()`, `finishCalib()`, and `loadPage()`.
- Snap-deviation check (in calib panel after 2nd click): if the captured reference line length differs >5% from the distance between the raw click points, an orange warning is shown in the calib panel: "snap จับเส้นต่างจากที่คลิก (raw→snapped pt) — ดูเส้นม่วงบน canvas … ยกเลิกแล้วซูมเข้าก่อนคลิกใหม่". This is the root-cause fix for the systematic ~1% measurement loss.
- `#btn-scale-verify` — ribbon button added beside Set Scale; calls `verifyScale()` (already present from INV-2026-05-20-001).
- Longest-baseline tip in calib panel: "ใช้เส้นที่ยาวที่สุดที่มีขนาดชัดเจนในแผน" for improved calibration accuracy.
- `activateAreaTool('land')` now hints to use arc mode (A) on curved boundaries.

New tooltip behaviour in `proto/static/js/status-bar.js`:
- `updateAnalyseUI` sets a `title` tooltip on `#lbl-scale` and `#scale-badge` containing exact `pts_per_m` (full float) and precise `1:N.x` (unrounded). The visible rounded label is unchanged. Area computation uses the full float throughout — this is a display-only transparency change, not a measurement change.

New E2E marker: `HT_ACC_OK` (5 sub-checks: verifyBtnExists, verifyBtnWired, longestTip, calibRawExists, devWarnsWrongLine/devQuietWhenClose). Total full E2E: 102 _OK markers.

HT-NAV-1 verdict: no code fix. `getNextPage`/`loadPage` navigation logic is sound. `REAL_OK` already exercises real multi-page navigation on the 45-page permit PDF. Exception observed in journey-tester session was a Playwright timing artifact.

## Active Feature State (2026-05-20 — INV-2026-05-20-002/003/004 Layer model rebuild)

New runtime functions added to `proto/ui.html`:
- `validLayerSlugForPage(slug, pageIdx)` — guarantees an object's slug exists in the page's layer preset; maps `land` → `site_boundary` on site pages, `room` → sub-area equivalent, etc.
- `getObjectLayerSlug(obj)` — resolves an object to its canonical layer slug: openings/deductions → `deduction`; refs/lines → `reference_geometry`; else `validLayerSlugForPage(obj.layerSlug)`
- `_slugVisible(slug, pageIdx)` — returns boolean from page-scoped `pageStore[pageIdx].layers` visibility state
- `_slugLocked(slug, pageIdx)` — returns boolean from page-scoped lock state
- `_objLayerVisible(obj, pageIdx)` — combines `_slugVisible(getObjectLayerSlug(obj), pageIdx)`
- `_objLayerLocked(obj, pageIdx)` — combines `_slugLocked(getObjectLayerSlug(obj), pageIdx)`
- `reassignSelectedObjectLayer(slug)` — moves selected object to a different layer slug on the current page; calls `pushUndo()` + `redraw()` + summary refresh

Render/hit paths updated (all now read page-scoped authority):
- `hitTest`, `hitTestAll`, `hitVertex`, `findNearest`, `drawRefLines` — use `_objLayerVisible`/`_objLayerLocked` instead of `layerVis[areaTypeLayer[obj.areaType]]`
- `_layerLockGateBeforeMode` + `toggleLayerLock` — use `_slugLocked`/`getObjectLayerSlug`

UI additions:
- "Layer" `<select>` dropdown in right-panel properties + left-panel properties (Bluebeam-style move-to-layer)
- `objLayerKey()` reports the object's real slug (was returning `areaTypeLayer[obj.areaType]` — now via `getObjectLayerSlug`)

Global `layerVis`/`layerLock` demoted to non-authoritative synced mirror. Toggles still write them for test/legacy compat; nothing reads them for render/hit/lock behaviour.

## Active Feature State (2026-05-20 — INV-2026-05-20-001 Verify Scale)

New runtime functions added to `proto/ui.html`:
- `verifyScale()` — entry point from Scale menu "Verify Scale"; guard if no calibration; enters verify draw mode
- `verifyFinish()` — called by `calibPanelOk()` in verify mode; computes `%dev = 100·|d_meas−d_enter|/d_enter`; calls `openVerifyModal()`
- `calibPanelOk()` — new router on calib panel OK button: routes to `verifyFinish()` in verify mode, `finishCalib()` otherwise; `finishCalib()` body unchanged
- `openVerifyModal(pct, measuredPpm, enteredPpm)` — renders `#verify-modal` with band color, numeric display, area-impact estimate, and 3 action buttons
- `_verifyBand(pct)` — returns `{color, label}` (green <0.5% / yellow <2% / red ≥2%)
- `_verifyWriteResult(pct, action, verifyPpm)` — writes `calibScale.verifyResult{pct, action, verifyPts_per_m, ts}` into current page's calibScale
- `_afterVerifyScaleChange()` — refreshes summary widget + bottom bar after scale mutation
- `verifyAccept()` — accepts current scale, writes result, closes modal
- `verifyRecalibrate()` — discards verify, resets `calibVerifyMode`, re-enters calibration
- `verifyAverage()` — averages calib + verify pts_per_m, writes result, applies new scale
- `closeVerifyModal()` — closes `#verify-modal`

New additive schema field: `calibScale.verifyResult = {pct, action, verifyPts_per_m, ts}` (optional; version stays 1). Round-trips through `_makeProjBlob()` / `applyLoadedProject()` automatically.

## Latest Sprint Results

| Sprint | Result |
|--------|--------|
| **INV-20260703-layer-linkage plan B COMPLETE + UX-batch-1 + save-fix follow-up — staged B0-B5 layer↔measurement redesign rollout** | PASS — the redesign that reached its human GO checkpoint the same morning (Approach B "one aggregation engine," 25/30) shipped end-to-end across 6 commits. B0 (`edc89ae`): NEW `lite/static/js/object-agg.js` single tuple-stream aggregation engine `{pg, catId, role, floorKey, area, counting}` + NEW I11 oracle `assertEnginesAgree` making H2 (Summary vs. Review disagreement) structurally impossible; `LITE_OBJECT_TUPLES_OK`. B1 (`6909486`): report-vars + Summary per-floor gfa/ded/net on tuples via opt-in `{useLive:true}` (default path byte-identical). B2 (`00ab9b9`): layer-tree Σ + Review Section-3 rows reduce the same tuple stream, closing H2 for good + M6 (root-unfiled layers now counted). B3 (`34594b7`): `layer-system.js` `reassignObjectsOfLayer` on `removeLayer` + load-time `sweepOrphanCatIds` heal, closing H3. B4 (`750d2f6`): NEW `lite/static/js/layer-move.js` (120 lines, DOM-injected) move-object-to-layer UI via Properties select + context menu, CFSS instances retarget MASTER with confirm — closes H1; all 3 HIGH findings now closed. Known flag: CFSS-master-only moves not undoable (pre-existing gap, follow-up candidate). B5 (`3d3741e`): report-vars operand dropdown Σ/▸ optgroups, closing M4 (display-only). Bundled UX batch 1 (`34aefa3`): F-7 modalOpen keydown guard (closes the exact hotkey-leak class the UX review filed), F-1/F-2/F-3 hotkey/discoverability fixes, cheatsheet corrections. Every commit independently verified pre-commit; `MEASURE_PARITY_OK` green at B4. Proto NOT TOUCHED; baseline 22 markers unchanged. [2026-07-03] |
| **UX-REVIEW-20260703 + BUG-20260703-lite-save-wipes-data — CRASH fix (Ctrl+S wiped all data) + full UI/UX journey review + layer↔measurement invent checkpoint** | PASS — (1) Save-wipe CRASH fix (`d40b20b`): Ctrl+S wrote an empty `.bmaplan` and destroyed the in-memory session — root cause was `pageMgr`'s empty `PS_by_id` snapshots (taken at upload) projected back over live `PS` at save; 72 previously-green tests missed it because every save test drove the API/serialization path, never the real `mi-save` click. Fixed via new `projectToGlobals(livePS)` resolving content from live `PS` by identity + an Apply/merge commit-order fix (2 sites in `page-manager-ui.js`). NEW `LITE_SAVE_CLICKPATH_OK` drives the REAL click, closing the click-path-vs-API-path testing gap; RED pre-fix (exact journey repro) → GREEN; regression 17/17. (2) UX review filed (`UX-20260703-review-findings`, `2249400`): 29-screenshot journey + static review on the real 45-page permit → 9 FRICTION (dead hotkey, key collisions, undiscoverable Page Manager, invisible verified state, silent mouse-block, misleading upload messaging, missed-modal hotkey leakage, raw errors, native prompt() in Verify Scale) + 6 COSMETIC + 3 GOODs pinned. (3) Layer↔measurement invent at checkpoint (`INV-20260703-layer-linkage`, `2249400`): 8 problems mapped (3 HIGH); Approach B "one aggregation engine" (`object-agg.js` tuple stream) scored 25/30 and won; staged B0-B5 GO'd; B0 + UX quick-wins batch 1 building now via 2 parallel `lite-builder` subagents — in-progress, not shipped as of this write. `MEASURE_PARITY_OK` green throughout. Proto NOT TOUCHED; baseline 22 markers unchanged. [2026-07-03] |
| **GO-20260703-invariants-streaming-worker-recycle — V2-U1 invariant registry + Range-streaming spike (NOGO→RESHAPE) + worker-recycle build** | PASS — Continuation of the same-day "GO" loop block, resumed after `BLOCK-20260703-clear-queue` closed and the Range-streaming human checkpoint cleared. (1) V2-U1 invariant registry (`6d6e39b`): NEW `lite/tests/INVARIANTS.md` (10 invariants I1-I10 + 2 mandatory SCOPE questions + new-object-kind fixture rule) + reconciled 16 stale `PHASE_INDEX.md` rows + closed `ptToScreen` parity card with rationale (registered as invariant I7). (2) Range-streaming SPIKE (`9466fe4`+`f8c2981`), GO received: 5-step spike run for real on RAMA4 19MB + real CHH 95MB — streaming cut RSS only 10% (1675→1503 MB) vs a ≥50% GO bar (FAILS); pdf.js worker-heap bug #10730 CONFIRMED on 4.0.379 (worker heap survives `destroy()`, the real ~1.5 GB ceiling). VERDICT: NOGO on streaming-as-memory-fix — the spike prevented a doomed build sprint. (3) worker-recycle BUILT (`d52ddbb`), the RESHAPE: explicit `PDFWorker` ownership (`_docWorker`/`_docSource`/`recycleDocWorker()`) with hidden/idle/manual triggers, transparent lazy reinit; `page-renderer.js` 790/1000, `ui-lite.html` +1 line (1170/1200); NEW `LITE_WORKER_RECYCLE_OK` 7/7 (incl. zero-refetch-via-blob, heap-not-worse −0.8%); production RSS re-probe on CHH queued as honest follow-up. (4) Streaming-as-bandwidth card DEFERRED-until-remote-deployment (localhost bytes are free on the desktop-only product). Lite suite now 72 files; `MEASURE_PARITY_OK` green throughout. Proto NOT TOUCHED; baseline 22 markers unchanged. Only item still open: production RSS re-probe of worker-recycle on the real CHH binder. [2026-07-03] |
| **BLOCK-20260703-clear-queue — 5-ship "ทำทั้งหมด" session (fitz lock + render fallback/scan-detect + V2 tiers/overlay-proof/streaming-research + Verify-Scale port + process docs)** | PASS — Ship 1 `AUDIT-20260702-s2-fitz-lock` (`d0a5dde`): per-case `_case_lock` serializes all `fitz.Document` access incl. threadpooled `/export-pdf-overlay`; `LITE_CASE_LOCK_OK` hammer test (96 req/8 threads, zero 5xx; hardening hammer, not deterministic RED proof), 9/9 regression. Ship 2 render-followups a+c (`aec375b`): raster fallback when PDF.js unavailable + scanned-page detection with `RS*4`-capped re-render scale (transform-compensated, bit-identical registration); `LITE_RENDER_FB_SCAN_OK` 6/6 (2 false alarms fixed test-side); 8/8 regression. Ship 3 (`13054b6`): `--tier t0/t1/t2` test runner (V2 blueprint U2, `t0`=1.26s); NEW `LITE_OVERLAY_REG_OK` — first pixel-level raster↔overlay registration proof (max offset 0.50px, tolerance 4); Range-streaming research (`docs/invent/lite-range-streaming.md`, `PRIOR_ART_PARTIAL`) HALTED at human checkpoint per Pack H. Ship 4 `ACC-20260703-verify-scale-port` (`bea2119`): Verify-Scale ported from proto `INV-2026-05-20-001`, closing the last known accuracy gap vs. Foxit; NEW `verify-scale.js` 225 lines, additive `verifyResult` schema field, `ui-lite.html` +3 net lines; `LITE_VERIFY_SCALE_OK` 7/7. Ship 5 (`16e6495`+`b676652`): `DEVELOPMENT_PILLARS.md` + `DEVELOPMENT_V2_BLUEPRINT.md` process docs. Full lite suite validated 70/70 files green (60→70 across 2 days); `MEASURE_PARITY_OK` green throughout. Proto NOT TOUCHED; baseline 22 markers unchanged. Only item still open: human GO/NOGO/RESHAPE on Range-streaming. [2026-07-03] |
| **PERF-20260702-lite-foxit-smoothness — Foxit-grade open smoothness (4-sprint block)** | PASS — Driven by an empirical perf probe (`artifacts/perf/probe_results_20260702.txt`): RAMA4 18.3MB 3.9s cold first-paint; CHH 90.8MB real customer file 9.6s first-paint + 766MB heap after 10 pages; flat ~1.2s PDF.js boot floor; `UPLOAD` dominates ~80ms/MB while `/raw` is nearly free; pan-blank suspicion REFUTED 0/10 across 3 files; "overview thumbs 0/0" was a probe selector artifact (real: 45/45 in 9.2s cold). Sprint 1 — page-cache LRU (`ae0f168`): `MAX_PAGE_CACHE=4` in `page-renderer.js`, `.cleanup()` on unreferenced eviction, `curPage` protected; CHH heap 766→~628 MB (−18%); remaining ~600 MB is PDF.js doc-level state left to the queued Range-streaming invent card. `LITE_PAGECACHE_LRU_OK` RED→GREEN, 10/10 regression. Sprint 2 — local-first open (`3ec9239`): `uploadPdf()` opens local bytes via `openLocal()` immediately, server upload backgrounds, `adoptCase()` binds later so `/raw` is NEVER fetched; paint at 475ms with upload in flight, removes the ~80ms/MB wait on real binders. `LITE_LOCAL_OPEN_OK` RED→GREEN, 10/10 regression. Sprint 3 — worker warm-up + adjacent prefetch (`e0fb856`): idle-time `requestIdleCallback` warms pdf.js boot + prefetches n±1 pages after `loadPage(n)`. `LITE_WARM_PREFETCH_OK` RED→GREEN, 9/9 regression. Sprint 4 — sequential thumbnail warm (`a0c1152`): `warmThumbs()` fetches `/thumb/1..N` sequentially (120ms gaps), deliberately sequential pending the still-queued per-case `fitz` lock. `LITE_THUMB_WARM_OK`, 8/8 regression. `MEASURE_PARITY_OK` green throughout all 4 sprints. Lite suite now 67 files, all green; `ui-lite.html` stays under 1200-line cap. Proto NOT TOUCHED; baseline 22 markers unchanged. Only remaining perf piece (doc-level memory, Range-streaming) routed to `PERF-20260702-open-streaming` invent card. [2026-07-02] |
| **BUG-20260702-lite-pagerot-registration — Manual page rotate desyncs stored geometry from raster + export** | PASS — Fixed the top-priority BROKEN bug filed earlier today by the `AUDIT-20260702-infra-bundle` render-engine accuracy review. `pageRot` rotated the PDF.js raster canvas but `ptToScreen`/`screenToPt` ignored `pgRot` (`getRot()` hardcoded to 0) — pre-existing geometry detached from the visible plan by up to a page diagonal (~84 m at 1:100 A1); geometry drawn while rotated stored bound to the wrong feature (correct area, wrong location — "right value, wrong location" bug); `/export-pdf-overlay` never applied `pageRot` either. Intrinsic PDF `/Rotate` always correct. Fixed (commit `9f4b298`, "Fix A — proto-parity" over "Fix B — geometry-baking" per an Opus specialist patch plan): `getRot()` (host contract fn) now returns `pageRot[pg]||0`; `ptToScreen`/`screenToPt` route through the vendored, parity-tested `pdfToC`/`cToPdf` rotation branches — net 0 lines, zero geometry mutation, no migration (`pageRotations` already persisted); server `/export-pdf-overlay` prerotates the raster via `Matrix(RS,RS).prerotate(rot)` + maps coordinates through new `_rp` helper — prerotate direction verified empirically with a pixel test before wiring. New guard test `LITE_PAGEROT_REG_OK` (5 checks: 4-angle mapping, exact-inverse round-trip, area invariance, save/load round-trip, export dimension/pixel check) proven RED (pre-fix mapped to (15,15) instead of (585,15), export un-rotated 450×600) → GREEN. Regression: 16 at-risk files + 26 more from a partial `run_all_tests.py` pass = 42 distinct files green; `MEASURE_PARITY_OK` green confirms drift-locked vendored math untouched (only host contract fn `getRot()` changed). Proto NOT TOUCHED; baseline 22 markers unchanged. Known gap: combined `V.rot≠0`+`pgRot≠0` asserted only via exact-inverse round-trip, not raster-pixel alignment — queued `AUDIT-20260702-render-followups` item (e) would add visual coverage. [2026-07-02] |
| **AUDIT-20260702-infra-bundle — Test-Runner Preflight + Export Payload Caps + Render-Engine Review** | PASS — Same-day follow-on to the 2-bug 2026-07-02 measurement-accuracy audit. Sprint A (`9c4c36e`): NEW `lite/tests/run_all_tests.py` aggregate test runner (discovers + runs every `test_*.py`, per-test timeout, summary table, `LITE_RUN_ALL_OK`/`FAIL`, `--filter`/`--fail-fast`/`--timeout`) with disk/dependency PREFLIGHT (fails fast on <2 GB free on repo or system drive — hardened after the 2026-07-02 ENOSPC incident — or missing `uvicorn`/`playwright`/`fitz`/`node`); first full run 60/60 PASS in 8.5 min. Sprint B (`60d424a`): `/export-pdf-overlay` + `/export-xlsx` now validate payloads BEFORE rendering (5 caps: `MAX_EXPORT_PAGES`/`MAX_OBJECTS_PER_PAGE`/`MAX_ANNOTS_PER_PAGE`/`MAX_PTS_PER_OBJECT`/`MAX_COORD_ABS`, HTTP 400 not silent truncation, ≥10x realistic worst case, bonus fix for a latent 500 on non-numeric page key); `wb.save()` offloaded via `run_in_threadpool`; NEW `lite/tests/test_export_endpoints.py` marker `LITE_EXPORT_ENDPOINTS_OK` 14/14 — first real HTTP tests of either export endpoint. Review C (read-only, Opus agent): `PDFJS-VIEWPORT-CLIPPED` render architecture verdict SOUND for `V.rot`/`pgRot`=0 (coordinate contract algebraically exact, residual ≈ ±0.5 device px = click-precision floor, not a measured-value error); filed real BROKEN bug `BUG-20260702-lite-pagerot-registration` (manual page rotate desyncs stored geometry from raster — `ptToScreen`/`screenToPt` ignore `pgRot`) as top-priority next work, plus bundle `AUDIT-20260702-render-followups` (pdfjs-fail fallback / pan double-buffer / scanned-PDF detection / memory-claim correction / real overlay-registration test) and `AUDIT-20260702-s2-fitz-lock` (per-case PyMuPDF lock needed before overlay-render threadpool offload). Proto NOT TOUCHED; baseline 22 markers unchanged. [2026-07-02] |
| **BUG-20260702-lite-cfss-summary — CFSS shared-shape instances excluded from every rollup consumer + export crash** | PASS — Bug 2 of 2 from the 2026-07-02 measurement-accuracy audit. CFSS instances (`{kind:'instance', masterId, offsetPt}`, no `.pts`/`.catId`) skipped by all 6 rollup consumers (`computeSummary`, `buildExportData`, `exportPdfOverlay`, `buildReportPayload`, `_ltOwnArea`, `_lovsLayerArea`) — promoting a poly to a shared shape silently removed its area from every total. CRASH sub-finding: `buildExportData`/`exportPdfOverlay` called `catOf(o.catId)` BEFORE the `kind` guard → TypeError → XLSX + annotated-PDF export crashed outright on any page with an instance. Root cause: `cfssCommitPromote` called `addMaster()` without the `opts` arg, so masters never captured `catId`/`semanticTag`. Fixed by (1) passing `{catId, semanticTag}` into `addMaster` at promote time (additive schema, free persistence via existing masters serialization), (2) NEW `rollupAreaM2(o,pg)` + `rollupCatId(o)` dispatch helpers in `cross-floor-shapes.js` co-located with `instanceAreaM2` (typeof-guarded — `cross-floor-shapes.js` loads after `layer-tree.js`/`export-annotate.js`), (3) rewiring all 6 rollup sites + fixing both crash sites (`catOf` moved after `catId` resolution, null-safe). Patch plan by `bma-path-geometry-reviewer` on Opus (user-requested model override). NEW `lite/tests/test_summary_cfss_parity.py` marker `LITE_SUMMARY_CFSS_OK` exercises the REAL promote flow (`__cfssTestPromote`), asserts ground truth 2100 m² across all 6 consumers + both export builders don't throw; proven RED on pre-fix code via `git stash` (totals 2000, exports threw) then GREEN after. 14/14 tests exit 0 (incl. `LITE_SUMMARY_ARC_OK` bug-1 guard staying green). Shipped commit `02e35af`. Legacy pre-fix masters degrade safely (instance skipped + one-time warn, no crash). Proto NOT TOUCHED; baseline 22 markers unchanged. Both 2026-07-02 audit bugs now SHIPPED. [2026-07-02] |
| **BUG-20260702-lite-arc-summary — Arc-edge polygon areas excluded from every rollup consumer** | PASS — 6 call sites (`ui-lite.html:1049` `computeSummary`; `export-annotate.js:14/27/58` `buildExportData`/`exportPdfOverlay`/`buildReportPayload`; `layer-tree.js:62` `_ltOwnArea`; `overview-setup.js:642` `_lovsLayerArea`) passed `{pts:o.pts}` into `polyMetrics` (drops `o.edges` → straight-chord area) instead of `polyMetricsAnyShape(o,pg)` (arc-correct) — curved rooms under-counted in every summary/export/report/layer-total consumer while the per-object canvas label (`areaOf`) was already correct, with no warning. Triage (`bma-bug-triager`) found 4 sites; specialist review (`bma-path-geometry-reviewer`) widened scope to 6. Fixed by swapping the callee at all 6 sites; zero edits to the drift-locked vendored `measure-engine.js`; non-arc behavior byte-identical (`polyMetricsAnyShape` falls through to `polyMetrics` for non-arc polys). NEW `lite/tests/test_summary_arc_parity.py` marker `LITE_SUMMARY_ARC_OK` asserts "every rollup consumer == Σ areaOf labels (arc-inclusive)" via an independent closed-form fixture (10000 + 1250π ≈ 13926.99 m² + 2000 m²); proven RED on pre-fix code via `git stash` then GREEN after fix. 8 regression suites GREEN: `MEASURE_PARITY_OK` (drift-lock intact), `LITE_ARC_EDGE_OK`, `test_report`, `LITE_REPORT_VARS_OK`, `LITE_REPORT_VARS_ROLLUP_OK`, `LITE_EXPORT_SUBMENU_OK`, `LITE_TREE_ROLLUP_OK`, `LITE_OVERVIEW_SETUP_OK`. Shipped commit `e5264e2`. `lite/ui-lite.html` stays 1,154/1,200 cap. Proto NOT TOUCHED; baseline 22 markers unchanged. Sibling bug `BUG-20260702-lite-cfss-summary` (CFSS shared-shape instances excluded from totals) queued next. [2026-07-02] |
| **SLICE report-edit-1 — Editable lite report** | PASS — `lite/static/js/report-edit.js` NEW 404 LOC: custom Excel-style cell-click formula picker (hooks `oneditionstart`+`oncreateeditor`, activates on leading "=", injects `B<n>` refs via capture-phase mousedown — CE lacks live-cell-click UX natively); stable-row-id subtotal mapper (`rowIds[]` parallel array + `subMeta` semantic capture + `rebuildSubtotals()` re-projects formulas after structural mutations — deleted referenced row → term dropped + red flag, not silent shift); render/persist/provenance (`baseline[]` stale detection orange warning; NaN-guard rejects non-numeric reverts to previous value; localStorage v1 keyed by payload hash). `lite/lite-report.html` +46 (override-overlay toggle markup + grid mount + 4 vendor tags). jspreadsheet-ce + jsuites vendored offline (~440 KB, MIT) into `lite/static/js/vendor/` (CDN forbidden after pdfjs-cdn bug). `lite/tests/test_report_edit.py` NEW 245 LOC — LITE_REPORT_EDIT_OK 7/7 (PICKER regression =B1+B2→86.93 / STABLE delete-unreferenced re-project 76.61 / STABLE delete-referenced term-drop 50.45 + red flag / STABLE multi-op re-project 66.61 / GUARD label-col no A2 injection / PERSIST subMeta survives save-reopen / NaN-GUARD "abc" reverts 50.45). Zero page errors. `docs/invent/lite-editable-report.md` NEW (invent record, 3 RESHAPE sections). `.claude/skills/lite-spike-iterate/SKILL.md` NEW. `docs/status/PHASE_INDEX.md` +11. Proto NOT TOUCHED; baseline 22 markers unchanged. Grid behind `#re-toggle` dev gate. [2026-06-05] |
| **BUG-20260526-lite-stale-pf-folder-cleanup** | PASS — `seedPageFolders()` now prunes stale `PF_floor_N` folders + 3 seed layers when source pages are re-tagged. `_pflFolderHasUserDrawnObjects(folderId)` + `_pflPrunePF(activeFolderIds)` added; `PF_excluded` never pruned; user-drawn objects in a folder block its prune. `lite/static/js/page-folder-layers.js` +47/-2 (743→790 lines, ≤1000 cap). `lite/tests/test_pf_cleanup_on_exclude.py` NEW 168 lines, marker `PF_CLEANUP_OK` 4/4 (basic cleanup / safety preservation / idempotency / PF_excluded-never-pruned). 5 regressions GREEN (LITE_PAGE_FOLDER_MODEL_OK / LITE_PAGE_FOLDER_PERSIST_OK / LITE_PF_KIND_OK 11/11 / LITE_LAYER_PERSIST_OK / LITE_TREE_PERSIST_OK). `/bma-simulate` verify PASS (stale_PF_floor_1_exists=false; dom_render_order=[PF_basement_1, PF_floor_2, PF_excluded]). Manual DBLCLICK_OK. Proto NOT TOUCHED; baseline 22 markers unchanged. [2026-05-26] |
| **Centerline Snap arc (invent → INV-002a proto → INV-002b lite → 2 post-ship bugfixes)** | PASS — Commits: `0208314` (invent spike GO) · `6db0461` (INV-002a proto) · `ad920c6` (INV-002b lite) · `916d379` (roadmap chore) · `ff3f9fe` (DPR bugfix) · `5783df4` (button position bugfix). `proto/static/js/centerline-snap.js` NEW 208 LOC (Otsu threshold + Zhang-Suen thinning + `CL_snapCanvasToCenterline` per-click ~4ms + `CL_refineCornersOnSkeleton` post-draw PCA ~6ms; IIFE no CDN). `proto/ui.html` +15 net lines: script include, "⊙ CL" Helpers ribbon button, `toggleCenterlineSnap()` + `centerlineSnapOn` state, area mousedown click-hook (fires only when no vector snap matched), `finishCurrentArea` poly-branch refine call (skips hasArc), `PREFS.measure.centerlineSnap` default false, `_applyCenterlineSnapPref()` at boot + Settings Apply. `proto/e2e_ui_test.py` +162 lines: `_test_centerline_snap` 10 sub-checks + `PHASE_CENTERLINE_SNAP_OK` marker GREEN (accuracy maxDelta=0.140% ≤ 0.5%). `lite/static/js/centerline-snap.js` NEW 306 LOC (Section A = proto algorithm IIFE byte-identical per drift-locked vendoring contract; Section B = lite glue: `CL_litePolyClick` + `CL_litePolyFinish` + self-installing floating toggle button + `localStorage["bma_cl_snap"]` persistence). `lite/ui-lite.html` +2 net lines (1197→1199, within 1200 cap): script include + poly click hook + finishDraft refinement hook. `lite/tests/test_centerline_snap.py` NEW 235 LOC — `LITE_CENTERLINE_SNAP_OK` 6/6 initial; expanded to 8/8 post-bugfix. Additive schema: `obj.traceMode = "centerline-roi"` on corrected polygons (optional; `PROJECT_OK` + `PERSIST_OK` confirm round-trip). BUG-20260525-lite-cl-dpr (`ff3f9fe`): `cv.width = clientWidth * dpr` so bitmap is dpr× wider than CSS → `getImageData` on CSS coords → wrong ROI on dpr>1 (Windows 125/150%, Retina) → no dark pixels → `found:false` → zero effect. Fix: multiply CSS coords by `window.devicePixelRatio` before algorithm, divide back before `screenToPt`. Also: `<style id="lite-btn-centerline-css">` injected at init — `.active` CSS rule was missing so toggle looked identical on/off. `dprBridge` + `activeCssRule` sub-checks added. BUG-20260525-lite-cl-position (`5783df4`): `position:fixed; bottom:8px; right:8px` overlapped `#hud-br` zoom controls. Fix: `insertBefore(firstChild)` into `#hud-br` as flex-column 4px gap; fallback to top-right float if `#hud-br` missing. All 21 prior proto markers GREEN. Total proto _OK: 22. `MEASURE_PARITY_OK` GREEN. `proto/server.py` NOT TOUCHED. `polyAreaM2`/`pdfToC`/`cToPdf`/`RS`/`snap` internals UNCHANGED. `.bmaplan` schema additive only (version stays 1). `lite/ui-lite.html` 1199 ≤ 1200 cap. [2026-05-25] |
| **SIM-2 — /bma-simulate regression-probe hardening** | PASS — `.claude/skills/bma-simulate/regression_probes.json` NEW (tracked, permanent hard channel). 2 probes registered: (1) LITE-BUG-MODAL-NEST (evaluate-type, 860ms): setup_js calls `openSetup()`; assert `#setupModal` has non-zero `getBoundingClientRect`, `parentElement.id === 'stage'`, `select.offsetParent !== null`; cleanup_js closes modal. (2) LITE-BUG-DBLCLICK-OVER-POP (mouse_sequence-type, 2919ms): 4 real `page.mouse.click` + 1 `page.mouse.dblclick` at last-click screen position; assert `PS[1].objects[0].pts.length === 4`; cleanup_js clears page state. Both PASS against current build. `SKILL.md`: Phase A step 4 reads probes file, step 5 prepends `regression_probe` steps to SCENARIO_PLAN before user scenario steps; REGRESSION severity added as highest tier (above CRASH > BROKEN > FRICTION > COSMETIC); SIM_REGRESSION + SIM_PROBES_MALFORMED stop conditions added; soft/hard memory table replaces old few-shot paragraph. `bma-sim-driver.md`: `regression_probe` added to step-types table; "How to execute regression_probe" sub-section: load `regression_probes.json`, for each probe check preconditions, run setup_js, execute trigger (evaluate or mouse_sequence), run assertion_js (false → REGRESSION finding), run cleanup_js. Verifier (`artifacts/sim/lite/regression-probes-verify-20260524T200000/probe_executor.py`): 2 PASS · 0 FAIL. JSON parse check PASS. Zero lite/proto runtime edits. Proto 102 _OK baseline unchanged. Closed bugs now guarded: commit `2dae5c0` (LITE-BUG-2-OPUS47-FINDINGS). [2026-05-24] |
| **LITE-BUG-2-OPUS47-FINDINGS — 2 lite bugs fixed (modal nesting + dblclick vertex pop)** | PASS — `lite/ui-lite.html` two surgical patches: (1) LITE-BUG-MODAL-NEST: added missing `</div>` after line 194 — `#modal` was unclosed, causing `#setupModal` to be nested inside hidden `#modal`, making Page Setup invisible (`getBoundingClientRect()=0×0`, `offsetParent=null`); (2) LITE-BUG-DBLCLICK-OVER-POP: replaced unbounded `while` with bounded `for(_np<2)` at lines 502-503 — unbounded loop consumed intentional vertices (4-pt polygon saved as 3-pt triangle, 713 m² reported as 356 m²). Zero net lines. `lite/ui-lite.html` stays at 1197 lines (cap 1200). Live Playwright verify (`verify_bug_fixes.py` in `artifacts/sim/lite/test-pdf-opus47-direct-20260524T194000/`): `BUG_A_modal_rect_nonzero` PASS (#setupModal 1600×958, parent=#stage); `BUG_A_calib_modal_still_works` PASS (no regression); `BUG_B_dblclick_preserves_vertex` PASS (4 pts, area=714.07 m², drift 0.13% acceptable). Static verify: `<div>`/`</div>` balance 92/92 delta=0 PASS (was delta=1). `py_compile server_lite.py` PASS. `BUG_20260521_LITE_PAN_OK` regression guard PASS. Surfaced by Opus-4.7 multi-model simulator (Pack J `/bma-simulate`) — both were silent bugs invisible to standard py_compile/smoke. ZERO proto/ edits. Proto 102 _OK baseline unchanged. [2026-05-24] |
| **LITE-REPORT (INV-2026-05-21-002) — editable web report page for lite** | PASS — `lite/lite-report.html`: standalone A4-landscape report opened from File menu "ส่งออกรายงาน (แก้ไขได้)…". Left = plan image (GET /page/{n}) + SVG polygon overlay + numbered badges. Right = area table grouped by semanticTag, per-group subtotals, page net (deductions sign −1), header (project/page#/tag/scale-state/date). `contenteditable` on header fields, row labels, per-row note column. Area number cells read-only gray (raw-geometry contract). `@page A4 landscape` + `@media print page-break-after:always` per sheet → WYSIWYG browser-print-to-PDF. Payload via `sessionStorage["bmaReportPayload"]`; images reference `/page/{n}` URLs (only geometry+metadata serialised — well under 5 MB quota). Standalone SAMPLE fallback when opened with no payload. `lite/server_lite.py` +9 lines: `_REPORT_FILE` const + `GET /report` FileResponse (additive). `lite/ui-lite.html` +52 lines: #mi-report menu item + `reportPageTitle()` / `buildReportPayload()` / `openReport()` (reads polyMetrics/PS/catOf READ-ONLY). `lite/tests/test_report.py` NEW LITE_REPORT_OK (17/17 checks). `artifacts/realflow_check.py` REALFLOW_OK: real 562 MB permit upload → openReport popup → plan image naturalWidth 3576 viewBox "0 0 3576 2526" → overlay polygon → net 222.22. `MEASURE_PARITY_OK` GREEN (16 fns + 2 consts unchanged). `BUG_20260521_LITE_MENU_CLIP_OK` + `BUG_20260521_LITE_PAN_OK` GREEN (no regression). ZERO proto/ edits. Proto 102 _OK baseline unchanged. Docs/invent artifact `docs/invent/lite-pdf-report-split.md` + spike `proto/sandbox/invent-lite-pdf-report-split-v2.html` committed earlier (e443131). [2026-05-22] |
| **BUG-20260521-lite-pan-controls — Fork proto view/navigation control system into lite** | PASS — Spacebar/middle-mouse pan now works in ANY mode including while a draw tool is selected (the headline bug). Adapted proto's entire view-control BEHAVIOR onto lite's `V={k,ox,oy,rot}` single-canvas model. Added: `state.panTool` (H key sticky pan); `setCursor()` helper (grab/grabbing/crosshair/default); smooth exponential wheel zoom `exp(-deltaY*0.0015)` clamped to [ZMIN=0.02, ZMAX=40] (anti-runaway); `zoomCenter(f)`; `actualSize()` (1:1 reset); keyboard shortcuts F/Ctrl+0=fit, Ctrl+1=actual, Ctrl+=/Ctrl+-=zoom; enriched hint text. NEW `lite/tests/test_pan_controls.py` Playwright guard: `BUG_20260521_LITE_PAN_OK` GREEN (13/13: midPan, spaceArmed, spacePanMidDraw, panToolOn/Drag/Off, selectPan, clampMax, clampMin, wheelZoomIn, actualSize, fit, ctrlZoomIn). `BUG_20260521_LITE_MENU_CLIP_OK` GREEN (no regression). `MEASURE_PARITY_OK` GREEN (ptToScreen/screenToPt/RS untouched). ZERO proto/ edits. Proto 102 _OK baseline unchanged. Known gap: lite per-page rotation parity with proto deferred (V.rot global vs. proto per-page server-side rotation — separate change). [2026-05-21] |
| **LITE-0 — scaffold standalone /lite/ tree (epic INV-2026-05-21-001 sub-sprint 1)** | PASS — `/lite/` sibling tree scaffolded. `lite/static/js/measure-engine.js` vendored verbatim from `proto/ui.html` (RS, pdfToC, cToPdf, polyAreaM2, polyMetrics, polySelfIntersects, pathAreaM2, 6 path helpers) + lite-only `objectAreaM2Lite`. Anti-drift parity gate (`lite/tests/test_measure_parity.py`): MEASURE_PARITY_OK — 10 fns + 2 consts byte-identical; 5 polys/2 paths/4 coords numeric parity via Node; unit square = 25.00 m2 verified. `lite/server_lite.py` skeleton FastAPI; `lite/launch_lite.py` free-port (8100+) launcher; `lite/ui-lite.html` LITE-0 shell self-test 0 console errors. `lite/README.md` vendoring contract. `docs/invent/bma-plan-lite-standalone.md` invent decision record. `proto/sandbox/invent-bma-plan-lite-standalone.html` spike. `docs/status/PHASE_INDEX.md` LITE-0 card done. py_compile both trees PASS. ZERO proto/ edits. Proto 102 _OK baseline unchanged. [2026-05-21] |
| **HT-ACC series (HT-ACC-1 + HT-ACC-2 + HT-ACC-3 + HT-NAV-1) — Calibration accuracy UX** | PASS — Area math proven exact (0.08% error). HT-ACC-1: `calibRaw[]` captures pre-snap clicks; deviation >5% triggers orange warning in calib panel — root cause of user's ~1% area loss (snap grabbing wrong reference line). HT-ACC-2: `#btn-scale-verify` ribbon button; longest-baseline tip; `finishCalib` Verify nudge; land-tool arc hint. HT-ACC-3: `updateAnalyseUI` tooltip on `#lbl-scale`/`#scale-badge` showing exact `pts_per_m` + precise `1:N.x` (no measurement change). HT-NAV-1: no fix — `getNextPage`/`loadPage` logic sound; exception was Playwright timing artifact. `proto/e2e_ui_test.py`: `_test_ht_acc_calibration` (5 sub-checks) + `HT_ACC_OK`. Static-asset safety: NO_BOM on app.css + status-bar.js; CACHE_OK + MAIN_UI_OK cssLinkPresent/statusBarJsLoaded true. Full E2E EXIT 0: 102 _OK markers; all prior 101 retained; 0 E2E_FAIL. `proto/server.py` NOT TOUCHED. `polyAreaM2`/`pdfToC`/`cToPdf`/`RS`/`snap` internals UNCHANGED. `.bmaplan` schema UNCHANGED (`calibRaw` in-memory only). Commit `c0834f0`. [2026-05-20] |
| **BUG-20260520-zen-exit-rp-restore — Zen Mode right-panel restore fix** | PASS — Defensive fix: F11 keydown always calls `preventDefault()` (prevents native-fullscreen desync leaving `body.zen` stuck); exit condition `body.zen \|\| (!anyModal && !mPts.length)` so stuck Zen always exits; F9→`toggleLeftPanel` + F10→`toggleRightPanel` added (restore tabs already showed [F9]/[F10] labels but had no handler). `proto/static/css/app.css`: dead `#right-panel.collapsed~#workspace #rp-restore-tab` sibling selector replaced with `body:has(#right-panel.collapsed) #rp-restore-tab{display:flex}` (workspace precedes panel in DOM so `~` never matched — comment in file admitted this). Attribute-based fallback `.canvas-wrap[data-right-collapsed="1"]` kept. Zen/overview `display:none !important` overrides unchanged. `proto/e2e_ui_test.py`: `_test_bug_zen_exit_rp_restore` 6 sub-checks (inZen, zenExitedMidDraw, f10Toggled, tabVisibleWhenCollapsed, tabHiddenInZen, tabVisibleAfterZenExit) + `BUG_20260520_ZEN_EXIT_RP_RESTORE_OK` marker. Static-asset safety: NO_BOM on app.css; CACHE_OK + MAIN_UI_OK cssLinkPresent/cssVarLoaded true. Full E2E EXIT 0: 101 _OK markers; all prior 100 retained; 0 E2E_FAIL. `proto/server.py` NOT TOUCHED. All forbidden surfaces UNCHANGED. `.bmaplan` schema UNCHANGED. Commit `9453777`. [2026-05-20] |
| **INV-2026-05-20-002/003/004 — Layer model rebuild L1+L2+L3** | PASS — Three-commit rebuild making page-scoped `pageStore[n].layers` the single authoritative source for render/hit/visibility/lock. Root cause: site-plan objects with `areaType="room"` collapsed to slug `"sub_area"` (absent from site preset) → `layerId=undefined` + overlap. L1 (`93c512f`): `validLayerSlugForPage` slug-guarantee + `getObjectLayerSlug` resolver + `_slugVisible`/`_slugLocked`/`_objLayerVisible`/`_objLayerLocked` helpers; all render/hit/label paths (`hitTest`/`hitTestAll`/`hitVertex`/`findNearest`/`drawRefLines`) repointed. L2 (`1301a12`): `reassignSelectedObjectLayer()` + Layer `<select>` dropdown in both panels; `objLayerKey()` reports real slug. L3 (`2e6b2f9`): `_layerLockGateBeforeMode`/`toggleLayerLock` repointed to page-scoped `_slugLocked`; global `layerVis`/`layerLock` demoted to non-authoritative mirror. E2E: `INV_LAYER_L1_OK` / `INV_LAYER_L2_OK` / `INV_LAYER_L3_OK` GREEN; HT8D5A all:True restored; 100 _OK markers; 0 E2E_FAIL. Forbidden-surface diff scan CLEAN. `proto/server.py` NOT TOUCHED. `.bmaplan` schema additive only. [2026-05-20] |
| **INV-2026-05-20-001 — Verify Scale tool** | PASS — Replaced `verifyScale()` stub with real second-reference cross-check flow. `verifyFinish()` computes `%dev`; `openVerifyModal()` shows green (<0.5%) / yellow (<2%) / red (≥2%) band + measured/entered/area-impact + Accept/Re-calibrate/Average. `calibPanelOk()` router added (`finishCalib()` unchanged). `#verify-modal` HTML inline-styled (no `app.css` edit). `calibScale.verifyResult` additive schema. `proto/e2e_ui_test.py` +124 lines: `_test_verify_scale` 9 sub-checks (domAndHelpers, guardWhenNoScale, greenBandZeroDev, acceptWritesResult, redBandHighDev, recalibrateSetsPpm, averageSetsPpm, finishCalibIntact, roundTripSaveLoad). `INV_VERIFY_SCALE_OK` = 9/9 all:True. Full E2E EXIT 0: ANNOT_OK/PERSIST_OK/REAL_OK/PROJECT_OK/XLSX_OK/PATH_GEOMETRY_OK all GREEN. Pre-existing 5 env-artifact markers unchanged. `proto/server.py` NOT TOUCHED. All forbidden surfaces UNCHANGED. [2026-05-20] |
| **BUG-20260520-sel-midpan — Middle-mouse + Space pan in Select mode** | PASS — `proto/ui.html` mousedown handler `mode==="sel"` branch: +1 line pan guard `if(e.button===1\|\|spaceDown){isPan=true;lastMx=e.clientX;lastMy=e.clientY;ws.style.cursor="grabbing";return;}` at top of branch (~L2064). Mirrors identical guard already in non-`sel` path. Root cause: `redraw();return` fired unconditionally before the pan check, making middle-mouse and Space pan dead code in Select mode. `proto/e2e_ui_test.py` +34 lines: `_test_bug_sel_midpan` drives real Playwright middle-button drag, asserts canvas `#cc` transform moved +70x/+45y, asserts `mode` stayed `'sel'`. New `BUG_20260520_SEL_MIDPAN_OK` marker. `docs/status/PHASE_INDEX.md` +1 row. Full E2E EXIT 0: all 21 prior baseline markers intact (incl. PATH_GEOMETRY_OK / ANNOT_OK / PERSIST_OK / REAL_OK). Total markers 22. `/bma-measure-ux` MEASURE_UX_PASS. `/bma-measure-regression` MEASURE_REGRESSION_PASS. All forbidden surfaces UNCHANGED. `proto/server.py` NOT TOUCHED. `.bmaplan` schema UNCHANGED. [2026-05-20] |
| **BLOAT-FLAKE-1 — Fix REAL_PDF `_wait_analyse_ready` flake** | PASS — `proto/e2e_ui_test.py` `_wait_analyse_ready` timeout raised 30.0→60.0 s; grace window added (+50% past deadline if status bar still shows active progress). ~15 LOC changed, one helper only. Full E2E EXIT 0: `PERSIST_OK` / `REAL_OK` / `ANNOT_OK` GREEN (flaked 3x during BLOAT-5; now stable). PHASE_BLOAT2/3/4/5_OK 8/8 each + INV_PAGE_SETUP_A_OK 8/8 + _B_OK 9/9 + _C_OK 7/7 + PHASE_HT11_OK 10/10 all GREEN. LOOP_STOP_REGRESSION halt cleared. BLOAT-5 retroactively full-validated. Dev-loop unblocked. No app code / schema / forbidden surfaces touched. `proto/server.py` NOT TOUCHED. `proto/ui.html` NOT TOUCHED. Bloat-reduction wave complete: ui.html 4,231→3,777 (−454, −10.7%, well under 5,000-line trigger). [2026-05-20] |
| **BLOAT-5 — Extract page-setup modal JS to proto/static/js/page-setup.js** | PASS (smoke only) — NEW `proto/static/js/page-setup.js` (125 LOC, plain classic script): 2 constants (`FLOOR_KIND_LABELS`, `FLOOR_KIND_OPTIONS`) + 15 fns (`autoNamePage`, `setPageFloorKind`, `setPageFloorNum`, `countTagBefore`, `selectSetupPage`, `_pageReadiness`, `_setupCountObjects`, `_renderSetupDashboard`, `_renderSetupPageCard`, `_setupBack`, `_renderSetupInspector`, `_pendingDeleteN`, `_openRenumberDialog`, `closeRebuildDialog`, `_executeRenumberDelete`, `_reindexPageDicts`). `<script src="/static/js/page-setup.js">` added to `proto/ui.html` after `annotations.js`. Net: ui.html −92 LOC (3,869→3,777). `proto/e2e_ui_test.py` +91 LOC: `pageSetupJsLoaded` field + `_test_bloat5_page_setup_extracted` (8 sub-checks) + `PHASE_BLOAT5_OK` marker. py_compile PASS. Smoke EXIT 0: 18 baseline + PHASE_BLOAT5_OK 8/8 + PHASE_INV_PAGE_SETUP_A_OK 8/8 + _B_OK 9/9 + _C_OK 7/7 + PHASE_HT11_OK 10/10 GREEN. MAIN_UI_OK `pageSetupJsLoaded: True`. Full FAILED (3/3 retries): pre-existing `_wait_analyse_ready` env flake on page 1/45 real permit — **BLOAT-FLAKE-1** (see KNOWN_ISSUES.md). Zero page-setup code runs during that path — confirmed NOT a BLOAT-5 regression. All forbidden surfaces UNCHANGED. `proto/server.py` NOT TOUCHED. `.bmaplan` schema UNCHANGED. Kept in `ui.html`: 15 cross-cluster glue fns + `pageFloorKind`/`pageFloorNum` state declarations. Session total ui.html 4,231→3,777 (−454 across BLOAT-1..5). Recipe 5-for-5. Loop halted per `LOOP_STOP_REGRESSION` safety rule. [2026-05-20] |
| **BLOAT-4 — Extract annotation JS to proto/static/js/annotations.js** | PASS — NEW `proto/static/js/annotations.js` (205 LOC, plain classic script): 13 fns (`ensureAnnotations`, `newAnnotationId`, `addAnnotation`, `renderStickyCards`, `_createStickyCard`, `clearAnnotations`, `annotationHitTest`, `deleteAnnotation`, `openAnnotationEditModal`, `closeAnnotationEditModal`, `saveAnnotationEdit`, `_annColor`, `drawAnnotations`). `<script src="/static/js/annotations.js">` added to `proto/ui.html` after `export-save.js`. Net: ui.html −188 LOC (4,057→3,869). `proto/e2e_ui_test.py` +99 LOC: `annotationsJsLoaded` load-check field + `_test_bloat4_annotations_extracted` (8 sub-checks) + `PHASE_BLOAT4_OK` marker. py_compile PASS. full EXIT 0 on retry (1st run: known REAL_PDF analyse flake page 1/45, unrelated; dev-loop one-retry rule). 22 baseline markers GREEN (incl. MAIN_UI_OK `annotationsJsLoaded: True` / ANNOT_OK / PERSIST_OK / REAL_OK). PHASE_INV_STICKY_OK 10/10 (sticky-note round-trip survives extraction). PHASE_HT11_OK 10/10 (edit+delete modal functional). PHASE_BLOAT2_OK 8/8 + PHASE_BLOAT3_OK 8/8 + PHASE_BLOAT4_OK 8/8. All forbidden surfaces UNCHANGED. `proto/server.py` NOT TOUCHED. `.bmaplan` schema UNCHANGED (`pageStore[pg].annotations` field preserved; PHASE_INV_STICKY_OK J_roundTrip sub-check verified). 7 per-mode mousedown branches remain in ui.html (unified pointer state coupling). Session total ui.html 4,231→3,869 (−362 across BLOAT-1..4). Recipe 4-for-4. BLOAT-5 (page-setup) next. [2026-05-20] |
| **BLOAT-3 — Extract export/save JS to proto/static/js/export-save.js** | PASS — NEW `proto/static/js/export-save.js` (188 LOC, plain classic script): 14 fns (`rowBase`, `buildRows`, `dlBlob`, `exportJSON`, `exportCSV`, `exportSummaryXLSX`, `exportXLSX`, `exportAllPagesAnnotatedPDF`, `exportCurrentPageAnnotatedPDF`, `exportPngZip`, `_makeProjBlob`, `_writeToHandle`, `_fallbackDownload`, `saveProjectAs`, `saveProject`, `saveSourcePdfInPlace`) + 13 consts (6 `COL_*` + 7 `TYPE_*`). `<script src="/static/js/export-save.js">` added to `proto/ui.html` after `status-bar.js`. Net: ui.html −155 LOC (4,208→4,057). `proto/e2e_ui_test.py` +111 LOC: `exportSaveJsLoaded` load-check field + `_test_bloat3_export_save_extracted` (8 sub-checks) + `PHASE_BLOAT3_OK` marker. py_compile PASS. smoke EXIT 0 (18/18 + PHASE_BLOAT2_OK + PHASE_BLOAT3_OK). full EXIT 0 (21/21 + PHASE_BLOAT2_OK + PHASE_BLOAT3_OK; XLSX_OK + PROJECT_OK + PERSIST_OK + ANNOT_OK + REAL_OK all GREEN on real 45-page permit). `schemaOk` sub-check confirms all 12 v1 `.bmaplan` fields present post-extraction. `asyncOk` confirms `saveProject`/`saveProjectAs`/`saveSourcePdfInPlace` are AsyncFunctions. All forbidden surfaces UNCHANGED. `proto/server.py` NOT TOUCHED. Recipe proven on largest cluster: FSA handle persistence (`currentProjectHandle`/`currentSourcePdfHandle`) + schema serialization safe in external script context. BLOAT-4 (annotations) + BLOAT-5 (page-setup) now formulaic. [2026-05-20] |
| **BLOAT-2 — Extract status-bar JS to proto/static/js/status-bar.js** | PASS — NEW `proto/static/js/status-bar.js` (49 LOC, plain classic script): 8 fns (`updateAnalyseUI`, `activeLayerLabel`, `currentObjectCount`, `currentWarningCount`, `updateBottomBar`, `updateModeLabel`, `_markSaved`, `_setDirty`) + 2 consts (`MODE_BASE_LABELS`, `SITE_TAG_THAI_LABELS`). `<script src="/static/js/status-bar.js">` tag added to `proto/ui.html` at line 822 (between `opening-parent.js` and main inline `<script>`). Extracted code removed from inline `<script>` (3 comment placeholders remain). Net: ui.html −29 +6 (4,231→4,208 lines). `proto/e2e_ui_test.py` +95 LOC: `statusBarJsLoaded` load-check field + `_test_bloat2_status_bar_extracted` (8 sub-checks) + `PHASE_BLOAT2_OK` marker. py_compile PASS. smoke EXIT 0 (18/18 + PHASE_BLOAT2_OK). full EXIT 0 (21/21 + PHASE_BLOAT2_OK; PERSIST_OK on real 45-page permit proves `_setDirty`/`_markSaved` extraction safe across save/reload). `/bma-human-test` SKIPPED (mechanical extraction, zero user-visible change). All forbidden surfaces UNCHANGED. `proto/server.py` NOT TOUCHED. `.bmaplan` schema unchanged. Recipe proven: cross-script `let`/`const` binding access works in classic non-module scripts. BLOAT-3..5 unblocked. Commit: pending. [2026-05-20] |
| **BLOAT-1 — CLAUDE.md LOC drift fix + consolidation trigger rule (docs-only)** | DOCS-ONLY — Corrected stale LOC baselines: `proto/ui.html` ~1,700 → ~4,230 lines (+149%); `proto/server.py` ~1,370 → ~1,750 lines. Added "Size discipline" paragraph with hard consolidation trigger rule (>5,000 lines → must extract one JS region to `static/js/<region>.js`, following `semantic-meta.js`/`opening-parent.js` pattern). Updated `bma-explorer` subagent table row LOC numbers. Inserted BLOAT-1..5 sprint cards into `docs/status/PHASE_INDEX.md` active queue (BLOAT-2 = status-bar extraction → BLOAT-3..5 = export-save/annotations/page-setup). Added `### bloat-audit 2026-05-19` block to Discovered backlog. Files: `CLAUDE.md` (+21 −2), `docs/status/PHASE_INDEX.md` (+26 −0). No code/schema/runtime changed. `python -m py_compile proto/server.py` PASS (sanity baseline). `/bma-e2e` + `/bma-human-test` skipped — docs-only sprint. Commit: pending. [2026-05-19] |
| **INV-2026-05-19-003b — /export-png ZIP endpoint (end-of-day bundle: INV-003b + HT-18c + INV-003a)** | PASS — End-of-day session bundle. (1) INV-003b (`612de96` feat + `7f0300f` docs): NEW `/export-png` POST endpoint in `proto/server.py` — additive; accepts `case_id + selected_pages[] + dpi_scale`; renders each page via PyMuPDF at requested scale; bundles into ZIP archive (`application/zip`). Export menu wired in `proto/ui.html`. New E2E marker `PHASE_INV_EXPORT_PNG_OK`. (2) HT-18c (`f1b4331` fix + `9297ed4` docs): Fixed `_test_ht18b_save_load_round_trip` — replaced deep `eq()` (too strict after `normalizeAllObjects` mutates pre-snapshot) with field-by-field checks for 13 sub-checks (A poly / B opening / C line / D ref / E parking / F-M page metadata + projectInfo + layer state). Also fixed `applyLoadedProject` `_projInfoSnap` restoration. `PHASE_HT18B_OK` 13/13 GREEN. HT-18 series complete (HT-18a + HT-18a-ext + HT-18b + HT-18c all done). (3) INV-003a (`b4f7235` feat + `8200ef6` docs): "Print Current Page" + "Print Selected Pages" in File menu — `canvas.toDataURL("image/png")` → synthetic print window + `window.print()`. New E2E marker `PHASE_INV_PRINT_CANVAS_OK` (8 sub-checks). py_compile PASS all 3. full EXIT 0 for INV-003b and INV-003a; smoke EXIT 0 for HT-18c. All predecessor markers retained: `PHASE_HT18_OK` 36/36, `PHASE_INV_ZEN_V2_OK` 10/10, `PHASE_INV_OVERVIEW_OK` 9/9, `PHASE_INV_ZEN_OK` 10/10, `PHASE_INV_PALETTE_OK` 10/10, `PHASE_INV_POLISH_001C_OK` 5/5. Forbidden-surface scan: `proto/server.py` additive new endpoint only (no rename/removal); all other surfaces UNCHANGED. Session: 33 commits pushed to `origin/main-v2-2026-05-19`. Pending uncommitted: Zen Mode user manual docs sprint (4 files). [2026-05-19] |
| **HT-18a-ext — Extended pushUndo() coverage to 22 more mutation sites** | PASS — Sequel to HT-18a (`895a9d7`). `pushUndo()` inserted at 22 additional mutation sites: layer reorder (`moveLayerUp`/`moveLayerDown`), `renameLayer`, `setLayerColor`, `toggleLayerLock`, `setAllLayersVisible`, `hideOtherLayers`, `lockOtherLayers`, `setAllLayersLocked`, `toggleLayer`, `layerHideOthers`, `layerShowAll`, `setQuickTag`, `setPageTag`, `setPageFloorKind`, `setPageFloorNum`, `applyAutoNames`, `excludePage` (+ `_skipUndo` param), `restorePage2` (+ `_skipUndo` param), `hideSelectedPages`, `rotatePage`, `resetPageScale`, `pageCtxMenu` inline `autoNamePage` call. `_test_ht18_pushundo_leaks` expanded 7 → 36 sub-checks (22 source-presence + 7 runtime isDirty-flip + 7 original). `PHASE_HT18_OK` = `{'all': True}` 36/36. py_compile PASS. `python proto/e2e_ui_test.py full` EXIT 0. HUMAN_TEST_PASS: `/bma-human-test` found 3 initially-missed sites (`toggleLayer` L2657, `layerHideOthers` L2659, `layerShowAll` L2666); fixed inline. `setQuickTag` / `resetPageScale` confirmed as correct early-exit code paths. `proto/server.py` NOT touched. No schema change. `.bmaplan` additive only (`_skipUndo` params are internal; version stays 1). `PHASE_INDEX.md`: HT-18a-ext done, HT-18b `done-with-test-design-caveat`, HT-18c upgraded to `queued`. Pre-existing non-regressions: `PHASE_HT8C_OK` 3/5, `PHASE_HT8D1_OK` 8/9, `PHASE_HT10_OK` 8/10, `PHASE_HT12H_OK` 4/5 (all pre-dating this sprint). `PHASE_HT18B_OK` 7/13 = test design issue (`eq()` too strict after `normalizeAllObjects` mutates pre-snapshot; NOT schema drift; Phase A audit clean); filed as HT-18c. +39 LOC JS + +295 LOC test. New artifact: `sprints/active/2026-05-19-ht-18-save-load-audit-fix/PHASE_A_AUDIT.md`. [2026-05-19] |
| **HT-18a — Save-state pushUndo leak fixes** | PASS — `pushUndo()` inserted at 6 mutation sites that previously modified state without setting `isDirty`: `toggleScaleLine` (L2814), `showLayer` / `hideLayer` / `lockLayer` / `unlockLayer` / `soloLayer` (L2824-2828), `applyLandEdgeTag` (L1788). Root cause audit via `bma-explorer`: `_makeProjBlob` uses `JSON.stringify(pageStore)` (auto-serializes all fields), `applyLoadedProject` restores by ref — save schema is complete; bug was purely missing isDirty triggers. `PHASE_HT18_OK` 7/7 (toggleScaleLineSetsDirty, hideLayerSetsDirty, showLayerSetsDirty, lockLayerSetsDirty, unlockLayerSetsDirty, soloLayerSetsDirty, applyLandEdgeTagHasPushUndo). smoke EXIT 0. full SKIPPED (additive insert only; PROJECT_OK + PERSIST_OK test save/load round-trip not isDirty path — unaffected). TEST-H SKIPPED (sub-50-LOC; all 6 mutation sites marker-covered). Sprint card split: HT-18 → HT-18a (done 895a9d7) + HT-18b (queued, round-trip E2E) + HT-18c (conditional, only if HT-18b finds drift). Session addenda (parallel commits): `c7e9334` fix 002b chip alignment (4→3 chips, solid-blue active); `d94b35e` fix 002a classic .menu-bar + .canvas-top-bar hidden in body.zen (user-reported "เมนูทับกัน"); `5468d13` invent GO for f12-overview-mockup-port; `1f57451` spike preview proto/sandbox/invent-f12-overview-mockup-port.html. Uncommitted working tree: HT-18b test draft + additional pushUndo sites (user/linter parallel edits) — pending user review. `proto/server.py` UNTOUCHED. No schema change. +~10 LOC JS + ~70 LOC test. [2026-05-19] |
| **INV-2026-05-19-002b — F12 Overview standalone (C)** | PASS — `body.overview` class hides canvas/ribbon/panels/status/HUDs; shows `#overview-content` card grid grouped by 6 discipline categories. `_OV_GROUPS` config (site/plan/elev/section/detail/none). `_ovBuildGrid()` groups pages by `pageTags`. `_ovCountObjects(n)` per-card object badge. `_ovCardClick(n)` atomic `closeOverview()` + `loadPage(n)`. Lazy IntersectionObserver per card fetches `thumbUrl(n)` only when in viewport — same malloc-safe pattern as 001a. `toggleOverview()` + F12 hotkey. Esc priority updated: overview → close overview; zen → exit zen; else → default. `#ztb-chip-overview` in `#zen-topbar` unstubbed (was stub in 002a). 6 group label colors: site=green, plan=blue, elev=amber, section=purple, detail=cyan, none=gray. `proto/server.py` UNTOUCHED. No new server endpoint. No schema change. Initial `cardClickExitOverview` + `cardClickSetCurPage` FAIL (test PDF < 3 pages; `data-page="3"` null); fixed with first-available-card selector + direct `_ovCardClick(targetPage)` → 9/9 PASS. `PHASE_INV_OVERVIEW_OK` 9/9. smoke EXIT 0. full EXIT 0. TEST-H SKIPPED (additive new mode; no measurement/canvas/server touch; 9 sub-checks cover all entry/exit/interaction paths; thumb IO reuses 001a already journey-tested). `PHASE_INV_ZEN_V2_OK` 9/9 + `PHASE_INV_ZEN_OK` 10/10 + `PHASE_INV_PALETTE_OK` 10/10 + `PHASE_INV_POLISH_001C_OK` 5/5 no regression. +~135 LOC JS + ~50 LOC CSS + ~80 LOC test. [2026-05-19] |
| **INV-2026-05-19-002a — F11 Zen top bar (A+D additive bundled)** | PASS — Additive `#zen-topbar` 40px overlay inside `body.zen`. 6 dropdowns (File/Page/Measure/Annotate/View/Help) wired to existing handlers; 4 icon chips (search/palette/circle/rect). `toggleZenFocus()` (F key = Focus sub-mode, `body.zen.focus` hides all HUDs with `!important`). `_setupZenEdgePeek()` (4px edge strip → `body.zen.focus.peek` restores HUDs on hover). `_ztbToggleMenu()` dropdown manager. `toggleZenMode` extended: v2 onboarding toast (green tint, once per `PREFS.layout.zenV2Onboarded`). F-key scope guard (blocks accidental focus toggle inside text inputs). 001a HUDs shifted top:34→50px to clear topbar. Original 002a plan was breaking (`toggleZen()` replacement); user redirected mid-SCOPE to non-breaking additive — 001a `toggleZen()` UNTOUCHED. Initial focusHidesHuds FAIL fixed with `!important` + transition suppression → 9/9. `PHASE_INV_ZEN_V2_OK` 9/9. smoke EXIT 0. full EXIT 0. HUMAN_TEST_PASS (13/13 journey steps; 45/45 pages; .bmaplan round-trip OK; 1 test-infra-only FRICTION not filed). `PHASE_INV_ZEN_OK` 10/10 + `PHASE_INV_PALETTE_OK` 10/10 no regression. Zero forbidden-surface edits. +133 LOC JS + +40 LOC CSS + +128 LOC test. [2026-05-19] |
| **INV-2026-05-19-001c — Zen+Palette FRICTION polish (HT-Z-1 + HT-Z-2 + HT-Z-3 bundle)** | PASS — Polish sprint clearing 3 FRICTION findings. `_zenSyncHud()` reads `pageNames[curPage]` directly (HT-Z-1: eliminates MutationObserver lag on fast minimap nav). `_zenSyncHud()` colors Scale chip amber when `getScaleForPage(curPage).state === 'auto-unverified'` or no scale; title tooltip added (HT-Z-2). `filterPalette()` empty branch appends "💡 แท็กภาษาไทยใช้ได้หลังตั้งค่าหน้าใน Page Setup" hint when Thai page-type word detected in query AND no pages are tagged (HT-Z-3). `PHASE_INV_POLISH_001C_OK` 5/5 (hudReadsPageNamesDirectly, unverifiedScaleAmber, manualScaleNotAmber, thaiTagHintShown, hintAbsentWhenTaggedOrNoThai). smoke EXIT 0. full EXIT 0. TEST-H SKIPPED (sub-200-LOC polish; all changed branches marker-covered). `PHASE_INV_ZEN_OK` 10/10 + `PHASE_INV_PALETTE_OK` 10/10 no regression. Zero forbidden-surface edits. `~15 LOC` JS + `+65 LOC` test. Trilogy 001a/001b/001c complete. [2026-05-19] |
| **INV-2026-05-19-001b — ⌘K Command Palette (fuzzy page jump)** | PASS — Additive Ctrl+K/Cmd+K command palette. `.cmd-palette` fixed-center modal (z-index 9500, above Zen HUDs at 1500). Filter input narrows pages by number, name, or Thai type tag. 5 helpers: `togglePalette`, `closePalette`, `filterPalette`, `_palJumpToIdx`, `_palMoveSel`. ArrowDown/Up/Enter/Esc keyboard nav placed before `inInput` guard. Mid-draw guard (`mPts.length===0`). Color-coded tag chips (site/plan/elev/section/detail). View menu item "🔍 ค้นหาหน้า (Command Palette) Ctrl+K". Composes with Zen Mode (z-index correct). HT-7 scale gate NOT bypassed by palette page jump. `PHASE_INV_PALETTE_OK` 10/10. smoke EXIT 0. full EXIT 0. JOURNEY_OK 13/13 steps; 0 JS errors. HT-Z-3 filed (empty-state hint missing for Thai tag filter on untagged PDF). Zero forbidden-surface edits. `~35 LOC` CSS + `~120 LOC` JS + `~95 LOC` test. Source: idea `2026-05-19-01-36` 001b of 2. [2026-05-19] |
| **INV-2026-05-19-001a — Zen Mode + Sheet Minimap** | PASS — `body.zen` class toggle hides ribbon/panels/status-bar/summary-widget; canvas expands to ~94.44% vh. Three corner HUDs: TL (scale + tool), TR (objects + layer), BL (save state), synced via MutationObserver. Lazy-loaded 5-col sheet minimap (`#zen-minimap`, IntersectionObserver per cell — malloc anti-pattern avoided). F11 toggle + Esc-exit-zen. `PREFS.layout.zenMode` + `PREFS.layout.zenOnboarded` additive (defaults false). View menu "⛶ Zen Mode" item. `applyLayoutPrefs()` extended with zen branch. `PHASE_INV_ZEN_OK` 10/10. smoke EXIT 0. full EXIT 0. `bma-human-journey-tester` JOURNEY_OK (45-page permit, 0 CRASH/BROKEN; HT-Z-1 minimap HUD timing + HT-Z-2 auto-unverified scale chip filed). Zero forbidden-surface edits. `~50 LOC` CSS + `~180 LOC` JS + `~110 LOC` test. Source: idea `2026-05-19-01-36`, `invent-done-go`, sprint split 001a/001b. [2026-05-19] |
| **2026-05-19 — Ribbon Cleanup Polish** | PASS — Pure cosmetic sprint. `body { font-size: 16px }` → `14px` in `proto/static/css/app.css` (revert; 16px caused layout shifts). `#scale-badge` `display:none` in ribbon Scale group (stays in DOM for JS; status bar already shows scale state). `.ribbon-group` wrapping `#active-layer-select` `display:none` + 2 `rdiv` dividers removed (`<select>` preserved for `activeLayerLabel`/`getActiveLayer`/`setActiveLayerMenu`/`updateActiveLayerControl`/draw functions; Right panel Layers tab is primary path). `#btn-report` Review button rewrapped: bare `.ribbon-group` → `.ribbon-group.rsection` + `.rlbl "📊 REVIEW"` + `.rrow` + leading `rdiv` — fixes flex-stretch to 78px, renders at uniform 60px. py_compile PASS. Smoke PASS (all 18 markers GREEN). No JS logic changed. No forbidden surfaces. No new markers. [2026-05-19] |
| **INV-2026-05-18-002 — Settings v2: export defaults + loupe prefs** | PASS — 4 new PREFS paths additive in `bmaPlan.settings.v1`: `export.csvSeparator` (`,`/`;`/`\t`, default `,`); `export.includeLawBasis` (bool, default true); `loupe.radius` (50–160 px, default 80); `loupe.zoomFactor` (2–8×, default 4). `exportCSV` reads separator + includeLawBasis (dynamic escape regex, conditional lawBasis column). `updateLoupe` uses `loupeZoomFactor` for constant magnification. NEW `_applyLoupePrefs()` syncs state vars on boot + after Apply. UI slots into existing 4-tab modal (loupe in "วาด", export defaults in "หน่วย") — no new tab. `SETTINGS_V2_OK` 6/6; `SETTINGS_OK` (v1 baseline 13/13) still GREEN. Commit `3e71865`. See `docs/invent/page-setup-redesign.md` for research context. [2026-05-19] |
| **INV-2026-05-18-001c — Page delete + renumber-map + `/rebuild-pdf`** | PASS — NEW `/rebuild-pdf` POST in `proto/server.py` (after `/project`, DOES NOT edit `/upload`/`/page/{n}`/`/analyse`). PyMuPDF `doc.delete_page()` in reverse order; in-place edit of `CASES[case_id]["doc"]`; flush `page_cache`/`image_cache`; returns `{totalPages, renumberMap: {oldN: newN}, deletedNumbers}`. Client: `_openRenumberDialog(n)` preview table (deleted row crossed out, "ลบ" in red); `_executeRenumberDelete` → POST /rebuild-pdf → `_reindexPageDicts(map)` walks 7 per-page dicts. Hard-block during draw (`mPts.length > 0`); last-page guard; `pushUndo()` before commit; Foxit-style warning. Research-first: `afd4e71` survey of Bluebeam/Adobe/Foxit/Nitro/AutoCAD/PlanGrid page-delete UX; verdict PRIOR_ART_PARTIAL; Q1-Q4 locked in `docs/invent/page-setup-redesign.md`. `PHASE_INV_PAGE_SETUP_C_OK` 7/7. Commit `ebb521c`. [2026-05-19] |
| **INV-2026-05-18-001b — Floor sub-types for plan pages** | PASS — NEW schema: `pageFloorKind ∈ {basement|normal|mechanical|rooftop|custom}` + `pageFloorNum` (additive per-page dicts in `.bmaplan v1`). `autoNamePage` floor-aware for `tag=plan`: basement N → "ชั้นใต้ดิน N", normal N → "ชั้น N", mechanical → "ชั้นห้องเครื่อง", rooftop → "ชั้นดาดฟ้า", custom → user-typed (no overwrite). `setPageTag` clears floor fields on tag change away from plan. Save/load round-trip via `_makeProjBlob`/`applyLoadedProject`. Floor-kind picker in page-card (visible when tag=plan). `PHASE_INV_PAGE_SETUP_B_OK` 9/9 (7 contract + 2 bonus: save/load round-trip, tag-change clear). Commit `798e5c3`. [2026-05-19] |
| **INV-2026-05-18-001a — Page Setup left inspector + traffic-light chips** | PASS — Replaced `#proj-form-panel` content with `#setup-inspector-content` (dashboard ⇄ page-card switch). Project-info form moved into `<details id="setup-pi-accordion">` (open-by-default for E2E compat). NEW helpers: `_pageReadiness`, `_setupCountObjects`, `_renderSetupDashboard`, `_renderSetupPageCard`, `_setupBack`, `_renderSetupInspector`. `#tag-grid` cells updated: traffic-light dot (green=tagged+manual / amber=auto-unverified / red=untagged or unknown) + object-count chip. `selectSetupPage`/`setPageName`/`buildTagGrid` hooked to inspector refresh. `PHASE_INV_PAGE_SETUP_A_OK` 8/8. This was the repo's INITIAL commit (bundled with full pre-existing project state). Commit `e85a5ce`. [2026-05-18] |
| **2026-05-18 — UI Redesign Batch HT-12..HT-15** | PASS — 15 sprints, 22 commits, smoke 54/54 GREEN. Top menu absorbs Workspace ribbon (9 dropdowns). Polygon dropdown popover surfaces sub-mode shortcuts (A/Alt/Shift/O). Right panel placeholders closed (List/Props renderers). Helpers section in Measure ribbon. 2 pre-existing test failures cleaned up. Zero forbidden-surface edits. See `log.md` for per-sprint breakdown. |
| INV-2026-05-17-001 — Freeform area measurement (Alt sub-mode of polygon) | PASS — NEW `rdpSimplify(pts, tol)` (~25 LOC, inline RDP) placed next to area-math block. Alt-at-mousedown enters freehand sub-mode; explicit early-return bypasses snap engine during drag. `PHASE_FREEFORM_OK` 7 sub-checks (err=0.46%, 240 raw → 16 decimated). full 44/44 GREEN. Commit `023b988`. [INV-2026-05-17-001, 2026-05-17] |
| HT-6 — Live arc preview during arc-mode draft | PASS — inside `redraw()` `mArcDraft.pending` block, added arc-preview branch: when `guidePoint` set and `mPts.length>=1`, calls `computeArcEdge(lastPdfVertex, mousePdf, throughPt, centroid)` and draws a dashed arc (`#ff453a`, 5/zoom dash, 0.85 alpha) from last vertex curving through through-point to cursor. Render math mirrors `_renderPolyEdges` committed-arc render: circumcircle center, start/end angles in canvas coords, CCW direction from through-point side of chord. Mirrors straight-line `guidePoint` pattern at L1478. ~15 LOC. NEW marker `PHASE_HT6_OK` 4 sub-checks (arcGuidelineRenderFnExists, arcDraftStateHasGuideSupport, guidePointBranchPresent, noRegressionOnLegacyPoly). full 42/42 GREEN. Zero forbidden-surface edits. Zero server/schema changes. Commit `ecb44d4`. Source: user-test 2026-05-17 "ขาดเส้น guideline เหมือนของเส้นตรง". [HT-6, 2026-05-17] |
| CIRCLE_RENDER — Analytic circle/ellipse render in `_renderPolyEdges` | PASS — `_renderPolyEdges` short-circuit branches at top: `poly.shape==='circle'` → `ctx.arc(...)` analytic; `poly.shape==='ellipse'` → `ctx.ellipse(...)` analytic; else → existing line/arc-edge flow unchanged. Storage (`poly.pts` 32-gon), hit-test, snap, area math all stay on legacy. `objectAreaM2` already routed to closed-form helpers. NEW marker `CIRCLE_RENDER_OK` 7 sub-checks (arcFnExists, circleUsesArc, ellipseUsesEllipse, legacyStillUsesLineTo, areaMathIdentical, pts32gonPreserved, roundTripOK). full 41/41 GREEN. Zero forbidden-surface edits. Last pre-loop leftover cleared. Refs: `docs/status/PHASE_H_PATH_GEOMETRY_VISUAL_AUDIT.md`. Commit `1bf61ca`. [CIRCLE_RENDER, 2026-05-17] |
| dev-website — Static docs site at `/static/docs/` | PASS — NEW `proto/static/docs/index.html` (~190 LOC): single static HTML, inline ~80-line micro-markdown renderer, fetches sibling `content.json`, exposes `window.__bmaDocs = {nav, pages}` for tests, offline file:// fallback. NEW `scripts/build_docs.py` (~120 LOC, stdlib-only): walks `proto/manual/*.md`, `log.md` (12 most recent sessions), `sprints/completed/**/RUN_*.md` (12 most recent), `docs/process/{ANTI_PATTERNS,TROUBLESHOOTING}.md`; emits `proto/static/docs/content.json` (28 pages, 4 groups: Manual / Dev Log / Sprint Cards / Process Docs, ~50 KB). NEW 5 Thai manual files: getting-started, set-scale, measure-tools, export, keyboard-shortcuts. `proto/ui.html` UNTOUCHED (Help menu link deferred to follow-up `/bma-ui-menu` sprint). NEW marker `DOCS_SITE_OK` 7 sub-checks (titleOK, contentJsonFetches, groupCount≥4, manualSlugsPresent×5, articleRenders, navLinksCount≥10, markdownProbe). full 41/41 GREEN. Zero forbidden-surface edits. User GO 2026-05-17 (invent-pending-checkpoint → invent-done-go → done). Commit `1bf61ca`. [dev-website, 2026-05-17] |
| INV-2026-05-15-002 — Unified Settings/Preferences modal | PASS — `bmaPlan.settings.v1` localStorage key (version:1); `getPref`/`setPref` path-based reader/writer; `migrateFromLegacy()` one-way migration from legacy `uiLayoutOptions.v1` + `widgetPlacement.v1` (flexible `preset||mode` lookup + `{visible:bool}` or plain-bool extraction); 4-tab modal `#settings-modal` (วาด/หน่วย/หน้าจอ/Widgets); `openSettingsModal`/`applySettings`/`resetSettings`; `Ctrl+,` shortcut; bad-JSON safety (try/catch → reset defaults); wrong-version safety (version≠1 → reset defaults); legacy keys preserved post-migration. NEW marker `SETTINGS_OK` 13 sub-checks. smoke GREEN + full 38/38 GREEN. Zero forbidden-surface edits. `bmaPlan.settings.v1` extends spike 7→13 sub-checks (adds badJsonSafe, wrongVersionSafe, legacyPreserved). [INV-2026-05-15-002, 2026-05-17] |
| I-E — Building-to-building distance + wallEdges schema | PASS — `WALL_EDGE_TYPES` catalog (4 types: exterior_wall, party_wall, fire_wall, glass_wall + Thai labels); `wallEdgeType` additive field on polygon edges (default null, version stays 1); `computeBuildingPairsForPage(pageObjs, pg)` vertex-to-edge metric across all `building_coverage` polygon pairs → `[{nameA, nameB, distM}]` sorted ascending; `computeAllBuildingPairs` aggregates across pages; "ระยะระหว่างอาคาร (2h pre-check)" section in siteplan tab (facts only, no verdict); round-trip-safe. NEW marker `PHASE_I_E_OK` 9 sub-checks. full GREEN. Zero forbidden-surface edits. Schema additive only. [Phase I-E, 2026-05-17] |
| I-D — 4-direction setback + compass overlay | PASS — `landEdgeRole` additive field on polygon edges (`front`/`back`/`side1`/`side2`/null); `computeEdgeSetback(poly, role, refPolys, pg)` generalizes front-only setback to all 4 directions using edge role filter; `collectSetbackReport` returns `{front, back, side1, side2}` all in metres; `northAngle` additive field in `pageTags[pageIdx]` (default 0, 0–359°); Page Setup input for north angle; `#canvas-compass` SVG overlay (48×48, top-right workspace, `transform="rotate(northAngle)"` driven by per-page north); `updateCompass(angle)` fn; siteplan tab shows all 4 setback rows. NEW marker `PHASE_I_D_OK` 10 sub-checks. full GREEN. Zero forbidden-surface edits. Schema additive only. [Phase I-D, 2026-05-17] |
| I-C — "ผังบริเวณ" 5th tab in Summary Widget | PASS — 5th tab `#tab-siteplan` + `#summary-siteplan` content div added to Summary Widget; `updateSiteplanTab()` renders `collectSummaryData()` output inline: BCR/OSR/FAR/Permeable ratios (plain numbers, no verdict), per-tag area breakdown table, marker-count table, front-setback (v1) row, Phase 1 footer note ("ไม่มีการพิจารณาผ่าน/ไม่ผ่านตามกฎหมาย" hard-coded as scope guardrail); hook in `updateSummaryWidget()`; reuses `collectSummaryData()` from U2 (no new server logic). NEW marker `PHASE_I_C_OK` 10 sub-checks. full GREEN. Zero forbidden-surface edits. `proto/server.py` untouched. [Phase I-C, 2026-05-17] |
| I-B4 — Site Plan 6-step stepper widget | PASS — `#site-stepper` DOM (6 steps, collapsible, site-page-only via `updateSiteRibbon`); `updateSiteStepperUI()` derives step-done state from existing page/project data (scale≠unknown, pageTags=site, projectInfo.buildingClassification, ≥1 building_coverage poly, ≥1 open_space/permeable poly, ≥1 marker); hooks in `updateBottomBar()` + `loadPage()`; advisory-only (no blocking); no new `.bmaplan` state. NEW marker `PHASE_I_B4_OK` 10 sub-checks. full GREEN. Zero forbidden-surface edits. [Phase I-B4, 2026-05-17] |
| I-B3 — Properties panel site fields + draw-then-classify | PASS — `isBuildingTag(tag)` helper (returns true for `building_coverage`); `buildPropertiesPanel(obj)` extended: `buildingHeight_m` editable `<input type="number">` row visible when `isBuildingTag`; 7 site area `semanticTag` options appended to Semantic Tag dropdown when `pageTags[curPage]==='site'` (draw-then-classify workflow); height `onchange` → update `obj.buildingHeight_m` + `pushUndo()` + `redraw()`; tag `onchange` uses existing handler. NEW marker `PHASE_I_B3_OK` 10 sub-checks. full GREEN. Zero forbidden-surface edits. Schema additive (`buildingHeight_m` defined in I-A). [Phase I-B3, 2026-05-17] |
| SB-2026-05-15-002 — Upload-cap UX modal + cold-start hint | PASS — `currentUploadCapMB` state var updated from `/upload` response echo (server-driven, not hard-coded); `showUploadCapModal(fileSizeMB, capMB)` pre-flight helper: shows modal before sending if `file.size > cap`; modal has file-size, cap, 3 actionable suggestions (compress, reduce pages, lower DPI); `updateCapBadge()` cold-start Setup screen hint; clear 413 modal with suggestions. `proto/server.py` untouched. NEW marker `SB002_UPLOAD_UX_OK` 8 sub-checks. full GREEN. Zero forbidden-surface edits. [SB-2026-05-15-002, 2026-05-17] |
| INV-2026-05-15-001 — Arc-polygon hybrid measurement | PASS — 3-click inline arc (vertex → A → through-point → vertex), new helpers `_arcCircumcenter` / `computeArcEdge` / `polyMetricsAnyShape` / `_renderPolyEdges` (~30 lines) + `mArcDraft` state. Reuses already-shipped `polygonAreaWithArcsM2`. Snap bypassed during arc through-point click. Bug fixed mid-sprint: replace_all caught `polyMetricsAnyShape`'s internal call → infinite recursion → `updatePageSummary` crash → fixed with one targeted Edit. NEW marker `ARC_POLYGON_OK` 7 sub-checks (err=0.000000% on canonical square+semicircle, sweep=π, area=13926.99 px² exact). smoke 29/29 + full 32/32 GREEN. Zero forbidden-surface edits. Schema additive only [user request 2026-05-17] |
| HT-5 — `.dd-submenu` overflow on short viewports | PASS — CSS-only: `.dd-submenu` += `max-height:calc(100vh - 120px); overflow-y:auto; overflow-x:hidden; scrollbar-width:thin` in `proto/static/css/app.css:25`. 15-item Measure site submenu now scrolls cleanly on short viewports (412 px tall). NEW marker `PHASE_HT5_OK` 3 sub-checks. smoke 28/28 + full 31/31 GREEN. Zero forbidden-surface edits [Autonomous Dev Loop iter 10] |
| HT-4 — Name panel dismissal paths | PASS — NEW `autoCloseNamePanel()` helper + 3 dismissal paths added: (1) click outside name-panel auto-closes after 300ms grace (auto-confirm if input has value, else cancel via consistent callback fire); (2) `loadPage()` calls `autoCloseNamePanel()` at top so page nav auto-commits; (3) global Esc handler intercepts `if (namePanel open) cancelName()` instead of just hiding — callback fires properly. NEW `namePanelOpenedAt` timestamp for grace logic. NEW marker `PHASE_HT4_OK` 9 sub-checks. smoke 27/27 + full 30/30 GREEN. Zero forbidden-surface edits [Autonomous Dev Loop iter 9] |
| HT-3 — `lbl-mode` site-tag context | PASS — Extracted `updateModeLabel(m)` helper + `SITE_TAG_THAI_LABELS` map (7 site tags) + `MODE_BASE_LABELS` constant. `lbl-mode` shows `วัดพื้นที่ ⬡ (ผังบริเวณ — ปกคลุมอาคาร)` when site area tool active; updates on site-tag switch; clears after `finishCurrentArea`. Marker tools with non-default markerType get `(<markerLabel>)` suffix too. setMode + finishCurrentArea both call updateModeLabel. NEW marker `PHASE_HT3_OK` 7 sub-checks. smoke 26/26 + full 29/29 GREEN [Autonomous Dev Loop iter 8] |
| HT-2 — `⬡ NaN ตร.ม.` display guard | PASS — NEW consumer-layer helpers `fmtAreaM2(v, hint)` + `fmtDistM(v, hint)` added next to `polyAreaM2` (`polyAreaM2` itself untouched — forbidden surface). 10 display sites updated to use `Number.isFinite()` guards: measure-result × 2, summary-widget area/floor/site tabs, objMetricText, rp-metric Gross/Opening/Net (both buildLeftProperties + buildRightPanel), drawPolyLabel. New marker `PHASE_HT2_OK` 12 sub-checks incl. live-DOM scan for "NaN ตร.ม." substring. smoke 25/25 + full 28/28 GREEN. Pre-existing formats unchanged (VECTOR_OK / RECAL_OK / XLSX_OK normal). Zero forbidden-surface edits [Autonomous Dev Loop iter 7] |
| HT-1 — `.dd-submenu` z-index 1→201 | PASS — 1-line CSS fix (`.dd-submenu` z-index in `proto/static/css/app.css:25`); submenu now renders above sibling overlays in Measure / Project / Snap-modes / Layer-set-active. NEW marker `PHASE_HT1_OK` 3 sub-checks. smoke 24/24 + full 27/27 GREEN. Zero forbidden-surface edits [Autonomous Dev Loop iter 6] |
| U2 — 1-Page Excel Summary | PASS — NEW `/export-xlsx-summary` server endpoint (~165 lines, xlsxwriter, A4 landscape `fit_to_pages(1,1)`, returns `X-Bma-Summary-Mode: 1-page` header + `_summary.xlsx` filename) + client `collectSummaryData()` aggregating by 7 site `semanticTag`s + ratios (BCR/OSR/FAR/Permeable, plain numbers — Phase 1 boundary: NO pass/fail verdict, user-limits echoed alongside actuals) + front-setback (min dist to any `road` ref via `collectRefDistanceReport`, back/side1/side2 deferred to I-D) + marker-count breakdown (via `markerType` from I-B1). Export panel button `#btn-export-summary` after Export Excel with "facts only, no pass/fail" subtitle. PHASE_U2_OK 16 sub-checks (9 structural + 7 live server: response 200, XLSX Content-Type, custom header, `_summary.xlsx` filename, 7354-byte body, PK magic bytes). smoke 23/23 + full 26/26 GREEN. Zero forbidden-surface / `.bmaplan` schema edits [Autonomous Dev Loop iter 5] |
| U1 — Save Annotated PDF in-place | PASS — FSA-first Open path (Option B from SCOPE checkpoint), `currentSourcePdfHandle` tracked through `uploadPdfFile(file, sourceHandle)`, new `#dd-save-pdf` Project-menu item + Ctrl+Shift+S, `saveSourcePdfInPlace()` writes annotated PDF via `FileSystemFileHandle.createWritable()` or falls back to `<safe>_annotated.pdf` download; new marker `PHASE_U1_OK` (9 sub-checks); smoke 22/22 + full 25/25 GREEN; zero forbidden-surface / `.bmaplan` schema / `server.py` edits [Autonomous Dev Loop iter 4] |
| SB-2026-05-15-001 — Raise MAX_UPLOAD_BYTES | PASS — MAX_UPLOAD_BYTES 80 MB → 256 MB + env-var BMA_MAX_UPLOAD_MB (default 256); /upload echoes max_upload_mb; UPLOAD_CAP_OK marker; smoke 21/21 + full 24/24 GREEN; HUMAN_TEST_PASS (45 pages, XLSX 17813B, .bmaplan 30024B) [Autonomous Dev Loop iter 3] |
| Pack G — Sandbox Test Pre-Release Gate | PASS — DOCS-ONLY, 3 new .claude files + CLAUDE/AGENTS/PHASE_INDEX updates; first run on real customer PDF (90.8 MB) filed SB-2026-05-15-001 (BROKEN upload cap 80 MB > 80 MB cap) + SB-2026-05-15-002 (FRICTION upload-cap UX); no app code touched; test baseline unchanged smoke 20/20 + full 22/22 GREEN |
| Phase I-B2b — Measure menu submenu (site plan) | PASS — smoke 20/20 + full 22/22 GREEN, PHASE_I_B2B_OK (trigger/submenu DOM, areaBtns:7, markerBtns:8, hiddenOnPlan, shownOnSite, area/marker dispatch, menu auto-close via bubble-up); 5 HT findings filed (HT-1/HT-2 BROKEN, HT-3/HT-4 FRICTION, HT-5 COSMETIC) |
| Phase I-B2a — Site Plan ribbon group + shared handlers | PASS — smoke 19/19 + full 22/22 GREEN, PHASE_I_B2A_OK (7 sub-checks: ribbonSiteExists/buttonsWired/hiddenOnPlan/shownOnSite/siteAreaToolSets/sitePolyGetsTag/markerToolSets), ribbon-site 15 buttons (site-page-only), semanticTag bridge consume-and-clear [Autonomous Dev Loop iter 1] |
| Phase I-B1 — markerType additive field | PASS — smoke 18/18 + full 21/21 GREEN, PHASE_I_B1_OK (3 sub-checks: registryComplete/markerTypeRoundTrips/backfillWorks), MARKER_TYPE_LABELS 9 types, parkingType unchanged |
| Phase I-A — Site Plan Schema + Project Setup | PASS — smoke 17/17 + full 20/20 GREEN, PHASE_I_A_OK (7 sub-checks), 7 semanticTags + 11 Project Setup fields |
| Menu Bar / Canvas Overlay Z-Index Fix | PASS — smoke 16/16, MENU_OK+MAIN_UI_OK green, layering scheme canvas≤700<loupe/recent 8998–8999<menu-bar 9000<setup 9001<modals 9002 |
| Phase I Open Questions DECIDED (docs-only) | PASS — Q1=A, Q2=A, Q3=A, Q4=Defer, Q5=A+B; Phase I-A unblocked |
| Site Plan UI Mockup (docs-only) | PASS — `SITE_PLAN_UI_MOCKUP.md` ~530 lines |
| Site Plan Measurement Plan (docs-only) | PASS — `SITE_PLAN_MEASUREMENT_PLAN.md` from กฎกระทรวง 33+55 |
| Phase H.1 Path Geometry Design (docs-only) | PASS — `PATH_GEOMETRY_MODEL.md` |
| Phase H.1 Path Geometry Implementation | PASS — all 19 E2E markers, PATH_GEOMETRY_OK |
| Page-Scoped Layer Model Lock (docs) | PASS — root `82e1e4e` |
| Layer Scope Audit (docs) | PASS — root `a3e1166` |
| Page Layer Instance Model | PASS — proto `ed9944d` |
| Page Type Layer Presets | PASS — proto `eefab31` |
| Object Layer Validation | PASS — proto `94db3d9` |
| Layer Tool Awareness | PASS — proto `a6c67e7` |
| Area Summary by Tag and Floor (docs) | PASS — root `186bee8` |
| 8-Sprint UI/Measurement Usability Batch | PASS — proto `4a09693` |
| Widget Placement Polish | PASS |
| Left Inspection Status Panel | PASS — proto `24b41c5` |
| Static 404 Fix (critical regression) | PASS — proto `a2099ec` |
| UI Layout Options (mockup v3 modes) | PASS — proto `087c769` |
| Panel Scroll + Page-Scoped Layer UI | PASS — proto `fb39f28` |
| Mockup V3 App Shell Theme | PASS — proto `b271577` |
| Save System Audit (docs) | PASS — root `9a46ff4` |
| Save / Save As / Overwrite | PASS — proto `266d365` |
| Open / Recent Project Workflow | PASS — proto `5ad2cc0` |
| Annotated PDF Export Audit (docs) | PASS — root `9a46ff4` |
| Export Current-Page Annotated PDF | PASS — proto `55dccdf` |
| Export All-Pages Annotated PDF | PASS — proto `55dccdf` |

## Active Feature State (2026-05-19 — INV-003b/HT-18c/INV-003a End-of-day bundle)

- **`/export-png` ZIP endpoint (INV-003b, additive)**: NEW `POST /export-png` in `proto/server.py`. Accepts `case_id` + `selected_pages[]` + `dpi_scale` (float, default 2.0 = 144 DPI at RS=1.5 render). Renders each selected page via `PyMuPDF fitz.Pixmap` at the requested scale factor. Bundles PNGs into an in-memory ZIP using Python `zipfile.ZipFile`. Returns the ZIP as `StreamingResponse` with `Content-Type: application/zip` and `Content-Disposition: attachment; filename="pages.zip"`. No new database state; uses existing `CASES[case_id]["doc"]` reference. Case isolation preserved. No impact on existing endpoints. Export menu in `proto/ui.html` wired with "Export PNG (ZIP)" item. This is Path C of the print-canvas-per-page invent — complements Path B (INV-003a, browser-side `canvas.toDataURL + window.print`). Together B+C give the user two export paths: fast single-page print for review, and archival high-DPI PNG bundle for submission/email.
- **Print canvas per page (INV-003a, client-side)**: "Print Current Page" and "Print Selected Pages" in the File menu. `printCurrentPage()` captures the current canvas via `canvas.toDataURL("image/png")`, writes into a synthetic `<html><body><img>` print window, and calls `window.print()`. `printSelectedPages()` iterates selected pages, loads each in sequence, captures, and adds to the print window. Path B provides instant browser-print dialog with no server round-trip. Works offline. Uses existing canvas state; no new DOM elements persist after print.
- **Save/load round-trip fully verified (HT-18c)**: `PHASE_HT18B_OK` 13/13 GREEN after fixing `_test_ht18b_save_load_round_trip`. Root causes: (a) deep `eq()` comparison was too strict — `normalizeAllObjects` mutated the pre-snapshot object before comparison, making legitimately equal fields appear different; replaced with field-by-field checks on 13 specific fields. (b) `applyLoadedProject` had a bug where `_projInfoSnap` was not fully restored from blob; fixed. The test now reads `_projInfoSnap` from post-load global state, proving the restoration is symmetric. The **HT-18 series is complete**: HT-18a (6 sites) + HT-18a-ext (22 sites) + HT-18b (test design) + HT-18c (eq fix) = full save/load integrity audit done.
- **New E2E markers added this session**: `PHASE_INV_EXPORT_PNG_OK` (export-png endpoint), `PHASE_INV_PRINT_CANVAS_OK` (8 sub-checks: print fns exist, menu items present, canvas.toDataURL called, print window created, filtered pages, no regression on measure).
- **Branch state**: local `main` tracks `origin/main-v2-2026-05-19` (push.default=upstream). Legacy remote `main` preserved at `24f5d94` (62 commits). 33 commits in this session. Pending: Zen Mode user manual docs sprint (4 files uncommitted).

## Active Feature State (2026-05-19 — HT-18a-ext Extended pushUndo() coverage)

- **`isDirty` / `pushUndo()` contract (complete after HT-18a + HT-18a-ext)**: All confirmed mutation functions in `proto/ui.html` now call `pushUndo()` as their first action. Total coverage: 6 sites from HT-18a + 22 sites from HT-18a-ext = 28 mutation sites guarded. The `_test_ht18_pushundo_leaks` E2E test (36 sub-checks) acts as a permanent regression guard: any future mutation function that forgets `pushUndo()` will fail a source-presence check in the test suite. `_skipUndo` parameter added to `excludePage` + `restorePage2` for safe batch-caller use (e.g. `hideSelectedPages` calls `excludePage(_skipUndo=true)` and issues its own single `pushUndo()`).
- **PHASE_HT18_OK** counter upgraded: 7/7 (after HT-18a) → 36/36 (after HT-18a-ext). The 36 sub-checks cover: 22 source-presence assertions + 7 runtime isDirty-flip assertions + 7 original HT-18a checks.
- **HT-18b / HT-18c status**: HT-18b `done-with-test-design-caveat` — `PHASE_HT18B_OK` 7/13 because `eq()` comparison is too strict after `normalizeAllObjects` mutates the pre-snapshot object; NOT a schema drift issue (Phase A audit confirmed `_makeProjBlob` round-trip is symmetric). HT-18c queued with scope: fix the `eq()` comparison to use a field-by-field check instead of deep equality, ~30-50 LOC, test-only.

## Active Feature State (2026-05-19 — HT-18a Save-state pushUndo leak fixes)

- **`isDirty` / `pushUndo()` contract (corrected)**: The save-state flow relies on `pushUndo()` being called at every mutation site. `pushUndo()` sets `isDirty = true`, enabling the "Unsaved changes" prompt and preventing silent data loss when the user closes without Ctrl+S. Prior to HT-18a, 6 mutation functions bypassed `pushUndo()`: `toggleScaleLine` (hides/shows scale reference line), `showLayer` / `hideLayer` / `lockLayer` / `unlockLayer` / `soloLayer` (layer visibility/lock state), and `applyLandEdgeTag` (sets landEdgeRole on polygon edges). After HT-18a, all 6 call `pushUndo()` as their first action, consistent with all other data-mutating functions. The undo stack is also updated correctly (these operations are now undoable).
- **Save-schema completeness (audit finding)**: `_makeProjBlob` serializes `pageStore` via `JSON.stringify` — all object fields auto-included; no explicit field list needed. `applyLoadedProject` assigns `pageStore` by reference from the parsed blob. Schema round-trip for all existing fields (including `landEdgeRole`, layer state) is complete. The "save ไม่ตรงกับ canvas" report was not a schema drift issue — it was solely the missing `isDirty` triggers.
- **HT-18b / HT-18c queue**: HT-18b (queued) will add a round-trip E2E test that exercises each mutation site, calls save/load, and verifies field persistence. HT-18c is conditional on HT-18b finding any schema gap (audit strongly suggests it will not). These remain in the HT queue for fresh-session investigation.

## Active Feature State (2026-05-19 — INV-2026-05-19-002b F12 Overview standalone)

- **F12 Overview mode (`toggleOverview()`)**: F12 keydown toggles `body.overview` class. When active, CSS rules under `body.overview` hide `#pdf-canvas`, `#overlay-canvas`, `#ribbon`, `#left-panel`, `#right-panel`, `#bottom-bar`, `#summary-widget`, and all `.hud` elements. `#overview-content` grid becomes visible (`display:grid`). The `#zen-topbar` from INV-002a remains visible as navigation chrome (z-index preserved). `Esc` keydown priority: if `body.overview` active → `closeOverview()`; else if `body.zen` active → `toggleZenMode()`; else → default Esc behavior.
- **`#overview-content` grid**: CSS grid at `top:40px` (below zen-topbar), `width:100vw`, auto-fill columns. Content built by `_ovBuildGrid()`. Pages grouped by `pageTags[n]` value using `_OV_GROUPS` config (6 groups: site / plan / elev / section / detail / none). Each group renders as `.ov-group` containing a group label (colored by discipline) and `.ov-card` elements for pages in that group. Pages not matching any group fall into "none".
- **`.ov-card` anatomy**: Each card shows: page number chip, page name, discipline tag chip (color-coded), object count badge from `_ovCountObjects(n)` (counts `pageStore[n]?.length || 0`). Thumbnail is lazy-loaded: `_ovBuildGrid()` sets `data-page="n"` on each card's `.ov-thumb` element and registers an `IntersectionObserver`; when the card enters the viewport, the observer sets `src = thumbUrl(n, case_id)` and disconnects. This reuses the same `/thumb/{n}?case_id=...` endpoint and IO pattern as INV-001a's sheet minimap — malloc-safe, no concurrent render overload.
- **Card click (`_ovCardClick(n)`)**: Calls `closeOverview()` first (removes `body.overview`, shows canvas/chrome again), then calls `loadPage(n)`. Atomic — no intermediate visible state. `closeOverview()` also removes `body.overview` from the Esc chain.
- **`#ztb-chip-overview` chip**: In INV-002a this was a stub with a placeholder `onclick`. Now wired to `toggleOverview()`. Clicking the chip from inside Zen Mode opens/closes the Overview grid while keeping `#zen-topbar` visible.
- **Group label colors**: CSS classes `.ov-group-site` (green `#4caf50`), `.ov-group-plan` (blue `#2196f3`), `.ov-group-elev` (amber `#ff9800`), `.ov-group-section` (purple `#9c27b0`), `.ov-group-detail` (cyan `#00bcd4`), `.ov-group-none` (gray `#757575`). Colors chosen to match the existing Command Palette tag-chip colors for visual consistency.

## Active Feature State (2026-05-19 — INV-2026-05-19-002a F11 Zen top bar)

- **`#zen-topbar` overlay**: 40px fixed bar appears at top of viewport when `body.zen` is active. Does not replace or modify 001a's `toggleZen()` — pure additive. Contains 6 dropdowns (File / Page / Measure / Annotate / View / Help) each wired directly to existing handler functions via `onclick`. Contains 4 icon chips: search opens Command Palette (`togglePalette()`), palette chip opens Zen palette, circle/ellipse and rectangle chips open their respective shape dialogs.
- **Focus sub-mode (`toggleZenFocus()`)**: F key inside Zen Mode (with scope guard: blocked inside `<input>`/`<textarea>`/`<select>`) toggles `body.zen.focus` class. When focused, all `.hud` elements are hidden with `display:none !important` and CSS transitions are suppressed so Playwright can detect the state change reliably. `body.zen.focus.peek` (set by `_setupZenEdgePeek()`) restores HUDs temporarily while hovering within 4px of the viewport top edge.
- **Edge peek (`_setupZenEdgePeek()`)**: Called once on page load. Attaches `mousemove` listener; when `body.zen.focus` is active and `clientY < 4`, adds `peek` class to body; removes on `clientY >= 4`. Allows keyboard-focused users to access HUD state by brushing the top edge without exiting Focus mode.
- **v2 onboarding toast**: On first-ever toggle of Zen Mode (after 002a ships), a green-tinted toast `#zen-v2-toast` with text "Zen v2 — F = Focus · Edge peek restores HUDs" appears for 3 seconds. Stored in `PREFS.layout.zenV2Onboarded` (session pref, not project schema). Toast is NOT shown on subsequent Zen toggles.
- **HUD vertical shift**: 001a HUDs (TL / TR / BL) shifted from `top: 34px` to `top: 50px` to clear the new 40px topbar. No other 001a HUD behavior changed.

## Active Feature State (2026-05-19 — INV-2026-05-19-001c Zen+Palette FRICTION polish)

- **Zen HUD page name (HT-Z-1 fix)**: `_zenSyncHud()` now reads `pageNames[curPage]` directly instead of polling `#bb-page-name` element text. Eliminates the 1–2 frame lag during fast minimap navigation where MutationObserver would fire before the DOM element was updated. HUD displays format "ชื่อหน้า (N/total)" where N = curPage+1 and total = totalPages.
- **Zen HUD scale chip amber (HT-Z-2 fix)**: `_zenSyncHud()` checks `getScaleForPage(curPage).state`. If `'auto-unverified'` or no scale (state `'unknown'`), the TL HUD Scale chip gains class `hud-scale-warn` and `color: var(--orange)` (amber). Manual scale = default white. A `title` attribute on the chip element explains the state in Thai ("scale ยังไม่ยืนยัน — ผลลัพธ์อาจไม่ถูกต้อง" vs "scale ตั้งค่าแล้ว"). Prevents silent measurement errors on auto-unverified pages in Zen Mode.
- **Palette Thai-tag empty-state hint (HT-Z-3 fix)**: `filterPalette(q)` empty-results branch now runs a secondary check: if `q` matches a Thai page-type word pattern (`/ผังบริเวณ|ชั้น|รูปด้าน|รูปตัด|รายละเอียด|ตาราง/`) AND no pages have any tag set (`pageTags` all null/undefined), the results list appends one non-navigable hint row: "💡 แท็กภาษาไทยใช้ได้หลังตั้งค่าหน้าใน Page Setup". Hint is absent when at least one page is tagged (real results) or when the query is not a Thai tag word (no false hint for number or name queries). `PHASE_INV_POLISH_001C_OK` sub-checks `thaiTagHintShown` and `hintAbsentWhenTaggedOrNoThai` verify both branches.

## Active Feature State (2026-05-19 — INV-2026-05-19-001b ⌘K Command Palette)

- **⌘K Command Palette (INV-2026-05-19-001b)**: `togglePalette()` shows/hides `#cmd-palette` fixed-center modal (CSS `position:fixed`, `z-index:9500` — layers above Zen Mode HUDs at 1500 and zen overlays). On open, `filterPalette('')` pre-populates results with all pages (default filter = show all). `filterPalette(q)` filters the `pageNames` + `pageTags` array: matches if `q` is numeric (page number substring), or if the normalized page name includes `q`, or if the Thai tag label includes `q`. Results rendered as `<li class="pal-row">` items with a color-coded tag chip (CSS classes `chip-site`/`chip-plan`/`chip-elev`/`chip-section`/`chip-detail`). `.sel` class marks the keyboard-selected row. `_palMoveSel(delta)` moves selection up/down with wraparound. `_palJumpToIdx(n)` calls `loadPage(n)` + `closePalette()`. Keyboard wiring: `Ctrl+K`/`Cmd+K` keydown → `togglePalette()` (guard: `mPts.length===0` — no-op during active polygon draw). Inside `#cmd-palette` input: `keydown` branch for `ArrowDown`/`ArrowUp` → `_palMoveSel`, `Enter` → `_palJumpToIdx`, `Esc` → `_palEsc` — this branch is placed **before** the generic `inInput` guard so palette navigation is not suppressed. `_palEsc()` = `closePalette()`. `closePalette()` hides modal + blurs input. View menu item "🔍 ค้นหาหน้า (Command Palette) Ctrl+K" calls `togglePalette()`. Hint bar shows "↑↓ เลื่อน · Enter เปิด · Esc ปิด". HT-7 scale gate is NOT bypassed — jumping to a page that lacks scale still blocks measurement tools as usual. `PHASE_INV_PALETTE_OK` 10/10.

## Active Feature State (2026-05-19 — INV-2026-05-19-001a Zen Mode + Sheet Minimap)

- **Zen Mode (INV-2026-05-19-001a)**: `toggleZenMode()` adds/removes `body.zen`. CSS rules under `body.zen` hide `#menu-bar` ribbon section, `#left-panel`, `#right-panel`, `#bottom-bar`, `#summary-widget` (NOT the top-bar menu — stays accessible). Canvas `#pdf-canvas` + `#overlay-canvas` fill ~94% viewport height. Three corner HUDs: `#zen-hud-tl` (`.zen-hud-tl` — scale mode chip + active tool label), `#zen-hud-tr` (`.zen-hud-tr` — object count + active layer label), `#zen-hud-bl` (`.zen-hud-bl` — save state). HUD sync via `_zenSyncHud()` called by a MutationObserver watching `#lbl-scale`, `#lbl-mode`, `#lbl-obj`, `#lbl-layer`, `#lbl-save-state` `textContent`. Sheet minimap `#zen-minimap` / `#zen-mm-grid`: `_zenBuildMinimapIfNeeded()` runs on first Zen activation, builds `N` thumb cells (one per page), sets up IntersectionObserver — each cell fetches `/thumb/{n}?case_id=...` only when it intersects the viewport (prevents concurrent render overload). `_zenUpdateMinimapActive()` highlights the current page cell. `_zenToggleMinimap()` toggles minimap visibility. Onboarding toast `#zen-onboarding-toast` auto-dismisses after 3s on first activation (`PREFS.layout.zenOnboarded` flag). Exit chip `.zen-exit-chip` provides visual affordance. F11 keydown → `toggleZenMode()`; Esc keydown → `toggleZenMode()` only if `PREFS.layout.zenMode` is true (after other Esc consumers). `PREFS.layout.zenMode` (default false) + `PREFS.layout.zenOnboarded` (default false) in `bmaPlan.settings.v1`. `PHASE_INV_ZEN_OK` 10/10.

## Active Feature State (2026-05-19 — Page Setup Redesign trilogy + Settings v2)

- **Page Setup left inspector (INV-001a, `e85a5ce`)**: `#proj-form-panel` content replaced with `#setup-inspector-content` hosting two sub-views managed by `_renderSetupInspector()`. Dashboard view: grid of page cards with traffic-light readiness dot (green = tag set AND `scaleMode==='manual'`; amber = `auto-unverified`; red = untagged or `unknown`) + object-count chip from `_setupCountObjects(pageIdx)`. Clicking a card calls `selectSetupPage(n)` → page-card view via `_renderSetupPageCard(n)`. Page-card view: tag dropdown, scale-state read-only, floor section (tag=plan only), name input, delete button. `_setupBack()` returns to dashboard. `setPageName`/`buildTagGrid`/`selectSetupPage` all call `_renderSetupInspector()` for live refresh. Project-info form preserved in `<details id="setup-pi-accordion">` (open-by-default for E2E compat). `PHASE_INV_PAGE_SETUP_A_OK` 8/8.

- **Floor sub-types for plan pages (INV-001b, `798e5c3`)**: Two new additive per-page dicts: `pageFloorKind: {[idx]: 'basement'|'normal'|'mechanical'|'rooftop'|'custom'}` and `pageFloorNum: {[idx]: number}`. Persisted via `_makeProjBlob()` / `applyLoadedProject()` (version stays 1). `autoNamePage(pg)` for tag=plan: basement N → "ชั้นใต้ดิน N", normal N → "ชั้น N", mechanical → "ชั้นห้องเครื่อง", rooftop → "ชั้นดาดฟ้า", custom → preserves user value. `setPageTag` clears floor fields on tag change away from plan. `PHASE_INV_PAGE_SETUP_B_OK` 9/9 (7 contract + 2 bonus: save/load round-trip, tag-change clear).

- **Permanent page delete + renumber-map (INV-001c, `ebb521c`)**: NEW `/rebuild-pdf` POST in `proto/server.py`. Accepts `{case_id, pagesToDelete: [int]}`. Deletes in reverse-index order via `doc.delete_page(n)`. Edits `CASES[case_id]["doc"]` in place; flushes `page_cache`/`image_cache` for case. Returns `{totalPages, renumberMap: {"0":0, "1":null, ...}, deletedNumbers: [1]}` (null = deleted). Client `_openRenumberDialog(n)` renders preview table (deleted rows crossed out in red). `_executeRenumberDelete()` POSTs → calls `_reindexPageDicts(map)` across 7 per-page dicts (pageStore, pageTags, pageNames, pageRotations, excludedPages, pageFloorKind, pageFloorNum). Hard-blocks: active draw (`mPts.length > 0`) + last-page guard. `pushUndo()` before commit. Foxit-style warning "ลบไม่ย้อนกลับได้หลังบันทึก". `PHASE_INV_PAGE_SETUP_C_OK` 7/7. Design: `docs/invent/page-setup-redesign.md` (Q1-Q4 locked, PRIOR_ART_PARTIAL). Research commit `afd4e71`.

- **Settings v2 — export defaults + loupe prefs (INV-002, `3e71865`)**: 4 new PREFS additive in `bmaPlan.settings.v1` (v1 SETTINGS_OK 13/13 still GREEN). `export.csvSeparator` (`,`/`;`/`\t`, default `,`) — `exportCSV` builds dynamic escape regex + replaces all join calls. `export.includeLawBasis` (bool, default true) — conditionally appends lawBasis column. `loupe.radius` (50–160 px, default 80) — `_applyLoupePrefs()` sets `loupeR`; `updateLoupe` uses it for canvas half-size. `loupe.zoomFactor` (2–8×, default 4) — `_applyLoupePrefs()` sets `loupeZoomFactor`; `updateLoupe` computes `sw = (loupeR*2)/loupeZoomFactor` for constant magnification. `_applyLoupePrefs()` called on boot + after Apply. UI slots into existing 4-tab modal (no new tab). `SETTINGS_V2_OK` 6/6.

## Active Feature State (2026-05-17 — INV-freeform-area)

- **Freeform area measurement (Alt sub-mode)**: In `mode==='area'`, holding `Alt` at `mousedown` sets `mFreehandActive=true`. Subsequent `mousemove` events sample raw PDF coordinates into `mFreehandRaw` when distance from `mFreehandLastSampled` >= `freehandSampleStepPx` (6 px gate). An explicit `early-return` fires BEFORE the snap branch, so `buildSnapIndex`/`snap` are bypassed entirely during drag. On `mouseup`, `rdpSimplify(mFreehandRaw, freehandTolerance)` decimates the burst; decimated points are converted to PDF coords via `cToPdf` and pushed into `mPts` (first point skipped to avoid duplicate vertex). Releasing `Alt` before mousedown keeps the polygon in normal click-vertex mode — mixed click+drag in a single polygon is fully supported. During burst, `redraw()` draws a red dashed trail from `mFreehandRaw[0]` through all raw samples. `Shift` during freehand decreases `freehandTolerance` (finer decimation, more detail); `Ctrl` increases it (coarser, fewer points). `finishCurrentArea` attaches `obj.freeform = {tolerance, freehandSegments, originalSamples}` as an additive optional field when any freehand segment was used. Legacy `.bmaplan v1` files without `obj.freeform` load unchanged. `polyAreaM2` computes area on the decimated `mPts` array — zero change to that function. `PHASE_FREEFORM_OK` 7 sub-checks. full 44/44 GREEN. Commit `023b988`.

## Active Feature State (2026-05-17 — HT-6 arc-guideline)

- **HT-6 live arc guideline preview**: Inside `redraw()` draft block, when `mArcDraft.pending` is true and the user has set a through-point (`mArcDraft.throughPt` set), moving the mouse now shows a dashed live arc from the last committed vertex curving through the through-point to the cursor. Implementation: `computeArcEdge(lastPdfVertex, mousePdf, throughPt, centroid)` called with the current mouse position in PDF coords; result rendered via `ctx.arc(cx, cy, r, startAngle, endAngle, ccw)` with `setLineDash([5/zoom, 5/zoom])`, color `#ff453a`, alpha 0.85. Matches the committed-arc render path in `_renderPolyEdges`. Pattern mirrors the existing straight-line `guidePoint` render (L1478: `ctx.moveTo → ctx.lineTo`). Snap bypass on through-point click unchanged. `PHASE_HT6_OK` 4 sub-checks. full 42/42 GREEN. Commit `ecb44d4`.

## Active Feature State (2026-05-17 — LOOP_DONE: dev-website + CIRCLE_RENDER)

- **dev-website static docs site**: `proto/static/docs/index.html` — single static HTML, inline micro-markdown renderer (~80 lines), fetches sibling `proto/static/docs/content.json` as JSON (sibling fetch, not inline embed). Exposes `window.__bmaDocs = {nav: [{group, slug, title}], pages: {slug: {group, title, content}}}` for automated testing. Offline file:// fallback: if fetch fails (file:// protocol or server down), uses hard-coded stub article. Accessible at `/static/docs/` when BMA-Plan server is running (uses the already-registered `StaticFiles` mount — no server changes). `scripts/build_docs.py` (stdlib-only, ~120 LOC) rebuilds `content.json` on demand — walks: `proto/manual/*.md` (5 Thai manual files), `log.md` (split on `## YYYY-MM-DD` headings, 12 most recent sessions), `sprints/completed/**/RUN_*.md` (12 most recent by mtime), `docs/process/ANTI_PATTERNS.md` + `docs/process/TROUBLESHOOTING.md`. Emits 28 pages in 4 groups (~50 KB). `DOCS_SITE_OK` 7 sub-checks. Help menu dropdown link deferred to follow-up `/bma-ui-menu` sprint. `proto/ui.html` UNTOUCHED.
- **CIRCLE_RENDER analytic render**: `_renderPolyEdges(ctx, poly, cp)` (already shipped by INV-001 arc-polygon) now has two short-circuit branches at top that fire BEFORE the existing line/arc-edge loop: (1) `poly.shape==='circle' && poly.center && poly.radius` → `ctx.beginPath(); ctx.arc(cx, cy, r, 0, 2*Math.PI); ctx.closePath()` — zero lineTo calls; (2) `poly.shape==='ellipse' && poly.center && poly.semiAxisA && poly.semiAxisB` → `ctx.beginPath(); ctx.ellipse(cx, cy, a, b, poly.rotation||0, 0, 2*Math.PI); ctx.closePath()` — zero lineTo calls; else → existing flow (legacy polygon + arc-polygon hybrid from INV-001). `poly.pts` 32-gon storage untouched; hit-test / snap / `buildSnapIndex` / `polySelfIntersects` / `polyAreaM2` / `objectAreaM2` all still use `poly.pts`. `objectAreaM2` already routes circle/ellipse to `circleAreaM2`/`ellipseAreaM2` closed-form helpers (shipped earlier). `CIRCLE_RENDER_OK` 7 sub-checks. Resolves "faceted" visual symptom noted in `docs/status/PHASE_H_PATH_GEOMETRY_VISUAL_AUDIT.md`. Last pre-loop leftover cleared.

## Active Feature State (2026-05-17 — Autonomous Loop 8-sprint batch)

- **INV-002 Unified Settings modal**: `bmaPlan.settings.v1` localStorage key (version:1, never embedded in `.bmaplan`). `getPref(dotPath, fallback)` / `setPref(dotPath, value)` path-based accessor pair. `migrateFromLegacy()` one-way migration: reads `bmaPlan.uiLayoutOptions.v1` via flexible `preset||mode` field lookup; reads `bmaPlan.widgetPlacement.v1` extracting `visible` from `{visible:bool}` or plain bool; legacy keys preserved after migration. `#settings-modal` DOM: 4 tabs (วาด=snap settings, หน่วย=unit/decimals, หน้าจอ=layout preset, Widgets=visibility). Draft/Apply/Cancel + "คืนค่าเริ่มต้น" (confirm before reset). `Ctrl+,` shortcut. Bad-JSON safety: `JSON.parse` in try/catch → resets to defaults on corrupt data. Wrong-version safety: `parsed.version !== 1` → reset to defaults + warn. `SETTINGS_OK` 13 sub-checks (spike's 7 → production 13: added badJsonSafe, wrongVersionSafe, legacyPreserved). 3 checkpoint carry-overs resolved: (1) legacy-key shape = flexible lookup; (2) widget coupling = visible-only migration; (3) Apply-on-save adopted, immediate-apply deferred.
- **I-E building-to-building distance + wallEdges**: `WALL_EDGE_TYPES` catalog (4: exterior_wall, party_wall, fire_wall, glass_wall + Thai labels). `wallEdgeType` additive field on polygon edge objects (null default, carried via `pageStore`, backward-compat via null check). `computeBuildingPairsForPage(pageObjs, pg)` — iterates all unique `building_coverage` polygon pairs; applies vertex-to-edge distance (same formula as I-D setback); returns `[{nameA, nameB, distM}]` sorted ascending. `computeAllBuildingPairs` aggregates across pages. Siteplan tab: "ระยะระหว่างอาคาร (2h pre-check)" section — each row `อาคาร A — อาคาร B: X.XX ม.` (facts only, no "PASS/FAIL"). `PHASE_I_E_OK` 9 sub-checks.
- **I-D 4-direction setback + compass**: `landEdgeRole` additive field on polygon edges (`front`/`back`/`side1`/`side2`/null). `computeEdgeSetback(poly, role, refPolys, pg)` filters edges by role; returns min vertex-to-edge distance in metres. `collectSetbackReport` returns `{front, back, side1, side2}`. `northAngle` additive in `pageTags[pageIdx]` (default 0, editable in Page Setup). `#canvas-compass` SVG (48×48, top-right workspace, `rotate(northAngle)` transform). Siteplan tab shows 4 setback rows. `PHASE_I_D_OK` 10 sub-checks.
- **I-C "ผังบริเวณ" Summary Widget tab**: 5th tab `#tab-siteplan` / `#summary-siteplan`. `updateSiteplanTab()` renders `collectSummaryData()` client-side: BCR/OSR/FAR/Permeable% ratios, per-tag area table (tag label / area m² / % site), marker-count table (by markerType), front-setback row (v1), Phase 1 footer note (hard-coded, non-removable). No new server endpoint. `PHASE_I_C_OK` 10 sub-checks.
- **I-B4 site stepper widget**: `#site-stepper` collapsible 6-step advisory widget, site-page-only. Step done-state derived live from existing data: step1=scale≠unknown, step2=pageTags=site, step3=projectInfo.buildingClassification set, step4=≥1 building_coverage poly, step5=≥1 open_space/permeable poly, step6=≥1 marker. `updateSiteStepperUI()` hooks in `updateBottomBar()` + `loadPage()`. No `.bmaplan` state added. `PHASE_I_B4_OK` 10 sub-checks.
- **I-B3 Properties panel site fields**: `isBuildingTag(tag)` helper (`tag==='building_coverage'`). Properties panel: `buildingHeight_m` input row (shown when `isBuildingTag(obj.semanticTag)`), `Number(val)||null` stored. Semantic Tag dropdown appends 7 site area tags when `pageTags[curPage]==='site'` (draw-then-classify workflow). Height onchange → `pushUndo()` + `redraw()`. `PHASE_I_B3_OK` 10 sub-checks.
- **SB-002 upload-cap UX**: `currentUploadCapMB` state updated from `/upload` response `max_upload_mb`. Pre-flight check in `uploadPdfFile()` before sending bytes. `showUploadCapModal(fileSizeMB, capMB)` shows modal with file-size, cap, 3 suggestions. `updateCapBadge()` cold-start Setup hint. `SB002_UPLOAD_UX_OK` 8 sub-checks.

## Active Feature State (2026-05-17 — INV-001 Arc-polygon)

- **Arc-polygon hybrid measurement (INV-2026-05-15-001)**: Three-click inline arc during polygon draw in `proto/ui.html`. Flow: draw vertices normally → press `A` when `mode==="area"` + `mPts.length>=1` + `!mArcDraft.pending` → arc-pending enters (red circle indicator on last vertex) → click once for through-point (raw `cToPdf(cx,cy)`, snap bypassed) → next vertex click flushes arc and records `edges[i] = {edgeType:"arc", arcSweep, arcThrough}`. State: `mArcDraft = {pending:false, throughPt:null, edges:[]}` at L459; mirrored to `mPts` indexing; reset on commit/cancel/mode-change/`clearMeasures`. New helpers (additive, right after `objectAreaM2`): `_arcCircumcenter(A,P,B)` circumcircle through 3 pts; `_arcPolygonCentroid(pts)` simple centroid; `computeArcEdge(A,B,P,centroid)` derives `{sweep,center,radius}` — sign of sweep set by which side of chord the through-point lies relative to centroid; degenerate sag<0.5px → sweep=0; `polyMetricsAnyShape(poly,pg)` shim routes arc-edged polys to `polygonAreaWithArcsM2` + `polySelfIntersects` check, routes legacy polys directly to `polyMetrics` (forbidden surface stays untouched); `_renderPolyEdges(ctx,poly,cp)` draws per-edge line vs arc in canvas — used by both committed polys in `redraw()` and draft polys during draw. Schema additive: `obj.edges[i] = {edgeType:"arc", arcSweep, arcThrough}` carried via existing `pageStore` round-trip; `Array.isArray(obj.edges)` guard in 3 helpers ensures legacy `.bmaplan v1` files load unchanged. 9 `polyMetrics(...)` display call sites swapped to `polyMetricsAnyShape(...)` (L808, L1286, L1292, L1320, L1340, L1517, L1606, L1665, L1903); opening calls at L1614/L1738 deliberately untouched (openings don't get arc edges in v1). NEW E2E marker `ARC_POLYGON_OK` 7 sub-checks: fnsExist (7 functions), closedFormPasses (err=0.000000% on square+semicircle canonical), dispatchOK, degenerateOK (sag<0.5px → sweep=0 no NaN), roundTripOK, legacyUnchanged, polyMetricsAnyShapeOK. smoke 29/29 + full 32/32 GREEN. **Mid-sprint bug fixed:** initial `replace_all polyMetrics → polyMetricsAnyShape` accidentally caught the call inside `polyMetricsAnyShape`'s own legacy branch → infinite recursion → `updatePageSummary` crashed → `VECTOR_OK` failed with "ยังไม่มีรายการพื้นที่"; fixed by one targeted Edit restoring the body to call `polyMetrics` directly. Trap note: future `replace_all` on a forbidden-surface name inside a shim body will hit the same problem.

## Active Feature State (2026-05-15 — SB-2026-05-15-001 Upload Cap Raised)

- **Upload cap (SB-2026-05-15-001)**: `MAX_UPLOAD_BYTES` raised from 80 MB to 256 MB in `proto/server.py:51`. Env-var override `BMA_MAX_UPLOAD_MB` (int MB, default 256) read at module load time before `app = FastAPI()` — allows deploy-time tuning without code change. `/upload` response body now includes field `max_upload_mb` (echoes actual server MB). New E2E marker `UPLOAD_CAP_OK` in `proto/e2e_ui_test.py` (`_test_upload_cap()`) — asserts cap ≥ 128 MB and API echo matches. Wired after `CACHE_OK` in `main()`. smoke 21/21 + full 24/24 GREEN. HUMAN_TEST_PASS (open → measure 45 pages → export XLSX 17813B / 12 sheets → save .bmaplan 30024B → reopen full state restoration). Trigger: Pack G first-run finding SB-2026-05-15-001 BROKEN (real customer PDF 90.8 MB → HTTP 413 on /upload). Autonomous Dev Loop iteration 3.

## Active Feature State (2026-05-15 — Pack G Sandbox Test Pre-Release Gate)

- **Sandbox Test Pre-Release Gate (Pack G)**: skill `/bma-sandbox-test` + subagent `bma-sandbox-journey-tester` (sonnet) + subagent `bma-issue-triager` (sonnet) deployed under `.claude/`. Gate workflow: enumerate `sandbox/*.pdf` → per-file Tier1 (open+render every page) + Tier2 (set-scale+draw+export+save+reopen, only if T1 PASS) → deduplicate findings → cluster by root cause → check existing `.claude/` coverage → propose NEW specialist specs only when needed → file `SB-YYYY-MM-DD-NNN` blocks into `PHASE_INDEX.md` Discovered backlog. Verdict tokens: SANDBOX_TEST_PASS / SANDBOX_TEST_ISSUES / SANDBOX_TEST_CRASH. Propose-first invariant: new `.claude/` files created only in a separate sprint with normal sprint discipline. **First run (2026-05-15):** `sandbox/251121_CHH_Submission_REV2 - Copy.pdf` (90.8 MB) → Tier1 FAIL (HTTP 413, `MAX_UPLOAD_BYTES=80 MB` in `proto/server.py:51`) → Tier2 skipped → verdict SANDBOX_TEST_ISSUES → SB-001 BROKEN (raise cap) + SB-002 FRICTION (UX message) filed. **Constraint:** `bma-sandbox-journey-tester` and `bma-issue-triager` need session restart to be invokable via `subagent_type` enum; fallback = general-purpose agent with inlined prompts (proven this session). Gate is operational.

## Active Feature State (2026-05-15 — Phase I-B2b)

- **Measure menu submenu — Site Plan (Phase I-B2b)**: "Site Plan (ผังบริเวณ) ▶" submenu trigger + submenu ใต้ Measure menu ใน `proto/ui.html` (DOM L74–L94). แสดงเฉพาะเมื่อ `pageTags[curPage]==="site"` (toggle ผ่าน `updateSiteRibbon()` ที่ extend จาก I-B2a). 7 area items dispatch `activateSiteAreaTool(semanticTag)`: building_coverage, open_space, permeable_area, hardscape, softscape, parking_area_outdoor, internal_road. 8 marker items dispatch `setMarkerType(markerType)`: parking_disabled, parking_fire, parking_ambulance, entrance, aed, sign, fire_escape, fire_elevator. Menu closes via bubble-up to `toggleMenu` (same pattern as all other dd-items). `PHASE_I_B2B_OK` E2E marker PASS. **Bug fixed mid-sprint**: explicit `closeAllMenus()` collision → removed, bubble-up handles closing. **5 HT findings filed**: HT-1 BROKEN (`.dd-submenu` z-index:1 < `.dropdown` z-index:200); HT-2 BROKEN (`⬡ NaN ตร.ม.` when scale not calibrated); HT-3 FRICTION (`lbl-mode` doesn't reflect site-tag context); HT-4 FRICTION (name panel blocks canvas); HT-5 COSMETIC (`.dd-submenu` overflows on 412px viewport).

## Active Feature State (2026-05-14 — Phase I-B2a)

- **Site Plan ribbon group (Phase I-B2a)**: `#ribbon-site` group ใน `proto/ui.html` — แสดงเฉพาะเมื่อ `pageTags[curPage]==="site"`, ซ่อนบน plan/elev/section page. 7 area buttons (`data-site-tag`): building_coverage, open_space, permeable_area, hardscape, softscape, parking_area_outdoor, internal_road. 8 marker buttons (`data-marker-type`): parking_disabled, parking_fire, parking_ambulance, entrance, aed, sign, fire_escape, fire_elevator. State: `curSiteSemanticTag` (consume-and-clear — one click = one tagged polygon), `curMarkerType` (sticky). Functions: `activateSiteAreaTool(semanticTag)`, `setMarkerType(markerType)`, `updateSiteRibbon()` (hooks ใน `updateBottomBar`), `updateSiteRibbonActive()` (hooks ใน `setMode`). **areaType↔semanticTag bridge**: `finishCurrentArea` reads `curSiteSemanticTag||defaultSemanticTag(...)` then sets `curSiteSemanticTag=null`; plain area tool also clears `curSiteSemanticTag`. `PHASE_I_B2A_OK` E2E marker (7 sub-checks) PASS. Sprint รันโดย Autonomous Dev Loop iteration 1 (supervised).
- **Handlers shared for I-B2b**: `activateSiteAreaTool` และ `setMarkerType` พร้อมให้ Measure menu submenu (I-B2b) เรียกใช้โดยตรง — ไม่ต้องเพิ่ม logic ใหม่.

## Active Feature State (2026-05-14 — Phase I-B1)

- **markerType additive field (Phase I-B1)**: `MARKER_TYPE_LABELS` registry 9 ประเภทใน `proto/ui.html` — `parking`, `parking_disabled`, `parking_fire`, `parking_ambulance`, `entrance`, `aed`, `sign`, `fire_escape`, `fire_elevator` พร้อม label ไทย. Field `markerType:"parking"` เพิ่มใน marker creation literal (วางข้าง `parkingType` เดิม — additive, ไม่ rename). Backfill loop ใน `applyLoadedProject` — marker เก่าไม่มี `markerType` → set `"parking"` อัตโนมัติ. `PHASE_I_B1_OK` E2E marker (3 sub-checks) PASS. **เครื่องมือวาง marker type ใหม่ (parking_fire/entrance/aed/...)** ยังไม่มี — รอ Phase I-B2 toolbar.
- **Phase I-A Site Plan Schema (additive)**: 7 new area semanticTags registered in `proto/static/js/semantic-meta.js` (5 maps) and `proto/ui.html` (`SEMANTIC_TAG_LABELS`): `building_coverage`, `open_space`, `permeable_area`, `hardscape`, `softscape`, `parking_area_outdoor`, `internal_road`. `buildingHeight_m:null` optional field on poly literal. `PHASE_I_A_OK` E2E marker (7 sub-checks) PASS.
- **Project Setup — Site Plan fields (additive)**: New section "ผังบริเวณ (Site Plan)" in Project Info form — `buildingClassification` (general/large/tall/extra_large), `buildingUseType` (11 values), `zoneCode` (string, no Rule Engine lookup), `siteAccessRoadWidth_m`, `userDefinedLimits` {far_max, osr_min_pct, permeable_min_pct, setback_front/side/back_min_m}. Old `.bmaplan` files load without error.

## Active Feature State (2026-05-13)

- **Path Geometry Model (additive)**: 9 new functions in `proto/ui.html` after line 955 — `_flattenCubicSeg`, `flattenPathToPoints`, `pathAreaM2`, `rectangleToPath`, `circleToPath`, `ellipseToPath`, `arcToCubic`, `renderPath`, plus `geometryType==='path'` branch in `objectAreaM2`. Legacy polygons/circles/ellipses/arcs unchanged. PATH_GEOMETRY_OK in smoke. Pen tool UI deferred to later sprint.
- **Site Plan specs ready (Phase I)**: `docs/design/SITE_PLAN_MEASUREMENT_PLAN.md` (~573 lines) and `docs/design/SITE_PLAN_UI_MOCKUP.md` (~530 lines). All 5 open questions DECIDED. Phase I-A implementation unblocked — schema additions + Project Setup fields + AREA_LABELS extension. Next E2E marker target: `SITE_AREA_TYPES_OK`.
- **`.claude/` Pack A**: 3 skills (`/bma-start`, `/bma-sprint-finalize`, `/bma-check-forbidden`) + 2 subagents (`bma-explorer` haiku, `bma-sprint-writer` sonnet). Token-saving workflow tooling. Tracked in git; `.claude/settings.local.json` stays ignored.

## Active Feature State (2026-05-10)

- **Save System**: `saveProject()` async — overwrites via FSAPI if handle exists, else `saveProjectAs()`. `saveProjectAs()` uses `showSaveFilePicker` with fallback to `dlBlob()` download. `isDirty` flag set by `pushUndo()`, cleared by save/load. Ctrl+S shortcut. "Save As" button in export panel.
- **Recent Projects**: `localStorage['bmaPlan.recentProjects.v1']` stores up to 10 recent `.bmaplan` filenames. `#top-open-project` shows dropdown with Browse + recent files. Both load paths (`proj-input`, `proj-input2`) call `addRecentProject()` after successful load.
- **Annotated PDF Export**: "Export หน้าปัจจุบัน + Annotations" and "Export ทุกหน้า + Annotations" buttons in export panel PDF section. Reuse `/export-pdf` endpoint with full `pageStore` annotations.
- **UI Layout Options**: `#btn-ui-layout` (⚙ Layout) button in topbar. `#ui-layout-panel` with Presets (Current Stable / Mockup V3 / Inspection Focus / Layer Focus / Compact) and per-section switches (Top / Left / Right / Widgets). Persisted in `localStorage['bmaPlan.uiLayoutOptions.v1']`. v3 modes = CSS-class overrides on `<body>`. Default = Current Stable.

## Active Feature State (prior)

- **Workflow**: Open PDF → Set Scale → Page Setup → Measure → Review → Export (locked)
- **Page-Scoped Layer Model**: layers are now per-page instances — each page has its own
  independent layer set with `Layer {id, pageId, name, slug, color, visible, locked, order, presetKey}`.
  Global `layerVis`/`layerLock` are kept in sync via bridge for all existing render code.
- **Layer Presets by Page Type**: site/plan/elev/section page types get appropriate default layers.
  Same preset applied to different pages creates independent layer instances with distinct IDs.
- **Object Layer Fields**: new objects get `pageIndex`, `layerSlug`, `layerId` assigned at creation.
  Existing objects get safe defaults on restore via `validateObjectLayerScope()`.
- **Right Panel Layers**: layer rows driven by `getCurrentPageLayers()` with live object counts.
- **Tool-Layer Sync**: `updateActiveLayerControl()` syncs active layer slug to page model via
  `setActiveLayerForPage()`. Tool mode changes keep page layer state consistent.
- **Area Summary**: confirmed — driven by `semanticTag`/`measurementProfile`/`reportTarget`,
  not layer name. Export groups by page number + semantic metadata.
- **Left Inspection Status Panel**: collapsible panel with workflow steps, per-page stats,
  measurement summary (gross/opening/land/parking), warnings, next action.
- **Right panel**: Layers-first (page-scoped), Legacy/Compatibility Properties+ObjectTree below.
- **Status bar**: Tool, Scale, Objects, Warnings, Layer, Save, Page.
- **5 measurement metadata fields**: measurementProfile, objectCategory, reportTarget, lawBasis, countingRule.
- **XLSX export**: Page Scales, Report Target Summary sheet, 5 metadata columns.

## Test Baseline (2026-05-10)

```
python -m py_compile proto/server.py proto/e2e_ui_test.py  → PASS
python proto/e2e_ui_test.py smoke                          → PASS
python proto/e2e_ui_test.py full                           → PASS
```

## Latest Commits (proto submodule)

- `55dccdf` export: add current-page and all-pages annotated PDF export buttons
- `5ad2cc0` save: improve open and recent project workflow
- `266d365` save: support save as and overwrite current project
- `b271577` ui: add mockup v3 app shell theme
- `fb39f28` ui: make panels scrollable and show page-scoped layers

## Latest Commits (root)

- `9a46ff4` docs: audit annotated pdf export feasibility
- (proto submodule pointer updated)
