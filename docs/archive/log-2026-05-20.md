# BMA-Plan — Log Archive (2026-05-20 session)

> Archived from root `log.md` — only 2 sessions kept in root file.
> See also: [log-2026-05-19.md](log-2026-05-19.md) (BLOAT-1, BLOAT-2 + 2026-05-19 bundle)
>
> Contains: BLOAT-3, BLOAT-4, BLOAT-5, BLOAT-FLAKE-1, BUG-20260520-sel-midpan, INV-2026-05-20-001, INV-2026-05-20-002/003/004, BUG-20260520-zen-exit-rp-restore (archived when HT-ACC series was added to root)

---

## 2026-05-20 — BUG-20260520-sel-midpan: Middle-mouse + Space pan in Select mode — PASS (branch: main)

**What changed:** In `proto/ui.html`, the `ws` mousedown handler's `mode==="sel"` branch received a one-line guard at its top: `if(e.button===1||spaceDown){isPan=true;lastMx=e.clientX;lastMy=e.clientY;ws.style.cursor="grabbing";return;}`. This mirrors the identical pan guard already in the non-`sel` path. `proto/e2e_ui_test.py` gained `_test_bug_sel_midpan` (+34 lines) + marker `BUG_20260520_SEL_MIDPAN_OK`. `docs/status/PHASE_INDEX.md` +1 row.

**Why:** Middle-mouse (button===1) and Space pan did nothing in Select mode — the `mode==="sel"` branch fired `redraw();return` unconditionally before the pan check. Fix restores parity with every other tool mode.

**Files touched:**
- `proto/ui.html`: +1 line — pan guard at top of `mode==="sel"` mousedown branch (~L2064)
- `proto/e2e_ui_test.py`: +34 lines — `_test_bug_sel_midpan` + call wiring + `BUG_20260520_SEL_MIDPAN_OK`
- `docs/status/PHASE_INDEX.md`: +1 row — bug filed and marked done

**Tests:** py_compile PASS; full EXIT 0; BUG_20260520_SEL_MIDPAN_OK GREEN (canvas #cc transform +70x/+45y; mode stayed 'sel'). Total markers: 22.

**Phase 1 scope check:** All ✅ — polyAreaM2/pdfToC/RS/snap/server.py/schema all UNCHANGED.

---

## 2026-05-20 — INV-2026-05-20-001 Verify Scale tool — PASS (branch: main)

**What changed:** Replaced the `verifyScale()` stub with a full second-reference cross-check flow. `verifyFinish()` computes `%dev = 100·|d_meas−d_enter|/d_enter` and shows `#verify-modal` with green (<0.5%) / yellow (<2%) / red (≥2%) confidence band, measured/entered/area-impact display, and Accept / Re-calibrate / Average actions. New `calibPanelOk()` router routes the calib panel OK button to `verifyFinish()` in verify mode and `finishCalib()` otherwise — `finishCalib()` body unchanged. `proto/e2e_ui_test.py` +124 lines: `_test_verify_scale` 9 sub-checks + `INV_VERIFY_SCALE_OK`.

**Why:** Single-reference calibration gives no quality signal. Verify Scale closes this gap with a second known dimension, a numeric %dev band, and three recovery paths. Approach A from the invent pipeline GO verdict (spike 10/10 in `proto/sandbox/invent-verify-scale.html`).

**Files touched:**
- `proto/ui.html`: +82/−5 — `#verify-modal` HTML; 10 new fns; `calibPanelOk` router; `cancelCalib` reset; `anyModal` guard extended
- `proto/e2e_ui_test.py`: +124 lines — `_test_verify_scale` + `INV_VERIFY_SCALE_OK`

**Tests:** py_compile PASS; full EXIT 0; INV_VERIFY_SCALE_OK 9/9 all:True. All key baselines (ANNOT_OK/PERSIST_OK/REAL_OK/PROJECT_OK/XLSX_OK/PATH_GEOMETRY_OK) GREEN. Zero regression.

**Phase 1 scope check:** All ✅ — polyAreaM2/pdfToC/RS/snap/server.py/schema all UNCHANGED. finishCalib() UNCHANGED. schema additive only (calibScale.verifyResult optional).

---

## 2026-05-20 — BLOAT-5 Extract page-setup modal JS to proto/static/js/page-setup.js — PASS (smoke; full ENV-FLAKE) (branch: main)

**What changed:** Created `proto/static/js/page-setup.js` (125 lines, classic non-module script) and extracted 15 page-setup functions + 2 floor constants from 3 non-contiguous ranges in `proto/ui.html`. Range A (L855–856): `FLOOR_KIND_LABELS` + `FLOOR_KIND_OPTIONS`. Range B (L1051–1070): floor helpers `autoNamePage`, `setPageFloorKind`, `setPageFloorNum`, `countTagBefore`. Range C (L3094–3167): inspector + renumber/delete cluster `selectSetupPage`, `_pageReadiness`, `_setupCountObjects`, `_renderSetupDashboard`, `_renderSetupPageCard`, `_setupBack`, `_renderSetupInspector`, `_pendingDeleteN`, `_openRenumberDialog`, `closeRebuildDialog`, `_executeRenumberDelete`, `_reindexPageDicts`. Kept in `proto/ui.html`: 15 cross-cluster glue functions. Added E2E marker `PHASE_BLOAT5_OK` (8 sub-checks). Net: `proto/ui.html` −93 +2 (3,869→3,777); `proto/static/js/page-setup.js` NEW 125 LOC; `proto/e2e_ui_test.py` +91 LOC. Full failed 3 retries — pre-existing REAL_PDF analyse flake (BLOAT-FLAKE-1). Loop halted per `LOOP_STOP_REGRESSION`.

**Why:** Page-Setup spans 3 prior sprints (INV-2026-05-18-001a/b/c). BLOAT-5 cleanly extracts the inspector + renumber-delete cluster. Recipe is 5-for-5. Full failure is NOT a BLOAT-5 regression. Root cause environmental.

**Files touched:**
- `proto/ui.html`: −93 +2 — extracted 96 lines across 3 ranges; added `<script src="/static/js/page-setup.js">` tag; net −92 LOC (3,869→3,777)
- `proto/static/js/page-setup.js`: NEW 125 LOC — 2 constants + 15 functions
- `proto/e2e_ui_test.py`: +91 LOC — `pageSetupJsLoaded` + `_test_bloat5_page_setup_extracted` + `PHASE_BLOAT5_OK`

**Tests:** smoke EXIT 0 (18/18 + PHASE_BLOAT5_OK 8/8 + INV_PAGE_SETUP A/B/C GREEN); full FAILED 3/3 retries (BLOAT-FLAKE-1, NOT BLOAT-5 regression; retroactively full-validated by BLOAT-FLAKE-1 sprint).

**Phase 1 scope check:** All ✅ — polyAreaM2/pdfToC/RS/snap/server.py/schema all UNCHANGED.

**Known gaps:** BLOAT-FLAKE-1 filed. Loop halted LOOP_STOP_REGRESSION.

---

## 2026-05-20 — BLOAT-FLAKE-1 Fix REAL_PDF `_wait_analyse_ready` flake — PASS (branch: main)

**What changed:** `_wait_analyse_ready` timeout raised 30.0→60.0 s; grace window added (+50% if status bar still shows active progress at deadline). ~15 LOC in `proto/e2e_ui_test.py` only. No app code, schema, or other test logic touched.

**Why:** Real 45-page A1 permit PDF occasionally caused `_wait_analyse_ready` to exceed 30 s ceiling — worst occurrence: 3 retries during BLOAT-5. Raised ceiling is free on fast smoke path. Grace window avoids false fail when page is actively loading. Unblocks dev-loop. BLOAT-5 retroactively full-validated.

**Files touched:**
- `proto/e2e_ui_test.py`: +15 −2 — timeout 30.0→60.0; grace-window branch

**Tests:** py_compile PASS; full EXIT 0 — PERSIST_OK/REAL_OK/ANNOT_OK GREEN (stable); all BLOAT/INV/HT11 markers GREEN. Total markers: 21.

**Phase 1 scope check:** All ✅ — `proto/ui.html` NOT TOUCHED; `proto/server.py` NOT TOUCHED; schema UNCHANGED.

---

## 2026-05-20 — BLOAT-3 Extract export/save JS to proto/static/js/export-save.js — PASS (branch: main)

**What changed:** Created `proto/static/js/export-save.js` (188 lines, classic non-module script) containing 14 functions and 13 constants extracted from `proto/ui.html`. Added `<script src="/static/js/export-save.js">` after `status-bar.js` in `proto/ui.html`. Functions extracted: `rowBase`, `buildRows`, `dlBlob`, `exportJSON`, `exportCSV`, `exportSummaryXLSX`, `exportXLSX`, `exportAllPagesAnnotatedPDF`, `exportCurrentPageAnnotatedPDF`, `exportPngZip`, `_makeProjBlob`, `_writeToHandle`, `_fallbackDownload`, `saveProjectAs`, `saveProject`, `saveSourcePdfInPlace`. Constants extracted: `COL_PAGE`, `COL_TYPE`, `COL_NAME`, `COL_VALUE`, `COL_UNIT`, `COL_RNW`, `TYPE_DISTANCE`, `TYPE_PATH`, `TYPE_REF`, `TYPE_PARKING`, `TYPE_AREA`, `TYPE_OPENING`, `TYPE_REF_DISTANCE`. Added E2E machinery in `proto/e2e_ui_test.py`: `exportSaveJsLoaded` field in UI-load test + new `_test_bloat3_export_save_extracted` function (8 sub-checks: `fileLoad`, `fnsOk`, `constsOk`, `dlBlobOk`, `buildRowsOk`, `blobIsBlob`, `schemaOk`, `asyncOk`) + new marker `PHASE_BLOAT3_OK`. Net: `proto/ui.html` −161 +6 (4,208→4,057 lines, −151 net); `proto/static/js/export-save.js` NEW 188 LOC; `proto/e2e_ui_test.py` +111 LOC.

**Why:** BLOAT-2 proved the no-bundler extraction recipe on a small module (status bar). BLOAT-3 scales it to the largest cohesive cluster: export + save = 14 fns + 13 consts, including the sensitive `.bmaplan` schema serialization (`_makeProjBlob`) and FSA file-handle persistence (`saveProject` / `saveSourcePdfInPlace` that mutate `currentProjectHandle` / `currentSourcePdfHandle` declared in `ui.html`). With the recipe now proven on the biggest and most complex piece — verified by `XLSX_OK`, `PROJECT_OK`, `PERSIST_OK`, and `ANNOT_OK` all GREEN on the real 45-page permit — BLOAT-4 (annotations) and BLOAT-5 (page-setup) become formulaic.

**Files touched:**
- `proto/ui.html`: −161 +6 — extracted 14 fns + 13 consts; added `<script src="/static/js/export-save.js">` tag; 3 one-line comment placeholders remain; net −155 LOC (4,208→4,057)
- `proto/static/js/export-save.js`: NEW 188 LOC — 6 column consts + 7 type consts + `rowBase` + `buildRows` + `dlBlob` + JSON/CSV/XLSX/PDF/PNG export fns + save fns (`_makeProjBlob`, `_writeToHandle`, `_fallbackDownload`, `saveProjectAs`, `saveProject`, `saveSourcePdfInPlace`)
- `proto/e2e_ui_test.py`: +111 LOC — `exportSaveJsLoaded` field + `_test_bloat3_export_save_extracted` (8 sub-checks) + `PHASE_BLOAT3_OK` marker print

**Tests:**
```
python -m py_compile proto/server.py proto/e2e_ui_test.py  → PASS
python proto/e2e_ui_test.py smoke                          → EXIT 0, 18/18 baseline + PHASE_BLOAT2_OK + PHASE_BLOAT3_OK GREEN
python proto/e2e_ui_test.py full                           → EXIT 0, 21/21 + PHASE_BLOAT2_OK + PHASE_BLOAT3_OK GREEN
  XLSX_OK (calls extracted exportXLSX) — GREEN
  PROJECT_OK (calls extracted saveProject + applyLoadedProject round-trip) — GREEN
  PERSIST_OK (save+reload multi-page on real 45-page permit) — GREEN
  ANNOT_OK (calls extracted exportCurrentPageAnnotatedPDF) — GREEN
  REAL_OK (real-PDF flow) — GREEN
  PHASE_BLOAT3_OK 8/8 sub-checks — GREEN (incl. schemaOk: all 12 v1 fields present; asyncOk: 3 async fns confirmed)
/bma-human-test — SKIPPED (mechanical extraction, zero user-visible change; PROJECT_OK + PERSIST_OK + ANNOT_OK on real permit cover most sensitive surfaces; schemaOk sub-check explicitly verifies 12-field v1 schema integrity post-extraction)
```

**Phase 1 scope check:**
- ✅ `polyAreaM2` / `polyMetrics` / `polySelfIntersects` — UNCHANGED
- ✅ `pdfToC` / `cToPdf` / `RS` / scale math — UNCHANGED
- ✅ `buildSnapIndex` / `snap` engine — UNCHANGED
- ✅ `proto/server.py` — NOT TOUCHED (extracted client functions still POST to `/export-pdf` / `/export-xlsx` / `/export-xlsx-summary` / `/export-png`; server side untouched)
- ✅ `.bmaplan` schema — UNCHANGED (`_makeProjBlob` still emits `version: 1` with same 12 fields; verified by `schemaOk` sub-check)
- ✅ No legal / OCR / AI / Rule Engine / FAR-OSR pass-fail

**Known gaps / follow-ups:**
- BLOAT-4 (annotation extraction, ~300 LOC, 7 annotation tool handlers + render + hit-test → `proto/static/js/annotations.js`) — next queued sprint.
- BLOAT-5 (page-setup modal extraction) — after BLOAT-4.
- Optional BLOAT-3b: extract print cluster (`printCurrentPage` / `printSelectedPages` / `_captureCanvasDataURL` / `_buildPrintDoc` / `_escForHtml` / `_waitForRedraw`) to `proto/static/js/print-canvas.js` (~50–60 LOC delta). Low-risk; self-contained. Not in scope of BLOAT-3.
- Optional BLOAT-6: shared helpers (`collectAreas` / `phase1Warnings` / `collectSummaryData` / `syncProjectInfoFromForm` / `normalizeAllObjects`) — high coupling (summary widget + layer + measurement); NOT for export-save.js; separate sprint if desired.

---

## 2026-05-20 — BLOAT-4 Extract annotation JS to proto/static/js/annotations.js — PASS (branch: main)

> Note: BLOAT-4 was the previous "Latest" in root log.md before BLOAT-5 was appended. Its full entry remains in root log.md as the current "Previous" session.

---

## 2026-05-20 — INV-2026-05-20-002/003/004 Layer model rebuild L1+L2+L3 — PASS (branch: main)

> Archived from root log.md when BUG-20260520-zen-exit-rp-restore was added as the new Latest.

Three-commit layer-model rebuild making page-scoped layer system the single authority for render/hit/visibility/lock. L1 (`93c512f`): `validLayerSlugForPage` + `getObjectLayerSlug` + `_slugVisible`/`_slugLocked`/`_objLayerVisible`/`_objLayerLocked`; all render/hit/label paths repointed. L2 (`1301a12`): `reassignSelectedObjectLayer()` + Layer dropdown in properties panels; `objLayerKey()` real slug. L3 (`2e6b2f9`): global `layerVis`/`layerLock` demoted to mirror. `INV_LAYER_L1_OK` / `L2_OK` / `L3_OK` GREEN; HT8D5A restored; 100 _OK markers; 0 E2E_FAIL.

---

## 2026-05-20 — BUG-20260520-zen-exit-rp-restore — PASS (branch: main)

> Archived from root log.md when HT-ACC series was added as the new Latest.

Defensive fix for user-reported bug where right-panel restore tab vanished after Zen Mode exit. F11 unconditional `preventDefault()` + widened exit condition; F9/F10 keybindings added (restore tabs showed labels but had no handler); dead `#right-panel.collapsed~#workspace #rp-restore-tab` sibling selector replaced with `body:has(#right-panel.collapsed) #rp-restore-tab{display:flex}`. `BUG_20260520_ZEN_EXIT_RP_RESTORE_OK` GREEN (6 sub-checks). Full E2E EXIT 0: 101 _OK markers. Commit `9453777`.

---
