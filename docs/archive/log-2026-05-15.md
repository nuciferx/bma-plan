# BMA-Plan — Log Archive (2026-05-15 sessions archived from root log.md)

> Archived from `log.md` on 2026-05-15 when session count exceeded 2.
> Sessions archived: PHASE_I_B2B_MEASURE_MENU_SUBMENU, Pack G (Sandbox Test), USER_PRIORITY_INSERT_U1_U2, PHASE_I_B2A_SITE_RIBBON, AUTONOMOUS_DEV_LOOP_PACK_F, RECONCILE_SITE_PLAN_UI_MOCKUP, PHASE_I_B1_MARKER_TYPE, PHASE_I_A_SCHEMA_AND_PROJECT_SETUP

---

## 2026-05-15

### [session] PHASE_I_B2B_MEASURE_MENU_SUBMENU — PASS (branch: main)

**What changed:** เพิ่ม "Site Plan (ผังบริเวณ) ▶" submenu ภายใต้ Measure menu ใน `proto/ui.html` — `dd-site-submenu-trigger` + `dd-site-submenu` (7 area items + 8 marker items). `updateSiteRibbon()` ถูก extend ให้ toggle submenu trigger ด้วย (visibility tracks `pageTags[curPage]==="site"`). Menu items dispatch ตรงไปยัง `activateSiteAreaTool(semanticTag)` / `setMarkerType(markerType)` ซึ่ง share กับ ribbon group จาก I-B2a. แก้ bug ระหว่าง sprint: version แรก call `closeAllMenus()` explicit จาก dd-item ทำให้ `toggleMenu` (bubble-up parent) เห็น `wasActive=false` แล้ว re-open menu — แก้โดย remove explicit close, ปล่อยให้ bubble-up จัดการเองตาม pattern เดิมของ codebase. เพิ่ม `_test_phase_i_b2b_site_menu()` + marker `PHASE_I_B2B_OK` ใน `proto/e2e_ui_test.py`. ยัง filed 5 human-test findings (HT-1…HT-5) ลงใน `docs/status/PHASE_INDEX.md`.

**Why:** Phase I-B2 ถูก SPLIT เป็น I-B2a (ribbon) + I-B2b (Measure menu submenu) โดย `/bma-ui-scope`. I-B2b ให้ keyboard-first / menu-driven path เข้าถึง site tools สำหรับ user ที่ prefer menu เหนือ ribbon — reuse handlers จาก I-B2a 100%, ไม่ต้องเพิ่ม logic ใหม่.

**Files touched:**
- `proto/ui.html`: DOM L74–L94 — `dd-site-submenu-trigger` + `dd-site-submenu` (7 area + 8 marker items); `updateSiteRibbon()` L1415 extended to toggle submenu trigger visibility
- `proto/e2e_ui_test.py`: เพิ่ม `_test_phase_i_b2b_site_menu(page)` + marker `PHASE_I_B2B_OK`; asserts trigger/submenu DOM, item counts (7+8), visibility toggle on pageTags, dispatch for area+marker, menu auto-close via bubble-up
- `docs/status/PHASE_INDEX.md`: I-B2b marked done; HT-1…HT-5 filed from human-test triage
- `NEXT_ACTION.md`: modified pre-sprint by user

**Tests:**
- `py -3.12 -m py_compile proto/server.py proto/e2e_ui_test.py` → PASS
- `py -3.12 proto/e2e_ui_test.py smoke` → PASS 20/20 markers GREEN (19 เดิม + PHASE_I_B2B_OK)
- `py -3.12 proto/e2e_ui_test.py full` → PASS 22/22 markers GREEN; no regressions in MENU_OK or PHASE_I_B2A_OK

**Phase 1 scope check:**
- ✅ `polyAreaM2`/`polyMetrics`/`polySelfIntersects` unchanged
- ✅ `pdfToC`/`cToPdf`/`RS`/scale math unchanged
- ✅ `proto/server.py` core endpoints unchanged
- ✅ `.bmaplan` schema additive only, version stays 1
- ✅ No legal / OCR / AI / Rule Engine / FAR-OSR pass-fail

**Known gaps / follow-ups:**
- HT-1 BROKEN: `.dd-submenu` z-index:1 < `.dropdown` z-index:200 — submenu can render under sibling overlays
- HT-2 BROKEN: `⬡ NaN ตร.ม.` in measure-result/summary when scale not calibrated
- HT-3 FRICTION: `lbl-mode` should reflect site-tag context when site area tool active
- HT-4 FRICTION: name panel blocks canvas without clear dismissal path
- HT-5 COSMETIC: `.dd-submenu` overflows on short viewports (412 px tall, 15 items)

---

## 2026-05-14

### [session] USER_PRIORITY_INSERT_U1_U2 — PASS (docs-only, branch: main)

**What changed:** Inserted 2 user-priority sprints at the TOP of `PHASE_INDEX.md` active queue, above Phase I-B remainder:
- **U1 — Save Annotated PDF in-place** (overwrite source PDF with embedded work; track source-PDF FSA handle like `currentProjectHandle`; fall back to download when no handle)
- **U2 — 1-Page Excel Summary** (single-sheet summary variant of XLSX — areas + BCR/OSR/FAR + setbacks + counts on one printable page; no verdict UI)

Also added a "User priority notes" section explaining intent + implementation hints + the likely `LOOP_STOP_DESIGN` on U1's FSA-handle acquisition path. Filled I-B2a's commit hash (`b9f9132`) into the queue table.

**Why:** User direct request after seeing iteration 1 (I-B2a) succeed. These are the "first-usable" features the user actually wants before more Phase I-B/C/D/E work: (1) "Save" must overwrite the source PDF in-place with annotations embedded (not download a new file each time); (2) one Excel summary sheet on one page. The user is about to restart the session — capturing these now so `/loop /bma-dev-loop` picks U1 first on next launch.

**Files touched:** `docs/status/PHASE_INDEX.md` (2 new queue rows + notes section + I-B2a hash filled in), `log.md`.

**Tests:** No-test rationale — docs-only roadmap edit, no `proto/` touched. Baseline = I-B2a (`b9f9132`, smoke 19/19 + full 22/22 GREEN).

**Phase 1 scope check:** ✅ ทั้งหมด docs-only.

**Known gaps / follow-ups:**
- Next session: `/loop /bma-dev-loop` picks U1 first.
- U1's FSA-handle-on-`<input type="file">` ambiguity may trigger `LOOP_STOP_DESIGN` — user will resolve when surfaced.
- Real `bma-human-journey-tester` subagent should wake up after session restart, restoring full "see problems" capability.

---

## 2026-05-14

### [session] PHASE_I_B2A_SITE_RIBBON — PASS (branch: main)

**What changed:** เพิ่ม Site Plan ribbon group (`#ribbon-site`) ใน `proto/ui.html` — 7 area buttons (`data-site-tag`: building_coverage/open_space/permeable_area/hardscape/softscape/parking_area_outdoor/internal_road) + 8 marker buttons (`data-marker-type`: parking_disabled/parking_fire/parking_ambulance/entrance/aed/sign/fire_escape/fire_elevator) แสดงเฉพาะเมื่อ page tag === "site". เพิ่ม 2 state vars (`curSiteSemanticTag`, `curMarkerType`), 4 functions (`activateSiteAreaTool`, `setMarkerType`, `updateSiteRibbon`, `updateSiteRibbonActive`). areaType↔semanticTag bridge: `finishCurrentArea` poly literal อ่าน `curSiteSemanticTag||defaultSemanticTag(...)` แล้ว consume-and-clear; marker literal อ่าน `curMarkerType||"parking"`. `activateAreaTool` clears `curSiteSemanticTag`, `setMode` calls `updateSiteRibbonActive`, `updateBottomBar` calls `updateSiteRibbon`. เพิ่ม `_test_phase_i_b2a_site_ribbon(page)` + marker `PHASE_I_B2A_OK` (7 sub-checks) ใน `proto/e2e_ui_test.py`. Sprint นี้รันโดย Autonomous Dev Loop iteration 1 (supervised).

**Why:** Phase I-B2 ถูก SPLIT จาก `/bma-ui-scope` เป็น I-B2a (ribbon, sprint นี้) + I-B2b (Measure menu submenu). I-B2a ทำ ribbon + shared handler logic ที่ I-B2b จะ reuse. areaType↔semanticTag bridge แก้ design wrinkle จาก I-A: 7 area type ใหม่เป็น `semanticTag` (ไม่ใช่ `areaType`) → ใช้ `curSiteSemanticTag` override ใน `finishCurrentArea` โดยไม่ต้อง pollute `AREA_LABELS` legacy model.

**Files touched:**
- `proto/ui.html`: +`curSiteSemanticTag`/`curMarkerType` state; ribbon DOM `#ribbon-site` (15 buttons); `activateSiteAreaTool`, `setMarkerType`, `updateSiteRibbon`, `updateSiteRibbonActive`; bridge ใน `finishCurrentArea`; `curMarkerType` ใน marker literal; clear ใน `activateAreaTool`; hooks ใน `setMode`/`updateBottomBar`
- `proto/e2e_ui_test.py`: เพิ่ม `_test_phase_i_b2a_site_ribbon(page)` + marker `PHASE_I_B2A_OK` (7 sub-checks)
- `sprints/completed/2026-05-14-phase-i-b2a-site-ribbon/RUN_PHASE_I_B2A_SITE_RIBBON.md`: sprint card ใหม่
- `log.md`, `PATCH_SUMMARY.md`, `TEST_RESULT.md`, `FINAL_REPORT_FOR_CHATGPT.md`, `CURRENT_STATUS.md`, `docs/status/LATEST_STATUS.md`, `docs/status/NEXT_ACTIONS.md` — sprint output discipline

**Tests:**
- `py -3.12 -m py_compile proto/server.py proto/e2e_ui_test.py` → PASS
- `py -3.12 proto/e2e_ui_test.py smoke` → PASS 19/19 markers GREEN (18 เดิม + PHASE_I_B2A_OK). One retry needed: first run failed `raw is not defined` (plain coords used instead).
- `py -3.12 proto/e2e_ui_test.py full` → PASS 22/22 markers GREEN (21 เดิม + PHASE_I_B2A_OK)
- `PHASE_I_B2A_OK`: all 7 sub-checks True — ribbonSiteExists, buttonsWired (7+8), hiddenOnPlan, shownOnSite, siteAreaToolSets, sitePolyGetsTag (`newTag:'open_space'`, override clears), markerToolSets
- No regression: `MAIN_UI_OK.primaryToolCount` 13; `MENU_OK.menuCounts.measure` 22; `EXT_MEASURE_OK` parking summary intact

**Phase 1 scope check:**
- ✅ `polyAreaM2`/`polyMetrics`/`polySelfIntersects` unchanged
- ✅ `pdfToC`/`cToPdf`/`RS`/scale math unchanged
- ✅ `proto/server.py` core endpoints unchanged
- ✅ `.bmaplan` schema additive only — version ยัง 1
- ✅ No legal / OCR / AI / Rule Engine / FAR-OSR pass-fail

**Known gaps / follow-ups:**
- I-B2b (Measure menu submenu) reuses `activateSiteAreaTool`/`setMarkerType` — next in queue
- `curSiteSemanticTag` เป็น consume-and-clear — stickiness อาจทำ polish ทีหลัง (FRICTION-level)

---

### [session] AUTONOMOUS_DEV_LOOP_PACK_F — PASS (docs/config-only, branch: main)

**What changed:** Designed + built the Autonomous Dev Loop (Pack F): `docs/status/PHASE_INDEX.md` (canonical roadmap), `.claude/agents/bma-human-journey-tester.md`, `.claude/skills/bma-human-test/SKILL.md`, `.claude/skills/bma-dev-loop/SKILL.md`, `CLAUDE.md` (Pack F sections).

**Why:** User asked for a self-driving development methodology. Full-auto autonomy: loop commits to main, halts on 6 stop-conditions. `bma-human-journey-tester` is the "see problems" mechanism (open → measure every page → export XLSX → save → reopen).

**Files touched:** `docs/status/PHASE_INDEX.md` (new), `.claude/agents/bma-human-journey-tester.md` (new), `.claude/skills/bma-human-test/SKILL.md` (new), `.claude/skills/bma-dev-loop/SKILL.md` (new), `CLAUDE.md`, `log.md`.

**Tests:** No-test rationale — `.claude/` config + project docs only. No `proto/` touched.

**Phase 1 scope check:** ✅ all — docs/config only.

---

### [session] RECONCILE_SITE_PLAN_UI_MOCKUP — PASS (docs-only, branch: main)

**What changed:** Reconciled `docs/design/SITE_PLAN_UI_MOCKUP.md` against shipped Phase I-A / I-B1 reality. 5 edits: header note; §4.1 field names (`frontageRoadWidth_m` → `siteAccessRoadWidth_m`, `buildingHeight_m` as object-level); §4.7 `siteMarkerCounts` pointer; §6 `.markers.push` → `.parking.push`; §11 sprint-split table revised to I-B1✅/I-B2/I-B3/I-B4.

**Why:** Two design docs disagreed on field names. Building I-C/I-D against mockup's wrong names would have read `undefined`. Renaming shipped fields is forbidden — corrected the doc instead.

**Files touched:** `docs/design/SITE_PLAN_UI_MOCKUP.md` (5 edits), `log.md`.

**Tests:** No-test rationale — docs-only. Baseline = Phase I-B1 (`c38c3e6`, smoke 18/18 + full 21/21 GREEN).

**Phase 1 scope check:** ✅ ทั้งหมด — docs-only.

---

### [session] PHASE_I_B1_MARKER_TYPE — PASS (branch: main)

**What changed:** เพิ่ม `MARKER_TYPE_LABELS` registry 9 ประเภทถัดจาก `PARKING_LABELS` ใน `proto/ui.html`. เพิ่ม field `markerType:"parking"` ใน marker creation literal (ข้าง `parkingType` เดิม — additive, ไม่ rename). เพิ่ม backfill loop ใน `applyLoadedProject`. เพิ่ม `_test_phase_i_b1_marker_type(page)` + marker `PHASE_I_B1_OK` (3 sub-checks).

**Why:** Phase I-B SPLIT → I-B1/I-B2/I-B3. I-B1 ทำก่อน (risk ต่ำสุด + ปลดล็อก data model). additive field ไม่ rename `parkingType`.

**Files touched:** `proto/ui.html`, `proto/e2e_ui_test.py`, `sprints/completed/2026-05-14-phase-i-b1-marker-type/RUN_PHASE_I_B1_MARKER_TYPE.md`, `CLAUDE.md`, sprint output discipline files.

**Tests:** py_compile PASS · smoke 18/18 GREEN · full 21/21 GREEN.

**Phase 1 scope check:** ✅ ทั้งหมด.

---

### [session] PHASE_I_A_SCHEMA_AND_PROJECT_SETUP — PASS (branch: main)

**What changed:** เพิ่ม 7 area semanticTags ใน `proto/static/js/semantic-meta.js` (5 maps) + `SEMANTIC_TAG_LABELS` ใน `proto/ui.html`. เพิ่ม Project Setup "ผังบริเวณ" section 11 inputs. เพิ่ม `buildingHeight_m:null` บน poly literal. เพิ่ม `PHASE_I_A_OK` marker (7 sub-checks).

**Why:** Schema groundwork ให้ Phase I-B/C/D ต่อยอด. additive-only 100%.

**Files touched:** `proto/static/js/semantic-meta.js`, `proto/ui.html`, `proto/e2e_ui_test.py`, sprint output discipline files.

**Tests:** py_compile PASS · smoke 17/17 GREEN · full 20/20 GREEN.

**Phase 1 scope check:** ✅ ทั้งหมด.

---

> ดู [docs/archive/log-2026-05-14.md](log-2026-05-14.md) สำหรับ sessions ก่อนหน้า (MEASURE_PACK_E, MENU_CANVAS_ZINDEX_FIX, UI_SPECIALIST_PACK_D, PATH_GEOMETRY_VISUAL_AUDIT)
