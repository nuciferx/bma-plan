# BMA-Plan — Log Archive (2026-07-03 sessions archived 2026-07-03 / 2026-07-04)

> Archived from root log.md on 2026-07-03 (BLOCK-20260703-clear-queue archived during the UX-REVIEW-20260703 + BUG-20260703-lite-save-wipes-data sprint block; GO-20260703-invariants-streaming-worker-recycle archived during the INV-20260703-layer-linkage plan-B-complete sprint block, both to keep root at last-2-sessions); further archived on 2026-07-04 (INV-20260703-layer-linkage plan-B-complete + UX-REVIEW-20260703/BUG-20260703-lite-save-wipes-data archived during the 2026-07-04 full-day 8-ship sprint block, again to keep root at last-2-sessions).
> Sessions in this file: BLOCK-20260703-clear-queue (2026-07-03, "ทำทั้งหมด" 5-ship session); GO-20260703-invariants-streaming-worker-recycle (2026-07-03, V2-U1 invariants + Range-streaming spike NOGO→RESHAPE + worker-recycle build); INV-20260703-layer-linkage plan-B-complete + UX-batch-1 + save-fix follow-up (2026-07-03, staged B0-B5 layer↔measurement redesign rollout); UX-REVIEW-20260703 + BUG-20260703-lite-save-wipes-data (2026-07-03, CRASH save-wipe fix + full UI/UX journey review + layer↔measurement invent checkpoint).

---

## 2026-07-03 — BLOCK-20260703-clear-queue ("ทำทั้งหมด" session: fitz lock + render followups a/c + V2 tiers/overlay-proof/streaming-research + Verify-Scale port + process docs) — PASS (branch: main)

**What changed:** Five same-session ships, run back-to-back overnight (2026-07-02 late → 2026-07-03) under a user directive to clear the entire remaining queue. **Ship 1 — `AUDIT-20260702-s2-fitz-lock` (commit `d0a5dde`):** added a per-case `threading.Lock` (`_case_lock`) in `lite/server_lite.py` serializing all `fitz.Document` access — `/page` + `/thumb` render blocks (double-checked cache), `/pageinfo`, and the whole `/export-pdf-overlay` render path (now ALSO moved off the event loop via `run_in_threadpool` around a new `_render_overlay` helper, completing the S2 threadpool goal deferred by `AUDIT-20260702-infra-bundle`), plus the copy/close/swap critical sections of apply-page-mutations + merge-pages (sync-only, no `await` under lock). Urgency was raised by `PERF-20260702-lite-foxit-smoothness` Sprint 4's thumbnail warm making concurrent `/thumb`+`/page` traffic routine. NEW guard `lite/tests/test_case_lock.py` (`LITE_CASE_LOCK_OK`): a 96-request 8-thread hammer test plus a doc-swap mutation fired mid-flight plus concurrent overlay-export calls, zero 5xx across all three. Honest limitation recorded: a native `fitz` race is probabilistic, so this is a hardening hammer test, not a deterministic RED-before-fix proof. Regression 9/9. **Ship 2 — render-followups (a)+(c) (commit `aec375b`):** two items from the `AUDIT-20260702-render-followups` bundle. (a) raster fallback: `loadPage()` no longer goes to a blank canvas when PDF.js is unavailable — it falls back to the server JPEG path (`/pageinfo` + `/page?rot` into `curImg`, drawn through the same view-transform math), with an `instanceof` guard keeping legacy render-path shims safe; measurement stays fully functional on the fallback path. (c) scanned-page detection: an operator-list heuristic (free, runs post-render, no extra pass) flags image-only pages; the re-render scale on such pages is capped at `RS*4` (~432 DPI) with a transform compensation (`up = want/use`) so on-screen registration is bit-identical — only wasted re-raster CPU on scans is saved, no accuracy change — plus a one-time Thai hint shown to the user. NEW guard `lite/tests/test_render_fallback_scanned.py` (`LITE_RENDER_FB_SCAN_OK`, 6 checks); two false-alarm findings were traced to the TEST itself (uniform-fill sampling artifact, un-recentered zoom in the test harness) and fixed there, not in app code. Regression 8/8. **Ship 3 — V2 tiers + overlay proof + streaming research (commit `13054b6`):** three parts. (i) `run_all_tests.py --tier t0/t1/t2` — implements migration step U2 from `docs/process/DEVELOPMENT_V2_BLUEPRINT.md`; `t0` (measure math via Node) now runs in 1.26 s, hitting the <5 s target. (ii) NEW `lite/tests/test_overlay_registration.py` (`LITE_OVERLAY_REG_OK`), built by a delegated agent and independently re-run by the main agent — the first PIXEL-level proof that raster and overlay stay registered: max offset 0.50 device px at zoom×2, 0.33 px at fit, 0.40 px at `pageRot=90°` (tolerance 4 px). This is the empirical evidence the 2026-05-28 render-quality spike never produced, and closes render-followups item (e)/(d) with real numbers instead of an exact-inverse-only proof. (iii) `docs/invent/lite-range-streaming.md` — Range-streaming research by `bma-researcher`: verdict `PRIOR_ART_PARTIAL`. Key risks surfaced: PDF.js worker-heap bug #10730 (`destroy()` may not free `commonObjs`, so ~600 MB of CHH's doc-level heap may be irreducible without a RESHAPE fallback of idle worker-recycling); `Blob.slice` local transport is untested; customer-PDF linearization status is unknown; Starlette's `/raw` route may already support HTTP Range without new code. Includes a 5-step spike design + explicit GO/NOGO criteria. **HALTED at the human checkpoint per Pack H invention discipline** — no code shipped for this piece, by design. **Ship 4 — `ACC-20260703-verify-scale-port` (commit `bea2119`):** ported Verify-Scale from proto's `INV-2026-05-20-001` — the "single remaining accuracy gap vs. Foxit" identified by the 2026-07-02 comparison (no incumbent viewer ships an integrated 2nd-reference cross-check). NEW `lite/static/js/verify-scale.js` (225 lines, self-contained): 2-point verify capture reusing the existing calibration pipeline, %deviation banding (green <0.5%, yellow <2%, red ≥2%) with implied area error shown as ≈2×%dev, three actions (accept / recalibrate / average), additive schema field `PS[pg].scale.verifyResult` riding the existing `calibScale` serialization (no migration). `lite/ui-lite.html` +3 net lines (new menu item `#mi-verify-scale`, scale-tool routing on `state.verifyMode`, `Shift+S` shortcut, script tag — stays inside the 1170/1200 line cap). `lite/static/js/menu-flyout.js` +2 lines (menu rebuild picks up the new item automatically). Built by the `lite-builder` subagent and independently verified by the main agent (line diff review + a 9-test targeted subset re-run). NEW guard `lite/tests/test_verify_scale.py` (`LITE_VERIFY_SCALE_OK`, 7 checks including a save/load round-trip of `verifyResult` and confirming the "average" action correctly re-derives downstream areas). **Ship 5 — process docs (commits `16e6495` + `b676652`, late 2026-07-02):** `docs/process/DEVELOPMENT_PILLARS.md` — a 6-pillar doctrine distilled from the day's work, each pillar backed by a concrete incident (e.g. the fitz-race hardening-not-proof honesty, the render-followups TEST-side false alarms) plus an explicit halt condition; `docs/process/DEVELOPMENT_V2_BLUEPRINT.md` — 6 evidenced structural weaknesses mapped to 6 structural upgrades (U1 `INVARIANTS.md`, U2 tiered test runner, U3 `SHIPS.jsonl` ledger, U4 roadmap split+reconcile, plus 2 more), a migration order, and a measurable success bar; migration step U2 (tiered test runner flag) was already partially landed by Ship 3 above, the rest remains queued. **Final validation across the whole block:** the full lite test suite — 70 files, all 70 green — was run in three chunks after background `run_all_tests.py` invocations kept getting killed (17 files in a partial run whose log survives at `artifacts/run_all_tests_20260703_final.log`, plus 27 and 26 more in two foreground chunks). `MEASURE_PARITY_OK` stayed green throughout every ship. The suite grew from 60 to 70 files across the two days (2026-07-02 → 2026-07-03).

**Why:** The user's directive ("ทำทั้งหมด") was to clear the entire remaining queue in one sitting rather than trickle it across future sessions. The queue's five items happened to span the full risk spectrum this project's process docs (Ship 5) exist to describe: a probabilistic-race hardening fix (Ship 1) that is explicit about NOT being a deterministic proof; two small, additive UX/perf correctness fixes (Ship 2); an evidence-gathering + tooling ship that intentionally halts before the risky part (Ship 3's Range-streaming halts at the human checkpoint, exactly as Pack H requires); a cross-file port of an already-proven proto feature closing the last known accuracy gap vs. the competitive benchmark (Ship 4); and capturing the day's operating lessons as durable doctrine before they're lost (Ship 5). Running them as one documented block, rather than five separate sprint-output cycles, keeps the housekeeping overhead proportional to a night where the actual code changes were each individually small and low-risk.

**Files touched:**
- `lite/server_lite.py`: Ship 1 — `_case_lock` threading.Lock wraps all `fitz.Document` access (`/page`, `/thumb`, `/pageinfo`, `/export-pdf-overlay` via new `_render_overlay` + `run_in_threadpool`, apply-page-mutations + merge-pages critical sections)
- `lite/tests/test_case_lock.py`: NEW (Ship 1) — `LITE_CASE_LOCK_OK`, 96-request 8-thread hammer + mid-flight doc-swap + concurrent overlay export, zero 5xx
- `lite/ui-lite.html`: Ship 2 — `loadPage()` raster fallback to server JPEG when PDF.js unavailable (`instanceof` guard); Ship 4 — +3 net lines (`#mi-verify-scale` menu item, verify-mode scale-tool routing, `Shift+S`, script tag)
- `lite/static/js/page-renderer.js` (or render module): Ship 2 — scanned-page operator-list heuristic + `RS*4` capped re-render scale with `up=want/use` transform compensation + one-time Thai hint
- `lite/tests/test_render_fallback_scanned.py`: NEW (Ship 2) — `LITE_RENDER_FB_SCAN_OK`, 6 checks; 2 false alarms fixed test-side
- `lite/tests/run_all_tests.py`: Ship 3 — `--tier t0/t1/t2` flag (V2 blueprint U2); `t0` measure-math-via-Node runs in 1.26 s
- `lite/tests/test_overlay_registration.py`: NEW (Ship 3) — `LITE_OVERLAY_REG_OK`, pixel-level raster↔overlay registration proof (max offset 0.50/0.33/0.40 device px across zoom/fit/rotate cases, tolerance 4)
- `docs/invent/lite-range-streaming.md`: NEW (Ship 3) — Range-streaming research, `PRIOR_ART_PARTIAL` verdict, 5-step spike design + GO/NOGO criteria; HALTED at human checkpoint
- `lite/static/js/verify-scale.js`: NEW (Ship 4) — 225 lines, ported from proto `INV-2026-05-20-001`, additive `PS[pg].scale.verifyResult`
- `lite/static/js/menu-flyout.js`: Ship 4 — +2 lines, menu rebuild picks up new Verify Scale item
- `lite/tests/test_verify_scale.py`: NEW (Ship 4) — `LITE_VERIFY_SCALE_OK`, 7 checks incl. save/load round-trip + average-re-derives-areas
- `docs/process/DEVELOPMENT_PILLARS.md`: NEW (Ship 5) — 6-pillar doctrine + incident evidence + halt conditions
- `docs/process/DEVELOPMENT_V2_BLUEPRINT.md`: NEW (Ship 5) — 6 weaknesses → 6 structural upgrades + migration order + success bar

**Tests:**
```
python lite/tests/test_case_lock.py               → LITE_CASE_LOCK_OK          PASS (NEW; hardening hammer, not deterministic RED proof)
python lite/tests/test_render_fallback_scanned.py → LITE_RENDER_FB_SCAN_OK     PASS (NEW, 6 checks)
python lite/tests/run_all_tests.py --tier t0       → t0 in 1.26s (<5s target)  PASS
python lite/tests/test_overlay_registration.py    → LITE_OVERLAY_REG_OK       PASS (NEW, pixel-level proof)
python lite/tests/test_verify_scale.py            → LITE_VERIFY_SCALE_OK      PASS (NEW, 7 checks)
python lite/tests/run_all_tests.py                → 70/70 files green (chunked: 17 + 27 + 26; artifacts/run_all_tests_20260703_final.log covers the first 17)
```
`MEASURE_PARITY_OK` green throughout every ship — zero vendored-math touch across the whole block. Lite suite grew 60→70 files across 2026-07-02 → 2026-07-03. Proto E2E n/a — lite-only block, zero `proto/` edits.

**Phase 1 scope check:**
- ✅ `polyAreaM2` / `polyMetrics` / `polySelfIntersects` unchanged
- ✅ `pdfToC` / `cToPdf` / `RS` / scale math unchanged
- ✅ `proto/server.py` core endpoints unchanged (proto NOT TOUCHED — lite-only block)
- ✅ `lite/static/js/measure-engine.js` drift-locked vendored math unchanged across all 5 ships
- ✅ `.bmaplan` schema version stays 1; only additive field this block is `PS[pg].scale.verifyResult` (Ship 4, mirrors proto's already-shipped equivalent)
- ✅ No legal / OCR / AI / Rule Engine / FAR-OSR pass-fail
- ✅ No forbidden surface touched; Ship 3's Range-streaming (the one piece that would touch the page-renderer's buffer-ownership contract) correctly HALTED at the human checkpoint rather than shipped
- ✅ `docs/status/PHASE_INDEX.md` / `lite/tests/bug-archive.jsonl` deliberately NOT touched by this docs-batching write (owned by the sprint pipelines that ran during the block, not this writer)

**Known gaps / follow-ups:**
- **(1, HUMAN DECISION) Range-streaming spike GO/NOGO/RESHAPE** (`docs/invent/lite-range-streaming.md`) — the only item in the whole queue awaiting anything other than more work; needs a human call before the invent pipeline can proceed to spike. **(Resolved same-day by `GO-20260703-invariants-streaming-worker-recycle` — see root `log.md`.)**
- **(2) V2 migration continuation** — U1 (`INVARIANTS.md`), U4 (roadmap split + reconcile), U3 (`SHIPS.jsonl` ledger) remain queued from `docs/process/DEVELOPMENT_V2_BLUEPRINT.md`; U2 (tiered test runner) is partially landed via Ship 3's `--tier` flag.
- **(3) `ptToScreen`/`screenToPt` into the parity fixture** — last drift-lock nicety carried over from `BUG-20260702-lite-pagerot-registration`; runtime already routes through the vendored kernel, only the parity-fixture entry itself remains.
- **(4) Stale `PHASE_INDEX.md` rows** — `lpm-1..9` rows show `queued` but `bug-archive.jsonl` says fixed; needs a reconciliation pass (out of scope for this docs-batching write per explicit instruction not to touch either file).
- `AUDIT-20260702-s2-fitz-lock`'s guard test is a probabilistic hardening hammer, not a deterministic RED-before-fix proof — any future native-race regression here would need a more targeted repro, not just more hammer iterations.

---

## 2026-07-03 — GO-20260703-invariants-streaming-worker-recycle (V2-U1 invariant registry + Range-streaming spike NOGO→RESHAPE + worker-recycle build) — PASS (branch: main)

**What changed:** Continuation of the same-day "GO" loop block, run right after `BLOCK-20260703-clear-queue` closed, 5 pieces of work: **(1) V2-U1 invariant registry (commit `6d6e39b`):** NEW `lite/tests/INVARIANTS.md` — a canonical registry of 10 invariants (I1–I10), each mapped to its guard test + tier, plus the two mandatory SCOPE questions ("which invariants does this feature touch" / "does it create a new object kind") that is the prevention mechanism `docs/process/DEVELOPMENT_V2_BLUEPRINT.md` U1 called for, born directly from the arc-summary/CFSS-summary postmortem where a new object kind silently bypassed 6 rollup consumers. The new-kind rule requires fixtures added to I2/I4/I5 in the same sprint a new kind is introduced, no exceptions. Same commit also reconciles 16 stale `PHASE_INDEX.md` rows (`lpm-1..9`, `cfss-guard`, `EVOLT-1..5`, `INV-2026-05-25-CFSS`) that claimed `queued` but were long since shipped — each now carries its real `fixed_commit` pulled from `bug-archive.jsonl`/the test suite, so the roadmap agrees with git history again. The queued `ptToScreen`/`screenToPt` parity-fixture card is closed with a rationale instead of code: the behavioral lock already exists as invariant I7 (`LITE_PAGEROT_REG_OK` asserts the 4-angle closed-form mapping + exact inverse at 1e-9, and runtime has routed through the parity-tested `pdfToC`/`cToPdf` kernel since `9f4b298`) — adding the two functions to the literal `test_measure_parity.py` fixture set was judged redundant work, not a real coverage gap. **(2) Range-streaming SPIKE (commits `9466fe4` + `f8c2981`), GO received via `/loop`:** the 5-step spike from `docs/invent/lite-range-streaming.md` was executed for real in `lite/sandbox/invent-range-streaming/` against RAMA4 (19 MB) and the real CHH binder (95 MB). Results: (S1) both customer PDFs are NON-linearized yet stream fine — only 20% of bytes were fetched, so the standing linearization concern turned out to be moot; (S2) Starlette's `/raw` route already serves `206 Partial Content` + `Content-Range` with zero backend changes needed; (S3) streaming cut CHH's RSS by only 10% (1675→1503 MB) against a ≥50% GO criterion — **FAILS**; PDF.js worker-heap bug #10730 was CONFIRMED on the pinned 4.0.379 build (`doc.destroy()` frees the main document heap 409→98 MB but the separate pdf.js WORKER heap survives untouched — this worker heap, not the document heap, is the real ~1.5 GB ceiling); (S4) `PDFDataRangeTransport` over `Blob.slice` works mechanically (no full local copy needed) but delivers no memory win given (S3). **VERDICT: NOGO on streaming-as-a-memory-fix**, RESHAPE to worker-recycle — the spike's whole point was to prevent a doomed build sprint from being attempted, and it did. **(3) worker-recycle BUILT (commit `d52ddbb`), the RESHAPE, GO via `/loop`:** `lite/static/js/page-renderer.js` gains explicit `PDFWorker` ownership (`_docWorker`, passed into `getDocument` from both the `openLocal` and `/raw` paths — previously pdf.js silently created an implicit worker per document that `doc.destroy()` could never fully reclaim) plus a cheap `_docSource` re-open handle (a retained local `File`/`Blob` reinit with zero network cost, or the `caseId` for a one-shot `/raw` refetch) plus `recycleDocWorker()` — guarded against re-entrant recycling, skipped outright if a render is in-flight or no source is retained, and preserving `pageDims`/`pageRot`/`_scanned` metadata across the teardown so the UI doesn't need to re-derive anything. The next `loadPage()` call after a recycle transparently triggers a lazy `_reinitDoc()` — no visible state change to the user beyond a brief re-render. Recycle triggers: tab hidden ≥60 s, app idle ≥5 min AND `pageCount>20` (small documents aren't worth recycling), and a manual `PageRenderer.recycleNow` hook for tests. `_resetCache()` now also destroys the explicit worker on new-upload, closing a latent leak where the old implicit-worker pattern would otherwise accumulate one abandoned worker per upload in a long session. `page-renderer.js` grows to 790/1000 lines; `lite/ui-lite.html` gains exactly one call-site line (1170 total). Built by the `lite-builder` subagent, independently verified by the main agent. NEW guard `lite/tests/test_worker_recycle.py` (`LITE_WORKER_RECYCLE_OK`, 7 checks: teardown state, transparent reinit repaint, zero-refetch when a local blob is retained, metadata survival, in-flight-render SKIP, and a heap-not-worse assertion measured at −0.8%). Honest scope note: the full "CHH RSS −50%" acceptance bar was measured in the earlier spike's pattern (S3), not re-measured against this specific production implementation — a production re-probe on the real CHH binder is recorded as the queued follow-up measurement, not assumed. Regression 6/6 files + `run_all_tests.py --tier t0` green. **(4) Streaming-as-bandwidth card DEFERRED-until-remote-deployment:** the optional second card proposed by the spike (Range-streaming purely as a bandwidth optimization, independent of the memory question) was evaluated and explicitly deferred rather than built, with a recorded rationale (เสา 1 — "build only what today's product needs"): all `/raw` byte transfer happens over localhost on the current desktop-only product (a real 91 MB `/raw` fetch measured at 498 ms), so the spike's 80% byte-count reduction has no user-facing benefit until a remote/networked deployment target actually exists. **Suite validation:** the lite test suite now stands at 72 files; `MEASURE_PARITY_OK` stayed green throughout every piece of this block — zero vendored-math touch.

**Why:** This block is the direct continuation of the invention discipline `BLOCK-20260703-clear-queue` Ship 3 deliberately halted at — Range-streaming research had reached a human checkpoint, and the "GO" loop resumed work only once that checkpoint was cleared. Running the spike for real (rather than trusting the desk-research verdict) is what caught that the ~1.5 GB memory ceiling is a pdf.js worker-heap defect (#10730), not a document-buffering problem that Range-streaming could ever have fixed — building the streaming feature first would have shipped real engineering effort against the wrong root cause. Pack H's RESHAPE path exists precisely for this outcome: redirect the same acceptance bar (large-PDF memory reduction) toward the mechanism the spike's own evidence pointed at (worker teardown), rather than either abandoning the goal or forcing the original approach through anyway. The V2-U1 invariant registry and the PHASE_INDEX reconciliation are process debt paid down opportunistically in the same session — the registry converts the arc/CFSS postmortem lesson into a mandatory SCOPE-time question instead of relying on memory of a past incident, and 16 stale roadmap rows were a small, mechanical fix once noticed. The streaming-as-bandwidth deferral is an explicit anti-scope-creep decision: it would have been easy to ship "80% fewer bytes" as a feel-good win, but the desktop-only product has no user who benefits from it today.

**Files touched:**
- `lite/tests/INVARIANTS.md`: NEW — 10 invariants (I1–I10) mapped to guard test + tier, 2 mandatory SCOPE questions, new-object-kind fixture rule
- `docs/status/PHASE_INDEX.md`: 16 stale rows reconciled with real `fixed_commit`; `ptToScreen` parity card closed with rationale (I7); streaming-spike verdict + worker-recycle GO + streaming-as-bandwidth deferral recorded on the perf card — updated via the block's own commits (`6d6e39b`, `f8c2981`, and one further uncommitted-at-write-time edit), not by this docs-batching write, which was explicitly instructed not to touch this file
- `docs/invent/lite-range-streaming.md`: updated with the 5-step spike's real results, the NOGO-as-memory-fix verdict, and the two proposed follow-on cards (worker-recycle RESHAPE, streaming-as-bandwidth)
- `lite/sandbox/invent-range-streaming/`: NEW — spike scripts + raw results (`s1_linearize.py`, `s2_range.py`, `spike.html`, `spike_run.py`, `results.json`, `results.md`)
- `lite/static/js/page-renderer.js`: worker-recycle — explicit `_docWorker` ownership, `_docSource` cheap re-open handle, `recycleDocWorker()` with guards, transparent lazy `_reinitDoc`, hidden/idle/manual triggers, `_resetCache()` destroys the explicit worker
- `lite/ui-lite.html`: +1 line, single worker-recycle call-site (1170/1200 line cap)
- `lite/tests/test_worker_recycle.py`: NEW — `LITE_WORKER_RECYCLE_OK`, 7 checks incl. zero-refetch-via-blob and heap-not-worse (−0.8%)

**Tests:**
```
python lite/tests/test_worker_recycle.py       → LITE_WORKER_RECYCLE_OK   PASS (NEW, 7/7 checks)
python lite/tests/run_all_tests.py --tier t0   → PASS (measure-math tier, <5s target)
python lite/tests/test_measure_parity.py       → MEASURE_PARITY_OK        PASS (drift-lock intact)
```
Regression 6/6 targeted at-risk files green + `run_all_tests.py --tier t0` green. `MEASURE_PARITY_OK` green throughout — zero vendored-math touch across all 5 pieces of this block. Lite suite now 72 test files. Proto E2E n/a — lite-only block, zero `proto/` edits.

**Phase 1 scope check:**
- ✅ `polyAreaM2` / `polyMetrics` / `polySelfIntersects` unchanged
- ✅ `pdfToC` / `cToPdf` / `RS` / scale math unchanged
- ✅ `proto/server.py` core endpoints unchanged (proto NOT TOUCHED — lite-only block)
- ✅ `lite/static/js/measure-engine.js` drift-locked vendored math unchanged
- ✅ `.bmaplan` schema version stays 1; no schema fields touched this block (invariants doc, spike research, worker lifecycle management — no persisted-state change)
- ✅ No legal / OCR / AI / Rule Engine / FAR-OSR pass-fail
- ✅ No forbidden surface touched; the spike ran in an isolated `lite/sandbox/` script tree, not against the live page-renderer's buffer-ownership contract, until the RESHAPE was independently built and guarded by its own test
- ✅ `lite/ui-lite.html` stays under 1200-line cap (1170/1200); `page-renderer.js` stays under 1000-line cap (790/1000)

**Known gaps / follow-ups:**
- **(1) Production RSS re-probe of worker-recycle on the CHH binder** — the honest scope gap this block itself records: the −50% acceptance bar was measured in the spike's pattern, not re-verified against the shipped implementation on the real 95 MB file. Small measurement task, extends the existing `artifacts/perf/` probe tooling.
- **(2) V2 migration continuation** — U1 (`INVARIANTS.md`) now DONE this block; U2 (tiered test runner) partially landed via `BLOCK-20260703-clear-queue` Ship 3. Still queued: U3 (`SHIPS.jsonl` ledger — the token-cost killer), U4 (full roadmap split into ACTIVE/DONE + a reconcile script — this block's 16-row reconcile was done by hand, not by tooling), plus 2 more upgrades per the blueprint's migration order.
- **(3) proto backlog decision** — proto is de-facto in maintenance mode; needs an explicit decision whether to formally freeze it or schedule a hardening pass against its known issues (circle 32-gon storage bias, snap-radius zoom growth, soft-pass markers).
- **(4) Release ritual per V2-U6** — tag + CHANGELOG + full 4-tier test run + `/lite-sandbox-test` — due before the next build hand-off.
- Streaming-as-bandwidth card stays DEFERRED, not closed — worth revisiting if/when a remote deployment target exists.

---

## 2026-07-03 — INV-20260703-layer-linkage (plan B complete) + UX-batch-1 + save-fix follow-up — PASS (branch: main)

**What changed:** The full staged rollout (B0→B5) of the layer↔measurement redesign that reached its human GO checkpoint earlier the same day (`INV-20260703-layer-linkage`) shipped end-to-end across 7 commits, plus a first batch of UX quick-wins and the bug-archive follow-up for the CRASH-tier save-wipe fix. **(1) Save-fix follow-up (`d40b20b`, already fixed and recorded in the prior finalize — included here for narrative completeness of the closed arc):** Ctrl+S no longer wipes measurements; `projectToGlobals(livePS)` resolves live content by identity via `_initialIds`/`dupSrc`; guarded by `LITE_SAVE_CLICKPATH_OK`. **(2) UX batch 1 (`34aefa3`):** F-7 — `modalOpen()` now recognizes `#sum`/Summary as an open modal via a keydown guard, closing the hotkey-leak gap the UX review filed; F-1 — the dead ⇧D Path hotkey wired to the actual tool; F-2 — the F/Focus key collision resolved; F-3 — Page Manager gained a menu entry (previously ⇧F12-only, undiscoverable); cheatsheet corrected to match real bindings. Guard `lite/tests/test_ux_quickwins.py`. **(3) B0 (`edc89ae`):** NEW `lite/static/js/object-agg.js` — the single tuple-stream aggregation engine at the center of the whole redesign: one generator emits `{pg, catId, role, floorKey, area, counting}` tuples for every measured object, and the new I11 invariant oracle (`assertEnginesAgree`) asserts any two consumers reducing the same tuple stream over the same partition always agree — making the H2 finding (Summary vs. Review disagreement) structurally impossible rather than patched case-by-case. Guard `lite/tests/test_object_tuples.py` (`LITE_OBJECT_TUPLES_OK`). **(4) B1 (`6909486`):** `report-vars.js` plus Summary per-floor `gfa`/`ded`/`net` rerouted onto the tuple engine behind an opt-in `{useLive:true}` flag — the default path stays byte-identical to before, a deliberate low-risk rollout pattern. Guard `test_b1_role_reroute.py`. **(5) B2 (`00ab9b9`):** layer-tree sigma totals and the Review-panel Section-3 rows now reduce the SAME tuple stream as Summary — oracle check (c) proves `byFloorRole` partitions equal `byRole` partitions by construction, closing H2 for good; root-unfiled layers (M6) are now counted where they previously silently dropped out. Guard `test_b2_single_engine.py`. **(6) B3 (`34594b7`):** `layer-system.js` gains `reassignObjectsOfLayer` on `removeLayer` plus a load-time `sweepOrphanCatIds` heal pass — closes H3, the finding that a deleted category's orphaned `catId` silently dropped objects from every total (and crashed on one path). Guard `test_b3_orphan_heal.py`. **(7) B4 (`750d2f6`):** NEW `lite/static/js/layer-move.js` (120 lines, DOM-injected — `ui-lite.html` itself untouched) — the move-object-to-layer UI that closes H1, the "no UI exists to move an object between layers" finding: a Properties-panel select plus a canvas context-menu entry "ย้ายไปเลเยอร์ ▸"; CFSS shared-shape instances retarget the MASTER with a confirm dialog since instances do not own their own layer. Guard `test_b4_move_layer.py`. Known flag carried forward: CFSS-master-only moves are not undoable — a pre-existing `_docSnap` gap, not a new regression, recorded as a follow-up candidate. **(8) B5 (`3d3741e`):** the report-vars operand dropdown now groups references into two optgroups — sigma role-refs (aggregate-by-role) vs. single-layer refs — closing M4, the finding that the two reference kinds were visually indistinguishable in the editor. Display-only change; the persisted expression format is unchanged.

**Why:** This is the completion of Pack H's staged rollout for Approach B "one aggregation engine," the structural fix scored 25/30 against 4 alternatives at the `INV-20260703-layer-linkage` checkpoint specifically because it closed all 3 HIGH findings (H1/H2/H3) with one shared mechanism instead of 3 separate patches — H2 in particular is closed by construction (the I11 oracle proves two consumers of the same tuple partition cannot disagree), not by careful-but-fragile manual synchronization the way the old dual-engine design required. Shipping it as 6 small staged commits (B0 building the engine before anything depends on it, B1-B2 rerouting consumers one at a time behind opt-in flags, B3 closing the crash-tier orphan bug, B4 finally giving users the missing UI, B5 a small clarity polish) kept every commit independently testable and reversible rather than one large risky rewrite. The UX quick-wins batch was bundled into the same session because F-7's modal-detection fix was a direct, low-risk follow-up to the UX review filed the same morning, and clearing it early prevents hotkey-leak friction from compounding while batch 2 (F-4/F-5/F-6/F-9) is still queued.

**Files touched:**
- `lite/static/js/object-agg.js`: NEW (B0) — single tuple-stream aggregation engine `{pg, catId, role, floorKey, area, counting}`
- `lite/static/js/report-vars.js`: (B1) per-floor `gfa`/`ded`/`net` on tuples behind opt-in `{useLive:true}`; (B5) operand dropdown sigma/single-layer optgroups
- `lite/static/js/layer-tree.js`: (B2) sigma totals reduce the tuple stream
- `lite/static/js/overview-setup.js`: (B2) Review-panel Section-3 rows reduce the tuple stream
- `lite/static/js/layer-system.js`: (B3) `reassignObjectsOfLayer` on `removeLayer` + load-time `sweepOrphanCatIds` heal
- `lite/static/js/layer-move.js`: NEW (B4, 120 lines) — move-object-to-layer via Properties select + context menu, CFSS master retarget with confirm
- `lite/static/js/page-folder-layers.js`, `lite/static/js/page-manager.js`, `lite/static/js/page-manager-ui.js`: save-fix follow-up bookkeeping (piece 1, already shipped)
- `lite/ui-lite.html`: UX batch 1 — F-7 modalOpen keydown guard, F-1 Path hotkey, F-2 F-key/Focus fix, F-3 Page Manager menu entry, cheatsheet corrections
- `lite/tests/test_save_clickpath.py`, `test_ux_quickwins.py`, `test_object_tuples.py`, `test_b1_role_reroute.py`, `test_b2_single_engine.py`, `test_b3_orphan_heal.py`, `test_b4_move_layer.py`, `test_b5_ref_badges.py`: all NEW, one guard per piece
- `docs/status/PHASE_INDEX.md`: updated via the block's own commits — NOT touched by this docs-batching write per explicit instruction

**Tests:** Every commit independently verified pre-commit with its own targeted suite plus a t0 measure-parity check. Latest verify (B5): `test_b5_ref_badges.py` + `test_report_vars_ui.py` + `test_report_vars_rollup.py` + `test_b1_role_reroute.py` + `lite/tests/run_all_tests.py --tier t0` — all green. No `proto/` E2E run — zero `proto/` files touched across the whole block (lite-only), so proto's 22-marker full-E2E baseline is unaffected by construction; this is the standing no-test rationale for the proto side, not an omission.

**Phase 1 scope check:**
- ✅ `polyAreaM2` / `polyMetrics` / `polySelfIntersects` unchanged
- ✅ `pdfToC` / `cToPdf` / `RS` / scale math unchanged
- ✅ `proto/server.py` core endpoints unchanged (proto NOT TOUCHED — lite-only block)
- ✅ `lite/static/js/measure-engine.js` drift-locked vendored math unchanged — `MEASURE_PARITY_OK` verified green at B4, the natural checkpoint for a block this heavy on layer/aggregation plumbing
- ✅ `.bmaplan` schema version stays 1; all new fields/files this block are additive (tuple engine is a pure read-side reduction over existing objects, not a new persisted shape)
- ✅ No legal / OCR / AI / Rule Engine / FAR-OSR pass-fail
- ✅ No forbidden surface touched

**Known gaps / follow-ups:**
- **(1) UX quick-wins batch 2** — F-4 (visible scale-verified badge post-modal), F-5/F-6 (setup-wizard mouse-block cue + accurate upload-in-flight messaging), F-9 (replace `window.prompt()` in Verify Scale with the apps own modal system), plus summary seeded-vars red-error display + a wizard Next-button gate. In progress at the time of this write.
- **(2) B4 CFSS-undo follow-up** — CFSS-master-only layer moves are not undoable; a pre-existing `_docSnap` gap surfaced by B4's UI, not a new regression. Candidate follow-up sprint.
- **(3) Worker-recycle production RSS re-probe on the CHH binder** — carried over from `GO-20260703-invariants-streaming-worker-recycle`, still open.
- **(4) Workflow-redesign proposal** — awaiting user GO; separate from this block.
- **(5) V2 U3/U4 migration** — `SHIPS.jsonl` ledger (U3) and roadmap split+reconcile tooling (U4), still queued.

---

## 2026-07-03 — UX-REVIEW-20260703 + BUG-20260703-lite-save-wipes-data (CRASH fix: Ctrl+S wiped all measurements + full UI/UX journey review + layer↔measurement invent checkpoint) — PASS (branch: main)

**What changed:** Three pieces filed/shipped back-to-back the same morning. **(1) CRASH fix — save wiped all data (commit `d40b20b`):** Ctrl+S wrote an empty `.bmaplan` and destroyed the in-memory session. Root cause: `pageMgr` seeded `PS_by_id` as `deepCopy` snapshots of `PS` taken at upload time (empty at that point); all drawing thereafter mutated live `PS` only, never the snapshots; `mi-save` → `_pmCommit` then projected those stale empty snapshots back over `PS` immediately before `buildPageStore()` serialized it — so save always wrote the empty upload-time state, discarding every measurement drawn since. 72 green tests missed this because every existing save test called `buildPageStore()` directly (the API path) and never drove the real `mi-save` click path. Fix: new `projectToGlobals(livePS)` resolves each page id's CONTENT from live `PS` by identity — baseline position via `_initialIds`, duplicates via a new `dupSrc` map that deep-copies the SOURCE page's live content, merged pages stay blank as intended; `_pmCommit` now passes `PS` through this resolver, and model snapshots are refreshed post-commit so they stop going stale. The same latent wipe existed on the Apply/merge path (`_pmuiApplyChanges` + merge, `lite/static/js/page-manager-ui.js`, 2 call sites) — commit order was backwards (flush-then-commit instead of commit-with-pre-flush-baseline-then-flush); reordered to commit first. NEW guard `lite/tests/test_save_clickpath.py` (`LITE_SAVE_CLICKPATH_OK`) drives the REAL `mi-save` click via `URL.createObjectURL` interception — the click-path-vs-API-path gap that let 72 green tests miss this bug in the first place. Proven RED pre-fix (exact journey repro: polys 0, calib null after save) → GREEN; 4 checks including "duplicate page content follows identity, not position." Regression 17/17 (metamorphic suite, page-manager suite, apply-mutations, all persist suites, arc/CFSS summary parities, `MEASURE_PARITY_OK`). Files: `lite/static/js/page-manager.js` (+38/−, `projectToGlobals`), `lite/static/js/page-manager-ui.js` (+8/−, Apply/merge commit-order fix, 2 sites), `lite/tests/test_save_clickpath.py` (NEW, 190 lines), `lite/ui-lite.html` (+4/−), plus `docs/status/PHASE_INDEX.md` + `lite/tests/bug-archive.jsonl` bookkeeping (commit `912c3e2`). **(2) UI/UX review complete (filed as `UX-20260703-review-findings`, commit `2249400`):** a full journey review on the real 45-page permit PDF (29 screenshots captured to `artifacts/ux-review-20260703/`) plus a static code inventory surfaced 9 FRICTION findings — F-1 dead ⇧D hotkey, F-2 `F`/Focus key collision, F-3 Page Manager undiscoverable (⇧F12-only, no menu/button path), F-4 scale-verified state invisible after the verify modal closes, F-5 the setup wizard silently blocks all mouse input with no visible modal cue, F-6 misleading "เปิด PDF ก่อน" message shown during a still-in-flight background upload, F-7 `modalOpen()` fails to recognize `#sum`/`#vs-modal`/the cheatsheet as open modals so typing in them fires tool hotkeys instead of being captured, F-8 raw/unfriendly error messages surfaced to the user, F-9 the Verify Scale flow uses a native `window.prompt()` instead of the app's own modal system — plus 6 COSMETIC findings (annotate mode has no hotkeys documented, Thai/English label inconsistency in a few places, the on-screen cheatsheet is stale vs. actual bindings, empty-state UI feels cluttered, a transient "scanned PDF" notice disappears too fast to read, no visible fallback-render badge when PDF.js degrades to raster) and 3 GOODs explicitly pinned so future refactors don't regress them (the setup wizard flow, the local-first "green dot" save status, and live in-progress measurement feedback on the canvas). This is a findings-filing sprint — no application code changed by the review itself (the CRASH bug it surfaced was already fixed same-morning by piece 1). **(3) Layer↔measurement invent at checkpoint (`INV-20260703-layer-linkage`, commit `2249400`):** an investigation mapped the current layer/measurement data model and enumerated 8 concrete problems (3 rated HIGH: no UI exists to move an object between layers once drawn; the Summary-widget aggregation engine and the Review-panel aggregation engine can disagree on net area because they are two independently-maintained rollup implementations; an orphaned `catId` — e.g. after a category is deleted — silently drops the object from every total with no warning, and in one path crashes). The `bma-inventor` diverge/score phase produced 5 candidate approaches; Approach B "one aggregation engine" won 25/30 — a single `object-agg.js` tuple-stream generator that every current consumer (summary, review, export, per-floor net) is rewritten as a pure reduction over, making per-floor net calculation native instead of bolted-on, and is fully additive (no schema break). Fallback approach D (a simpler `floorKey` field directly on objects) was also scored and recorded in case B proves too large. A staged rollout plan B0→B5 was written. The pipeline received GO from the human checkpoint; **B0 (the tuple generator + a new `I11` invariant oracle) and a first batch of UX quick-wins (F-7 modal-detection fix, F-1/F-2/F-3 hotkey/discoverability fixes, plus a cheatsheet accuracy pass) are being built right now by two `lite-builder` subagents running in parallel** — recorded here as in-progress, not yet shipped; nothing from B0 or the UX quick-wins batch landed in this docs-write's commit range.

**Why:** The save-wipe bug is the most severe class of defect this project tracks — total, silent, undetected data loss on the single most-used action in the app (Ctrl+S) — and it slipped through 72 previously-green tests because every one of them called the serialization function directly rather than driving the actual UI click handler a real user triggers. That gap is now closed with a guard that fires the real event, and it is the concrete incident this block wants captured as an `INVARIANTS.md`-style lesson: click-path coverage is not implied by API-path coverage, and any future save/persistence-adjacent guard must drive the real handler, not just the underlying function. The UX review exists to surface exactly this kind of pre-catastrophic friction before a user hits it in the field — the CRASH it found (piece 1) is the review methodology validating itself in the same session it was run. The layer-linkage investigation is upstream, deliberate R&D discipline (Pack H): rather than patch each of the 8 problems ad hoc as separate bug reports, the investigation first asked whether they share a root cause (they do — two independently-maintained rollup engines) and scored a structural fix before committing engineering time, exactly the RESEARCH-before-BUILD discipline this project's process docs require. The checkpoint halted for human GO rather than being auto-promoted, per Pack H's non-negotiable human-risk-taking gate for invention work.

**Files touched:**
- `lite/static/js/page-manager.js`: +38/− — NEW `projectToGlobals(livePS)` resolves page content from live `PS` by identity at commit time (baseline via `_initialIds`, duplicates via new `dupSrc` map, merges stay blank)
- `lite/static/js/page-manager-ui.js`: +8/− — Apply/merge commit-order fix (commit with pre-flush baseline THEN applyFlush, was backwards; 2 call sites)
- `lite/tests/test_save_clickpath.py`: NEW (190 lines) — `LITE_SAVE_CLICKPATH_OK`, drives the real `mi-save` click via `URL.createObjectURL` interception, 4 checks incl. dup-content-follows-identity
- `lite/ui-lite.html`: +4/−, minor wiring for the click-path guard
- `docs/status/PHASE_INDEX.md`: bug filed + `fixed_commit` recorded (`d40b20b`); UX review + invent-checkpoint bundle filed (`2249400`) — NOT further touched by this docs-batching write per explicit instruction
- `lite/tests/bug-archive.jsonl`: `BUG-20260703-lite-save-wipes-data` fixed_commit recorded (commit `912c3e2`) — NOT further touched by this docs-batching write per explicit instruction
- `artifacts/ux-review-20260703/`: NEW — 29 journey screenshots backing the UX review findings (review artifact, not app code)

**Tests:**
```
python lite/tests/test_save_clickpath.py   → LITE_SAVE_CLICKPATH_OK   PASS (NEW; RED pre-fix = exact journey repro, polys 0/calib null; 4 checks)
```
Regression 17/17: metamorphic suite, page-manager suite, apply-mutations suite, all persist suites, arc-summary parity, CFSS-summary parity, `MEASURE_PARITY_OK` (drift-lock intact — zero vendored-math touch). UX review and invent checkpoint are findings-filing / research work, not app-code changes, and carry no dedicated test marker — covered by the same regression run. B0 tuple generator + UX quick-wins batch 1 are in-progress with two `lite-builder` subagents at the time of this write; not yet tested or shipped.

**Phase 1 scope check:**
- ✅ `polyAreaM2` / `polyMetrics` / `polySelfIntersects` unchanged
- ✅ `pdfToC` / `cToPdf` / `RS` / scale math unchanged
- ✅ `proto/server.py` core endpoints unchanged (proto NOT TOUCHED — lite-only block)
- ✅ `lite/static/js/measure-engine.js` drift-locked vendored math unchanged
- ✅ `.bmaplan` schema version stays 1; save-fix changes commit-time resolution logic only, no new/renamed persisted fields
- ✅ No legal / OCR / AI / Rule Engine / FAR-OSR pass-fail
- ✅ No forbidden surface touched; the layer-linkage invent is HALTED at the human checkpoint per Pack H — B0/UX-quick-wins building now is a separate in-progress sprint, not part of this docs-write's shipped commit range

**Known gaps / follow-ups:**
- **(1) B0 tuple generator + I11 oracle, and UX quick-wins batch 1 (F-7/F-1/F-2/F-3 + cheatsheet truth pass)** — actively being built by two `lite-builder` subagents in parallel at the time of this write; needs verification + commit as its own follow-on sprint.
- **(2) B1-B2** — reroute existing summary/review/export consumers onto the new tuple-stream aggregation engine once B0 lands.
- **(3) UX quick-wins batch 2** — F-4 (visible scale-verified badge post-modal), F-5/F-6 (wizard mouse-block + upload-in-flight messaging), F-9 (replace `window.prompt()` in Verify Scale with the app's own modal), plus summary seeded-vars red-error display + a wizard Next-button gate.
- **(4) Production RSS re-probe of worker-recycle on the CHH binder** — carried over from `GO-20260703-invariants-streaming-worker-recycle`, still open.
- **(5) V2 U3/U4 migration** — `SHIPS.jsonl` ledger (U3) and roadmap split+reconcile tooling (U4), still queued.
- The click-path-vs-API-path testing gap this bug exposed is a candidate lesson for `lite/tests/INVARIANTS.md` — not yet added there (deliberately out of scope for this docs-batching write; `lite/tests/` files were off-limits while the two parallel builders are active).

---
